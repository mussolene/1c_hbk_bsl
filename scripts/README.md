# `scripts/`

В публичном репозитории отдельных parity-скриптов нет. Сборка и тесты — через Makefile и pytest из корня.

```bash
onec-hbk-bsl --help
```

Или цели `make format-parity`, `make diag-bslls`.
