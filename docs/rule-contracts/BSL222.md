# BSL222 — <span class="doc-lang doc-lang-ru">Частично локализованный текст используется в функции СтрШаблон</span><span class="doc-lang doc-lang-en">Partially localized text is used in the StrTemplate function</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Частично локализованный текст используется в функции СтрШаблон

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Partially localized text is used in the StrTemplate function

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL222` |
| Совместимый псевдоним / Compatible alias | `MultilingualStringUsingWithTemplate` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `localization`, `style` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL222` и псевдоним `MultilingualStringUsingWithTemplate` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL222"]
ignore = ["MultilingualStringUsingWithTemplate"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL222
Значение = "пример";  // bsl-disable: BSL222
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MultilingualStringUsingWithTemplate-off
// код без этой диагностики
// BSLLS:MultilingualStringUsingWithTemplate-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL222` and alias `MultilingualStringUsingWithTemplate` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL222"]
ignore = ["MultilingualStringUsingWithTemplate"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL222
Value = "example";  // bsl-disable: BSL222
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MultilingualStringUsingWithTemplate-off
// code without this diagnostic
// BSLLS:MultilingualStringUsingWithTemplate-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

НСтр в мультиязычной конфигурации имеет разные фрагменты для разных языков.
Если запустить сеанс под кодом языка, которого нет в строке передаваемой в NStr то она вернет пустую строку.
При совместном использовании с СтрШаблон возвращенная из НСтр пустая строка будет выброшено исключение.

## Источники

- [Требования по локализации](https://its.1c.ru/db/v8std/content/763/hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

NStr in a multilingual configuration has different fragments for different languages.
If you start a session under a language code that is not in the string passed to NStr, it will return an empty string. When used with StrTemplate, an empty string returned from NStr will throw an exception.

## Sources

- [localization requirements (RU)](https://its.1c.ru/db/v8std/content/763/hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL222
- BSLLS compatible name: MultilingualStringUsingWithTemplate
- Public name/i18n key: BSL222 / MultilingualStringUsingWithTemplate
- Default message/i18n key: RULE_MESSAGES_RU["BSL222"]
- Description/i18n key: RULE_METADATA["BSL222"].description
- Severity: WARNING
- Parameters/defaults: `declaredLanguages=ru`.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: localized `НСтр` values used as `СтрШаблон` templates must include all declared languages.
- Why this diagnostic exists: if `НСтр` lacks the active session language, it can return an empty string and make `СтрШаблон` fail at runtime.
- Non-goals: validating placeholder counts owned by BSL202, validating all `НСтр` calls outside `СтрШаблон`, or resolving dynamically computed localized strings.
- Keep/drop decision: keep as a localization correctness diagnostic.

## Semantic Oracle

- Checked language fact: a `СтрШаблон`/`StrTemplate` call uses `НСтр`/`NStr` text that does not cover all declared languages.
- Must diagnose when: an `НСтр`/`NStr` literal inside a `СтрШаблон`/`StrTemplate` call lacks at least one declared language.
- Must not diagnose when: the localized string covers all declared languages, the `НСтр` is not used as a template argument, or the template is dynamically computed beyond this rule's scope.
- Diagnostic anchor token/range: the localized string/call range used as the template argument.
- Quick-fix target, if any: add missing language fragments to the `НСтр` literal.

## CST Or Domain Fact Model

- Parser/tree source: lightweight method-call/string analysis in the module model.
- CST/domain object types: `СтрШаблон` call, nested `НСтр` call/string literal, declared language list.
- Extracted attributes: call name, localized string body, language keys, source range, and severity.
- Parse-error behavior: malformed syntax may be skipped; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: bounded localized-string parsing is allowed for `НСтр` language fragments.

## Examples

### Invalid Examples

1. With declared `ru,en`: `СтрШаблон(НСтр("ru='Готово'"), Значение)`
2. With declared `ru`: `СтрШаблон(НСтр("en='Done'"), Значение)`
3. `StrTemplate(NStr("ru='Value %1'"), Value)`

### Valid Negative Twins

1. With declared `ru`: `СтрШаблон(НСтр("ru='Готово'"), Значение)`
2. `НСтр("en='Done'")` outside `СтрШаблон`
3. `СтрШаблон("%1", Значение)`

### Edge Examples

- Comments: ignored by source masking.
- Strings: only `НСтр` localized-string literals are interpreted.
- Preprocessor: not a separate target.
- Multiline: current lightweight implementation handles the supported call shapes.
- Nested constructs: nested `НСтр` inside `СтрШаблон` is the semantic target.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: `СтрШаблон` calls and localized text.
- Rules that may report on the same object/range: BSL202 validates placeholder/value count, BSL221 validates missing localization for `НСтр`, and BSL224 may inspect nested calls.
- Intended duplicate policy: BSL222 owns partial localization specifically when the localized text is used as a template string.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py` BSL222 selected test.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics with default `declaredLanguages=ru`.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: lightweight module-model pass over candidate call lines.
- Expected complexity: O(total source characters) with bounded regex/call matching.
- Shared-pass reuse: shares localized string concerns with BSL221 and template concerns with BSL202.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: existing lightweight call analysis covers the documented BSLLS-compatible scope.
- Rollback signal: BSL202 placeholder checks duplicate BSL222, default `ru` cases regress, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl222 or MultilingualStringUsingWithTemplate"`.
- Synthetic fixture/module: existing selected test covers partial localization inside `СтрШаблон`.
- Related-rule tests: BSL202/BSL221 only if shared localization/template extraction changes.
- Parity command/artifact: preserved-source-root selected BSL222 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
