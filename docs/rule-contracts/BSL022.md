# BSL022 — <span class="doc-lang doc-lang-ru">Использование модальных окон</span><span class="doc-lang doc-lang-en">Using modal windows</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование модальных окон

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Using modal windows

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL022` |
| Совместимый псевдоним / Compatible alias | `UsingModalWindows` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `deprecated`, `ui` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL022` и псевдоним `UsingModalWindows` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL022"]
ignore = ["UsingModalWindows"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL022
Значение = "пример";  // bsl-disable: BSL022
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UsingModalWindows-off
// код без этой диагностики
// BSLLS:UsingModalWindows-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL022` and alias `UsingModalWindows` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL022"]
ignore = ["UsingModalWindows"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL022
Value = "example";  // bsl-disable: BSL022
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UsingModalWindows-off
// code without this diagnostic
// BSLLS:UsingModalWindows-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Модальные и всплывающие окна считаются плохим тоном, пользователи привыкли к работе "в одном окне". При разработке конфигураций, предназначенных для работы в веб-клиенте, запрещено использовать модальные формы и диалоги. В противном случае, конфигурация окажется неработоспособной в ряде веб-браузеров, так как модальные окна не входят в стандарт веб-разработки.

### Ограничения диагностики

На данный момент диагностируется **только использование методов глобального контекста**.

Список методов:

|Метод|Английский вариант|
| :-- | :-- |
|ВОПРОС|DOQUERYBOX|
|ОТКРЫТЬФОРМУМОДАЛЬНО|OPENFORMMODAL|
|ОТКРЫТЬЗНАЧЕНИЕ|OPENVALUE|
|ПРЕДУПРЕЖДЕНИЕ|DOMESSAGEBOX|
|ВВЕСТИДАТУ|INPUTDATE|
|ВВЕСТИЗНАЧЕНИЕ|INPUTVALUE|
|ВВЕСТИСТРОКУ|INPUTSTRING|
|ВВЕСТИЧИСЛО|INPUTNUMBER|
|УСТАНОВИТЬВНЕШНЮЮКОМПОНЕНТУ|INSTALLADDIN|
|УСТАНОВИТЬРАСШИРЕНИЕРАБОТЫСФАЙЛАМИ|INSTALLFILESYSTEMEXTENSION|
|УСТАНОВИТЬРАСШИРЕНИЕРАБОТЫСКРИПТОГРАФИЕЙ|INSTALLCRYPTOEXTENSION|
|ПОМЕСТИТЬФАЙЛ|PUTFILE|

## Примеры

```bsl
// Пример "Плохо"
Предупреждение(НСтр("ru = 'Выберите документ!'; en = 'Select a document!'"), 10);

// Пример "Хорошо"
ПоказатьПредупреждение(, НСтр("ru = 'Выберите документ!'; en = 'Select a document!'"), 10);
```

## Источники

* [Ограничение на использование модальных окон и синхронных вызовов](https://its.1c.ru/db/v8std/content/703/hdoc/)
* Полезная информация: [Отказ от использования модальных окон](https://its.1c.ru/db/metod8dev#content:5272:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Modal windows and pop-ups are considered bad taste. Users are accustomed to working "in one window". When developing configurations designed to work in the web client, it is forbidden to use modal windows and dialogs. Otherwise, the configuration will be inoperative in a number of web browsers, since modal windows are not part of the web development standard.

### Diagnostic restrictions

Currently, **only the use of global context methods** is diagnosed.

Method list:

|Russian variant|English variant|
| :-- | :-- |
|ВОПРОС|DOQUERYBOX|
|ОТКРЫТЬФОРМУМОДАЛЬНО|OPENFORMMODAL|
|ОТКРЫТЬЗНАЧЕНИЕ|OPENVALUE|
|ПРЕДУПРЕЖДЕНИЕ|DOMESSAGEBOX|
|ВВЕСТИДАТУ|INPUTDATE|
|ВВЕСТИЗНАЧЕНИЕ|INPUTVALUE|
|ВВЕСТИСТРОКУ|INPUTSTRING|
|ВВЕСТИЧИСЛО|INPUTNUMBER|
|УСТАНОВИТЬВНЕШНЮЮКОМПОНЕНТУ|INSTALLADDIN|
|УСТАНОВИТЬРАСШИРЕНИЕРАБОТЫСФАЙЛАМИ|INSTALLFILESYSTEMEXTENSION|
|УСТАНОВИТЬРАСШИРЕНИЕРАБОТЫСКРИПТОГРАФИЕЙ|INSTALLCRYPTOEXTENSION|
|ПОМЕСТИТЬФАЙЛ|PUTFILE|

## Examples

```bsl
// Incorrect
DoMessageBox(NStr("ru = 'Выберите документ!'; en = 'Select a document!'"), 10);

// Correct
ShowMessageBox(, NStr("ru = 'Выберите документ!'; en = 'Select a document!'"), 10);
```

## Sources

* [Limit on the use of modal windows and synchronous calls (RU)](https://its.1c.ru/db/v8std/content/703/hdoc/)
* Useful information: [Refusal to use modal windows (RU)](https://its.1c.ru/db/metod8dev#content:5272:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL022
- BSLLS compatible name: UsingModalWindows
- Public name/i18n key: BSL022 / UsingModalWindows
- Default message/i18n key: get_rule("BSL022").message
- Description/i18n key: RULE_METADATA["BSL022"].description
- Severity: WARNING
- Parameters/defaults: modal global method list
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Managed UI code should avoid deprecated modal global methods and prefer asynchronous APIs.
- Why this diagnostic exists: It prevents modal UI behavior that blocks managed application workflows.
- Non-goals: Typical client command handlers explicitly exempted by product behavior and method names in comments or strings.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A method call to a deprecated modal global method in executable code.
- Must diagnose when: The BSL CST contains a `method_call` to a modal global method outside exempted handler context.
- Must not diagnose when: The text is in comments/strings or the call is in an exempted typical client command handler.
- Diagnostic anchor token/range: the global method call expression from method identifier through the closing parenthesis.
- Quick-fix target, if any: replace with an asynchronous API manually.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through `_bsl022_modal_facts_from_tree`, with line fallback only when CST is unavailable.
- CST/domain object types: `method_call`, `identifier`, containing `ProcInfo`.
- Extracted attributes: method name, call expression LSP range, containing procedure context, exempted-handler state.
- Parse-error behavior: use line fallback only when the tree is not usable; `BSL001` owns syntax errors.
- Textual/regex behavior, only if genuinely textual: fallback line scan is a degraded path for modal global method text.

## Examples

### Invalid Examples

1. A code line calls a modal question method.
2. A code line calls a modal warning/message method from the deprecated list.
3. A non-exempt procedure calls a modal global method.

### Valid Negative Twins

1. The method name appears only in a string literal.
2. The method name appears only in a comment.
3. The call appears in an exempted typical client command handler.

### Edge Examples

- Comments: skipped.
- Strings: skipped.
- Preprocessor: not a separate target.
- Multiline: the identifier token range anchors the diagnostic.
- Nested constructs: containing procedure exemption still applies.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: UI method call.
- Rules that may report on the same object/range: BSL041 and deprecated API rules can report nearby deprecated UI/API usage.
- Intended duplicate policy: keep BSL022 for modal-window semantics only.
- Cluster fixture or probe: tests/test_diagnostics_core_snapshot.py::TestBsl022UsingModalWindows

## BSLLS Parity Taxonomy

- Compared input-file set: local synthetic BSL022 taxonomy plus original-workspace batch aggregate.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS `forceModalityMode=true` for standalone synthetic semantics.
- File-key normalization: synthetic labels for local probe; original source-root relative keys for private batch aggregate.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: sampled simple modal call, nested modal call, object call, string, and inline-comment cases match BSLLS exactly after this iteration.
- Line/common semantic common: private batch pre-change 150/150 count and line parity.
- Ours-only categories: none reproduced in the sampled synthetic taxonomy.
- BSLLS-only categories: none reproduced in the sampled synthetic taxonomy.
- Range-only categories: before this iteration onec reported only the method identifier; BSLLS reports the whole global method call through the closing parenthesis.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `deprecated_warning_facts`.
- Expected complexity: O(CST nodes) when tree is available, otherwise O(lines).
- Shared-pass reuse: shares procedure context with snapshot facts.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: `DocumentSnapshot.deprecated_warning_facts`, BSL022 contract, and synthetic coverage.
- Delta category fixed by this change: range-only anchor delta for modal global method calls.
- Why not another layer: modal call semantics and expression ranges are already extracted from CST/domain context.
- Rollback signal: strings/comments report, object method calls report, nested call range collapses to identifier, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_core_snapshot.py -q -k bsl022 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL022 tests.
- Related-rule tests: deprecated UI/API tests.
- Parity command/artifact: sampled local synthetic taxonomy against BSLLS 0.29.0; private batch parity remains aggregate-only.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
