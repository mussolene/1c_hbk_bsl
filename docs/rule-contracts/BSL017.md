# BSL017 — <span class="doc-lang doc-lang-ru">Экспортные методы в модулях команд и общих команд</span><span class="doc-lang doc-lang-en">Export methods in command and general command modules</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Экспортные методы в модулях команд и общих команд

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Export methods in command and general command modules

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL017` |
| Совместимый псевдоним / Compatible alias | `CommandModuleExportMethods` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `design` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL017` и псевдоним `CommandModuleExportMethods` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL017"]
ignore = ["CommandModuleExportMethods"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL017
Значение = "пример";  // bsl-disable: BSL017
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:CommandModuleExportMethods-off
// код без этой диагностики
// BSLLS:CommandModuleExportMethods-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL017` and alias `CommandModuleExportMethods` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL017"]
ignore = ["CommandModuleExportMethods"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL017
Value = "example";  // bsl-disable: BSL017
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:CommandModuleExportMethods-off
// code without this diagnostic
// BSLLS:CommandModuleExportMethods-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Не следует размещать экспортные методы в модулях команд и общих команд. К этим модулям нет возможности
обращаться из внешнего по отношению к ним кода, поэтому экспортные методы в этих модулях не имеют смысла.

## Источники

* [Источник](https://its.1c.ru/db/v8std/content/544/hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

You should not place export methods in command and General command modules. You can't access these modules from code external to them, so export methods in these modules do not make sense.

## Sources

* [Source (RU)](https://its.1c.ru/db/v8std/content/544/hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL017
- BSLLS compatible name: CommandModuleExportMethods
- Public name/i18n key: BSL017 / CommandModuleExportMethods
- Default message/i18n key: get_rule("BSL017").message
- Description/i18n key: RULE_METADATA["BSL017"].description
- Severity: WARNING
- Parameters/defaults: path-derived command/form module classification
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Command and form modules should not expose exported methods as a public API surface.
- Why this diagnostic exists: It prevents accidental public entry points in UI/command modules.
- Non-goals: Non-exported methods and exported methods in module kinds where export is allowed.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: An exported procedure/function declaration appears in a command or form module.
- Must diagnose when: `ProcInfo.is_export` is true and the file path is classified as a command/form module.
- Must not diagnose when: The method is not exported or the module kind is not command/form.
- Diagnostic anchor token/range: the method declaration line from `ProcInfo.header_col` to line end.
- Quick-fix target, if any: remove `Экспорт` / `Export` or move the API.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` and `command_or_form_export_facts`.
- CST/domain object types: `ProcInfo`, exported method header, path-derived module kind.
- Extracted attributes: method name, kind, start line, header column, export flag, file stem/path.
- Parse-error behavior: skip missing procedure model facts; `BSL001` owns broken-document reporting.
- Textual/regex behavior, only if genuinely textual: path/module-kind classification uses filename conventions.

## Examples

### Invalid Examples

1. A command module contains an exported procedure.
2. A form module contains an exported procedure.
3. A form module contains an exported function.

### Valid Negative Twins

1. A command module contains a non-exported procedure.
2. A common module contains an exported procedure.
3. A non-form object module contains an exported method allowed by module policy.

### Edge Examples

- Comments: commented method headers are not procedure facts.
- Strings: strings are not procedure declarations.
- Preprocessor: not a separate target.
- Multiline: method header range starts at the declaration line.
- Nested constructs: methods are top-level module declarations.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: method declaration in command/form module.
- Rules that may report on the same object/range: BSL040 uses form-module context; method-contract rules can report on the same method declaration.
- Intended duplicate policy: keep export-policy diagnostics distinct from method documentation/contract diagnostics.
- Cluster fixture or probe: tests/test_diagnostics_core_snapshot.py::TestBsl017ExportInCommandModule

## BSLLS Parity Taxonomy

- Compared input-file set: not compared in this iteration because no BSL017 rule logic changed.
- Config/exclude mode: not compared in this iteration.
- File-key normalization: use rule-contract parity procedure before any semantic BSL017 edit.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: not measured in this iteration.
- Line/common semantic common: not measured in this iteration.
- Ours-only categories: not measured in this iteration.
- BSLLS-only categories: not measured in this iteration.
- Range-only categories: not measured in this iteration.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `command_or_form_export_facts`.
- Expected complexity: O(procedures).
- Shared-pass reuse: shares procedure model with method-contract rules.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: BSL017 contract and audit state, not diagnostic logic.
- Delta category fixed by this change: process-state gap.
- Why not another layer: export-policy fact is already available from `ProcInfo` plus module-kind context.
- Rollback signal: non-exported methods report or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_core_snapshot.py -q -k bsl017 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL017 tests.
- Related-rule tests: BSL040 form-module path tests.
- Parity command/artifact: deferred until a BSL017 semantic rule change is made.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
