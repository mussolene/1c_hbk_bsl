# BSL240 — <span class="doc-lang doc-lang-ru">Перезапись параметров метода</span><span class="doc-lang doc-lang-en">Rewrite method parameter</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Перезапись параметров метода

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Rewrite method parameter

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL240` |
| Совместимый псевдоним / Compatible alias | `RewriteMethodParameter` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `suspicious`, `correctness` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL240` и псевдоним `RewriteMethodParameter` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL240"]
ignore = ["RewriteMethodParameter"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL240
Значение = "пример";  // bsl-disable: BSL240
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:RewriteMethodParameter-off
// код без этой диагностики
// BSLLS:RewriteMethodParameter-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL240` and alias `RewriteMethodParameter` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL240"]
ignore = ["RewriteMethodParameter"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL240
Value = "example";  // bsl-disable: BSL240
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:RewriteMethodParameter-off
// code without this diagnostic
// BSLLS:RewriteMethodParameter-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Иногда разработчик пишут функции таким образом, когда аргументы функции перезаписываются сразу на входе в функцию/процедуру.

Такое поведение вводит в заблуждение других разработчиков, которые вызывают подобные функции/процедуры.
Эти функции нужно исправить. Например, убрать параметры, преобразовав их в локальные переменные.

## Примеры

Подозрительный код
```bsl
Функция Конфигуратор(Знач СтрокаПодключения, Знач Пользователь = "", Знач Пароль = "") Экспорт
  СтрокаПодключения = "/F""" + КаталогБазы + """"; // Здесь
...
КонецФункции
```

Исправленный код
```bsl
Функция Конфигуратор(Знач КаталогБазы, Знач Пользователь = "", Знач Пароль = "") Экспорт
  СтрокаПодключения = "/F""" + КаталогБазы + """"; // Здесь
...
КонецФункции
```
или
```bsl
Функция Конфигуратор(Знач КаталогБазы, Знач Пользователь = "", Знач Пароль = "") Экспорт
  Если Не ПустаяСтрока(КаталогБазы) Тогда
   НоваяСтрокаПодключения = "/F""" + КаталогБазы + """";
  Иначе
   НоваяСтрокаПодключения = СтрокаПодключения;
  КонецЕсли;
...
КонецФункции
```

## Источники

<!-- Примеры источников

* [PVS-Studio V763. Parameter is always rewritten in function body before being used](https://pvs-studio.com/ru/docs/warnings/v6023)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

It is wrong to write methods in which their arguments are overwritten immediately on entry.

It is necessary to correct this deficiency by removing the parameters, converting them to local variables.

## Examples

Suspicious code
```bsl
Procedure Configor(Val ConnectionString, Val User = "", Val Pass = "") Export
  ConnectionString = "/F""" + DataBaseDir + """"; // Error
...
EndProcedure
```

Сorrected:
```bsl
Procedure Configor(Val DataBaseDir, Val User = "", Val Pass = "") Export
ConnectionString = "/F""" + DataBaseDir + """"; // No error
...
EndProcedure
```
or
```bsl
Procedure Configor(Val DataBaseDir, Val User = "", Val Pass = "") Export
 If Not EmpyString(DataBaseDir) Then
NewConnectionString = "/F""" + DataBaseDir + """";
Else
NewConnectionString = ConnectionString; // Hmm, where is this from?
EndIf;

...
EndProcedure
```

## Sources

<!-- Примеры источников

* [PVS-Studio V763. Parameter is always rewritten in function body before being used](https://pvs-studio.com/ru/docs/warnings/v6023)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL240
- BSLLS compatible name: RewriteMethodParameter
- Public name/i18n key: BSL240 / RewriteMethodParameter
- Default message/i18n key: RULE_MESSAGES_RU["BSL240"] and RULE_MESSAGES_RU_FMT["BSL240"]
- Description/i18n key: RULE_METADATA["BSL240"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: a parameter passed by value should not be overwritten before its original value is used.
- Why this diagnostic exists: rewriting a by-value parameter before reading it hides a local variable and makes the method contract harder to reason about.
- Non-goals: reporting non-`Знач` parameters, outgoing function parameters, unused parameters, or intentional normalization where the parameter is read on the right side first.
- Keep/drop decision: keep as a BSLLS-compatible suspicious-correctness diagnostic.

## Semantic Oracle

- Checked language fact: a procedure or function assigns to a `Знач`/`Val` parameter before that parameter has been read in the method body.
- Must diagnose when: the left side of an executable assignment is a by-value parameter and the parameter has not been used earlier or in the assignment right side.
- Must not diagnose when: the parameter is not by-value, the apparent assignment is a default value in the method signature, the parameter is read before assignment, or the syntax tree is invalid.
- Diagnostic anchor token/range: the assigned parameter identifier on the assignment left side.
- Quick-fix target, if any: introduce a local variable for the rewritten value or use the parameter value before reassignment.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST and `ProcInfo`/procedure-node mapping from `DocumentSnapshot`.
- CST/domain object types: `procedure_definition`, `function_definition`, `parameter`, `VAL_KEYWORD`, `assignment_statement`, left-side `identifier`, and right-side `expression`.
- Extracted attributes: by-value parameter names, body start after the full method signature, assignment lvalue name/range, right-side identifier reads, non-assignment identifier reads, and already-used parameter names.
- Parse-error behavior: structural method-contract checks skip invalid trees; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: none for valid CST paths.

## Examples

### Invalid Examples

1. `Процедура Тест(Знач П)` with `П = 1;` before any read.
2. `Процедура Тест(Знач П = 1)` with `П = 2;` in the body.
3. Form/module event method with `Знач Отказ` and `Отказ = Истина;` before any read.

### Valid Negative Twins

1. `Процедура Тест(П)` with `П = 1;`.
2. `Процедура Тест(Знач П)` with `П = СокрЛП(П);`.
3. Multiline signature default `П = Неопределено` where the body never assigns to `П`.

### Edge Examples

- Comments: comments do not create assignment facts.
- Strings: strings are only considered inside an already selected assignment expression and do not create left-side writes.
- Preprocessor: parsed body assignment facts are checked the same way.
- Multiline: method body starts after the complete multiline signature.
- Nested constructs: assignments inside nested blocks of the same method body count.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: method parameters and body assignments.
- Rules that may report on the same object/range: BSL062 checks unused parameters, BSL193 checks outgoing function parameters, BSL228 checks parameter order, and BSL254 checks client/server by-value parameter transfer.
- Intended duplicate policy: keep BSL240 separate; it reports by-value parameter overwrite before use, not unusedness, function out parameters, parameter order, or client/server transfer policy.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl240RewriteMethodParameter`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: zero BSL240 diagnostics on the private batch after CST read-before-write tracking.
- Line/common semantic common: synthetic by-value parameter overwrites still anchor on the assignment left-side identifier.
- Ours-only categories: fixed 89 false positives where a by-value parameter was read before assignment in a non-assignment expression such as a condition or call argument.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing shared method-contract pass, procedure-node map, and one CST body event walk per parsed method.
- Expected complexity: O(method body identifier and assignment nodes plus by-value parameters).
- Shared-pass reuse: shares `ProcInfo` and procedure-node mapping with other method-contract diagnostics.
- Before/after measurement, if changed: targeted BSL240 pytest covers behavior; selected parity is exact zero; no reparsing or additional full-file traversal was added.

## Implementation Decision

- Change target: BSL240 CST fact extraction, synthetic coverage, contract, and audit state.
- Delta category fixed by this change: onec-only semantic false positives caused by tracking parameter reads only in assignment right sides and ignoring earlier reads in conditions/calls/other expressions.
- Why not another layer: the defect is local to BSL240's existing method-body CST walk; `ProcInfo` and procedure-node mapping already provide the needed scope boundaries.
- Rollback signal: multiline parameter defaults report as rewrites, non-`Знач` parameters report, read-before-write in conditions/calls reports, actual first-statement overwrites stop reporting, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl240 --no-cov`.
- Synthetic fixture/module: existing inline BSL240 tests cover multiline defaults, by-value overwrite, non-by-value negative, read-before-write in RHS and conditions, optional by-value parameters, form modules, late overwrite, and RHS-read normalization.
- Related-rule tests: include BSL193 targeted coverage if method-contract assignment logic changes.
- Parity command/artifact: preserved-source-root selected BSL240 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_diagnostics_method_contracts.py src/onec_hbk_bsl/analysis/diagnostic/rules/method_contract_rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
