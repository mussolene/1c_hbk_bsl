# BSL206 JoinWithSubQuery Contract

## Rule Identity

- Code: BSL206
- BSLLS compatible name: JoinWithSubQuery
- Public name/i18n key: JoinWithSubQuery / BSL206
- Default message/i18n key: RULE_MESSAGES_RU["BSL206"]
- Description/i18n key: RULE_METADATA["BSL206"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: query joins should not use nested queries as joined data sources.
- Why this diagnostic exists: joining subqueries can make query plans slower and harder to optimize; the preferred form is usually a temporary table or direct source.
- Non-goals: do not diagnose a subquery used as the only `ИЗ/FROM` data source with no join.
- Keep/drop decision: keep as a BSLLS-compatible query performance diagnostic.

## Semantic Oracle

- Checked language fact: an SDBL data source participating in a join contains a nested query source.
- Must diagnose when: a `nested_query_source` is the joined data source, or the initial data source is a nested query and the same table source has join clauses.
- Must not diagnose when: a nested query source is used without any join clause.
- Diagnostic anchor token/range: the nested subquery source range, including its parentheses, mapped to BSL query-string coordinates.
- Quick-fix target, if any: extract the nested query to a temporary table or join direct metadata sources.

## CST Or Domain Fact Model

- Parser/tree source: unescaped SDBL tree-sitter CST from query text blocks.
- CST/domain object types: `QueryTextBlockInfo`, `table_source`, `join_clause`, `nested_query_source`.
- Extracted attributes: whether `nested_query_source` is inside `join_clause`, whether its containing `table_source` has a `join_clause`, and mapped BSL line/character range.
- Parse-error behavior: skip malformed query fragments that do not produce the relevant source nodes.
- Textual/regex behavior, only if genuinely textual: legacy regex fallback may remain only for environments without query block/SDBL facts; valid CST paths must be structural.

## Examples

### Invalid Examples

1. `ЛЕВОЕ СОЕДИНЕНИЕ (ВЫБРАТЬ ... ) КАК T`.
2. `(ВЫБРАТЬ ... ) КАК T ЛЕВОЕ СОЕДИНЕНИЕ Catalog.X КАК X`.
3. `LEFT JOIN (SELECT ... ) AS T`.

### Valid Negative Twins

1. `ИЗ (ВЫБРАТЬ ... ) КАК T` with no join.
2. `ЛЕВОЕ СОЕДИНЕНИЕ Справочник.Тест КАК T`.
3. A query text string containing the word `СОЕДИНЕНИЕ` inside a literal.

### Edge Examples

- Comments: query comments do not produce data-source CST nodes.
- Strings: join-like text inside query string literals must not report.
- Preprocessor: BSL preprocessor is irrelevant after query text extraction.
- Multiline: join subquery ranges may span several query-string lines.
- Nested constructs: nested query data sources are diagnosed at the outer data-source range.
- BSL/SDBL variants, if relevant: Russian and English join syntax are equivalent when parsed by SDBL CST.

## Related-Rule Cluster

- Shared semantic object: SDBL query join/data-source structure.
- Rules that may report on the same object/range: BSL207 reports virtual-table joins; BSL209 reports logical OR in join conditions; BSL187/BSL191 may report nearby query join issues.
- Intended duplicate policy: keep BSL206 independent because a subquery join is distinct from virtual-table join and join-condition OR.
- Cluster fixture or probe: query join tests in `tests/test_diagnostics_extended.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus synthetic query fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final private parity is exact at `onec=22`, `bslls=22`.
- Line/common semantic common: baseline private parity had `onec=16`, `bslls=22`, `only_onec=16`, `only_bslls=22`; final parity has no deltas.
- Ours-only categories: none in final private parity.
- BSLLS-only categories: none in final private parity.
- Range-only categories: fixed by reporting the nested query child range inside `nested_query_source`, matching BSLLS `subquery()` range.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing query text blocks and SDBL CST.
- Expected complexity: O(query CST nodes) for selected query blocks.
- Shared-pass reuse: remains in the BSL206/BSL207/BSL209 query join pool.
- Before/after measurement, if changed: targeted join tests pass in 0.12s; private parity improved from `onec=16`, `bslls=22`, `only_onec=16`, `only_bslls=22` to exact `onec=22`, `bslls=22`.

## Implementation Decision

- Change target: BSL206 branch in `query_text_rules.py`.
- Delta category fixed by this change: regex semantic/range mismatch for nested query join sources.
- Why not another layer: SDBL CST already models `nested_query_source` and `join_clause`.
- Rollback signal: nested query joins are missed, standalone subquery sources report, or BSL207/BSL209 behavior regresses.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k 'bsl206 or bsl207 or bsl209' --no-cov`.
- Synthetic fixture/module: joined subquery positive, initial subquery with later join positive, standalone subquery negative.
- Related-rule tests: BSL207 and BSL209 query join cluster.
- Parity command/artifact: preserved-source-root private batch BSL206 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/query_text_rules.py tests/test_diagnostics_extended.py`.
- Leak check: `git diff --check` and changed-file private token scan.
- OACS evidence/checkpoint: ingest verification outputs and add checkpoint before claiming done.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
