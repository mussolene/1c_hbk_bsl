# BSL186 — <span class="doc-lang doc-lang-ru">Запятые без указания параметра в конце вызова метода</span><span class="doc-lang doc-lang-en">Commas without a parameter at the end of a method call</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Запятые без указания параметра в конце вызова метода

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Commas without a parameter at the end of a method call

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL186` |
| Совместимый псевдоним / Compatible alias | `ExtraCommas` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `syntax`, `style` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL186` и псевдоним `ExtraCommas` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL186"]
ignore = ["ExtraCommas"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL186
Значение = "пример";  // bsl-disable: BSL186
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ExtraCommas-off
// код без этой диагностики
// BSLLS:ExtraCommas-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL186` and alias `ExtraCommas` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL186"]
ignore = ["ExtraCommas"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL186
Value = "example";  // bsl-disable: BSL186
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ExtraCommas-off
// code without this diagnostic
// BSLLS:ExtraCommas-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Не следует указывать запятую в конце вызова метода без указания параметра. Это затрудняет восприятие и не несет важной информации.
Необязательные параметры попадают под принцип Бритва Оккама "Не следует множить сущности без необходимости", так как "висящая" запятая малоинформативна.

Плохо:

```bsl
Результат = Действие(П1, П2,,);
```

Хорошо:

```bsl
Результат = Действие(П1, П2);
```

## Источники

* [Соглащения о написании кода. Параметры процедур и функций. Пункт 7](https://its.1c.ru/db/v8std#content:640:hdoc).

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Do not include a comma at the end of a method call without specifying a parameter. It is difficult to understand and does not carry important information.
Not required parameters under the principle of Occam's Razor "Do not multiply entities without need", since the "hanging" comma is not very informative.

Bad:

```bsl
Result = Action (P1, P2 ,,);
```

Good:

```bsl
Result = Action (P1, P2);
```

## Sources

* [Code-writing conventions. Parameters of procedures and functions. Item 7](https://its.1c.ru/db/v8std#content:640:hdoc).

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL186
- BSLLS compatible name: ExtraCommas
- Public name/i18n key: BSL186 / ExtraCommas
- Default message/i18n key: RULE_MESSAGES_RU["BSL186"]
- Description/i18n key: RULE_METADATA["BSL186"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: method calls must not contain an empty trailing parameter represented by a comma before the closing parenthesis.
- Why this diagnostic exists: a trailing comma in a call usually indicates a missing argument or stale edit.
- Non-goals: commas between real arguments, commas inside strings, comments, and non-call punctuation.
- Keep/drop decision: keep as a BSLLS-compatible correctness/style diagnostic.

## Semantic Oracle

- Checked language fact: a call argument list has a comma immediately before its closing parenthesis.
- Must diagnose when: `, )` appears on one line or a line ending with a comma is followed by the closing `)` line.
- Must not diagnose when: the comma separates a real next argument, appears inside a string/comment, or belongs to ordinary text.
- Diagnostic anchor token/range: the extra comma only.
- Quick-fix target, if any: remove the extra comma or add the missing argument.

## CST Or Domain Fact Model

- Parser/tree source: physical source lines with comment stripping and string-aware same-line checks.
- CST/domain object types: textual comma facts in call argument lists.
- Extracted attributes: comma line/column, next significant code line, and closing-parenthesis adjacency.
- Parse-error behavior: malformed calls may still report a clear trailing comma; BSL001 owns broader syntax structure.
- Textual/regex behavior, only if genuinely textual: textual scan is sufficient because the diagnostic anchor is a comma token before `)`.

## Examples

### Invalid Examples

1. `Метод(Параметр,);`
2. `Метод(Параметр,\n);`
3. `Метод(Первый,\n    Второй,\n);`

### Valid Negative Twins

1. `Метод(Первый, Второй);`
2. `Метод("Комментарий", "");`
3. `// Метод(Параметр,);`

### Edge Examples

- Comments: ignored.
- Strings: commas inside string literals are ignored.
- Preprocessor: not a separate semantic target.
- Multiline: trailing comma before a next-line `)` is reported.
- Nested constructs: only the comma token is diagnosed.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: call syntax and argument lists.
- Rules that may report on the same object/range: BSL001 malformed syntax and BSL212 missing required parameters may touch nearby call syntax.
- Intended duplicate policy: BSL186 owns the extra comma token; BSL001 owns broader parse errors.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestBsl186ExtraCommas`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 22.
- Line/common semantic common: 22 exact reports.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0 after adding multiline trailing comma detection.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one reverse pass builds the next-significant-line closing-parenthesis index, followed by one diagnostic pass over source lines.
- Expected complexity: O(total source characters) time and O(total source lines) compact byte-index space.
- Shared-pass reuse: standalone runtime rule.
- Before/after measurement, if changed: private batch exact 22/22 after multiline fix.

## Implementation Decision

- Change target: preserve BSL186 facts while replacing repeated forward lookahead with a linear reverse index.
- Delta category fixed by this change: performance-only; diagnostic facts and ranges must remain identical.
- Why not another layer: the rule is a textual comma adjacency check and does not need a CST pass.
- Rollback signal: `Метод("x", "")` reports, multiline `arg,\n)` stops reporting, or comments/strings report.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_line_text.py::TestBsl186ExtraCommas -q --no-cov`.
- Synthetic fixture/module: existing fixture/negative test plus private multiline parity sample.
- Related-rule tests: run BSL001/BSL212 only if call parsing changes.
- Parity command/artifact: preserved-source-root selected BSL186 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time.
- Ruff/lint: `./.venv/bin/python -m ruff check src/onec_hbk_bsl/analysis/diagnostic/diagnostic_runtime/rules.py tests/test_diagnostics_line_text.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
