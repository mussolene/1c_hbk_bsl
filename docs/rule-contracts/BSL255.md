# BSL255 — <span class="doc-lang doc-lang-ru">Приведение к числу в попытке</span><span class="doc-lang doc-lang-en">Cast to number of try catch block</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Приведение к числу в попытке

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Cast to number of try catch block

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL255` |
| Совместимый псевдоним / Compatible alias | `TryNumber` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `error-handling`, `suspicious` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL255` и псевдоним `TryNumber` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL255"]
ignore = ["TryNumber"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL255
Значение = "пример";  // bsl-disable: BSL255
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:TryNumber-off
// код без этой диагностики
// BSLLS:TryNumber-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL255` and alias `TryNumber` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL255"]
ignore = ["TryNumber"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL255
Value = "example";  // bsl-disable: BSL255
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:TryNumber-off
// code without this diagnostic
// BSLLS:TryNumber-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Неправильно использовать исключения для приведения значения к типу. Для таких операций необходимо использовать возможности объекта ОписаниеТипов.

## Примеры

Неправильно:

```bsl
Попытка
 КоличествоДнейРазрешения = Число(Значение);
Исключение
 КоличествоДнейРазрешения = 0; // значение по умолчанию
КонецПопытки;
```

Правильно:

```bsl
ОписаниеТипа = Новый ОписаниеТипов("Число");
КоличествоДнейРазрешения = ОписаниеТипа.ПривестиЗначение(Значение);
```

## Источники

* [Стандарт: Перехват исключений в коде](https://its.1c.ru/db/v8std#content:499:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

It is incorrect to use exceptions to cast a value to a type. For such operations, it is necessary to use the capabilities of the TypeDescription object.

## Examples

Incorrect:

```bsl
Try
 NumberDaysAllowance = Number(Value);
Raise
 NumberDaysAllowance = 0; // default value
EndTry;
```

Correct:

```bsl
TypeDescription = New TypeDescription("Number");
NumberDaysAllowance = TypeDescription.CastValue(Value);
```

## Sources

* [Catching Exceptions in Code (RU)](https://its.1c.ru/db/v8std#content:499:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL255
- BSLLS compatible name: TryNumber
- Public name/i18n key: TryNumber / BSL255
- Default message/i18n key: RULE_MESSAGES_RU["BSL255"]
- Description/i18n key: RULE_METADATA["BSL255"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: exception handling should not be used as the normal mechanism for converting a value to number.
- Why this diagnostic exists: `Число(...)` / `Number(...)` inside `Попытка` / `Try` bodies commonly hides conversion validation errors and makes control flow expensive.
- Non-goals: number conversions outside try bodies, conversions in exception handlers, comments/strings, and unrelated methods.
- Keep/drop decision: keep as a BSLLS-compatible suspicious error-handling diagnostic.

## Semantic Oracle

- Checked language fact: a `method_call` named `Число` / `Number` appears in the try-body part before `Исключение` / `Except`.
- Must diagnose when: a number conversion call is structurally inside the protected try block.
- Must not diagnose when: the call is outside try, inside the exception handler, or appears only in comments/strings.
- Diagnostic anchor token/range: full `Число(...)` / `Number(...)` call expression.
- Quick-fix target, if any: use explicit validation/parsing before conversion instead of exception-driven control flow.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through `DiagnosticDocumentContext`.
- CST/domain object types: `try_statement`, child nodes before `EXCEPT_KEYWORD`, and nested `method_call`.
- Extracted attributes: try-body boundary, method call name, call range, and duplicate guard.
- Parse-error behavior: if tree facts are unavailable, a bounded legacy fallback scans try depth; valid CST paths use structured try/method nodes.
- Textual/regex behavior, only if genuinely textual: fallback exists only for missing CST and is not used as the primary path.

## Examples

### Invalid Examples

1. `Попытка Результат = Число(Текст); Исключение ... КонецПопытки;`.
2. `Try Result = Number(Text); Except EndTry;`.
3. Nested call `Значение = Окр(Число(Текст));` inside the try body.

### Valid Negative Twins

1. `Результат = Число(Текст);` outside try.
2. `Исключение Результат = Число(Текст);`.
3. `"Число(Текст)"` inside a string.

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored by CST.
- Preprocessor: no separate semantic target.
- Multiline: method-call range may span lines and is anchored by CST.
- Nested constructs: nested blocks inside the try body are checked.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: try statements and suspicious method calls.
- Rules that may report on the same object/range: BSL028 empty exception, BSL151 transaction placement, BSL157 commit placement, BSL255 number conversion.
- Intended duplicate policy: BSL255 reports conversion-as-control-flow; other try rules report different try/transaction defects.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl255TryNumber`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 5,803 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final selected parity is recorded in the batch contract evidence.
- Line/common semantic common: no current deltas after BSL258 fix.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none established for this iteration.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: typed `try_statement` nodes, then nested method-call walk inside each try body.
- Expected complexity: O(try body nodes) only when BSL255 is enabled.
- Shared-pass reuse: independent runtime rule; uses existing parsed tree and typed-node cache.
- Before/after measurement, if changed: no runtime change required.

## Implementation Decision

- Change target: contract and verification only; existing implementation matches current oracle and parity.
- Delta category fixed by this change: none.
- Why not another layer: structured CST try/method facts already express the rule.
- Rollback signal: `Число(...)` inside try body stops reporting or exception-handler conversions report.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py -q -k bsl255 --no-cov`.
- Synthetic fixture/module: existing BSLLS fixture test when fixture is available; local targeted tests cover selected behavior indirectly in parity.
- Related-rule tests: BSL028/BSL151 only if try body boundary logic changes.
- Parity command/artifact: preserved-source-root private batch `--select BSL252,BSL255,BSL257,BSL258,BSL259`.
- Performance command/artifact: selected parity wall time.
- Ruff/lint: touched files.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming completion.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
