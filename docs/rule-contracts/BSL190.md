# BSL190 Contract

## Rule Identity

- Code: BSL190
- BSLLS compatible name: FormDataToValue
- Public name/i18n key: BSL190 / FormDataToValue
- Default message/i18n key: get_rule("BSL190").message
- Description/i18n key: RULE_METADATA["BSL190"].description
- Severity: WARNING
- Parameters/defaults: none
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: UI form data conversion APIs should not be used in performance-sensitive or discouraged contexts when safer alternatives exist.
- Why this diagnostic exists: It flags direct use of `ДанныеФормыВЗначение` / `FormDataToValue`.
- Non-goals: Comments, strings, and unrelated method names.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: Executable code calls `ДанныеФормыВЗначение` or `FormDataToValue`.
- Must diagnose when: A non-comment code line contains the target method call token.
- Must not diagnose when: The same text appears in a comment, string, or non-call context.
- Diagnostic anchor token/range: the method name token.
- Quick-fix target, if any: replace with the recommended conversion pattern manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.form_data_to_value_facts`.
- CST/domain object types: masked code line, method name token, comment boundary.
- Extracted attributes: line index, method name start and end character.
- Parse-error behavior: textual method-name scan can run without a valid CST; `BSL001` owns syntax errors.
- Textual/regex behavior, only if genuinely textual: the rule detects a specific API spelling after masking comments and strings.

## Examples

### Invalid Examples

1. A code line calls `ДанныеФормыВЗначение(...)`.
2. A code line calls `FormDataToValue(...)`.
3. A call appears as part of an assignment expression.

### Valid Negative Twins

1. The method name appears only in a full-line comment.
2. The method name appears only inside a string literal.
3. A different method with a similar name is used.

### Edge Examples

- Comments: skipped.
- Strings: masked before matching.
- Preprocessor: not a separate target.
- Multiline: the method name token line anchors the diagnostic.
- Nested constructs: not relevant.
- BSL/SDBL variants, if relevant: Russian and English method spellings are covered.

## Related-Rule Cluster

- Shared semantic object: UI/form API method call.
- Rules that may report on the same object/range: BSL040 and other UI/design diagnostics can report nearby UI coupling.
- Intended duplicate policy: keep BSL190 distinct because it targets a specific conversion API.
- Cluster fixture or probe: tests/test_diagnostics_extended.py::TestBsl190FormDataToValue

## BSLLS Parity Taxonomy

- Compared input-file set: not compared in this iteration because no BSL190 rule logic changed.
- Config/exclude mode: not compared in this iteration.
- File-key normalization: use rule-contract parity procedure before any semantic BSL190 edit.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: not measured in this iteration.
- Line/common semantic common: not measured in this iteration.
- Ours-only categories: not measured in this iteration.
- BSLLS-only categories: not measured in this iteration.
- Range-only categories: not measured in this iteration.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `form_data_to_value_facts`.
- Expected complexity: O(lines).
- Shared-pass reuse: uses masked line cache and comment boundaries.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: BSL190 contract and audit state, not diagnostic logic.
- Delta category fixed by this change: process-state gap.
- Why not another layer: the current masked line fact source is sufficient for this API-name diagnostic.
- Rollback signal: strings/comments report or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k bsl190 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL190 tests.
- Related-rule tests: BSL040 UI-path tests.
- Parity command/artifact: deferred until a BSL190 semantic rule change is made.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
