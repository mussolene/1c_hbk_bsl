# BSL013 — <span class="doc-lang doc-lang-ru">Закомментированный фрагмент кода</span><span class="doc-lang doc-lang-en">Commented out code</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Закомментированный фрагмент кода

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Commented out code

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL013` |
| Совместимый псевдоним / Compatible alias | `CommentedCode` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `unused` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL013` и псевдоним `CommentedCode` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL013"]
ignore = ["CommentedCode"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL013
Значение = "пример";  // bsl-disable: BSL013
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:CommentedCode-off
// код без этой диагностики
// BSLLS:CommentedCode-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL013` and alias `CommentedCode` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL013"]
ignore = ["CommentedCode"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL013
Value = "example";  // bsl-disable: BSL013
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:CommentedCode-off
// code without this diagnostic
// BSLLS:CommentedCode-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Программные модули не должны иметь закомментированных фрагментов кода, а также фрагментов,
которые каким-либо образом связаны с процессом разработки (отладочный код, служебные отметки, например, !!!_, MRG и т.п.)
и с конкретными разработчиками этого кода.

Например, недопустимо оставлять подобные фрагменты в коде после завершения отладки или рефакторинга:

```bsl
Процедура ПередУдалением(Отказ)
//    Если Истина Тогда
//        Сообщение("Для отладки");
//    КонецЕсли;
КонецПроцедуры
```
также неправильно:
```bsl
Процедура ПередУдалением(Отказ)
    Если Истина Тогда
        // Иванов: доделать
    КонецЕсли;
КонецПроцедуры
```

Правильно: после завершения отладки или рефакторинга удалить обработчик ПередУдалением из кода.

**ВНИМАНИЕ**:
Блок комментарием считается кодом, если хотя бы одна строка внутри блока определяется как код.

## Источники

* [Источник](https://its.1c.ru/db/v8std/content/456/hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Software modules should not have commented out code fragments, as well as fragments, which are in any way connected with the development process (debugging code, service marks, i.e. !!! _, MRG, etc.) and with specific developers of this code.

For example, it is unacceptable to leave such fragments in the code after debugging or refactoring is completed:

```bsl
Procedure BeforeDelete(Failure)
    //If True Then
    //  Message("For debugging");
    //EndIf;
EndProcedure
```
also wrong:
```bsl
Procedure BeforeDelete(Failure)
    If True Then
        // Ivanov: need fix
    EndIf;
EndProcedure
```

Correct: after debugging or refactoring is completed, remove the handler BeforeDelete from the code.

**ATTENTION**:
A code block is considered commented, if at least one line inside the block is defined as code.

## Sources

* [Standard: Modules texts(RU)](https://its.1c.ru/db/v8std#content:456:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL013
- BSLLS compatible name: CommentedCode
- Public name/i18n key: BSL013 / CommentedCode
- Default message/i18n key: get_rule("BSL013").message
- Description/i18n key: RULE_METADATA["BSL013"].description
- Severity: WARNING
- Parameters/defaults: none
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Dead code should be deleted instead of kept as commented source.
- Why this diagnostic exists: It prevents stale code fragments from hiding real behavior and misleading maintainers.
- Non-goals: Normal prose comments, examples explicitly marked as examples, and query comments that are not BSL code.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A full-line comment group contains disabled BSL/query source text.
- Must diagnose when: CST `line_comment` nodes form a full-line comment group that, after removing leading `//`, parses as BSL with no `BSL001` parse errors and no CST error root. Adjacent commented annotations/directives are included when the uncommented group parses as code.
- Must not diagnose when: The comment is prose, marked as an example, a markdown-style heading, an incomplete expression fragment, or only mentions BSL words but the uncommented text does not parse as BSL.
- Diagnostic anchor token/range: the full-line comment group range.
- Quick-fix target, if any: delete the commented code manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.commented_code_facts`.
- CST/domain object types: `line_comment` CST nodes, grouped adjacent full-line comments, and BSL parse result of uncommented comment text.
- Extracted attributes: group start/end node ranges, uncommented text, example marker state, query-continuation state, and whether the uncommented group parses without `BSL001`.
- Parse-error behavior: comment groups are considered commented code only when their uncommented text parses without `BSL001` and without CST root errors.
- Textual/regex behavior, only if genuinely textual: regex is retained only for non-tree-sitter fallback snapshots and query-continuation legacy behavior; tree-sitter snapshots use CST comment nodes plus BSL parse success as the primary source.

## Examples

### Invalid Examples

1. A block of `// Если ... Тогда` code lines.
2. A comment group containing a method call with BSL syntax.
3. A full-line comment group containing a complete disabled BSL statement.
4. A commented BSL annotation adjacent to a commented method body.
5. A commented BSL preprocessor block.

### Valid Negative Twins

1. A prose comment explaining business behavior.
2. A comment explicitly marked as an example.
3. A comment that mentions keywords without executable structure.
4. A markdown-style heading such as `// # Heading`.
5. A prose comment containing an embedded expression-like phrase that does not parse as a BSL statement/group.
6. A single commented expression continuation that cannot parse without surrounding live code.
7. Inline trailing comments after live code.

### Edge Examples

- Comments: comments are the target.
- Strings: string contents inside active code are not comment groups.
- Preprocessor: commented BSL preprocessor directives are code-shaped fragments; markdown-style headings are not.
- Multiline: adjacent comment lines are grouped.
- Nested constructs: code-like nested comment groups are reported as one group.
- BSL/SDBL variants, if relevant: query-looking comment lines can be classified as commented code.

## Related-Rule Cluster

- Shared semantic object: comment text and disabled code fragments.
- Rules that may report on the same object/range: BSL150 and typo/comment rules can inspect comment text; BSL014 can report long comment lines.
- Intended duplicate policy: keep distinct stale-code and textual-quality diagnostics.
- Cluster fixture or probe: tests/test_diagnostics_core_snapshot.py::TestBsl013CommentedCode

## BSLLS Parity Taxonomy

- Compared input-file set: original-workspace corpus parity through `check --no-config --select BSL013` and BSLLS `diagnostics.mode=ONLY`.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS `ONLY` mode.
- File-key normalization: original source-root relative keys with BSLLS LSP line conversion.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: private BSLLS 1.0 batch after CST parse-based alignment has onec 666, BSLLS 1198, only-BSLLS 702, and only-onec 170. The remaining BSLLS-only side is dominated by BSLLS lexical/inline recognizer behavior that onec intentionally rejects unless the full-line uncommented group parses as BSL.
- Line/common semantic common: private batch has shared comment-code facts plus intentionally divergent grouping for some disabled procedure blocks.
- Ours-only categories: sampled short/generic commented procedure groups, inline assignment comments, annotation+method groups, and grouped disabled procedure bodies are intentionally retained as product diagnostics when the uncommented group parses as BSL.
- BSLLS-only categories: private batch largest classified category after CST parse alignment is BSLLS lexical/inline recognizer behavior and incomplete fragments. onec rejects these as product-correct negative twins unless the full-line uncommented group parses as BSL.
- Range-only categories: none reproduced in the sampled BSLLS-recognized cases.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `line_comment` CST node grouping in `commented_code_facts`, with line fallback only when tree-sitter is unavailable.
- Expected complexity: O(comment nodes) plus one small BSL parse per adjacent full-line comment group and per inline candidate.
- Shared-pass reuse: none.
- Before/after measurement, if changed: private corpus `--select BSL013 --jobs 1` measured real 83.46s after CST parse-based alignment.

## Implementation Decision

- Change target: BSL013 comment source and fragment classifier: primary tree-sitter path now reads full-line `line_comment` groups and classifies uncommented groups by strict successful BSL parse, instead of broadening token regexes.
- Delta category fixed by this change: BSLLS-only/pre-existing directive and fragment categories are re-evaluated through CST comment nodes; exact batch counts are recorded after implementation.
- Why not another layer: the snapshot already owns the parsed CST and project parser, so BSL013 can reuse that fact layer without a separate tokenizer artifact.
- Rollback signal: prose comments with BSL-looking words report as BSL013, markdown-style prose headings such as `// # Heading` report as BSL013, or clear commented code fragments stop reporting.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_core_snapshot.py -q -k bsl013 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL013 tests.
- Related-rule tests: comment and suppression tests.
- Parity command/artifact: sampled local synthetic taxonomy against BSLLS 0.29.0 plus original-workspace aggregate.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
