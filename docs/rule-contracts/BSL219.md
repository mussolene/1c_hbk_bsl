# BSL219 — <span class="doc-lang doc-lang-ru">Все объявления переменных должны иметь описание</span><span class="doc-lang doc-lang-en">All variables declarations must have a description</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Все объявления переменных должны иметь описание

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

All variables declarations must have a description

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL219` |
| Совместимый псевдоним / Compatible alias | `MissingVariablesDescription` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `documentation`, `convention` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL219` и псевдоним `MissingVariablesDescription` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL219"]
ignore = ["MissingVariablesDescription"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL219
Значение = "пример";  // bsl-disable: BSL219
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MissingVariablesDescription-off
// код без этой диагностики
// BSLLS:MissingVariablesDescription-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL219` and alias `MissingVariablesDescription` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL219"]
ignore = ["MissingVariablesDescription"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL219
Value = "example";  // bsl-disable: BSL219
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MissingVariablesDescription-off
// code without this diagnostic
// BSLLS:MissingVariablesDescription-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Все переменные модулей, а также все экспортируемые переменные должны иметь комментарии.

## Примеры

Неправильно:

```bsl
Перем Контекст;
```

Правильно:

```bsl
Перем Контекст; // Некое подробное описание, которое объясняет назначение переменной

// Некое подробное описание, которое объясняет назначение переменной
Перем Контекст;
```

## Источники

* Источник: [Соглашения при написания кода. Структура модуля](https://its.1c.ru/db/v8std#content:455:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

All module variables and export variables must have comments.

## Examples

Incorrect:

```bsl
Var Context;
```

Correct:

```bsl
Var Context; // Detailed description that explains the purpose of the variable

// Detailed description that explains the purpose of the variable
Var Context;
```

## Sources

* Source: [Standard: Module structure (RU)](https://its.1c.ru/db/v8std#content:455:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL219
- BSLLS compatible name: MissingVariablesDescription
- Public name/i18n key: BSL219 / MissingVariablesDescription
- Default message/i18n key: get_rule("BSL219").message
- Description/i18n key: RULE_METADATA["BSL219"].description
- Severity: INFORMATION
- Parameters/defaults: none
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Module-level variables should have descriptions that explain their purpose.
- Why this diagnostic exists: It flags undocumented module-level `Перем` / `Var` declarations.
- Non-goals: Local procedure variables, variables with preceding descriptions, and variables with inline descriptions.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A module-level variable declaration lacks an accepted description.
- Must diagnose when: A module-level `Перем` / `Var` declaration, including multiline declarations, has no preceding, inline, or previous-inline variable description.
- Must not diagnose when: The declaration is inside a procedure/function or has an accepted description.
- Diagnostic anchor token/range: the declared variable names in the module-level declaration.
- Quick-fix target, if any: add a variable description comment manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.module_variable_description_facts`.
- CST/domain object types: module-level clean code line, `ProcInfo` ranges, variable declaration match, description comment predicates.
- Extracted attributes: line index, per-variable name start/end, exported declaration span for the last name, procedure containment, description presence, multiline declaration continuation lines.
- Parse-error behavior: skip uncertain procedure containment when the procedure model is unavailable; `BSL001` owns syntax errors.
- Textual/regex behavior, only if genuinely textual: variable declaration and description comments are textual documentation conventions.

## Examples

### Invalid Examples

1. A module-level `Перем Имя;` declaration without description.
2. A module-level `Var Name;` declaration without description.
3. A module-level exported variable declaration without description.
4. A module-level declaration with multiple undocumented variable names.
5. A multiline module-level declaration with undocumented continuation names.

### Valid Negative Twins

1. A module-level variable with a preceding description comment.
2. A module-level variable with an inline description.
3. A local variable declaration inside a procedure.

### Edge Examples

- Comments: description comments can satisfy the rule.
- Compiler directives: a description comment before adjacent compiler directives satisfies the declaration below; previous-inline group descriptions do not cross blank lines.
- Strings: strings are not variable declarations.
- Preprocessor: not a separate target.
- Multiline: declaration facts are anchored on the continuation lines that contain variable names; continuation inline comments describe that variable.
- Nested constructs: procedure/function ranges exclude local declarations.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: module-level variable declaration.
- Rules that may report on the same object/range: BSL054 and module-state rules can report global variable usage; BSL014 can report long declaration lines.
- Intended duplicate policy: keep documentation diagnostics distinct from global-state diagnostics.
- Cluster fixture or probe: tests/test_diagnostics_core_snapshot.py::TestBsl219MissingVariablesDescription

## BSLLS Parity Taxonomy

- Compared input-file set: local synthetic BSL219 taxonomy plus original-workspace batch aggregate.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS default behavior.
- File-key normalization: synthetic labels for local probe; original source-root relative keys for private batch aggregate.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: sampled single variable, export variable, multi-name, multi-export, described-variable, local-variable, multiline declaration, inline-continuation-description, comment-before-directive, and blank-line-boundary cases match BSLLS exactly; current preserved-source-root private batch is exact at 492/492.
- Line/common semantic common: sampled module-level declaration semantics match.
- Ours-only categories: none reproduced in the sampled synthetic taxonomy.
- BSLLS-only categories: previous preserved-source-root batch had multiline declaration continuation facts and blank-line previous-inline boundary facts; onec now matches both categories.
- Range-only categories: sampled multi-name declaration was onec single-span vs BSLLS per-name spans before an earlier iteration; onec emits per-name facts. Current preserved-source-root batch has zero range deltas.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `module_variable_description_facts`.
- Expected complexity: O(lines + procedures).
- Shared-pass reuse: uses procedure ranges and comment helpers from `DocumentSnapshot`.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: `DocumentSnapshot.module_variable_description_facts`, BSL219 contract, and synthetic coverage.
- Delta category fixed by this change: semantic/range parity for multiline declarations, compiler-directive-adjacent descriptions, inline continuation descriptions, and blank-line boundary of previous-inline group descriptions.
- Why not another layer: variable documentation depends on module-level line classification and comment context; per-name ranges belong in the fact extractor.
- Rollback signal: local variables report, multi-name declarations collapse into one diagnostic, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_core_snapshot.py -q -k bsl219 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL219 tests, including multi-name declarations.
- Related-rule tests: module variable/global-state diagnostics.
- Parity command/artifact: preserved-source-root BSL219 parity against BSLLS 1.0 over the private corpus; sanitized aggregate result `onec=492`, `bslls=492`, exact match by file/range/code.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
