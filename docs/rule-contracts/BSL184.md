# BSL184 — <span class="doc-lang doc-lang-ru">Выполнение произвольного кода в общем модуле на сервере</span><span class="doc-lang doc-lang-en">Executing of external code in a common module on the server</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Выполнение произвольного кода в общем модуле на сервере

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Executing of external code in a common module on the server

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL184` |
| Совместимый псевдоним / Compatible alias | `ExecuteExternalCodeInCommonModule` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `security`, `module` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL184` и псевдоним `ExecuteExternalCodeInCommonModule` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL184"]
ignore = ["ExecuteExternalCodeInCommonModule"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL184
Значение = "пример";  // bsl-disable: BSL184
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ExecuteExternalCodeInCommonModule-off
// код без этой диагностики
// BSLLS:ExecuteExternalCodeInCommonModule-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL184` and alias `ExecuteExternalCodeInCommonModule` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL184"]
ignore = ["ExecuteExternalCodeInCommonModule"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL184
Value = "example";  // bsl-disable: BSL184
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ExecuteExternalCodeInCommonModule-off
// code without this diagnostic
// BSLLS:ExecuteExternalCodeInCommonModule-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При разработке решений следует учитывать, что опасно использование не только непосредственного выполнения кода, написанного в режиме Предприятие, но и алгоритмов, где методами `Выполнить` или `Вычислить` исполняется код в серверных функциях и процедурах.
Если выполнение произвольного кода необходимо, то такой код должен предварительно располагаться в общем модуле и пройти аудит.

**Ограничение не распространяется на код, выполняемый на клиенте.**

## Примеры

## Источники

* [Ограничения на использование Выполнить и Вычислить на сервере](https://its.1c.ru/db/v8std#content:770:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

When you develop applications, note that not only execution of a code written in the Enterprise mode is unsafe, but also places, where the `Execute` or `Eval` methods are used to execute the code created based on parameters passed to server functions and procedures.
If the execution of arbitrary code Is necessary then it has to be preliminarily checked.

**This restriction is not applicable to the code being executed on the client.**

## Examples

## Sources

* [Restrictions on the use of Execute and Eval on the server (RU)](https://its.1c.ru/db/v8std#content:770:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL184
- BSLLS compatible name: ExecuteExternalCodeInCommonModule
- Public name/i18n key: BSL184 / ExecuteExternalCodeInCommonModule
- Default message/i18n key: RULE_MESSAGES_RU["BSL184"]
- Description/i18n key: RULE_METADATA["BSL184"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: server common modules must not execute arbitrary enterprise-mode code through `Выполнить` or `Вычислить`.
- Why this diagnostic exists: dynamic code execution on the server is a security risk and should be audited or replaced by explicit APIs.
- Non-goals: client-only common modules, non-common modules owned by BSL183, comments, strings without an actual call, or member calls such as `Запрос.Выполнить()`.
- Keep/drop decision: keep as the common-module counterpart of BSL183.

## Semantic Oracle

- Checked language fact: a server-capable common module calls global `Выполнить/Execute` or `Вычислить/Eval`.
- Must diagnose when: the file is a common module applicable on server and a global dynamic-code call appears in executable code.
- Must not diagnose when: the common module is not server-capable, the call text is in a comment/string only, or the name is part of another identifier/member call.
- Diagnostic anchor token/range: full dangerous call from method name through the outer closing parenthesis.
- Quick-fix target, if any: remove dynamic execution or move audited explicit logic into a safe common module API.

## CST Or Domain Fact Model

- Parser/tree source: common-module metadata scope plus source-line scan with string/comment masking.
- CST/domain object types: common module path/metadata applicability, physical source line, global dynamic-code call span.
- Extracted attributes: file path, server applicability, method name, call start, multiline call end.
- Parse-error behavior: clear call spelling can still report; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: exact call-name scan is allowed after string/comment masking; the rule is a small security API usage detector.

## Examples

### Invalid Examples

1. Server common module: `Выполнить(Строка);`
2. Server common module: `Возврат Вычислить(Строка);`
3. Server common module: `Eval("...")`

### Valid Negative Twins

1. Non-server common module: `Выполнить(Строка);`
2. Non-common module: `Выполнить(Строка);`
3. Query object method call: `Результат = Запрос.Выполнить();`

### Edge Examples

- Comments: ignored by masking.
- Strings: call-looking text inside strings is ignored, but real call arguments may be strings.
- Preprocessor: not a separate semantic target.
- Multiline: call range may span multiple physical lines.
- Nested constructs: nesting does not change the security fact.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: dynamic code execution on server.
- Rules that may report on the same object/range: BSL183 covers non-common modules, BSL200 covers broader execute/eval usage, and BSL267 covers external-code tools.
- Intended duplicate policy: BSL184 owns common modules; BSL183 excludes them.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl184_execute_external_code_in_common_module`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: metadata applicability check and one pass over source lines.
- Expected complexity: O(total source characters) for applicable common modules; early return otherwise.
- Shared-pass reuse: shares call regex and range helpers with BSL183 dynamic-code security rules.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: existing metadata scope plus masked line scan expresses the security fact.
- Rollback signal: non-server common modules report, member calls report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl184 or ExecuteExternalCodeInCommonModule"`.
- Synthetic fixture/module: existing tests cover server common module positives and non-server common module negative.
- Related-rule tests: BSL183 if common-module routing changes.
- Parity command/artifact: preserved-source-root selected BSL184 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
