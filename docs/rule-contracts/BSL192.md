# BSL192 FunctionNameStartsWithGet Contract

## Rule Identity

- Code: BSL192
- BSLLS compatible name: FunctionNameStartsWithGet
- Public name/i18n key: FunctionNameStartsWithGet / BSL192
- Default message/i18n key: RULE_MESSAGES_RU["BSL192"]
- Description/i18n key: RULE_METADATA["BSL192"].description
- Severity: INFORMATION
- Parameters/defaults: none. BSLLS marks this diagnostic as not activated by default; onec supports explicit selection/config enablement through normal rule selection.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: function names should describe returned values without the redundant Russian verb prefix `Получить`.
- Why this diagnostic exists: `Получить*` function names add noise and duplicate the fact that functions return values.
- Non-goals: do not diagnose procedures; do not diagnose English `Get*` names because BSLLS only checks Russian `Получить.*`.
- Keep/drop decision: keep as a BSLLS-compatible naming diagnostic.

## Semantic Oracle

- Checked language fact: a function declaration name starts with `Получить`, case-insensitively.
- Must diagnose when: `proc.kind == "function"` and the function name matches `^Получить.*$`.
- Must not diagnose when: the routine is a procedure, the function name starts with English `Get`, the function name contains `Получить` after the first character, or the method declaration is not parsed.
- Diagnostic anchor token/range: function identifier token.
- Quick-fix target, if any: rename the function and update callers manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` / `ProcInfo`, created from tree-sitter where available.
- CST/domain object types: function/procedure declaration and identifier span.
- Extracted attributes: routine kind, routine name, start line, identifier range.
- Parse-error behavior: only parsed procedure/function facts are checked; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: regex applies only to the already extracted function name, not to source text.

## Examples

### Invalid Examples

1. `Функция ПолучитьДанные()`.
2. `Функция получитьЗначение()`.
3. `Функция Получить()`.

### Valid Negative Twins

1. `Процедура ПолучитьДанные()`.
2. `Function GetData()`.
3. `Функция ДанныеПолучить()`.

### Edge Examples

- Comments: ignored.
- Strings: ignored.
- Preprocessor: compiler directives before the function do not change the function name fact.
- Multiline: multiline signatures use `ProcInfo` name span.
- Nested constructs: nested parsed function declarations are checked independently.
- BSL/SDBL variants, if relevant: query text is irrelevant.

## Related-Rule Cluster

- Shared semantic object: method declaration name.
- Rules that may report on the same object/range: BSL008/BSL011/BSL019/BSL020/BSL031/BSL032/BSL148 may report on the same function declaration or body; BSL193/BSL194/BSL228/BSL266 share the method-contract runtime pass.
- Intended duplicate policy: keep BSL192 independent; naming and control-flow/parameter diagnostics explain different user actions.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestBsl192FunctionNameStartsWithGet`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final aggregate has exact match, `onec=2390`, `bslls=2390`.
- Line/common semantic common: all selected diagnostics match by file, range, and code.
- Ours-only categories: none after removing English `Get*` from the predicate.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none for this rule in the final batch.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing method contract pass over parsed `ProcInfo` records.
- Expected complexity: O(number of parsed methods).
- Shared-pass reuse: shared with BSL193, BSL194, BSL228, and BSL266.
- Before/after measurement, if changed: no dedicated timing needed; predicate narrowed only.

## Implementation Decision

- Change target: BSL192 name predicate, focused tests, contract/audit.
- Delta category fixed by this change: semantic mismatch with BSLLS for English `Get*` names.
- Why not another layer: the rule is a method-name fact and belongs in the existing method-contract pass.
- Rollback signal: private parity shows any BSL192 only_ours/only_bslls/range delta, or tests show English `Get*` being diagnosed again.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k bsl192 --no-cov`.
- Synthetic fixture/module: inline tests cover Russian function, English function, and Russian procedure.
- Related-rule tests: method-contract regression tests if shared pass changes structurally.
- Parity command/artifact: preserved-source-root private batch BSL192 parity, final exact.
- Performance command/artifact: no dedicated performance artifact; predicate narrowed.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostics.py tests/test_diagnostics_extended.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
