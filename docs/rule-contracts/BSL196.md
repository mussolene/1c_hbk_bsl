# BSL196 — <span class="doc-lang doc-lang-ru">Конфликт имен методов с методами глобального контекста</span><span class="doc-lang doc-lang-en">Global context method names collision</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Конфликт имен методов с методами глобального контекста

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Global context method names collision

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL196` |
| Совместимый псевдоним / Compatible alias | `GlobalContextMethodCollision8312` |
| Серьёзность / Severity | `ERROR` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `correctness`, `compatibility` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL196` и псевдоним `GlobalContextMethodCollision8312` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL196"]
ignore = ["GlobalContextMethodCollision8312"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL196
Значение = "пример";  // bsl-disable: BSL196
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:GlobalContextMethodCollision8312-off
// код без этой диагностики
// BSLLS:GlobalContextMethodCollision8312-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL196` and alias `GlobalContextMethodCollision8312` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL196"]
ignore = ["GlobalContextMethodCollision8312"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL196
Value = "example";  // bsl-disable: BSL196
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:GlobalContextMethodCollision8312-off
// code without this diagnostic
// BSLLS:GlobalContextMethodCollision8312-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Начиная с версии платформы `8.3.12`, реализованы новые методы глобального контекста, которые могут совпаcть по имени с существующими функциями в коде конфигурации прикладного решений.

Метод|Английский вариант
:-: | :-:
ПроверитьБит|CheckBit
ПроверитьПоБитовойМаске|CheckByBitMask
УстановитьБит|SetBit
ПобитовоеИ|BitwiseAnd
ПобитовоеИли|BitwiseOr
ПобитовоеНе|BitwiseNot
ПобитовоеИНе|BitwiseAndNot
ПобитовоеИсключительноеИли|BitwiseXor
ПобитовыйСдвигВлево|BitwiseShiftLeft
ПобитовыйСдвигВправо|BitwiseShiftRight

Необходимо существующие функции конфигурации прикладного решения переименовать или удалить, заменив обращение к ним на методы глобального контекста.

## Примеры

## Источники

* Источник: [Перевод конфигураций на платформу "1С:Предприятие 8.3" без режима совместимости с версией 8.2](https://its.1c.ru/db/metod8dev#content:5293:hdoc:pereimenovaniya_metodov_i_svojstv)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

The platform version `8.3.12` implements new methods of the global context, which may coincide with the configuration functions of the application solution.

Russian variant|English variant
:-: | :-:
ПроверитьБит|CheckBit
ПроверитьПоБитовойМаске|CheckByBitMask
УстановитьБит|SetBit
ПобитовоеИ|BitwiseAnd
ПобитовоеИли|BitwiseOr
ПобитовоеНе|BitwiseNot
ПобитовоеИНе|BitwiseAndNot
ПобитовоеИсключительноеИли|BitwiseXor
ПобитовыйСдвигВлево|BitwiseShiftLeft
ПобитовыйСдвигВправо|BitwiseShiftRight

The configuration functions must either be renamed or deleted, replacing the call to them with the methods of the global context.

## Examples

## Sources

* [Transfer of configurations to the 1C: Enterprise 8.3 platform without compatibility mode with version 8.2 (RU)](https://its.1c.ru/db/metod8dev#content:5293:hdoc:pereimenovaniya_metodov_i_svojstv)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL196
- BSLLS compatible name: GlobalContextMethodCollision8312
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: Platform 8.3.12 introduced global context bit methods; configuration methods with the same names must be renamed or removed to avoid unpredictable resolution.
- Non-goals: Do not diagnose calls to platform methods; only declarations in the configuration module are checked.

## Semantic Oracle

- Checked language fact: A procedure or function declaration name equals one of the 8.3.12 global bit-method names.
- Must diagnose when: a method declaration is named `ПроверитьБит`, `ПроверитьПоБитовойМаске`, `УстановитьБит`, `ПобитовоеИ`, `ПобитовоеИли`, `ПобитовоеНе`, `ПобитовоеИНе`, `ПобитовоеИсключительноеИли`, `ПобитовыйСдвигВлево`, `ПобитовыйСдвигВправо`, or the documented English equivalents.
- Must not diagnose when: the name appears only as a call, string, comment, or longer identifier.
- Diagnostic anchor token/range: The colliding method declaration name.

## CST Or Domain Fact Model

- CST/domain object types: module method model / procedure declaration symbol.
- Extracted attributes: method name, declaration start line, method-name span.
- Parse-error behavior: Do not report if the module model cannot prove a method declaration.
- Shared semantic object: Declared module method symbol.

## Examples

### Invalid Examples

1. `Процедура ПроверитьБит()`.
2. `Function BitwiseShiftRight()`.
3. `Функция ПобитовоеИли()`.

### Valid Negative Twins

1. `ПроверитьБит(Значение);` as a call.
2. `Процедура ПроверитьБитДополнительно()`.
3. `// Процедура ПроверитьБит()`.

## Related-Rule Cluster

- Rules that may report on the same object/range: BSL042, BSL077, BSL169.
- Intended duplicate policy: Keep BSL196 for global-context name collision; other declaration rules may still report separate defects on the same declaration.

## BSLLS Parity Taxonomy

- Compared input-file set: Private representative configuration corpus with 5803 BSL files plus synthetic fixtures.
- Config/exclude mode: Preserve source root for BSLLS; normal onec CLI path.
- File-key normalization: Repository parity runner normalized relative paths.
- Coordinate normalization: UTF-8 byte columns converted to LSP character columns.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: Existing module method model.
- Expected complexity: O(number of declared methods), no additional CST traversal.

## Implementation Decision

- Change target: No runtime change required; existing module-model implementation matches the contract.
- Delta category fixed by this change: No BSLLS delta on the compared corpus; added isolated synthetic coverage.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q -k BSL196 --no-cov`.
- Leak check: `git diff --check` and secret-pattern scan of staged/documented changes.
- OACS evidence/checkpoint: Record verification commands and checkpoint after successful parity run.

## Completion Checklist

- [x] Contract has semantic oracle.
- [x] Contract has CST/domain fact model.
- [x] Contract has positive and negative examples.
- [x] Contract has parity taxonomy.
- [x] Contract has performance contract.
- [x] Contract has verification plan.

</div>

</details>

<!-- engineering-contract:end -->
