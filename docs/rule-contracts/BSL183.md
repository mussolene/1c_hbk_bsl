# BSL183 — <span class="doc-lang doc-lang-ru">Выполнение произвольного кода на сервере</span><span class="doc-lang doc-lang-en">Executing of external code on the server</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Выполнение произвольного кода на сервере

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Executing of external code on the server

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL183` |
| Совместимый псевдоним / Compatible alias | `ExecuteExternalCode` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `security` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL183` и псевдоним `ExecuteExternalCode` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL183"]
ignore = ["ExecuteExternalCode"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL183
Значение = "пример";  // bsl-disable: BSL183
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ExecuteExternalCode-off
// код без этой диагностики
// BSLLS:ExecuteExternalCode-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL183` and alias `ExecuteExternalCode` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL183"]
ignore = ["ExecuteExternalCode"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL183
Value = "example";  // bsl-disable: BSL183
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ExecuteExternalCode-off
// code without this diagnostic
// BSLLS:ExecuteExternalCode-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При разработке решений следует учитывать, что опасно использование не только непосредственного выполнения кода, написанного в режиме Предприятие, но и алгоритмов, где методами `Выполнить` или `Вычислить` исполняется код в серверных функциях и процедурах.
Запрещено использование методов `Выполнить` и `Вычислить` в серверных методах модулей форм, команд, объектов и т.д.

**Ограничение не распространяется на код, выполняемый на клиенте.**

## Примеры

## Источники

* [Ограничения на использование Выполнить и Вычислить на сервере](https://its.1c.ru/db/v8std#content:770:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

When you develop applications, note that not only execution of a code written in the Enterprise mode is unsafe, but also places, where the `Execute` or `Eval` methods are used to execute the code created based on parameters passed to server functions and procedures.
It is forbidden to use the `Execute` and` Eval` methods in server methods of form modules, commands, objects, etc.

**This restriction is not applicable to the code being executed on the client**

## Examples

## Sources

* [Restrictions on the use of Execute and Eval on the server (RU)](https://its.1c.ru/db/v8std#content:770:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL183
- BSLLS compatible name: ExecuteExternalCode
- Public name/i18n key: BSL183 / ExecuteExternalCode
- Default message/i18n key: RULE_MESSAGES_RU["BSL183"]
- Description/i18n key: RULE_METADATA["BSL183"].description
- Severity: ERROR
- Parameters/defaults: none; BSLLS config can disable `ExecuteExternalCode`.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: server-side BSL must not execute arbitrary enterprise-mode code through `Выполнить` or `Вычислить`.
- Why this diagnostic exists: executing dynamic code on the server is a critical security risk.
- Non-goals: client-only methods, common modules handled by BSL184, split-module fragments duplicated from aggregate modules, ordinary form `Ext/module.bsl` client modules, comments, strings without a call, and member calls.
- Keep/drop decision: keep as a BSLLS-compatible security diagnostic.

## Semantic Oracle

- Checked language fact: non-client, non-common-module source calls global `Выполнить/Execute` or `Вычислить/Eval`.
- Must diagnose when: a global dynamic-code call appears in object modules, managed form modules, command modules, or other non-client module contexts.
- Must not diagnose when: the containing method is client-only, the file is a common module, the file is a split fragment, or the file is an ordinary form `Ext/module.bsl`.
- Diagnostic anchor token/range: full dangerous call from method name through the outer closing parenthesis, including multiline string arguments.
- Quick-fix target, if any: remove dynamic execution or replace it with explicit API calls/dispatch.

## CST Or Domain Fact Model

- Parser/tree source: file-level line scan with procedure/client directive ranges.
- CST/domain object types: procedure ranges from snapshot/fallback parser, client-only method ranges, dynamic-code global call facts.
- Extracted attributes: path module kind, split-fragment status, ordinary-form path, call method name, call start, multiline call end, and client-only procedure range.
- Parse-error behavior: clear call spelling can still report; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: exact method-name matching is used after string/comment masking; the diagnostic span is allowed to include string arguments.

## Examples

### Invalid Examples

1. `Выполнить(Строка);` in `&НаСервере` method.
2. `Возврат Вычислить(Строка);` in a method without a client-only directive.
3. `Eval("...")` in an object module.

### Valid Negative Twins

1. `&НаКлиенте` method containing `Выполнить(Строка);`.
2. Common module code, which is owned by BSL184.
3. Ordinary form `Forms/.../Ext/module.bsl`, matching BSLLS BSL183 scope.

### Edge Examples

- Comments: ignored by masking.
- Strings: full diagnostic spans may include string arguments and are explicitly allowed through the string-overlap filter.
- Preprocessor: not a separate semantic target.
- Multiline: call range can span multiple physical lines.
- Nested constructs: nested assignments/conditions do not change the rule.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: dynamic code execution and external-code security APIs.
- Rules that may report on the same object/range: BSL184 covers common modules; BSL200 covers `Выполнить/Execute` broader runtime usage; BSL267 covers external code tools.
- Intended duplicate policy: BSL183 excludes common modules and split fragments so BSL184/aggregate modules own those surfaces.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl183_execute_external_code_matches_bslls_fixture`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 155.
- Line/common semantic common: 155 exact reports.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0 after multiline call range and string-overlap allowlist fixes.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one file-level pass over lines plus procedure/directive ranges.
- Expected complexity: O(total source characters).
- Shared-pass reuse: uses existing runtime rule and path/split helpers.
- Before/after measurement, if changed: private batch exact 155/155 after scope and range fixes.

## Implementation Decision

- Change target: BSL183 runtime rule, runtime fork grouping, string-overlap allowlist, contract, and audit state.
- Delta category fixed by this change: server-scope under-reporting, multiline range mismatch, string-argument filtering, split-fragment duplicates, common-module duplicates, and ordinary-form scope noise.
- Why not another layer: the rule's fact is a small global-call/path-scope policy and does not require a heavier semantic pass.
- Rollback signal: common modules report BSL183, split fragments duplicate aggregate module reports, string-argument calls disappear, or multiline calls shrink to method-name-only ranges.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl183_execute_external_code_matches_bslls_fixture -q --no-cov`.
- Synthetic fixture/module: existing fixture covers client-only, server, no directive, client-server directives, and method-name negatives.
- Related-rule tests: run BSL184 if common-module routing changes.
- Parity command/artifact: preserved-source-root selected BSL183 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time.
- Ruff/lint: `./.venv/bin/python -m ruff check src/onec_hbk_bsl/analysis/diagnostic/diagnostic_runtime/rules.py src/onec_hbk_bsl/analysis/diagnostic/diagnostic_runtime/runner.py src/onec_hbk_bsl/analysis/diagnostics.py tests/test_diagnostics_line_text.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
