# BSL029 — <span class="doc-lang doc-lang-ru">Магические числа</span><span class="doc-lang doc-lang-en">Magic numbers</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Магические числа

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Magic numbers

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL029` |
| Совместимый псевдоним / Compatible alias | `MagicNumber` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `convention`, `readability` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL029` и псевдоним `MagicNumber` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL029"]
ignore = ["MagicNumber"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL029
Значение = "пример";  // bsl-disable: BSL029
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MagicNumber-off
// код без этой диагностики
// BSLLS:MagicNumber-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL029` and alias `MagicNumber` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL029"]
ignore = ["MagicNumber"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL029
Value = "example";  // bsl-disable: BSL029
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MagicNumber-off
// code without this diagnostic
// BSLLS:MagicNumber-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Магические числа - это любое число в вашем коде, которое не сразу становится очевидным без погружения в контекст.

## Примеры

Плохо

```bsl
Функция ПопадаетВИнтервал(Длительность)

    Возврат Длительность < 10 * 60 * 60;

КонецФункции
```

Хорошо

```bsl
Функция ПопадаетВИнтервал(ДлительностьВСекундах)

    МинутВЧасе      = 60;
    СекундВМинуте   = 60;
    СекундВЧасе     = СекундВМинуте * МинутВЧасе;
    ЧасовВИнтервале = 10;
    Возврат Длительность < ЧасовВИнтервале * СекундВЧасе;

КонецФункции
```

## Исключения

Магические числа, используемые в структурах и соответствиях, не считаются ошибками, так как они используются в качестве ключей или значений в структурах данных, где контекст понятен:

```bsl
// Операция Вставить структуры - нет ошибки
Структура = Новый Структура;
Структура.Вставить("МояПеременная", 20); // Нет ошибки
Структура.Вставить("ДругаяПеременная", 42); // Нет ошибки

// Конструктор структуры - нет ошибки
Структура2 = Новый Структура("Поле1, Поле2", 5, 15); // Нет ошибки

// Прямое присваивание свойству структуры - нет ошибки
СтруктураСПолями = Новый Структура("МояПеременная, ДругоеПоле");
СтруктураСПолями.МояПеременная = 20; // Нет ошибки
СтруктураСПолями.ДругоеПоле = 50; // Нет ошибки

// Фиксированная структура - нет ошибки
ФиксированнаяСтруктура = Новый ФиксированнаяСтруктура("Значение", 200); // Нет ошибки

// Соответствие - нет ошибки (как ключ, так и значение)
Соответствие = Новый Соответствие;
Соответствие.Вставить("Код", 123); // Нет ошибки
Соответствие.Вставить(1980, "Олимпиада в Москве"); // Нет ошибки
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Magic numbers are any number in your code that does not immediately become apparent without being immersed in context.

## Examples

Bad

```bsl
Function GetsTheInterval(Duration)

     Return Duration < 10 * 60 * 60;

End Function
```

Good

```bsl
Function GetsTheInterval (Duration in Seconds)

    MinutesHour     = 60;
    SecondsMinute   = 60;
    SecondsHour     = SecondsMinute * MinutesHour;
    HoursIninterval = 10;
    Return Duration < HoursWininterval * SecondsHour;

End Function
```

## Exceptions

Magic numbers used in structures and correspondences are not considered errors, as they are used as keys or values in data structures where the context is clear:

```bsl
// Structure insert - no error
Structure = New Structure;
Structure.Insert("MyVariable", 20); // No error
Structure.Insert("AnotherVariable", 42); // No error

// Structure constructor - no error
Structure2 = New Structure("Field1, Field2", 5, 15); // No error

// Direct structure property assignment - no error
StructureWithFields = New Structure("MyVariable, AnotherField");
StructureWithFields.MyVariable = 20; // No error
StructureWithFields.AnotherField = 50; // No error

// Fixed structure - no error
FixedStructure = New FixedStructure("Value", 200); // No error

// Correspondence - no error (both key and value)
Correspondence = New Correspondence;
Correspondence.Insert("Code", 123); // No error
Correspondence.Insert(1980, "Olympics in Moscow"); // No error
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL029
- BSLLS compatible name: MagicNumber
- Public name/i18n key: BSL029 / MagicNumber
- Default message/i18n key: RULE_MESSAGES_RU["BSL029"]
- Description/i18n key: RULE_METADATA["BSL029"].description
- Severity: INFORMATION
- Parameters/defaults: `authorizedNumbers`, default `-1,0,1`; `allowMagicIndexes`, default `true` in BSLLS-compatible policy.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: unexplained numeric literals reduce readability; symbolic constants or named variables should explain their domain meaning.
- Why this diagnostic exists: arithmetic thresholds, limits, offsets, and call arguments should be named unless their meaning is conventional or structurally obvious.
- Non-goals: authorized numbers, simple index literals when magic indexes are allowed, comments, strings, query text, syntax-error ownership, and simple container data literals whose context is the structure/map data itself.
- Keep/drop decision: keep as a BSLLS-compatible readability diagnostic.

## Semantic Oracle

- Checked language fact: executable BSL code contains a numeric literal outside an allowed/exempt context.
- Must diagnose when: a parsed `number` token is not in the authorized set and participates in an expression where its business meaning is not structurally obvious, such as return expressions, binary expressions, call arguments, loop bound expressions, or nested constructor/call arguments.
- Must not diagnose when: the value is `0`, `1`, or `-1`; the token is inside a string/query literal; the number is a simple array/index access and magic indexes are allowed; the number is a default parameter value; the number is a simple structure/fixed-structure/map constructor or insert value/key allowed by the BSLLS exception; or the tree is invalid enough that BSL001 owns the syntax problem.
- Diagnostic anchor token/range: the numeric literal token itself, without the unary minus sign for binary-minus compatibility.
- Quick-fix target, if any: extract the literal to a named variable/constant or replace the expression with a named domain value.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST.
- CST/domain object types: `number`, `expression`, `unary_expression`, `binary_expression`, `index`, `parameter`, `assignment_statement`, `property_access`, `call_expression`, `arguments`, and `new_expression`.
- Extracted attributes: literal text, signed literal value for authorization, numeric token range, expression ancestor, argument index, index/simple-number state, parameter-default ancestry, known local assignments to `Структура`/`ФиксированнаяСтруктура`/`Соответствие`, call receiver/method, and string ancestry.
- Parse-error behavior: invalid trees should not manufacture BSL029 semantic facts from recovery text; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: none for valid CST paths.

## Examples

### Invalid Examples

1. `Возврат 42;`
2. `Возврат Длительность < 10 * 60 * 60;`
3. `Возврат Окр((Сумма - НДС) / Количество, 2);`

### Valid Negative Twins

1. `Если А = 0 Тогда Возврат 1; КонецЕсли;`
2. `Возврат Массив[2];`
3. `Структура.Вставить("Код", 42);`

### Edge Examples

- Comments: numeric text in comments is ignored.
- Strings: numeric text and format placeholders inside strings are ignored.
- Preprocessor: not a separate semantic target for this rule.
- Multiline: multiline string/query content is ignored, while real numeric call arguments after the string still use the numeric token range.
- Nested constructs: nested call/constructor arguments are diagnosed unless they are inside an allowed structure/map literal context.
- BSL/SDBL variants, if relevant: SDBL/query text is not a BSL029 target; query-specific rules own parsed query semantics.

## Related-Rule Cluster

- Shared semantic object: expression numeric literals and call arguments.
- Rules that may report on the same object/range: BSL005 can inspect IP-shaped dotted numeric text, BSL014 line length can report the same source line, BSL036 can report complex conditions containing numbers, BSL123/BSL124-style call/parameter rules may be nearby, and query diagnostics may inspect numbers inside parsed SDBL text.
- Intended duplicate policy: keep BSL029 separate when the numeric literal itself is the readability problem; do not report numbers in strings/query text or simple data-container literals to avoid duplicate or noisy diagnostics.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestBsl029MagicNumber` and `tests/test_diagnostic_spans.py::test_bsl029_preserves_column_after_string_literals`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 5609.
- Line/common semantic common: 5609 exact reports.
- Ours-only categories: 0 after fixing overlapping BSLLS suppression ranges and over-broad property-assignment suppression.
- BSLLS-only categories: 0 after fixing property-assignment RHS handling.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one CST node grouping pass over `number` and `assignment_statement` nodes, then local ancestor/argument checks for each numeric literal.
- Expected complexity: O(CST nodes + numeric literals * local ancestor depth).
- Shared-pass reuse: uses the document tree and `ts_nodes_for_types` helper; no reparsing, no query parsing, and no regex fallback for valid CST paths.
- Before/after measurement, if changed: targeted BSL029 tests pass; preserved-source-root private parity changed from onec 5339 / BSLLS 5609 with 106 onec-only and 376 BSLLS-only to exact 5609 / 5609.

## Implementation Decision

- Change target: shared BSLLS suppression parsing, BSL029 property-assignment exception, BSL029 contract, and audit state.
- Delta category fixed by this change: `config-or-suppression` for overlapping/whole-line BSLLS suppression comments and `semantic-mismatch` for over-broad property-assignment RHS suppression.
- Why not another layer: CST already distinguishes real numeric tokens from comments, strings, query text, indexes, parameters, and data-container constructs.
- Rollback signal: numbers in strings/query text report, authorized numbers report, simple indexes report with default policy, simple structure/map values report, or magic call/return/binary expression literals stop reporting.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_line_text.py::TestBsl029MagicNumber tests/test_diagnostic_spans.py::test_bsl029_preserves_column_after_string_literals -q --no-cov`.
- Synthetic fixture/module: existing tests cover positives, authorized numbers, strings/query text, indexes, defaults, structure/map exceptions, property assignments, nested constructor/call arguments, ternary branches, multiline strings, and diagnostic spans.
- Related-rule tests: run wider expression/diagnostic tests if implementation changes.
- Parity command/artifact: preserved-source-root selected BSL029 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time if implementation changes.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_diagnostics_line_text.py tests/test_diagnostic_spans.py src/onec_hbk_bsl/analysis/diagnostic/diagnostic_runtime/rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
