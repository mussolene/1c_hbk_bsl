# BSL195 — <span class="doc-lang doc-lang-ru">Использование метода ПолучитьФорму</span><span class="doc-lang doc-lang-en">GetForm method call</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование метода ПолучитьФорму

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

GetForm method call

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL195` |
| Совместимый псевдоним / Compatible alias | `GetFormMethod` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `deprecated`, `ui` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL195` и псевдоним `GetFormMethod` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL195"]
ignore = ["GetFormMethod"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL195
Значение = "пример";  // bsl-disable: BSL195
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:GetFormMethod-off
// код без этой диагностики
// BSLLS:GetFormMethod-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL195` and alias `GetFormMethod` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL195"]
ignore = ["GetFormMethod"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL195
Value = "example";  // bsl-disable: BSL195
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:GetFormMethod-off
// code without this diagnostic
// BSLLS:GetFormMethod-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Для открытия форм следует применять метод глобального контекста ОткрытьФорму (при использовании версии платформы 1С:Предприятие 8.2 и более ранних версий - также ОткрытьФормуМодально).
Применение альтернативного способа, с получением формы и ее последующим открытием с помощью метода ПолучитьФорму, не рекомендуется.
## Примеры

```bsl
Процедура Тест()
    Док=Документы.ЗаявкаНаОперацию.СоздатьДокумент();
    Форма=Док.ПолучитьФорму("ФормаДокумента"); // Срабатывание здесь
КонецПроцедуры
```
```bsl
Процедура Тест2()
    ФормаРедактора = ПолучитьФорму("Обработка.УниверсальныйРедактор.Форма");
КонецПроцедуры
```

## Источники

Источник: [Стандарты разработки](https://its.1c.ru/db/v8std/content/404/hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

To open forms, use the OpenForm global context method (when using the 1C: Enterprise 8.2 platform version and earlier versions, also use OpenFormModal). An alternative method, using the GetForm method, is not recommended.

## Examples

```bsl
Procedure Test()
    Doc = Documents.PlanOperation.CreateDocument();
    Form = Doc.GetForm("DocumentForm"); // here
EndProcedure
```

```bsl
Procedure Test2()
    Form = GetForm("CommonForms.MyForm");
EndProcedure
```

## Sources

Source: [Development standards (RU)](https://its.1c.ru/db/v8std/content/404/hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL195
- BSLLS compatible name: GetFormMethod
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: Opening forms through `ОткрытьФорму`/`OpenForm` is preferred; getting a form object through `ПолучитьФорму`/`GetForm` and opening it manually is discouraged.
- Non-goals: Do not diagnose text inside comments or string literals.

## Semantic Oracle

- Checked language fact: Code calls a method named `ПолучитьФорму` or `GetForm`.
- Must diagnose when: the call is global.
- Must diagnose when: the call is receiver-qualified, such as `Док.ПолучитьФорму(...)`.
- Must not diagnose when: the name is only a prefix of a longer identifier.
- Must not diagnose when: the text is inside comments or string literals.
- Diagnostic anchor token/range: The `ПолучитьФорму`/`GetForm` method-name token.

## CST Or Domain Fact Model

- CST/domain object types: masked source line, method-call token.
- Extracted attributes: method name, line, start column, end column.
- Parse-error behavior: Line-local complete calls may still be reported in otherwise broken files.
- Shared semantic object: Deprecated form retrieval method call.

## Examples

### Invalid Examples

1. `Форма = ПолучитьФорму("Обработка.УниверсальныйРедактор.Форма");`.
2. `Форма = Док.ПолучитьФорму("ФормаДокумента");`.
3. `Form = Object.GetForm("Form");`.

### Valid Negative Twins

1. `Текст = "ПолучитьФорму()"`.
2. `// Форма = ПолучитьФорму("Форма");`.
3. `ПолучитьФормуДокумента();`.

## Related-Rule Cluster

- Rules that may report on the same object/range: BSL176, BSL177, BSL178.
- Intended duplicate policy: Keep BSL195 for the form-opening standard; deprecated API rules remain separate when applicable.

## BSLLS Parity Taxonomy

- Compared input-file set: Private representative configuration corpus with 5803 BSL files plus synthetic fixtures.
- Config/exclude mode: Preserve source root for BSLLS; normal onec CLI path.
- File-key normalization: Repository parity runner normalized relative paths.
- Coordinate normalization: UTF-8 byte columns converted to LSP character columns.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: Single pass over masked source lines.
- Expected complexity: O(lines), no project-wide state.

## Implementation Decision

- Change target: No runtime change required; existing `GetFormMethodRule` matches the contract.
- Delta category fixed by this change: No BSLLS delta on the compared corpus; added receiver-call and negative synthetic coverage.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q -k BSL195 --no-cov`.
- Leak check: `git diff --check` and secret-pattern scan of staged/documented changes.
- OACS evidence/checkpoint: Record verification commands and checkpoint after successful parity run.

## Completion Checklist

- [x] Contract has semantic oracle.
- [x] Contract has CST/domain fact model.
- [x] Contract has positive and negative examples.
- [x] Contract has parity taxonomy.
- [x] Contract has performance contract.
- [x] Contract has verification plan.

</div>

</details>

<!-- engineering-contract:end -->
