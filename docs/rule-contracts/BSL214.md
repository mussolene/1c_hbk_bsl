# BSL214 Contract

## Rule Identity

- Code: BSL214
- BSLLS compatible name: MissingEventSubscriptionHandler
- Public name/i18n key: BSL214 / MissingEventSubscriptionHandler
- Default message/i18n key: RULE_MESSAGES_RU["BSL214"]
- Description/i18n key: RULE_METADATA["BSL214"].description
- Severity: ERROR
- Parameters/defaults: no user parameters
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: every event subscription must reference an existing exported handler method in a server common module.
- Why this diagnostic exists: broken event-subscription handlers fail at runtime and are configuration correctness defects.
- Non-goals: checking arbitrary common-module calls, scheduled job handlers, form event handlers, or handler parameter signatures.
- Keep/drop decision: keep as a BSLLS-compatible correctness diagnostic anchored on the session module.

## Semantic Oracle

- Checked language fact: an event subscription handler is empty, malformed, references a missing common module, references a non-server common module, references a missing method, or references a non-exported method.
- Must diagnose when: any event subscription in the configuration violates one of those handler requirements.
- Must not diagnose when: the handler points to an existing exported method of a server common module, or the current checked file is not the configuration session module.
- Diagnostic anchor token/range: metadata owner range at line 1, character 0 through character 9 of `Ext/SessionModule.bsl` when the first line is long enough; shorter first lines use their visible length with minimum 1.
- Quick-fix target, if any: fill or correct the event subscription handler, make the common module server-side, create the method, or export the method.

## CST Or Domain Fact Model

- Parser/tree source: Designer/EDT event subscription XML, common module XML, and common module BSL procedure/function definitions.
- CST/domain object types: `EventSubscriptions/*.xml`, `CommonModules/*.xml`, target common module `Ext/Module.bsl`.
- Extracted attributes: subscription name, handler path, module name, method name, common module server flag, all target method names, exported target method names.
- Parse-error behavior: unreadable/malformed XML without a valid handler is treated as invalid; malformed target module BSL with no exported method produces a missing/non-exported handler diagnostic.
- Textual/regex behavior, only if genuinely textual: XML tag extraction is textual because the existing metadata helper layer uses compact regex-based XML helpers for these rules.

## Examples

### Invalid Examples

1. An event subscription has an empty handler.
2. An event subscription references a missing common module.
3. An event subscription references an existing method that is not exported.

### Valid Negative Twins

1. An event subscription references `CommonModule.Target.Exists` where `Target` is server and `Exists` is exported.
2. A common module contains a private method that is not referenced by an event subscription.
3. A non-session module is checked while event subscriptions exist.

### Edge Examples

- Comments: BSL comments do not matter.
- Strings: BSL strings do not matter.
- Preprocessor: preprocessor directives do not matter.
- Multiline: XML formatting may span lines.
- Nested constructs: all event subscriptions in the configuration are checked from the session module.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: common module metadata, common module exported methods, and metadata handler references.
- Rules that may report on the same object/range: BSL213 checks common-module method calls, BSL242 checks scheduled job handlers, and BSL232 also anchors on the session module.
- Intended duplicate policy: keep BSL214 separate; event subscription defects are metadata handler defects, not call-site defects.
- Cluster fixture or probe: common module cross-reference and BSL214 session-module tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus local synthetic session-module event-subscription fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: private batch has no BSL214 diagnostics on either side.
- Line/common semantic common: both implementations check event subscriptions from the session module and validate empty/malformed/missing/non-server/missing-method/non-exported handlers.
- Ours-only categories: none in the current private batch.
- BSLLS-only categories: none in the current private batch.
- Range-only categories: none in the current private batch; synthetic tests anchor the metadata owner range on the session module.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: event subscription XML scan plus lazy common-module method loading only for referenced common modules.
- Expected complexity: O(event subscriptions + referenced common modules), cached per config root.
- Shared-pass reuse: shares common-module indexes with BSL213 and BSL242, and session-module anchoring with BSL232.
- Before/after measurement, if changed: targeted pytest runtime 0.11s; private preserved-source-root BSL214 parity over 31,797 files completed in 103.90s with `onec=0`, `bslls=0`.

## Implementation Decision

- Change target: BSL214 applicability/implementation in the metadata pool and event subscription helper extraction.
- Delta category fixed by this change: semantic alignment with BSLLS session-module event-subscription handler validation.
- Why not another layer: metadata pool already owns event subscription and common module metadata checks.
- Rollback signal: BSL214 reports on common module files, valid exported server handlers report, or invalid handlers stop reporting on the session module.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k "bsl214 or common_module_cross_reference_tail_pool" --no-cov`
- Synthetic fixture/module: inline event subscription fixtures for empty, malformed, missing module, non-server, missing method, non-exported, and valid handlers.
- Related-rule tests: common module cross-reference tail pool covers BSL213, BSL231, and BSL242; session module tests cover BSL214 and BSL232 separation.
- Parity command/artifact: preserved-source-root private batch BSL214 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: changed files check.
- Leak check: `git diff --check` and no private corpus paths/source in committed dossier.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
