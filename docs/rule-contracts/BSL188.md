# BSL188 — <span class="doc-lang doc-lang-ru">Доступ к файловой системе</span><span class="doc-lang doc-lang-en">File system access</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Доступ к файловой системе

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

File system access

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL188` |
| Совместимый псевдоним / Compatible alias | `FileSystemAccess` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `security`, `compatibility` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL188` и псевдоним `FileSystemAccess` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL188"]
ignore = ["FileSystemAccess"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL188
Значение = "пример";  // bsl-disable: BSL188
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:FileSystemAccess-off
// код без этой диагностики
// BSLLS:FileSystemAccess-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL188` and alias `FileSystemAccess` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL188"]
ignore = ["FileSystemAccess"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL188
Value = "example";  // bsl-disable: BSL188
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:FileSystemAccess-off
// code without this diagnostic
// BSLLS:FileSystemAccess-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При код-ревью или аудите кода необходимо проверять обращения к файлам, каталогам и набор действий, выполняемых с ними, для исключения передачи конфиденциальной или защищенной информации, а также для исключения деструктивных действий с файловой системой.
Важно проверять код от сторонних разработчиков, подрядчиков, из различных интернет-сервисов, каталогов и т.п.

По найденным замечаниям рекомендуется выполнить ручной аудит кода на предмет его правильности и безопасности.

## Примеры

```bsl
    Текст = Новый ЧтениеТекста(ПутьФайла, КодировкаТекста.ANSI); // есть замечание
    Текст = Новый ЗаписьТекста(ПутьФайла, КодировкаТекста.ANSI); // есть замечание

    ЗначениеВФайл(ПутьФайла, ЛичныеДанные); // есть замечание
    КопироватьФайл(ПутьФайла, ДругойПутьФайла); // есть замечание

    МассивИмен = Новый Массив();
    МассивИмен.Добавить(ПутьФайла);
    ОбъединитьФайлы(МассивИмен, ДругойПутьФайла); // есть замечание

    ПереместитьФайл(ПутьФайла, ДругойПутьФайла); // есть замечание
    РазделитьФайл(ПутьФайла, 1024 * 1024 ); // есть замечание
    СоздатьКаталог(ИмяКаталога); // есть замечание
    УдалитьФайлы(ПутьФайла); // есть замечание
```

## Источники

* [Стандарт Доступ к файловой системе из кода конфигурации](https://its.1c.ru/db/v8std#content:542:hdoc)
* [Стандарт Безопасность запуска приложений](https://its.1c.ru/db/v8std#content:774:hdoc)
* [Безопасный режим работы - руководство разработчика](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000186)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

It is important to review your code. Be sure to pay attention to accessing the file system and using “external code”

The found sections of the code must be analyzed, a manual audit of the code must be performed for its correctness and safety.

## Examples

```bsl
    Текст = Новый ЧтениеТекста(ПутьФайла, КодировкаТекста.ANSI); // есть замечание
    Текст = Новый ЗаписьТекста(ПутьФайла, КодировкаТекста.ANSI); // есть замечание

    ЗначениеВФайл(ПутьФайла, ЛичныеДанные); // есть замечание
    КопироватьФайл(ПутьФайла, ДругойПутьФайла); // есть замечание

    МассивИмен = Новый Массив();
    МассивИмен.Добавить(ПутьФайла);
    ОбъединитьФайлы(МассивИмен, ДругойПутьФайла); // есть замечание

    ПереместитьФайл(ПутьФайла, ДругойПутьФайла); // есть замечание
    РазделитьФайл(ПутьФайла, 1024 * 1024 ); // есть замечание
    СоздатьКаталог(ИмяКаталога); // есть замечание
    УдалитьФайлы(ПутьФайла); // есть замечание
```

## Sources

* [File system access from application code (RU)](https://its.1c.ru/db/v8std#content:542:hdoc)
* [Standard: Application launch security (RU)](https://its.1c.ru/db/v8std#content:774:hdoc)
* [Safe operation - Developer's Guide (RU](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000186)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL188
- BSLLS compatible name: FileSystemAccess
- Public name/i18n key: BSL188 / FileSystemAccess
- Default message/i18n key: RULE_MESSAGES_RU["BSL188"]
- Description/i18n key: RULE_METADATA["BSL188"].description
- Severity: WARNING
- Parameters/defaults: BSLLS-compatible `globalMethods` and `newExpression` allowlists for filesystem APIs/classes.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: filesystem access requires security review because it may read, write, copy, move, or delete protected data.
- Why this diagnostic exists: direct filesystem APIs and filesystem-related objects are security-sensitive and should be audited.
- Non-goals: member calls with the same method names, call-looking text in strings/comments, or unrelated identifiers containing filesystem words.
- Keep/drop decision: keep as a CST-backed security audit diagnostic.

## Semantic Oracle

- Checked language fact: executable code uses a configured filesystem global method or constructs a configured filesystem-related class.
- Must diagnose when: a global call matches `globalMethods`, `Новый Type` matches `newExpression`, or dynamic `Новый("Type", ...)` names a configured filesystem class.
- Must not diagnose when: the same text appears in a string/comment, the method is called as a member, or the constructed/called name is not in the configured set.
- Diagnostic anchor token/range: the constructor/call expression node or global method token span.
- Quick-fix target, if any: none; manual security review is required.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST.
- CST/domain object types: `new_expression`, `new_expression_method`, global method call nodes/facts.
- Extracted attributes: type name, dynamic string type name, global call name, source range, and severity.
- Parse-error behavior: structural rule skips when the CST root is unavailable; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: only fixed name-set matching over CST-extracted call/type names.

## Examples

### Invalid Examples

1. `Ф = Новый Файл("a.txt");`
2. `Данные = Новый("ДвоичныеДанные", "a.txt");`
3. `КопироватьФайл("a.txt", "b.txt");`

### Valid Negative Twins

1. `Объект.КопироватьФайл("a.txt", "b.txt");`
2. `Текст = "Новый Файл(""a.txt"")";`
3. `// УдалитьФайлы("a.txt");`

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored except when the string is the dynamic `Новый("Type")` type argument.
- Preprocessor: ordinary CST parse behavior applies.
- Multiline: constructor/call ranges come from CST or call facts.
- Nested constructs: nested calls are diagnosed independently.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: security-sensitive platform APIs.
- Rules that may report on the same object/range: BSL185 external application start, BSL203 internet access, BSL218/BSL250 temporary-file lifecycle, BSL264 system information, and BSL267 external-code tools.
- Intended duplicate policy: BSL188 owns filesystem APIs/classes; sibling security rules own their own API families.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl188_file_system_access_uses_cst_calls_and_constructors`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 129.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: CST node indexes for constructor nodes plus global-call facts.
- Expected complexity: O(CST nodes) with constant-time set lookups for candidate names.
- Shared-pass reuse: inherited base implementation is reused by BSL203 and BSL264 with different name sets.
- Before/after measurement, if changed: no runtime change; current corpus exact parity onec 129 / bslls 129.

## Implementation Decision

- Change target: contract and audit state only; existing CST implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact.
- Why not another layer: CST constructors/calls already express the semantic target precisely.
- Rollback signal: member calls report, comments/strings report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl188 or FileSystemAccess"`.
- Synthetic fixture/module: existing test covers static/dynamic constructors, global calls, member-call negative, string negative, and comment negative.
- Related-rule tests: BSL203/BSL264 only if shared base API detection changes.
- Parity command/artifact: preserved-source-root selected BSL188 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
