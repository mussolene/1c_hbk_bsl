# BSL218 — <span class="doc-lang doc-lang-ru">Отсутствует удаление временного файла после использования</span><span class="doc-lang doc-lang-en">Missing temporary file deletion after using</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Отсутствует удаление временного файла после использования

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Missing temporary file deletion after using

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL218` |
| Совместимый псевдоним / Compatible alias | `MissingTemporaryFileDeletion` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `resource-management` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL218` и псевдоним `MissingTemporaryFileDeletion` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL218"]
ignore = ["MissingTemporaryFileDeletion"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL218
Значение = "пример";  // bsl-disable: BSL218
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MissingTemporaryFileDeletion-off
// код без этой диагностики
// BSLLS:MissingTemporaryFileDeletion-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL218` and alias `MissingTemporaryFileDeletion` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL218"]
ignore = ["MissingTemporaryFileDeletion"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL218
Value = "example";  // bsl-disable: BSL218
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MissingTemporaryFileDeletion-off
// code without this diagnostic
// BSLLS:MissingTemporaryFileDeletion-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

После окончания работы с временным файлом или каталогом, его необходимо удалить самостоятельно.
Нельзя рассчитывать на автоматическое удаление файлов и каталогов при следующем запуске платформы,
это может привести к исчерпанию свободного места в каталоге временных файлов.

## Примеры

Неправильно:

```bsl
ИмяПромежуточногоФайла = ПолучитьИмяВременногоФайла("xml");
Данные.Записать(ИмяПромежуточногоФайла);
// далее нет удаления файла
```

Правильно:

```bsl
ИмяПромежуточногоФайла = ПолучитьИмяВременногоФайла("xml");
Данные.Записать(ИмяПромежуточногоФайла);

// Работа с файлом
...

// Удаляем временный файл
Попытка
   УдалитьФайлы(ИмяПромежуточногоФайла);
Исключение
   ЗаписьЖурналаРегистрации(НСтр("ru = 'Мой механизм.Действие'"), УровеньЖурналаРегистрации.Ошибка, , , ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
КонецПопытки;
```

## Особенности работы

Диагностика определяет корректность работы с временными файлами по наличию методов удаления либо перемещения.

Если в прикладном решении используется свой метод удаления/перемещения поверх платформенного, то стоит указать его в параметре диагностики, добавив через `|`. Диагностика понимает как глобальные методы, так и расположенные в общих модулях или модулях менеджеров.

Ниже приведены примеры настройки

- Глобальный метод `МоеУдалениеФайлов` в модуле `ГлобальныйСервер` в параметре указывается как `МоеУдалениеФайлов`
- Метод `МоеУдалениеФайлов` в общем модуле `ФайлыКлиентСервер` в параметре указывается как `ФайлыКлиентСервер.МоеУдалениеФайлов`
- Метод `МоеУдалениеФайлов` в модуле менеджера справочника `ФайловыеОперации` в параметре указывается как `Справочники.ФайловыеОперации.МоеУдалениеФайлов`

и так далее.

## Источники

* [Доступ к файловой системе из кода конфигурации](https://its.1c.ru/db/v8std#content:542:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

After you finished working with temporary file or folder, you need to delete it yourself.
You should not rely on automatic deletion of files and folders before platform start. This can cause temp folder free space shortage.

## Examples

Incorrect:

```bsl
TempFileName = GetTempFileName("xml");
Data.Write(TempFileName);
// Not delete temporary file
```

Correct:

```bsl
TempFileName = GetTempFileName("xml");
Data.Write(TempFileName);

// Work with file
...

// Delete temporary file
Try
   DeleteFiles(TempFileName);
Catch
   WriteLogEvent(НСтр("ru = 'My subsystem.Action'"), EventLogLevel.Error, , , DetailErrorDescription(ErrorInfo()));
EndTry;
```

## Nuances

Diagnostics determines the correctness of working with temporary files by the presence of methods for deleting or moving.

If the applied solution uses its own method of removing/moving over the platform one, then it is worth specifying it in the diagnostic parameter, adding it after `|`. Diagnostics understands both global methods and those located in common modules or manager modules.

The following is an examples of a settings

- The global method `MyFileDeletion` in the `GlobalServer` module in the parameter is specified as `MyFileDeletion`
- Method `MyFileDeletion` in the common module `FilesClientServer` in the parameter is specified as `FilesClientServer.MyFileDelete`
- Method `MyFileOperations` in the module of the catalog manager `FileOperations` in the parameter is specified as `Catalogs.FileOperations.MyFileOperations`

and so on.

## Sources

* [File system access from application code (RU)](https://its.1c.ru/db/v8std#content:542:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL218
- BSLLS compatible name: MissingTemporaryFileDeletion
- Public name/i18n key: BSL218 / MissingTemporaryFileDeletion
- Default message/i18n key: RULE_MESSAGES_RU["BSL218"]
- Description/i18n key: RULE_METADATA["BSL218"].description
- Severity: ERROR
- Parameters/defaults: `searchDeleteFileMethod` default is represented by built-in delete/move method names.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: temporary files created by `ПолучитьИмяВременногоФайла` / `GetTempFileName` must be explicitly deleted or moved after use.
- Why this diagnostic exists: relying on platform cleanup can exhaust temporary storage and leave stale files.
- Non-goals: whole-program dataflow, custom deletion helper configuration beyond the built-in default set, or proving cleanup in unrelated branches.
- Keep/drop decision: keep as a resource-management diagnostic.

## Semantic Oracle

- Checked language fact: a temporary file name acquired in a code block is not passed later in the same block to a configured delete/move method.
- Must diagnose when: a bare temp-file-name call appears, or an assigned temp-file variable has no later `УдалитьФайлы` / `DeleteFiles` / async delete / `ПереместитьФайл` / `MoveFile` call with that same variable in the same code-block root.
- Must not diagnose when: the assigned variable is deleted or moved later in the same block, the call is not global, or the deletion exists only in another branch/scope.
- Diagnostic anchor token/range: the `ПолучитьИмяВременногоФайла` / `GetTempFileName` method call.
- Quick-fix target, if any: add deletion/move in the same cleanup scope, preferably with error handling.

## CST Or Domain Fact Model

- Parser/tree source: BSL CST `method_call`, assignment, routine, branch, loop, try, and source-file nodes.
- CST/domain object types: global method call facts, assignment ancestor, lvalue text, code-block roots, and delete/move call arguments.
- Extracted attributes: call name/range, assigned variable, containing code block, call order by line, delete/move method name, and argument expression text.
- Parse-error behavior: requires CST global call extraction; malformed files are not forced through textual fallback.
- Textual/regex behavior, only if genuinely textual: none for valid CST paths.

## Examples

### Invalid Examples

1. `ПолучитьИмяВременногоФайла(".xml");`
2. `Имя = ПолучитьИмяВременногоФайла(".xml");\nСообщить(Имя);`
3. `Если Условие Тогда\n    Имя = ПолучитьИмяВременногоФайла(".xml");\nКонецЕсли;\nУдалитьФайлы(Имя);`

### Valid Negative Twins

1. `Имя = ПолучитьИмяВременногоФайла(".xml");\nУдалитьФайлы(Имя);`
2. `Имя = GetTempFileName(".xml");\nDeleteFiles(Имя);`
3. `Имя = ПолучитьИмяВременногоФайла(".xml");\nПереместитьФайл(Имя, НовоеИмя);`

### Edge Examples

- Comments: ignored by CST call extraction.
- Strings: ignored by CST call extraction.
- Preprocessor: module-level source-file roots skip preprocessor nodes.
- Multiline: CST call/assignment ranges own multiline calls.
- Nested constructs: cleanup must be in the same branch/loop/try/routine code-block root after acquisition.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: temporary resource lifecycle.
- Rules that may report on the same object/range: BSL217 covers temporary storage deletion; BSL188 covers broader filesystem access; BSL250 covers temp directory access.
- Intended duplicate policy: BSL218 owns temporary file-name lifecycle; BSL217 owns temporary storage data lifecycle.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl218MissingTemporaryFileDeletion`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 52.
- Line/common semantic common: 52 exact reports.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: CST global method calls plus bounded subtree scan in the owning code block.
- Expected complexity: O(method calls + owning block subtree size for temp-file acquisitions).
- Shared-pass reuse: process-fork grouped with BSL230, BSL202, and BSL223 for heavy files.
- Before/after measurement, if changed: no runtime change; private selected parity exact 52/52.

## Implementation Decision

- Change target: contract and audit state only.
- Delta category fixed by this change: none.
- Why not another layer: existing CST implementation already models method call, assignment, and same-block cleanup semantics accurately enough for exact BSLLS parity.
- Rollback signal: branch-external cleanup suppresses the diagnostic, qualified helper calls satisfy the default rule unexpectedly, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py::TestBsl218MissingTemporaryFileDeletion -q --no-cov`.
- Synthetic fixture/module: inline tests for bare call, assigned call, delete/move positives, same-branch restriction, module-level cleanup, and qualified-helper negative.
- Related-rule tests: BSL217/BSL250 only if shared temporary-resource semantics change.
- Parity command/artifact: preserved-source-root selected BSL218 private parity; sanitized aggregate only.
- Performance command/artifact: no dedicated performance artifact; runtime unchanged.
- Ruff/lint: no code lint needed for contract-only close; run contract validator and diff checks.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
