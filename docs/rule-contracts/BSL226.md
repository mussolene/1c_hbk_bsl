# BSL226 OSUsersMethod Contract

## Rule Identity

- Code: BSL226
- BSLLS compatible name: OSUsersMethod
- Public name/i18n key: BSL226 / OSUsersMethod
- Default message/i18n key: RULE_MESSAGES_RU["BSL226"]
- Description/i18n key: RULE_METADATA["BSL226"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: calls that enumerate operating-system users are security-sensitive and must be reviewed.
- Why this diagnostic exists: `ПользователиОС`/`OSUsers` can support unauthorized user discovery or malicious logic.
- Non-goals: member calls, identifiers containing the same text, comments, or strings.
- Keep/drop decision: keep as a BSLLS-compatible security diagnostic.

## Semantic Oracle

- Checked language fact: executable code calls the global method `ПользователиОС` or `OSUsers`.
- Must diagnose when: a global call to `ПользователиОС(...)` or `OSUsers(...)` appears in source code.
- Must not diagnose when: the name appears as a member call, ordinary identifier, string, or comment.
- Diagnostic anchor token/range: method name call span from method token through the closing call range when available.
- Quick-fix target, if any: remove the call or replace it with an audited explicit user source.

## CST Or Domain Fact Model

- Parser/tree source: source-line scan after string/comment masking.
- CST/domain object types: physical source line and global call regex match.
- Extracted attributes: method name, line, start/end column, and severity.
- Parse-error behavior: clear call spelling can still report; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: exact global method-name regex after masking is sufficient for this small security API detector.

## Examples

### Invalid Examples

1. `Пользователи = ПользователиОС();`
2. `Users = OSUsers();`
3. `Если OSUsers() <> Неопределено Тогда`

### Valid Negative Twins

1. `Объект.ПользователиОС();`
2. `Текст = "ПользователиОС()";`
3. `// ПользователиОС();`

### Edge Examples

- Comments: ignored by masking.
- Strings: ignored by masking.
- Preprocessor: not a separate target.
- Multiline: simple method call range is line-local in current implementation.
- Nested constructs: nested calls are still diagnosed.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: security-sensitive platform APIs.
- Rules that may report on the same object/range: BSL180 safe mode, BSL184 dynamic code execution, BSL188 filesystem access, BSL203 internet access, BSL247 privileged mode, and BSL264 system information.
- Intended duplicate policy: BSL226 owns OS user enumeration specifically.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestSecurityApiParityBatch::test_bsl226_os_users_method_matches_bslls_fixture`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over masked source lines.
- Expected complexity: O(total source characters).
- Shared-pass reuse: uses the same lightweight security API line-scan pattern as nearby rules.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: masked line scan is enough to identify the global method call.
- Rollback signal: member calls report, strings/comments report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl226 or OSUsersMethod"`.
- Synthetic fixture/module: existing selected test covers Russian/English positives and string/comment/member negatives.
- Related-rule tests: broader security API batch only if shared masking changes.
- Parity command/artifact: preserved-source-root selected BSL226 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
