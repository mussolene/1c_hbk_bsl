# BSL025 — <span class="doc-lang doc-lang-ru">Пустой оператор</span><span class="doc-lang doc-lang-en">Empty statement</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Пустой оператор

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Empty statement

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL025` |
| Совместимый псевдоним / Compatible alias | `EmptyStatement` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `syntax`, `convention` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL025` и псевдоним `EmptyStatement` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL025"]
ignore = ["EmptyStatement"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL025
Значение = "пример";  // bsl-disable: BSL025
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:EmptyStatement-off
// код без этой диагностики
// BSLLS:EmptyStatement-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL025` and alias `EmptyStatement` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL025"]
ignore = ["EmptyStatement"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL025
Value = "example";  // bsl-disable: BSL025
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:EmptyStatement-off
// code without this diagnostic
// BSLLS:EmptyStatement-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Пустой оператор - это оператор, состоящий только из точки с запятой (";"). Появляется он в обычно

- при рефакторинге, когда разработчик удалил часть кода, но забыл удалить последнюю ";"
- при "копипасте", когда разработчик вставил скопированный код, содержащий конечный символ ";"
- при невнимательности, когда разработчик дважды (а то и больше) раз напечатал символ ";"

Пустой оператор не приводит к ошибкам работы кода, но захламляет его, снижая восприятие.

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

An empty statement is an operator consisting only of a semicolon (";"). Usually appears when:

- refactoring, when the developer deleted a part of the code, but forgot to delete the last ";"
- "copy paste", when the developer pasted the copied code containing the final character ";"
- inattentive, when the developer twice (or even more) times clicked the symbol ";"

An empty statement does not lead to code errors, but clutters it, reducing perception.

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL025
- BSLLS compatible name: EmptyStatement
- Public name/i18n key: BSL025 / EmptyStatement
- Default message/i18n key: RULE_MESSAGES_RU["BSL025"]
- Description/i18n key: RULE_METADATA["BSL025"].description
- Severity: INFORMATION
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: a standalone `;` is a useless empty operator and should not stay in production code.
- Why this diagnostic exists: empty statements usually appear after refactoring, copy-paste, or accidental duplicate semicolon typing.
- Non-goals: reporting ordinary statement terminators, block-closing semicolons, semicolons inside strings/comments/query text, or parse errors owned by BSL001.
- Keep/drop decision: keep as a CST-backed bad-practice diagnostic.

## Semantic Oracle

- Checked language fact: a semicolon token is parsed as an empty standalone statement rather than as a valid terminator for a real statement.
- Must diagnose when: the CST contains a semicolon token whose surrounding line text and parent context indicate a standalone empty statement, including duplicate semicolons and header/footer stray semicolons.
- Must not diagnose when: the semicolon terminates a valid statement or block, appears inside string/comment/query text, or belongs to malformed syntax better handled by BSL001.
- Diagnostic anchor token/range: the offending `;` token only.
- Quick-fix target, if any: remove the standalone semicolon.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST.
- CST/domain object types: semicolon token nodes and their parent statement/block context.
- Extracted attributes: token start/end point, physical source line, neighboring non-space characters, and parent node type.
- Parse-error behavior: avoid manufacturing facts from parse-error recovery; BSL001 owns syntax failures.
- Textual/regex behavior, only if genuinely textual: limited line-neighborhood checks are allowed only to distinguish standalone semicolons from ordinary terminators.

## Examples

### Invalid Examples

1. `;`
2. `А = 1;;`
3. `Процедура Тест();`

### Valid Negative Twins

1. `А = 1;`
2. `Пока Истина Цикл\nКонецЦикла;`
3. `Сообщение = "строка;";`

### Edge Examples

- Comments: `// ;` is a comment and is ignored.
- Strings: string-literal semicolons are ignored.
- Preprocessor: not a separate target.
- Multiline: each semicolon token is anchored by its real CST position.
- Nested constructs: standalone semicolons inside exception/loop/branch blocks are still diagnosed.
- BSL/SDBL variants, if relevant: SDBL/query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: statement separators and syntax recovery.
- Rules that may report on the same object/range: BSL001 parse errors, BSL030 missing semicolon, and BSL216 spacing around punctuation.
- Intended duplicate policy: BSL025 owns extra/standalone semicolons; BSL030 owns missing terminators; BSL001 owns malformed syntax.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestBsl025EmptyStatement` and BSL030 header-semicolon tests.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: CST walk over semicolon tokens only.
- Expected complexity: O(CST nodes) with constant-time line checks for candidate semicolons.
- Shared-pass reuse: standalone CST rule; may share future punctuation facts with BSL030/BSL216 only if a common statement-token layer exists.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing CST implementation is retained.
- Delta category fixed by this change: none; parity is already exact-zero on the current corpus.
- Why not another layer: CST token detection already expresses the semantic target precisely.
- Rollback signal: ordinary statement terminators report, standalone semicolons stop reporting in targeted tests, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_line_text.py -q --no-cov -k "Bsl025 or EmptyStatement"`.
- Synthetic fixture/module: existing tests cover standalone module/procedure semicolons, duplicate semicolons, header semicolons, block terminators, and exception blocks.
- Related-rule tests: BSL030 header-semicolon tests are included by the selected pytest expression.
- Parity command/artifact: preserved-source-root selected BSL025 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless touched code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
