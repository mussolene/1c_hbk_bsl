# BSL208 LatinAndCyrillicSymbolInWord Contract

## Rule Identity

- Code: BSL208
- BSLLS compatible name: LatinAndCyrillicSymbolInWord
- Public name/i18n key: BSL208 / LatinAndCyrillicSymbolInWord
- Default message/i18n key: RULE_MESSAGES_RU["BSL208"]
- Description/i18n key: RULE_METADATA["BSL208"].description
- Severity: INFORMATION
- Parameters/defaults: `excludeWords=ЧтениеXML, ЧтениеJSON, ЗаписьXML, ЗаписьJSON, ComОбъект, ФабрикаXDTO, ОбъектXDTO, СоединениеFTP, HTTPСоединение, HTTPЗапрос, HTTPСервисОтвет, SMSСообщение, WSПрокси`; `allowTrailingPartsInAnotherLanguage=true`.
- Rule status: verified-classified

## Standard And Product Decision

- Standard or product policy: identifiers should not accidentally mix Latin and Cyrillic letters because homoglyphs and layout switches make code harder to read and maintain.
- Why this diagnostic exists: mixed-script words often indicate a mistaken keyboard layout or a visually confusing homoglyph.
- Non-goals: reporting known technical names, configured excluded words, pure Latin identifiers, pure Cyrillic identifiers, strings/comments, or BSL256 typo duplicates for the same word.
- Keep/drop decision: keep as a text/identifier quality diagnostic with a deliberate duplicate-noise policy.

## Semantic Oracle

- Checked language fact: an identifier-like word in executable module text contains both Latin and Cyrillic letters.
- Must diagnose when: a word contains both scripts, is not an excluded standard technical name, is not allowed by the trailing-language policy, and is not suppressed as a duplicate/noise case.
- Must not diagnose when: the mixed text is inside comments/strings, is an excluded technical name, only starts/ends with an allowed technical acronym part, or is owned by BSL256 typo detection policy.
- Diagnostic anchor token/range: the mixed-script word span.
- Quick-fix target, if any: rename the identifier or replace the accidental homoglyph.

## CST Or Domain Fact Model

- Parser/tree source: module text line scan over masked lines with comment truncation.
- CST/domain object types: identifier-like word spans, comment boundaries, string-masked source text, and BSL256 duplicate ownership state.
- Extracted attributes: word text, line, start/end columns, script composition, standard-tech exclusion state, trailing-language state, and duplicate key.
- Parse-error behavior: can run on masked text even when CST has errors; BSL001 owns syntax failures.
- Textual/regex behavior, only if genuinely textual: allowed. The diagnostic targets word spelling/script composition, not a structured AST construct.

## Examples

### Invalid Examples

1. `Перем Значeние;`
2. `ДанныеHttpЛок = Неопределено;`
3. `_СтдJSONКэш = Новый Соответствие;`

### Valid Negative Twins

1. `Перем Значение;`
2. `Перем Value;`
3. `HTTPСоединение = Новый HTTPСоединение("example.org");`

### Edge Examples

- Comments: ignored.
- Strings: ignored.
- Preprocessor: treated as masked source text if identifier-like words remain.
- Multiline: each physical line is scanned independently.
- Nested constructs: nesting is irrelevant.
- BSL/SDBL variants, if relevant: query text inside strings is ignored by masking.

## Related-Rule Cluster

- Shared semantic object: source words and identifiers.
- Rules that may report on the same object/range: BSL256 typo checks, BSL279 letter `ё` checks, and naming rules BSL056/BSL057.
- Intended duplicate policy: BSL208 owns mixed-script words; BSL256 should not duplicate the same mixed-script fact as a typo.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py` BSL208/BSL256 overlap block.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 381.
- Line/common semantic common: 381 exact common facts.
- Ours-only categories: 14 occurrence-policy deltas where onec reports a later/use-site occurrence that BSLLS suppresses or reports elsewhere.
- BSLLS-only categories: 32 occurrence-policy deltas where BSLLS reports additional declaration/assignment/receiver occurrences for words already handled by onec's duplicate/noise policy.
- Range-only categories: 0 material range-only categories after exact common matching.
- Known BSLLS defects: none asserted; the difference is an occurrence/deduplication policy divergence.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over masked physical lines and regex word spans.
- Expected complexity: O(total source characters).
- Shared-pass reuse: shares mixed-script ownership decisions with BSL256 typo suppression.
- Before/after measurement, if changed: no runtime change; current corpus classified parity onec 395 / bslls 413 with 32 BSLLS-only and 14 ours-only occurrence-policy deltas.

## Implementation Decision

- Change target: contract and audit state only; existing implementation and duplicate policy are retained.
- Delta category fixed by this change: none; remaining deltas are classified as occurrence/deduplication policy, not unknown semantics.
- Why not another layer: exact BSLLS occurrence matching would add noisy duplicate reports and risk breaking the BSL256 mixed-script ownership contract.
- Rollback signal: mixed-script words stop reporting, known excluded tech names report, comments/strings report, or BSL256 starts duplicating mixed-script facts.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl208 or LatinAndCyrillicSymbolInWord"`.
- Synthetic fixture/module: existing tests cover mixed-script positives, excluded technical names, trailing-language policy, comments/strings, duplicates, and BSL256 overlap.
- Related-rule tests: include BSL256 overlap cases in the targeted selector.
- Parity command/artifact: preserved-source-root selected BSL208 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
