# BSL257 — <span class="doc-lang doc-lang-ru">Унарный плюс в конкатенации строк</span><span class="doc-lang doc-lang-en">Unary Plus sign in string concatenation</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Унарный плюс в конкатенации строк

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Unary Plus sign in string concatenation

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL257` |
| Совместимый псевдоним / Compatible alias | `UnaryPlusInConcatenation` |
| Серьёзность / Severity | `ERROR` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `suspicious`, `brainoverload` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL257` и псевдоним `UnaryPlusInConcatenation` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL257"]
ignore = ["UnaryPlusInConcatenation"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL257
Значение = "пример";  // bsl-disable: BSL257
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UnaryPlusInConcatenation-off
// код без этой диагностики
// BSLLS:UnaryPlusInConcatenation-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL257` and alias `UnaryPlusInConcatenation` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL257"]
ignore = ["UnaryPlusInConcatenation"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL257
Value = "example";  // bsl-disable: BSL257
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UnaryPlusInConcatenation-off
// code without this diagnostic
// BSLLS:UnaryPlusInConcatenation-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При конкатенации строк разработчик может ошибочно написать код вида Строка + + Строка2, т.е. второй плюс платформа распознает как унарный и попытается выолнить преобразование к числу, что в большинстве случаев приведет к ошибке исполнения

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

When concatenating strings, a developer may mistakenly write code String + + String2, i.e. the second plus, the platform recognizes as unary and tries to cast to a number, which in most cases will lead to a runtime exception

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL257
- BSLLS compatible name: UnaryPlusInConcatenation
- Public name/i18n key: UnaryPlusInConcatenation / BSL257
- Default message/i18n key: RULE_MESSAGES_RU["BSL257"]
- Description/i18n key: RULE_METADATA["BSL257"].description
- Severity: ERROR
- Parameters/defaults: none.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: unary plus immediately following string concatenation is ambiguous and can cause runtime errors.
- Why this diagnostic exists: `Строка + +Значение` is usually a typo where the second plus should not be unary.
- Non-goals: numeric unary plus not following another plus, binary addition, comments, and strings.
- Keep/drop decision: keep as a BSLLS-compatible suspicious expression diagnostic.

## Semantic Oracle

- Checked language fact: a `unary_expression` with operator `+` is preceded by another non-space `+` and its operand is not a numeric literal expression.
- Must diagnose when: a unary plus token appears in a concatenation/addition context like `Текст + +Значение`.
- Must not diagnose when: the unary plus is not preceded by plus, the operand is numeric, or the text appears in comments/strings.
- Diagnostic anchor token/range: the unary `+` operator token.
- Quick-fix target, if any: remove the unary plus or add explicit intended operator/parentheses.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through `DiagnosticDocumentContext`.
- CST/domain object types: `unary_expression`, `operator`, operand `expression`, and source bytes for previous-token check.
- Extracted attributes: operator token text/range, previous non-space byte, operand numeric-literal presence.
- Parse-error behavior: without a usable CST root text, no BSL257 diagnostic is emitted.
- Textual/regex behavior, only if genuinely textual: none for valid paths; previous-token check uses source bytes around the CST operator token.

## Examples

### Invalid Examples

1. `Текст = "A" + +Значение;`.
2. `Сообщение = Часть1 + +Часть2;`.
3. `Результат = ПолучитьСтроку() + +Параметр;`.

### Valid Negative Twins

1. `Число = +1;`.
2. `Сумма = A + +1;`.
3. `"A + +B"` inside a string.

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored by CST.
- Preprocessor: no separate semantic target.
- Multiline: previous non-space token may be on a previous line and is checked by source bytes.
- Nested constructs: nested expressions are checked the same way.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: expressions and operator tokens.
- Rules that may report on the same object/range: BSL052 identical expressions, BSL251 ternary usage, BSL265 useless ternary.
- Intended duplicate policy: BSL257 owns unary plus in concatenation; expression-shape rules may report separate issues elsewhere in the expression.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl257UnaryPlusInConcatenation`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 5,803 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final selected parity is recorded in the batch contract evidence.
- Line/common semantic common: no current deltas after BSL258 fix.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none established for this iteration.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: typed `unary_expression` nodes and constant-time source-byte lookback per candidate.
- Expected complexity: O(unary expressions) only when BSL257 is enabled.
- Shared-pass reuse: independent runtime rule using existing parsed tree and typed-node cache.
- Before/after measurement, if changed: no runtime change required.

## Implementation Decision

- Change target: contract and verification only; existing implementation matches current oracle and parity.
- Delta category fixed by this change: none.
- Why not another layer: CST unary expression facts already express the rule.
- Rollback signal: `A + +B` stops reporting or `A + +1` reports.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py -q -k bsl257 --no-cov`.
- Synthetic fixture/module: existing BSLLS fixture test when fixture is available.
- Related-rule tests: expression rules only if operator extraction changes.
- Parity command/artifact: preserved-source-root private batch `--select BSL252,BSL255,BSL257,BSL258,BSL259`.
- Performance command/artifact: selected parity wall time.
- Ruff/lint: touched files.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming completion.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
