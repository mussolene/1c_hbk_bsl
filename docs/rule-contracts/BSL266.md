# BSL266 — <span class="doc-lang doc-lang-ru">Работа с параметром "Отказ"</span><span class="doc-lang doc-lang-en">Using parameter "Cancel"</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Работа с параметром "Отказ"

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Using parameter "Cancel"

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL266` |
| Совместимый псевдоним / Compatible alias | `UsingCancelParameter` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `correctness`, `events` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL266` и псевдоним `UsingCancelParameter` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL266"]
ignore = ["UsingCancelParameter"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL266
Значение = "пример";  // bsl-disable: BSL266
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UsingCancelParameter-off
// код без этой диагностики
// BSLLS:UsingCancelParameter-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL266` and alias `UsingCancelParameter` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL266"]
ignore = ["UsingCancelParameter"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL266
Value = "example";  // bsl-disable: BSL266
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UsingCancelParameter-off
// code without this diagnostic
// BSLLS:UsingCancelParameter-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

В обработчиках событий модулей объектов, наборов записей, форм и т.п., содержащих параметр Отказ (ПриЗаписи, ОбработкаПроверкиЗаполнения, ТоварыПередНачаломДобавления и т.п.), не следует присваивать этому параметру значение Ложь.
Это требование обусловлено тем, что, как правило, в коде обработчиков событий параметр Отказ может устанавливаться сразу в нескольких последовательных проверках (или в нескольких подписках на одно и то же событие). В таком случае к моменту выполнения очередной проверки параметр Отказ уже может заранее содержать значение Истина, и можно ошибочно сбросить его обратно в Ложь.
Кроме того, при доработках конфигурации на внедрении число этих проверок может увеличиться.

## Примеры

### Неправильно

```bsl
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
  ...
  Отказ = ЕстьОшибкиЗаполнения();
  ...
КонецПроцедуры
```

### Правильно

```bsl
Процедура ОбработкаПроверкиЗаполнения(Отказ, ПроверяемыеРеквизиты)
  ...
  Если ЕстьОшибкиЗаполнения() Тогда
   Отказ = Истина;
  КонецЕсли;
  ...
КонецПроцедуры
```

или

```bsl
Отказ = Отказ Или ЕстьОшибкиЗаполнения();
```

## Источники

* [Работа с параметром "Отказ" в обработчиках событий](https://its.1c.ru/db/v8std#content:686:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

In event handlers of object's modules, record sets, forms and etc. using parameter "Cancel" (for example BeforeWrite and etc.) it should not be assigned value "false".
This is due to the fact, that in code of event handlers the parameter "Cancel" can be set in several consecutive checks (or in several subscriptions on the same event). In this case, by the time the next check is performed, the Cancel parameter may already contain the True value, and you can erroneously reset it back to False.
In addition, with configuration improvements, the number of these checks may increase.

## Examples

### Incorrect

```bsl
Procedure BeforeWrite(Cancel)
  ...
  Cancel = CheckName();
  ...
EndProcedure
```

### Correct

```bsl
Procedure BeforeWrite(Cancel)
  ...
  If CheckName() Then
   Cancel = True;
  EndIf;
  ...
EndProcedure
```

or

```bsl
Cancel = Cancel or CheckName();
```

## Sources

* [Standart: Working with the "Cancel" option in event handlers (RU)](https://its.1c.ru/db/v8std#content:686:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL266
- BSLLS compatible name: UsingCancelParameter
- Public name/i18n key: BSL266 / UsingCancelParameter
- Default message/i18n key: RULE_MESSAGES_RU["BSL266"] and RULE_MESSAGES_RU_FMT["BSL266"]
- Description/i18n key: RULE_METADATA["BSL266"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: event handlers should set the cancel parameter only to cancel execution, not reset it to a non-cancel value.
- Why this diagnostic exists: assigning `Отказ`/`Cancel` to anything other than `Истина`/`True` or an accumulated cancel expression can accidentally clear a cancellation requested by another check.
- Non-goals: reporting reads of the cancel parameter, assignments to unrelated parameters, or validating event-handler names.
- Keep/drop decision: keep as a BSLLS-compatible event-correctness diagnostic.

## Semantic Oracle

- Checked language fact: a method parameter named `Отказ` or `Cancel` is assigned in the method body to a value other than true or an accumulated self-OR expression.
- Must diagnose when: the assignment left side is the cancel parameter in the method body and the right side is not `Истина`/`True` and not an expression containing both the cancel parameter and `Или`/`Or`.
- Must not diagnose when: the method has no `Отказ`/`Cancel` parameter, another variable is assigned, the apparent assignment is a default value in the method signature, the assignment is `Отказ = Истина`, the assignment is `Cancel = True`, or the assignment accumulates through `Отказ Или ...` / `Cancel Or ...`.
- Diagnostic anchor token/range: the full assignment line from the cancel parameter identifier to the line end.
- Quick-fix target, if any: replace reset-style assignment with `Истина`/`True` or preserve accumulated cancellation through `Отказ = Отказ Или ...`.

## CST Or Domain Fact Model

- Parser/tree source: `ProcInfo` parameter facts and bounded method-body assignment lines.
- CST/domain object types: procedure/function declarations, parameter identifiers, body assignment lvalue occurrences.
- Extracted attributes: method parameter names, cancel parameter case-insensitive match, full signature end line, assignment left-side name/range, and right-side expression text.
- Parse-error behavior: only parsed method facts are checked; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: current implementation uses a bounded line-level assignment-lvalue regex inside parsed method bodies; it must not scan outside method bodies or parameter lists.

## Examples

### Invalid Examples

1. `Процедура ПередЗаписью(Отказ)` with `Отказ = Ложь;`.
2. `Процедура ПередЗаписью(Отказ)` with `Отказ = Проверить();`.
3. `Procedure BeforeWrite(Cancel)` with `Cancel = False;`.

### Valid Negative Twins

1. `Отказ = Истина;`.
2. `Отказ = Отказ Или Не Проверить();`.
3. A method without an `Отказ`/`Cancel` parameter assigns another variable.

### Edge Examples

- Comments: inline comments after the assignment do not change the right-side value.
- Strings: string contents are not lvalue positions.
- Preprocessor: parsed method-body lines are checked the same way.
- Multiline: multiline signatures are skipped before body scanning; only single-line assignment facts are currently handled by the bounded lvalue scanner.
- Nested constructs: assignments inside nested blocks of the same method body count.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: method parameters and assignments.
- Rules that may report on the same object/range: BSL062 checks unused parameters, BSL193 checks outgoing function parameters, BSL240 checks by-value parameter rewrites, and BSL254 checks client/server by-value transfer.
- Intended duplicate policy: keep BSL266 separate; it reports cancel-parameter semantics, not general parameter usage or assignment style.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl266UsingCancelParameter`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 16 BSL266 diagnostics matched by file, range, and code.
- Line/common semantic common: current implementation anchors on the assignment line.
- Ours-only categories: fixed six false positives where `Отказ = Ложь` appeared as a default value in a multiline method signature.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing shared method-contract pass over `ProcInfo` objects and bounded body-line scan only for methods with a cancel parameter.
- Expected complexity: O(body lines of methods whose parameters include `Отказ` or `Cancel`).
- Shared-pass reuse: shared with BSL192, BSL193, BSL194, and BSL228 method-contract diagnostics.
- Before/after measurement, if changed: targeted BSL266 pytest covers behavior; selected parity is exact.

## Implementation Decision

- Change target: BSL266 body start selection, synthetic coverage, contract, and audit state.
- Delta category fixed by this change: onec-only false positives from scanning multiline signature default values as method-body assignments.
- Why not another layer: `ProcInfo` already exposes the full parameter-list end line needed to start the bounded body scan correctly.
- Rollback signal: `Отказ = Ложь` in the body stops reporting, signature defaults report, `Отказ = Истина` reports, self-OR accumulation reports, assignment character loses indentation, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl266 --no-cov`.
- Synthetic fixture/module: inline tests for false/true assignments, multiline signature default values, self-OR accumulation, unrelated parameter, and English `Cancel`.
- Related-rule tests: full extended suite because BSL266 shares the method-contract pass.
- Parity command/artifact: preserved-source-root selected BSL266 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_diagnostics_method_contracts.py src/onec_hbk_bsl/analysis/diagnostic/rules/method_contract_rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
