# BSL179 — <span class="doc-lang doc-lang-ru">Устаревшее использование типа "УправляемаяФорма"</span><span class="doc-lang doc-lang-en">Deprecated ManagedForm type</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Устаревшее использование типа "УправляемаяФорма"

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Deprecated ManagedForm type

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL179` |
| Совместимый псевдоним / Compatible alias | `DeprecatedTypeManagedForm` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `deprecated`, `ui` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL179` и псевдоним `DeprecatedTypeManagedForm` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL179"]
ignore = ["DeprecatedTypeManagedForm"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL179
Значение = "пример";  // bsl-disable: BSL179
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:DeprecatedTypeManagedForm-off
// код без этой диагностики
// BSLLS:DeprecatedTypeManagedForm-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL179` and alias `DeprecatedTypeManagedForm` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL179"]
ignore = ["DeprecatedTypeManagedForm"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL179
Value = "example";  // bsl-disable: BSL179
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:DeprecatedTypeManagedForm-off
// code without this diagnostic
// BSLLS:DeprecatedTypeManagedForm-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Начиная с версии платформы 8.3.14 тип "УправляемаяФорма" был переименован, теперь правильно использовать "ФормаКлиентскогоПриложения"

## Источники

* Источник: [Описание обновлений](https://dl03.1c.ru/content/Platform/8_3_16_1148/1cv8upd_8_3_16_1148.htm)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Starting from the platform version 8.3.14, the "ManagedForm" type has been renamed, now it is correct to use the "ClientApplicationForm"

## Sources

* Source: [Platform 8.3.16 changelog (RU)](https://dl03.1c.ru/content/Platform/8_3_16_1148/1cv8upd_8_3_16_1148.htm)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL179
- BSLLS compatible name: DeprecatedTypeManagedForm
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: Since platform 8.3.14 the `УправляемаяФорма`/`ManagedForm` type name is deprecated in favor of `ФормаКлиентскогоПриложения`.
- Non-goals: Do not diagnose arbitrary string literals or user methods whose names merely contain `Тип`/`Type`.

## Semantic Oracle

- Checked language fact: A global `Тип`/`Type` call receives the deprecated managed-form type string.
- Must diagnose when: ordinary code calls `Тип("УправляемаяФорма")` or `Type("ManagedForm")`.
- Must diagnose when: the call appears inside a larger expression such as comparison with another type.
- Must not diagnose when: the text is inside a comment or string literal.
- Must not diagnose when: the method name is only a prefix of a longer identifier.
- Diagnostic anchor token/range: The deprecated string argument token.

## CST Or Domain Fact Model

- CST/domain object types: masked source line, global `Тип`/`Type` call token.
- Extracted attributes: deprecated string literal, line, start column, end column.
- Parse-error behavior: Line-local complete calls may still be reported in otherwise broken files.
- Shared semantic object: Deprecated platform type-name reference.

## Examples

### Invalid Examples

1. `Если Тип(Форма) = Тип("УправляемаяФорма") Тогда`.
2. `If Type(Form) = Type("ManagedForm") Then`.
3. `Тип("УправляемаяФорма");`.

### Valid Negative Twins

1. `Текст = "Тип(""УправляемаяФорма"")";`.
2. `// Если Тип(Форма) = Тип("УправляемаяФорма") Тогда`.
3. `ТипФормы("УправляемаяФорма");`.

## Related-Rule Cluster

- Rules that may report on the same object/range: BSL175, BSL176, BSL178.
- Intended duplicate policy: Keep BSL179 as the dedicated managed-form type diagnostic.

## BSLLS Parity Taxonomy

- Compared input-file set: Private representative configuration corpus with 5803 BSL files plus synthetic fixtures.
- Config/exclude mode: Preserve source root for BSLLS; normal onec CLI path.
- File-key normalization: Repository parity runner normalized relative paths.
- Coordinate normalization: UTF-8 byte columns converted to LSP character columns.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: Single pass over masked source lines.
- Expected complexity: O(lines), no whole-project state required.

## Implementation Decision

- Change target: No runtime change required; existing `DeprecatedTypeManagedFormRule` matches the contract.
- Delta category fixed by this change: No BSLLS delta on the compared corpus; added contract and synthetic negative coverage.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q -k BSL179 --no-cov`.
- Leak check: `git diff --check` and secret-pattern scan of staged/documented changes.
- OACS evidence/checkpoint: Record verification commands and checkpoint after successful parity run.

## Completion Checklist

- [x] Contract has semantic oracle.
- [x] Contract has CST/domain fact model.
- [x] Contract has positive and negative examples.
- [x] Contract has parity taxonomy.
- [x] Contract has performance contract.
- [x] Contract has verification plan.

</div>

</details>

<!-- engineering-contract:end -->
