# BSL269 UsingLikeInQuery Contract

## Rule Identity

- Code: BSL269
- BSLLS compatible name: UsingLikeInQuery
- Public name/i18n key: UsingLikeInQuery / BSL269
- Default message/i18n key: RULE_MESSAGES_RU["BSL269"]
- Description/i18n key: RULE_METADATA["BSL269"].description
- Severity: INFORMATION
- Parameters/defaults: none; BSLLS rule is not activated by default, but is supported in explicit selection mode.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: query `ПОДОБНО` / `LIKE` usage is discouraged because it can produce unpredictable or slow query plans.
- Why this diagnostic exists: LIKE predicates often prevent efficient indexed search and should be reviewed or replaced.
- Non-goals: do not diagnose BSL string text that is not a successfully parsed SDBL query, comments, malformed partial query fragments without an SDBL `like_expression`, or non-LIKE query predicates.
- Keep/drop decision: keep as a BSLLS-compatible informational query diagnostic.

## Semantic Oracle

- Checked language fact: a parsed SDBL query contains a `like_expression`.
- Must diagnose when: SDBL CST has a `like_expression` produced from `ПОДОБНО` / `LIKE`.
- Must not diagnose when: query text is malformed enough that no SDBL `like_expression` exists, `ПОДОБНО` appears only inside a query string literal/comment, or the BSL string is not a query block.
- Diagnostic anchor token/range: full SDBL `like_expression` range, mapped back to original BSL query-string coordinates.
- Quick-fix target, if any: rewrite the query predicate to use exact/range comparisons, normalized search fields, or another indexed search strategy.

## CST Or Domain Fact Model

- Parser/tree source: unescaped SDBL tree-sitter CST from query text blocks for semantic facts, with the original `QueryTextBlockInfo.sdbl_tree` used only as a coordinate source when it has the same like-expression start.
- CST/domain object types: `QueryTextBlockInfo`, SDBL `like_expression`, unescaped line/byte maps, and original query-head CST ranges.
- Extracted attributes: like-expression start/end row and byte columns, mapped BSL line/character range; original-source range is ignored when it would introduce a `LIKE` fact that does not exist in the unescaped semantic CST.
- Parse-error behavior: no regex fallback when query blocks have SDBL CST available; malformed query fragments are reported only if the SDBL parser still produces a `like_expression`.
- Textual/regex behavior, only if genuinely textual: fallback regex is retained only for environments without SDBL CST/query block facts.

## Examples

### Invalid Examples

1. `ГДЕ Т.Код ПОДОБНО &Шаблон`.
2. `WHERE T.Code LIKE &Pattern`.
3. `ГДЕ Т.Наименование ПОДОБНО "А%"`.

### Valid Negative Twins

1. `ГДЕ Т.Код = &Код`.
2. `ГДЕ Т.Код В (&Коды)`.
3. `ГДЕ Т.Наименование >= &Начало И Т.Наименование < &Конец`.
4. A malformed string fragment containing `ПОДОБНО` but no parsed SDBL `like_expression`.

### Edge Examples

- Comments: SDBL comments do not produce `like_expression`.
- Strings: `ПОДОБНО` / `LIKE` inside SDBL string literals must not report.
- Preprocessor: BSL preprocessor is irrelevant after query text extraction.
- Multiline: like expressions may span query-string lines; mapped range may span BSL lines.
- Nested constructs: LIKE inside nested query expressions is diagnosed the same way.
- BSL/SDBL variants, if relevant: SDBL CST is the source of truth.

## Related-Rule Cluster

- Shared semantic object: SDBL query predicates.
- Rules that may report on the same object/range: BSL201 reports incorrect LIKE right-hand operands; BSL235 reports query parse errors.
- Intended duplicate policy: BSL269 and BSL201 may both report the same LIKE expression because BSL269 is informational usage guidance and BSL201 is correctness-focused invalid operand guidance.
- Cluster fixture or probe: query text tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus local synthetic query fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final private parity has `190` exact common facts after switching BSL269 from keyword regex ranges to parsed `like_expression` facts with original-CST range mapping.
- Line/common semantic common: final private parity has `114` same-start range-only pairs; the remaining range delta is around escaped quote boundaries where BSLLS and tree-sitter expose different end-affinity.
- Ours-only categories: final private parity has `120` ours-only diagnostics: `114` are range counterparts to BSLLS same-start facts, and `6` are additional parsed-CST facts not emitted by BSLLS.
- BSLLS-only categories: final private parity has `114` BSLLS-only diagnostics, all classified as same-start range counterparts.
- Range-only categories: fixed the high-volume keyword-only range mismatch; remaining range-only deltas are escaped quote boundary end-column differences.
- Known BSLLS defects: none established for this iteration.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing query text blocks; one unescaped SDBL parse supplies semantic facts, while the already-built original SDBL CST supplies BSLLS-like ranges for matching starts.
- Expected complexity: O(query CST nodes) for selected query blocks.
- Shared-pass reuse: BSL269 remains in the BSL220/BSL235/BSL269 query text pool.
- Before/after measurement, if changed: private parity improved from keyword/range-only `onec=310`, `bslls=304`, `only_bslls=304`, `only_onec=310` to `onec=310`, `bslls=304`, `only_bslls=114`, `only_onec=120`; targeted query tests pass in 0.13s.

## Implementation Decision

- Change target: BSL269 branch in `query_text_rules.py`.
- Delta category fixed by this change: high-volume keyword-only range mismatch and original-CST false positives inside query string literals.
- Why not another layer: SDBL parsing/mapping helpers already exist in `query_text_rules.py` for BSL220/BSL235.
- Rollback signal: keyword-only ranges return, source-CST-only `LIKE` inside strings reports, malformed regex-only fragments report, or private parity worsens.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k 'bsl269 or bsl201 or bsl235' --no-cov`.
- Synthetic fixture/module: SDBL query fixtures for RU/EN LIKE, explicit non-LIKE negatives, LIKE inside string literal, and malformed no-expression fragment.
- Related-rule tests: BSL201 LIKE operand tests and BSL235 parse-error tests.
- Parity command/artifact: preserved-source-root private batch BSL269 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/query_text_rules.py tests/test_diagnostics_query_metadata.py`.
- Leak check: `git diff --check` and changed-file private token scan.
- OACS evidence/checkpoint: ingest verification outputs and add checkpoint before claiming done.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
