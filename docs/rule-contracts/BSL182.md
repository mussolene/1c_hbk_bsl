# BSL182 — <span class="doc-lang doc-lang-ru">Избыточная проверка параметра АвтоТест</span><span class="doc-lang doc-lang-en">Excessive AutoTest Check</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Избыточная проверка параметра АвтоТест

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Excessive AutoTest Check

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL182` |
| Совместимый псевдоним / Compatible alias | `ExcessiveAutoTestCheck` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `testing` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL182` и псевдоним `ExcessiveAutoTestCheck` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL182"]
ignore = ["ExcessiveAutoTestCheck"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL182
Значение = "пример";  // bsl-disable: BSL182
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ExcessiveAutoTestCheck-off
// код без этой диагностики
// BSLLS:ExcessiveAutoTestCheck-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL182` and alias `ExcessiveAutoTestCheck` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL182"]
ignore = ["ExcessiveAutoTestCheck"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL182
Value = "example";  // bsl-disable: BSL182
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ExcessiveAutoTestCheck-off
// code without this diagnostic
// BSLLS:ExcessiveAutoTestCheck-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики
Стандарт 772 "Взаимодействие со средствами автоматизированного тестирования" был отменен.
В связи с этим, больше не нужна проверка параметра "АвтоТест" в коде форм.

## Примеры
```bsl
Если Параметры.Свойство("АвтоТест") Тогда
    Возврат;
КонецЕсли;
```

и обработчике ОбработкаЗаполнения модуля объекта (набора записей):

```bsl
// Пропускаем обработку, чтобы гарантировать получение формы при передаче параметра "АвтоТест".
Если ДанныеЗаполнения = "АвтоТест" Тогда
    Возврат;
КонецЕсли;
```

## Источники
* Источник: [Стандарт: Тексты модулей пункт 3](https://its.1c.ru/db/v8std#content:456:hdoc:3)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description
Standard 772, Interaction with Automated Testing Tools, has been canceled. In this regard, verification of the "АвтоТест" parameter in the form code is no longer necessary.

## Examples
```bsl
If Parameters.Property("АвтоТест") Then
    Return;
EndIf;
```

and in handler Filling in object module:

```bsl
// Skip processing to get the form when sending the "АвтоТест" program.
If FillData = "АвтоТест" Then
    Return;
EndIf;
```

## Sources
* Источник: [Standard: Modules. Part 3 (RU)](https://its.1c.ru/db/v8std#content:456:hdoc:3)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL182
- BSLLS compatible name: ExcessiveAutoTestCheck
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: The old automated-testing interaction standard was cancelled; form/object code should not short-circuit execution only because the `АвтоТест`/`AutoTest` parameter is present.
- Non-goals: Do not report business logic that checks the same string but executes additional actions before returning.

## Semantic Oracle

- Checked language fact: An `if`/`elseif` branch checks `.Свойство("АвтоТест")`, `.Property("AutoTest")`, `= "АвтоТест"`, or `= "AutoTest"` and the branch body contains only `Возврат`/`Return`.
- Must diagnose when: the checked branch body has exactly one return statement.
- Must diagnose when: the condition is in the first `Если` branch or in an `ИначеЕсли` branch.
- Must not diagnose when: the branch body contains another statement in addition to return.
- Must not diagnose when: the same text is inside strings or comments.
- Diagnostic anchor token/range: The whole `if`/`elseif` branch construct reported by BSLLS.

## CST Or Domain Fact Model

- CST/domain object types: `if_statement`, `elseif_clause`, child `expression`, child `return_statement`.
- Extracted attributes: branch expression text, branch body statement nodes, branch start/end range.
- Parse-error behavior: Do not report if the CST branch shape cannot prove an expression and a single return body.
- Shared semantic object: AutoTest compatibility branch.

## Examples

### Invalid Examples

1. `Если Параметры.Свойство("АвтоТест") Тогда Возврат; КонецЕсли;`.
2. `Если ДанныеЗаполнения = "АвтоТест" Тогда Возврат; КонецЕсли;`.
3. `If Parameters.Property("AutoTest") Then Return; EndIf;`.

### Valid Negative Twins

1. `Если Перечень.Свойство("АвтоТест") Тогда Перечень.Удалить("АвтоТест"); Возврат; КонецЕсли;`.
2. `Текст = "Параметры.Свойство(""АвтоТест"")";`.
3. `// Если Параметры.Свойство("АвтоТест") Тогда`.

## Related-Rule Cluster

- Rules that may report on the same object/range: BSL004, BSL030, BSL199.
- Intended duplicate policy: Keep BSL182 only for the obsolete AutoTest-return branch; structural if diagnostics remain independent.

## BSLLS Parity Taxonomy

- Compared input-file set: Private representative configuration corpus with 5803 BSL files plus synthetic fixtures.
- Config/exclude mode: Preserve source root for BSLLS; normal onec CLI path.
- File-key normalization: Repository parity runner normalized relative paths.
- Coordinate normalization: UTF-8 byte columns converted to LSP character columns.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: Single CST pass over `if_statement` nodes.
- Expected complexity: O(number of if branches), no project-wide state.

## Implementation Decision

- Change target: Replace legacy light-pool duplicate-name heuristic with `ExcessiveAutoTestCheckRule` over CST if branches.
- Delta category fixed by this change: Synthetic semantic mismatch for official BSLLS examples; current corpus still has exact parity.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q -k BSL182 --no-cov`.
- Leak check: `git diff --check` and secret-pattern scan of staged/documented changes.
- OACS evidence/checkpoint: Record verification commands and checkpoint after successful parity run.

## Completion Checklist

- [x] Contract has semantic oracle.
- [x] Contract has CST/domain fact model.
- [x] Contract has positive and negative examples.
- [x] Contract has parity taxonomy.
- [x] Contract has performance contract.
- [x] Contract has verification plan.

</div>

</details>

<!-- engineering-contract:end -->
