# BSL260 — <span class="doc-lang doc-lang-ru">Небезопасное использование метода НайтиПоКоду()</span><span class="doc-lang doc-lang-en">Unsafe FindByCode() method usage</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Небезопасное использование метода НайтиПоКоду()

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Unsafe FindByCode() method usage

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL260` |
| Совместимый псевдоним / Compatible alias | `UnsafeFindByCode` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `correctness`, `robustness` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL260` и псевдоним `UnsafeFindByCode` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL260"]
ignore = ["UnsafeFindByCode"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL260
Значение = "пример";  // bsl-disable: BSL260
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UnsafeFindByCode-off
// код без этой диагностики
// BSLLS:UnsafeFindByCode-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL260` and alias `UnsafeFindByCode` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL260"]
ignore = ["UnsafeFindByCode"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL260
Value = "example";  // bsl-disable: BSL260
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UnsafeFindByCode-off
// code without this diagnostic
// BSLLS:UnsafeFindByCode-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Диагностика обнаруживает использование метода `НайтиПоКоду()` (или `FindByCode()`) для справочников, у которых:

- отключен контроль уникальности кода (свойство **Контроль уникальности** установлено в значение `Ложь`)
- или включены серии кодов не по всему справочнику (свойство **Серии кодов** не равно `ПоВсемуСправочнику`)

В таких случаях использование метода `НайтиПоКоду()` может привести к неожиданному поведению, так как код может быть не уникальным в пределах всего справочника или может существовать несколько элементов с одинаковым кодом в разных сериях.

## Примеры

Неправильно:

```bsl
// Справочник без контроля уникальности
СправочникБезКонтроля = Справочники.СправочникБезКонтроляУникальности.НайтиПоКоду("001");
```

```bsl
// Справочник с сериями кодов "В пределах подчинения"
СправочникССериями = Справочники.СправочникССериямиПодчинения.НайтиПоКоду("001");
```

Правильно:

```bsl
// Использование метода НайтиПоКоду() для справочника с контролем уникальности
// и сериями кодов по всему справочнику
СправочникСКонтролем = Справочники.СправочникСКонтролемУникальности.НайтиПоКоду("001");
```

```bsl
// Альтернативный вариант - использовать метод НайтиПоНаименованию() или другие методы поиска
Справочник = Справочники.СправочникБезКонтроляУникальности.НайтиПоНаименованию("Элемент");
```

## Источники

- Источник: [Метод НайтиПоКоду()](https://its.1c.ru/db/v8std#content:456:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

The diagnostic detects the use of the `FindByCode()` method (or `НайтиПоКоду()`) for catalogs that have:

- code uniqueness control disabled (the **Check unique** property is set to `False`)
- or code series enabled not for the entire catalog (the **Code series** property is not equal to `Whole catalog`)

In such cases, using the `FindByCode()` method can lead to unexpected behavior, as the code may not be unique within the entire catalog or there may be multiple elements with the same code in different series.

## Examples

Incorrect:

```bsl
// Catalog without uniqueness control
CatalogWithoutControl = Catalogs.CatalogWithoutUniquenessControl.FindByCode("001");
```

```bsl
// Catalog with code series "Within subordination"
CatalogWithSeries = Catalogs.CatalogWithSubordinationSeries.FindByCode("001");
```

Correct:

```bsl
// Using FindByCode() method for catalog with uniqueness control
// and code series for the entire catalog
CatalogWithControl = Catalogs.CatalogWithUniquenessControl.FindByCode("001");
```

```bsl
// Alternative option - use FindByName() method or other search methods
Catalog = Catalogs.CatalogWithoutUniquenessControl.FindByName("Element");
```

## Sources

- Source: [FindByCode() method](https://its.1c.ru/db/v8std#content:456:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL260
- BSLLS compatible name: UnsafeFindByCode
- Public name/i18n key: UnsafeFindByCode / BSL260
- Default message/i18n key: RULE_MESSAGES_RU["BSL260"]
- Description/i18n key: RULE_METADATA["BSL260"].description
- Severity: WARNING
- Parameters/defaults: metadata safety is derived from object XML `CheckUnique` and `CodeSeries`.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: `НайтиПоКоду()` / `FindByCode()` is unsafe when metadata does not guarantee unique whole-object codes.
- Why this diagnostic exists: code lookup can return an unexpected object if code uniqueness or numbering scope is unsafe.
- Non-goals: safe metadata, non-code lookup methods, comments/strings, and calls whose metadata root cannot be resolved.
- Keep/drop decision: keep as a BSLLS-compatible metadata correctness diagnostic.

## Semantic Oracle

- Checked language fact: a `method_call` to `НайтиПоКоду` / `FindByCode` is made on a metadata object whose code settings are unsafe.
- Must diagnose when: metadata index says the target object has non-unique codes or unsafe code series.
- Must not diagnose when: metadata is safe, missing, unrelated, or the call appears outside code.
- Diagnostic anchor token/range: method name token.
- Quick-fix target, if any: use a unique reference, safe lookup, or correct metadata settings.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST plus cached configuration metadata index.
- CST/domain object types: `method_call`, receiver/member access text, metadata XML object settings.
- Extracted attributes: method name, receiver metadata key, `CheckUnique`, `CodeSeries`, and method-name range.
- Parse-error behavior: no CST method-call facts means no BSL260 diagnostic.
- Textual/regex behavior, only if genuinely textual: none for valid paths.

## Examples

### Invalid Examples

1. `Справочники.Товары.НайтиПоКоду("1")` when `CheckUnique=false`.
2. `Catalogs.Items.FindByCode("1")` with unsafe code series.
3. `ПланыСчетов.План.НайтиПоКоду("1")` with unsafe metadata.

### Valid Negative Twins

1. `Справочники.Товары.НайтиПоНаименованию("A")`.
2. `НайтиПоКоду` on safe whole-catalog unique codes.
3. `"Справочники.Товары.НайтиПоКоду()"` inside a string.

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored by CST.
- Preprocessor: no separate semantic target.
- Multiline: method-call range comes from CST.
- Nested constructs: nested calls are checked the same way.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: metadata lookup method calls.
- Rules that may report on the same object/range: BSL268 can also report find-by-code/name performance usage; BSL175/BSL176 can report deprecated API nearby.
- Intended duplicate policy: BSL260 reports unsafe metadata semantics; BSL268 reports broad find-by-string usage.
- Cluster fixture or probe: metadata tail tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 5,803 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec coordinates.
- Exact common: final selected batch parity is exact at `onec=13`, `bslls=13`.
- Line/common semantic common: no BSL260 deltas.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing light pool method-call nodes and cached metadata index.
- Expected complexity: O(method calls + metadata object XML files once per config root).
- Shared-pass reuse: BSL169/170/181/182/196/260 light pool.
- Before/after measurement, if changed: no runtime change required.

## Implementation Decision

- Change target: contract and verification only.
- Delta category fixed by this change: none.
- Why not another layer: existing CST plus metadata cache already matches current parity.
- Rollback signal: safe metadata reports or unsafe metadata stops reporting.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k unsafe_find_by_code --no-cov`.
- Synthetic fixture/module: unsafe metadata positive and safe metadata negative.
- Related-rule tests: BSL268 if shared lookup logic changes.
- Parity command/artifact: selected private batch over remaining batch rules.
- Performance command/artifact: selected parity wall time.
- Ruff/lint: touched files.
- Leak check: changed-file private marker scan.
- OACS evidence/checkpoint: required.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
