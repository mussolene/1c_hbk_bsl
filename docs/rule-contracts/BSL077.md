# BSL077 — <span class="doc-lang doc-lang-ru">Использование 'ВЫБРАТЬ ПЕРВЫЕ' без 'УПОРЯДОЧИТЬ ПО'</span><span class="doc-lang doc-lang-en">Using 'SELECT TOP' without 'ORDER BY'</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование 'ВЫБРАТЬ ПЕРВЫЕ' без 'УПОРЯДОЧИТЬ ПО'

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Using 'SELECT TOP' without 'ORDER BY'

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL077` |
| Совместимый псевдоним / Compatible alias | `SelectTopWithoutOrderBy` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `performance`, `maintainability` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL077` и псевдоним `SelectTopWithoutOrderBy` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL077"]
ignore = ["SelectTopWithoutOrderBy"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL077
Значение = "пример";  // bsl-disable: BSL077
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:SelectTopWithoutOrderBy-off
// код без этой диагностики
// BSLLS:SelectTopWithoutOrderBy-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL077` and alias `SelectTopWithoutOrderBy` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL077"]
ignore = ["SelectTopWithoutOrderBy"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL077
Value = "example";  // bsl-disable: BSL077
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:SelectTopWithoutOrderBy-off
// code without this diagnostic
// BSLLS:SelectTopWithoutOrderBy-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Использование конструкции `ПЕРВЫЕ N` без указания порядка сортировки в `УПОРЯДОЧИТЬ ПО` или условий в секции `ГДЕ` чревато получением неожиданных результатов:

- В разных СУБД порядок возвращаемых результатов может отличаться
- Порядок в разных копиях ИБ будет отличаться от порядка, который ожидает разработчик

По стандарту, отсутствие предложения `УПОРЯДОЧИТЬ ПО` оправдано только в тех случаях, когда

- алгоритм обработки результатов запроса не рассчитывает на определенный порядок записей
- результат обработки выполненного запроса не показывается пользователю
- результат запроса - заведомо одна запись

В приведенных случаях рекомендуется не добавлять предложение `УПОРЯДОЧИТЬ ПО` в текст запроса, так как это приводит к дополнительным затратам времени при выполнении запроса.

### Алгоритм работы диагностики

В процессе анализа считаются ошибочными конструкции:

- Использование `ПЕРВЫЕ N` в объединении вне зависимости от наличия `УПОРЯДОЧИТЬ ПО`, т.к. упорядочивание происходит уже после объединения
- Использование `ПЕРВЫЕ N`, где `N > 1` при отсутствии `УПОРЯДОЧИТЬ ПО`
- Использование `ПЕРВЫЕ 1`, при отсутствии `УПОРЯДОЧИТЬ ПО` и условий в `ГДЕ`. Данное правило по умолчанию отключено параметром диагностики

## Примеры

```bsl
ВЫБРАТЬ ПЕРВЫЕ 1                            // <-- Нет ошибка, есть условие
   Справочник.Ссылка
ИЗ
   Справочник.Контрагенты КАК Справочник
ГДЕ
   Справочник.Ссылка В (
       ВЫБРАТЬ ПЕРВЫЕ 10                   // <-- Ошибка, нет сортировки
           Ссылка
       ИЗ
           Справочник.Контрагенты)

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ ПЕРВЫЕ 10                          // <-- Ошибка, нет сортировки (и не может быть)
   Справочник.Ссылка
ИЗ
   Справочник.Контрагенты КАК Справочник

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ ПЕРВЫЕ 1                          // <-- Всегда ошибка, даже 1
   Справочник.Ссылка
ИЗ
   Справочник.Контрагенты КАК Справочник

УПОРЯДОЧИТЬ ПО
   Ссылка
```

## Источники

- [Стандарт: Упорядочивание результатов запроса](https://its.1c.ru/db/v8std#content:412:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Using the `TOP N` construct without specifying the sort order in `ORDER BY` or conditions in the `WHERE` section is fraught with unexpected results:

- The order of the returned results may differ in different DBMSs
- The order in different copies of information security will differ from the order expected by the developer

According to the standard, the absence of the sentence `ORDER BY` is justified only in cases where

- the algorithm for processing query results does not rely on a specific order of records
- the result of processing the executed request is not shown to the user
- query result - obviously one record

In the above cases, it is recommended not to add the clause `ORDER BY` to the request body, as this leads to additional time-consuming execution of the request.

### Diagnostic ignorance in code

During the analysis, constructions are considered erroneous:

- Using `TOP N` in the union regardless of the presence of `ORDER BY` because ordering occurs after the union
- Using `TOP N` where `N> 1` if missing `ORDER BY`
- Using `TOP 1`, if there is no `ORDER BY` and conditions in `WHERE`. This rule is disabled by default by a diagnostic option

## Examples

```bsl
SELECT TOP 1 // < - No error, there is a condition
   Reference.Link
OF
   Directory.Contractors AS Directory
WHERE
   Reference.Ref. IN (
       SELECT TOP 10 // < - Error, no sorting
           Link
       OF
           Reference, Contractors)

UNION ALL

SELECT TOP 10 // < - Error, no sorting (and cannot be)
   Reference.Link
OF
   Directory.Contractors AS Directory

UNION ALL

SELECT TOP 1 // < - Always error, even 1
   Reference.Link
OF
   Directory.Contractors AS Directory

SORT BY
   Link
```

## Sources

- [Standard: Ordering Query Results (RU)](https://its.1c.ru/db/v8std#content:412:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL077
- BSLLS compatible name: SelectTopWithoutOrderBy
- Public name/i18n key: BSL077 / SelectTopWithoutOrderBy
- Default message/i18n key: get_rule("BSL077").message
- Description/i18n key: RULE_METADATA["BSL077"].description
- Severity: WARNING
- Parameters/defaults: none
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Query result truncation with `ПЕРВЫЕ` / `TOP` should be deterministic; use `УПОРЯДОЧИТЬ ПО` / `ORDER BY` when limiting result rows.
- Why this diagnostic exists: It prevents unstable query results and performance-sensitive logic that depends on an undefined row order.
- Non-goals: Queries without `ПЕРВЫЕ` / `TOP`, ordered limited queries, SDBL parse-error blocks, and BSLLS-compatible `TOP 0` / `TOP 1` exceptions are not reported.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: An SDBL `top_clause` in a `select_section` whose owning `query` has no direct `order_by_clause` that makes the selected limited result deterministic.
- Must diagnose when: A query package contains `ВЫБРАТЬ ПЕРВЫЕ` / `SELECT TOP` without an applicable `УПОРЯДОЧИТЬ ПО` / `ORDER BY`.
- Must not diagnose when: The same query has an applicable direct order clause, the embedded SDBL block has parse errors, or the limited selection matches the documented small-limit compatibility exceptions.
- Diagnostic anchor token/range: the SDBL `top_clause` mapped back to the original BSL string-literal line and LSP character range.
- Quick-fix target, if any: none

## CST Or Domain Fact Model

- Parser/tree source: SDBL CST attached to `DocumentSnapshot.query_text_blocks`.
- CST/domain object types: SDBL `query`, `select_section`, `top_clause`, `order_by_clause`, `union_clause`, and `where_clause`.
- Extracted attributes: original BSL LSP position, top limit text, query-union flag, select-where flag, SDBL parse-error flag.
- Parse-error behavior: skip SDBL blocks with parser errors; broken query syntax is not repaired by this rule.
- Textual/regex behavior, only if genuinely textual: none for BSL077; query selection is derived from SDBL CST and original-position mapping.

## Examples

### Invalid Examples

1. `ВЫБРАТЬ ПЕРВЫЕ 1000` from a table without `УПОРЯДОЧИТЬ ПО`.
2. The first query in a query package uses `ПЕРВЫЕ`, while only a later package query is ordered.
3. A union query uses `ПЕРВЫЕ 1` without the compatible filtering exception.

### Valid Negative Twins

1. `ВЫБРАТЬ ПЕРВЫЕ 1000` followed by `УПОРЯДОЧИТЬ ПО` in the same query.
2. A query package where the limited query itself is ordered and later package queries are ordinary reads.
3. A BSLLS-compatible `ПЕРВЫЕ 1` union branch with a `ГДЕ` condition.

### Edge Examples

- Comments: query text extraction preserves SDBL content and ignores BSL comments outside query literals.
- Strings: only embedded query literal content is parsed as SDBL.
- Preprocessor: not a direct target; preprocessed BSL structure is outside BSL077.
- Multiline: original-position mapping anchors diagnostics back into multiline query literals.
- Nested constructs: nested SDBL sections are scoped to their owning `query`.
- BSL/SDBL variants, if relevant: Russian `ВЫБРАТЬ ПЕРВЫЕ` and English `SELECT TOP` are SDBL variants for the same fact.

## Related-Rule Cluster

- Shared semantic object: embedded query text block and SDBL query CST.
- Rules that may report on the same object/range: BSL174, BSL187, BSL191, BSL201, BSL206, BSL207, BSL209, BSL220, BSL235, BSL236, BSL238, BSL269 may also analyze query text, but they target metadata access, aliases, joins, or query text style rather than `top_clause` determinism.
- Intended duplicate policy: do not duplicate BSL077 for the same `top_clause`; allow distinct query diagnostics on the same query block when they describe different semantic facts.
- Cluster fixture or probe: tests/test_diagnostics_query_metadata.py BSL077 tests using SDBL CST.

## BSLLS Parity Taxonomy

- Compared input-file set: original-workspace corpus parity through `check --no-config --select BSL077` and BSLLS `diagnostics.mode=ONLY`.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS `ONLY` mode.
- File-key normalization: original source-root relative keys with BSLLS LSP line conversion.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters; BSLLS LSP lines must be converted before comparison.
- Exact common: 14.
- Line/common semantic common: 14.
- Ours-only categories: none.
- BSLLS-only categories: 2 reports in SDBL parse-error blocks that also contain an order clause.
- Range-only categories: not measured in this iteration.
- Known BSLLS defects: BSLLS may report this rule inside parse-error query text; onec skips malformed SDBL structural rules and `BSL001` owns broken syntax.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `DocumentSnapshot.query_text_blocks` and one SDBL CST traversal per query text block.
- Expected complexity: O(query CST nodes) after query blocks are extracted.
- Shared-pass reuse: shares query text extraction and SDBL CST parsing with query diagnostics that consume `query_text_blocks`.
- Before/after measurement, if changed: no BSL077 semantic traversal changed in this iteration.

## Implementation Decision

- Change target: BSL077 contract and parity taxonomy, not diagnostic logic.
- Delta category fixed by this change: BSLLS-only parse-error/order-present delta classified without changing correct CST rule semantics.
- Why not another layer: the current fact source is already the SDBL CST layer; the missing artifact was the rule state.
- Rollback signal: contract validator fails, BSL077 stops using SDBL CST facts, or targeted BSL077 tests lose the ordered/unordered distinction.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k bsl077 --no-cov`
- Synthetic fixture/module: inline synthetic query modules in `tests/test_diagnostics_query_metadata.py` BSL077 cases.
- Related-rule tests: query diagnostic tests that consume `query_text_blocks`.
- Parity command/artifact: original-workspace local parity artifact with sanitized aggregate reporting only.
- Performance command/artifact: no dedicated performance artifact; traversal source is unchanged.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
