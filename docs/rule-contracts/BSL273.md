# BSL273 — <span class="doc-lang doc-lang-ru">Обращение к виртуальной таблице без параметров</span><span class="doc-lang doc-lang-en">Virtual table call without parameters</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Обращение к виртуальной таблице без параметров

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Virtual table call without parameters

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL273` |
| Совместимый псевдоним / Compatible alias | `VirtualTableCallWithoutParameters` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `query`, `performance` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL273` и псевдоним `VirtualTableCallWithoutParameters` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL273"]
ignore = ["VirtualTableCallWithoutParameters"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL273
Значение = "пример";  // bsl-disable: BSL273
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:VirtualTableCallWithoutParameters-off
// код без этой диагностики
// BSLLS:VirtualTableCallWithoutParameters-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL273` and alias `VirtualTableCallWithoutParameters` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL273"]
ignore = ["VirtualTableCallWithoutParameters"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL273
Value = "example";  // bsl-disable: BSL273
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:VirtualTableCallWithoutParameters-off
// code without this diagnostic
// BSLLS:VirtualTableCallWithoutParameters-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При использовании виртуальных таблиц в запросах, следует передавать в параметры таблиц все условия, относящиеся к данной виртуальной таблице.

Не рекомендуется обращаться к виртуальным таблицам при помощи условий в секции ГДЕ и т.п.

Такой запрос будет возвращать правильный (с точки зрения функциональности) результат, но СУБД будет намного сложнее выбрать оптимальный план для его выполнения. В некоторых случаях это может привести к ошибкам оптимизатора СУБД и значительному замедлению работы запроса.

## Примеры

Например, следующий запрос использует секцию `ГДЕ` запроса для выборки из виртуальной таблицы:
```bsl
Запрос.Текст = "ВЫБРАТЬ
| Номенклатура
|ИЗ
| РегистрНакопления.ТоварыНаСкладах.Остатки()
|ГДЕ
| Склад = &Склад";
```
Возможно, что в результате выполнения этого запроса сначала будут выбраны все записи виртуальной таблицы, а затем из них будет отобрана часть, соответствующая заданному условию.

Рекомендуется ограничивать количество выбираемых записей на самом раннем этапе обработки запроса. Для этого следует передать условия в параметры виртуальной таблицы.

```bsl
Запрос.Текст = "ВЫБРАТЬ
| Номенклатура
|ИЗ
| РегистрНакопления.ТоварыНаСкладах.Остатки(, Склад = &Склад)";
```

## Источники

* Источник: [Стандарт: Обращения к виртуальным таблицам](https://its.1c.ru/db/v8std#content:657:hdoc)
* Источник: [Стандарт: Эффективное обращение к виртуальной таблице «Остатки»](https://its.1c.ru/db/v8std#content:733:hdoc)
* Источник: [Рекомендация 1С: Использование параметра Условие при обращении к виртуальной таблице](https://its.1c.ru/db/metod8dev/content/5457/hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

When using virtual tables in queries, you should specify all conditions related to it in the table parameters.

It is not recommended to refer to virtual tables using conditions in the WHERE section, etc.

Such a query will return the correct (in terms of functionality) result, but it will be much more difficult for the DBMS to choose the optimal plan for its execution. In some cases, this can lead to errors in the DBMS optimizer and a significant slowdown in query performance.

## Examples

For example, a query uses the `WHERE` section to filter virtual table data:
```bsl
Query.Text = "SELECT
| Good
|FROM
| AccumulationRegister.MyGoods.Turnovers()
|WHERE
| Warehouse = &Warehouse";
```
When executing this query, first all records of the virtual table will be selected, then the part corresponding to the specified condition will be selected from them.

It is recommended that you limit the number of selected records as early as possible. To do this, pass conditions to the parameters of the virtual table.

```bsl
Query.Text = "SELECT
| Good
|FROM
| AccumulationRegister.MyGoods.Turnovers(, Warehouse = &Warehouse)";
```

## Sources

* Standard: [Using virtual tables (RU)](https://its.1c.ru/db/v8std#content:657:hdoc)
* Standard: [Effective use of the virtual table «Turnovers» (RU)](https://its.1c.ru/db/v8std#content:733:hdoc)
* 1C Recommendation: [Using the Condition parameter when accessing a virtual table (RU)](https://its.1c.ru/db/metod8dev/content/5457/hdoc) (RU)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL273
- BSLLS compatible name: VirtualTableCallWithoutParameters
- Public name/i18n key: VirtualTableCallWithoutParameters / BSL273
- Default message/i18n key: RULE_MESSAGES_RU["BSL273"]
- Description/i18n key: RULE_METADATA["BSL273"].description
- Severity: ERROR
- Parameters/defaults: none.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: 1C query virtual tables should be called with parameters when parameters are expected for efficient filtering.
- Why this diagnostic exists: virtual-table calls without parameters can read excessive data and hurt performance.
- Non-goals: ordinary table sources, virtual tables with non-empty parameters, comments, strings outside query blocks.
- Keep/drop decision: keep as a BSLLS-compatible query performance diagnostic.

## Semantic Oracle

- Checked language fact: extracted query text contains a virtual table source/call without parameters.
- Must diagnose when: query source matches the supported virtual-table pattern and has no parameter list or an empty parameter list.
- Must not diagnose when: parameters are present, source is not a virtual table, or text is outside an extracted query block.
- Diagnostic anchor token/range: virtual table source name or call range.
- Quick-fix target, if any: add required virtual table parameters.

## CST Or Domain Fact Model

- Parser/tree source: extracted query text block lines.
- CST/domain object types: `QueryTextBlockInfo`, content lines, virtual-table source regex.
- Extracted attributes: query content base, source text, open parenthesis state, range.
- Parse-error behavior: line-local query block facts may still be checked; syntax errors are not repaired here.
- Textual/regex behavior, only if genuinely textual: bounded query-source matcher inside extracted query blocks.

## Examples

### Invalid Examples

1. `РегистрНакопления.Товары.Остатки` without parameters.
2. `РегистрБухгалтерии.Хозрасчетный.Обороты()` with empty parameters.
3. English virtual-table equivalent without parameters.

### Valid Negative Twins

1. `...Остатки(&Дата, Склад = &Склад)`.
2. Ordinary catalog/table source.
3. `"Остатки"` outside query text.

### Edge Examples

- Comments: query inline comments are stripped by extractor.
- Strings: only query blocks are checked.
- Preprocessor: no separate semantic target.
- Multiline: query line mapping preserves ranges.
- Nested constructs: nested query sources are checked when present in content lines.
- BSL/SDBL variants, if relevant: SDBL query text only.

## Related-Rule Cluster

- Shared semantic object: query source text.
- Rules that may report on the same object/range: BSL207 virtual table joins, BSL236/238 query metadata rules, BSL258 union usage.
- Intended duplicate policy: BSL273 owns missing virtual-table parameters.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl273VirtualTableCallWithoutParameters`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 5,803 `.bsl` files.
- Config/exclude mode: project config disabled; BSLLS diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: normalized BSLLS LSP ranges.
- Exact common: final selected batch parity is exact at `onec=13`, `bslls=13`.
- Line/common semantic common: no BSL273 deltas.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing query block content lines.
- Expected complexity: O(extracted query text length).
- Shared-pass reuse: query block extraction shared with other query rules.
- Before/after measurement, if changed: no runtime change required.

## Implementation Decision

- Change target: contract and verification only.
- Delta category fixed by this change: none.
- Why not another layer: existing query block facts match current parity.
- Rollback signal: parameterized virtual table reports or unparameterized virtual table stops reporting.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py -q -k bsl273 --no-cov`.
- Synthetic fixture/module: virtual table no-params positive and parameterized negative.
- Related-rule tests: query text rules if query extraction changes.
- Parity command/artifact: selected private batch.
- Performance command/artifact: selected parity wall time.
- Ruff/lint: touched files.
- Leak check: changed-file private marker scan.
- OACS evidence/checkpoint: required.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
