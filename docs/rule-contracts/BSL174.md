# BSL174 — <span class="doc-lang doc-lang-ru">Запрет незаполненных значений у измерений регистров</span><span class="doc-lang doc-lang-en">Deny incomplete values for dimensions</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Запрет незаполненных значений у измерений регистров

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Deny incomplete values for dimensions

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL174` |
| Совместимый псевдоним / Compatible alias | `DenyIncompleteValues` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `transaction`, `error-handling` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL174` и псевдоним `DenyIncompleteValues` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL174"]
ignore = ["DenyIncompleteValues"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL174
Значение = "пример";  // bsl-disable: BSL174
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:DenyIncompleteValues-off
// код без этой диагностики
// BSLLS:DenyIncompleteValues-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL174` and alias `DenyIncompleteValues` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL174"]
ignore = ["DenyIncompleteValues"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL174
Value = "example";  // bsl-disable: BSL174
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:DenyIncompleteValues-off
// code without this diagnostic
// BSLLS:DenyIncompleteValues-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Довольно часто при проектировании структуры метаданных соблюдается правило - измерения регистра всегда должны быть заполнены конкретными значениями и не должны быть пустыми.

Проверку на заполненность измерений удобно выполнять с помощью простых встроенных средств платформы, а именно, флага "Запрет незаполненных значений" для измерения регистра. В этом случае платформа самостоятельно будет проверять заполненность измерений и не нужно дополнительно контролировать заполнение измерения ни при программной обработке регистров, ни при интерактивной обработке.
Фактически записи с незаполненным измерением не имеют смысла в информационной базе, каким бы образом они в нее ни попали: в результате интерактивного ввода или в результате выполнения программного кода.

Без установки указанного флага могут возникать различные ситуации, которые приводят к проблемам или усложняют сопровождение систем 1С. Например,
- пользователи смогут интерактивно указать пустые значения
- или разработчики могут при разработке ошибаться, не указывая значения измерения при подготовке записей регистра.

Текущее правило может выдавать ложные срабатывания для измерений, которые могут быть не заполнены.

Правило применяется для следующих регистров:
- сведений
- накопления
- бухгалтерских
- расчетных

## Примеры

## Источники

- [Книга "Разработка интерфейса прикладных решений на платформе "1С:Предприятие 8" - глава "Проверка заполнения и проверка при записи"](https://its.1c.ru/db/pubv8devui#content:225:1)
- [Документация разработчика 1С 8.3 - Свойства измерения (ресурса, реквизита) регистра сведений](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000349)
- [Документация разработчика 1С 8.3 - Свойства измерения (ресурса, реквизита) регистра накопления](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000363)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Often when designing a metadata structure, it is required that the dimensions of a register must always be filled with values (should not be empty).

Checking for completeness of the dimension value should be done using the "Deny Incomplete Values" flag for the register dimension; additional software control of measurement filling is not required.
It is assumed that records with an empty dimension value do not make sense in the infobase.
The absence of a set flag can lead to potential problems if the application developers have not provided a value validation algorithm.

The current rule may give a lot of false positives, use at your own risk.

The rule applies to the following registers:
- information register
- accumulation register
- accounting register
- calculation register

## Examples

## Sources

- [Development of the interface for applied solutions on the "1C:Enterprise" platform (RU).  Ch "Fill check and check on write"](https://its.1c.ru/db/pubv8devui#content:225:1)
- [Developer's Guide - Properties of a dimension (resource, attribute) of the information register (RU)](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000349)
- [Developer's Guide - Properties of a dimension (resource, attribute) of the accumulation register (RU)](https://its.1c.ru/db/v8323doc#bookmark:dev:TI000000363)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL174
- BSLLS compatible name: DenyIncompleteValues
- Public name/i18n key: BSL174 / DenyIncompleteValues
- Default message/i18n key: RULE_MESSAGES_RU["BSL174"]
- Description/i18n key: RULE_METADATA["BSL174"].description
- Severity: WARNING
- Parameters/defaults: no user parameters
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: register dimensions that must be meaningful in database records should deny incomplete values through the platform metadata flag.
- Why this diagnostic exists: the platform can enforce non-empty register dimensions uniformly; otherwise empty dimension values may appear from UI or code paths and make register records ambiguous.
- Non-goals: deciding whether a business-specific dimension is allowed to be empty, inspecting query text, or proving runtime record writes are safe.
- Keep/drop decision: keep, with BSLLS-compatible metadata ownership.

## Semantic Oracle

- Checked language fact: a dimension of an information, accumulation, accounting, or calculation register has `DenyIncompleteValues` set to `false`.
- Must diagnose when: the current module is the diagnostic owner module for such a register and the register metadata XML contains a dimension whose `DenyIncompleteValues` flag is false.
- Must not diagnose when: the object is not one of the register kinds above, all dimensions deny incomplete values, the current module is a form module, or another module already owns the metadata diagnostic for the same register.
- Diagnostic anchor token/range: BSLLS anchors metadata diagnostics at line 1, character 0 through character 9 of the selected owner document; onec mirrors that owner-module range.
- Quick-fix target, if any: set the register dimension metadata flag to deny incomplete values.

## CST Or Domain Fact Model

- Parser/tree source: EDT/designer metadata XML plus current module path context.
- CST/domain object types: register object XML, `Dimension` XML blocks, module path owner context.
- Extracted attributes: metadata folder, object name, dimension name, `DenyIncompleteValues` boolean flag, manager-module existence.
- Parse-error behavior: BSL parser errors do not affect this metadata rule; missing or unreadable XML produces no BSL174 diagnostics.
- Textual/regex behavior, only if genuinely textual: XML dimension blocks are read from metadata text with the existing shared XML helper regex.

## Examples

### Invalid Examples

1. `InformationRegisters/<Name>.xml` has a dimension with `<DenyIncompleteValues>false</DenyIncompleteValues>` and the checked file is `<Name>/Ext/ManagerModule.bsl`.
2. `AccumulationRegisters/<Name>.xml` has such a dimension and the checked file is the register owner manager module.
3. `AccountingRegisters/<Name>.xml` or `CalculationRegisters/<Name>.xml` has such a dimension and the checked file is the register owner manager module.

### Valid Negative Twins

1. A catalog XML contains a dimension-shaped block with `DenyIncompleteValues=false`.
2. A register dimension has `<DenyIncompleteValues>true</DenyIncompleteValues>`.
3. The same register has a manager module, but the checked file is `RecordSetModule.bsl` or a form module.

### Edge Examples

- Comments: BSL comments do not matter.
- Strings: BSL strings and query text do not matter.
- Preprocessor: preprocessor directives do not matter.
- Multiline: XML dimension blocks may span lines.
- Nested constructs: only metadata child dimensions of supported register objects are relevant.
- BSL/SDBL variants, if relevant: SDBL is not relevant; this rule is in the query-metadata batch only because it shares metadata scheduling infrastructure with query metadata rules.

## Related-Rule Cluster

- Shared semantic object: metadata object XML and current module path context.
- Rules that may report on the same object/range: BSL189, BSL211, BSL241, and BSL274 also inspect metadata/form XML near the same object; BSL187, BSL236, and BSL238 share the runtime pool but target SDBL query facts.
- Intended duplicate policy: BSL174 reports once per register owner module, not once for every module or form under the same register.
- Cluster fixture or probe: `tests/test_diagnostics_query_metadata.py::TestTailParityBatches::test_metadata_tail_pool`.

## BSLLS Parity Taxonomy

- Compared input-file set: targeted synthetic owner-module fixture plus preserved-source-root BSLLS private batch when available.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS default behavior.
- File-key normalization: original source-root relative keys for batch aggregate; synthetic temp paths for unit tests.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: preserved-source-root BSLLS 1.0 private batch over 31,797 files is exact at onec 0, BSLLS 0; mini Designer metadata fixture is exact at onec 1, BSLLS 1.
- Line/common semantic common: BSLLS and onec both detect false `DenyIncompleteValues` on supported register dimensions.
- Ours-only categories: previously included duplicated diagnostics on form or record-set modules for the same register owner.
- BSLLS-only categories: none in the current preserved-source-root private batch.
- Range-only categories: closed; mini Designer metadata fixture previously showed onec first-line-length range versus BSLLS 0..9 owner range.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached current object XML lookup and cached XML text read.
- Expected complexity: O(size of current register XML) for eligible owner modules, O(1) skip for non-owner modules.
- Shared-pass reuse: shares query metadata runtime pool and metadata helper caches.
- Before/after measurement, if changed: owner-module gating should reduce repeated XML scans for register form and record-set modules.

## Implementation Decision

- Change target: `query_metadata_rules.py` BSL174 owner-module gate and synthetic coverage.
- Delta category fixed by this change: ours-only duplicate diagnostics on non-owner modules and range-only owner-anchor deltas.
- Why not another layer: the rule already owns metadata eligibility in the query metadata pool; adding ownership there prevents unnecessary XML reads and preserves other metadata helpers.
- Rollback signal: manager modules stop reporting BSL174, non-register metadata reports BSL174, or preserved-source-root parity gains BSLLS-only ownership misses.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k "metadata_tail_pool or deny_incomplete" --no-cov`
- Synthetic fixture/module: inline register manager, record-set, and form modules in extended diagnostics tests.
- Related-rule tests: metadata tail pool checks BSL211, BSL241, BSL274, and BSL246 still report as expected.
- Parity command/artifact: preserved-source-root BSLLS private batch for BSL174, aggregate only.
- Performance command/artifact: BSL174 parity runtime and targeted pytest runtime.
- Ruff/lint: not applicable unless broader suite requests it.
- Leak check: `git diff --check` and no private corpus paths/source in committed dossiers.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.

</div>

</details>

<!-- engineering-contract:end -->
