# BSL024 SpaceAtStartComment Contract

## Rule Identity

- Code: BSL024
- BSLLS compatible name: SpaceAtStartComment
- Public name/i18n key: BSL024 / SpaceAtStartComment
- Default message/i18n key: RULE_MESSAGES_RU["BSL024"]
- Description/i18n key: RULE_METADATA["BSL024"].description
- Severity: INFORMATION
- Parameters/defaults: `commentsAnnotation=//@,//(c),//©`, `useStrictValidation=true`.
- Rule status: verified-classified

## Standard And Product Decision

- Standard or product policy: line comments should have a space between `//` and comment text.
- Why this diagnostic exists: comments without the separating space are harder to read and violate the documented BSLLS formatting convention.
- Non-goals: reporting empty comments, configured annotation comments, comment-looking text inside string literals, or fully parsing commented-out BSL code.
- Keep/drop decision: keep as a cheap comment-style diagnostic; classify the small remaining BSLLS CodeRecognizer divergence instead of adding receiver-specific exceptions.

## Semantic Oracle

- Checked language fact: a real line comment begins with `//` and has non-empty text immediately after the slashes.
- Must diagnose when: comment text starts immediately after `//`, including strict-mode multi-slash comments with text and BSLLS-compatible query-pipe/commented-code cases.
- Must not diagnose when: the comment is empty, starts with whitespace after `//`, is a configured annotation such as `//@`, starts a commented region marker `//#Область`, is an internal suppression directive, or is recognized as a BSLLS-compatible commented-code skip.
- Diagnostic anchor token/range: from the `//` column to the end of the physical line.
- Quick-fix target, if any: insert one space after `//`.

## CST Or Domain Fact Model

- Parser/tree source: source-line comment-start facts from `DiagnosticDocumentContext.comment_starts`, with a narrow query-pipe fallback for BSLLS parity when a commented query literal confuses multiline string state.
- CST/domain object types: physical source line, detected comment start column, comment body prefix.
- Extracted attributes: line number, comment start column, comment text, full-line versus inline comment state, and small BSLLS-compatible skip categories.
- Parse-error behavior: BSL024 is line-oriented and may run when the module has parse errors; BSL001 owns syntax diagnostics.
- Textual/regex behavior, only if genuinely textual: allowed. This is a textual style rule over line comments; regexes are bounded to comment prefixes and skip categories.

## Examples

### Invalid Examples

1. `//Без пробела`
2. `////Текст с ошибкой`
3. `Перем1 = 7; //И это плохо`

### Valid Negative Twins

1. `// С пробелом`
2. `//`
3. `//@УстановитьОбработчик`

### Edge Examples

- Comments: `//#Область СлужебныйИнтерфейс` is skipped, while `//#КонецОбласти` is diagnosed like BSLLS.
- Strings: `Ссылка = "https://support.example/path";` is not a comment.
- Preprocessor: commented region start markers are treated as annotations for parity.
- Multiline: each physical comment line is checked independently; selected `//|` query-pipe lines may report even inside a commented query block.
- Nested constructs: full-line commented code is not parsed as code; only a bounded BSLLS-compatible skip taxonomy is used.
- BSL/SDBL variants, if relevant: query-pipe fallback is textual and does not parse SDBL.

## Related-Rule Cluster

- Shared semantic object: source comments.
- Rules that may report on the same object/range: BSL013 detects commented-out code, BSL014 checks physical line length, and BSL023 checks service tags in comments.
- Intended duplicate policy: BSL024 owns spacing after `//`; it may coexist with BSL023 on the same comment. BSL013 may still own the separate question of whether code is commented out.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestBsl024SpaceAtStartComment`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 1474 BSLLS facts are all covered after the rule update.
- Line/common semantic common: exact for all BSLLS facts; no BSLLS-only facts remain.
- Ours-only categories: 8 current-corpus facts, all full-line commented assignment/constructor-style statements that BSLLS CodeRecognizer suppresses but onec-hbk-bsl still reports as missing comment spacing.
- BSLLS-only categories: 0 after the query-pipe and commented-if refinements.
- Range-only categories: 0 after the rule update.
- Known BSLLS defects: BSLLS behavior is inconsistent across similar commented assignments, for example simple receiver assignments may be suppressed while simple English receiver assignments may report.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over source lines using precomputed comment starts, plus bounded regex checks over each candidate comment line.
- Expected complexity: O(total source line length).
- Shared-pass reuse: may share a future comment-fact layer with BSL013/BSL023, but current rule remains standalone and cheap.
- Before/after measurement, if changed: current-corpus parity improved from onec 1469 / bslls 1474 with 54 BSLLS-only and 49 ours-only deltas to onec 1482 / bslls 1474 with 0 BSLLS-only and 8 classified ours-only deltas.

## Implementation Decision

- Change target: BSL024 comment skip taxonomy, query-pipe fallback, targeted tests, contract, and audit state.
- Delta category fixed by this change: BSLLS-only commented query/region/if deltas and broad false suppression of incomplete commented code.
- Why not another layer: fully emulating BSLLS CodeRecognizer would require brittle parsing of arbitrary commented-out BSL; the remaining divergences are small and safer as classified stricter diagnostics.
- Rollback signal: selected parity gains BSLLS-only deltas, annotation comments start reporting, URL strings report, or targeted comment tests regress.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_line_text.py::TestBsl024SpaceAtStartComment -q --no-cov`.
- Synthetic fixture/module: targeted tests cover spacing positives, annotation/empty/string negatives, region start/end parity, incomplete commented calls, commented-if variants, and inline comments.
- Related-rule tests: BSL013/BSL023 only if shared comment extraction changes.
- Parity command/artifact: preserved-source-root selected BSL024 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because the change remains a single line pass with bounded regex checks.
- Ruff/lint: touched runtime/test files.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
