# BSL008 TooManyReturns Contract

## Rule Identity

- Code: BSL008
- BSLLS compatible name: TooManyReturns
- Public name/i18n key: TooManyReturns / BSL008
- Default message/i18n key: RULE_MESSAGES_RU["BSL008"]
- Description/i18n key: RULE_METADATA["BSL008"].description
- Severity: WARNING
- Parameters/defaults: max_returns / default 3.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: methods should keep exit points limited enough that control flow remains easy to audit.
- Why this diagnostic exists: too many `Возврат` / `Return` statements make method behavior harder to reason about and usually indicate a method that should be simplified or split.
- Non-goals: do not diagnose methods at or below the configured threshold; do not diagnose return-like words in comments or strings; do not check query text.
- Keep/drop decision: keep as a BSLLS-compatible method contract diagnostic.

## Semantic Oracle

- Checked language fact: a procedure or function contains more return statements than the configured `max_returns` threshold.
- Must diagnose when: the count of executable `Возврат` / `Return` statements inside one method body is greater than `max_returns`.
- Must not diagnose when: the return count is less than or equal to `max_returns`, or apparent return tokens appear only in comments, strings, or outside the method body.
- Diagnostic anchor token/range: method name range resolved through the shared `proc_name_span` helper.
- Quick-fix target, if any: none.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` / `ProcInfo` method ranges and masked method body lines.
- CST/domain object types: `ProcInfo` procedure/function body plus return-statement lexical facts.
- Extracted attributes: method start/end line, method header column, configured max return threshold, return token count.
- Parse-error behavior: skip uncertain procedure containment when the procedure model is unavailable; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: current implementation uses `_RE_RETURN` over a method-body slice; it must operate on code facts that exclude comments and strings.

## Examples

### Invalid Examples

1. A function with four `Возврат` statements when `max_returns=3`.
2. A procedure with four early `Возврат;` guard exits when `max_returns=3`.
3. A method with nested branches whose total executable return statements exceeds the threshold.

### Valid Negative Twins

1. A function with exactly three `Возврат` statements when `max_returns=3`.
2. A method with return-like words only in comments.
3. A method with return-like words only in string literals.

### Edge Examples

- Comments: return-like tokens in comments do not count.
- Strings: return-like tokens in strings do not count.
- Preprocessor: visible return statements inside a parsed method count normally; inactive preprocessor branch handling is not owned by this rule.
- Multiline: a multiline method header still anchors the method diagnostic on the method header fact.
- Nested constructs: nested branches do not create separate scopes; all executable returns in the method count together.
- BSL/SDBL variants, if relevant: SDBL query text is not a target.

## Related-Rule Cluster

- Shared semantic object: procedure/function body and method contract.
- Rules that may report on the same object/range: BSL011, BSL019, BSL020, BSL031, BSL032, BSL062, BSL148, BSL192, BSL193, BSL194, BSL215.
- Intended duplicate policy: keep BSL008 distinct because it measures number of exits, not complexity, nesting, missing return, unused parameters, or documentation.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl008TooManyReturnStatements`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: current private parity is exact at `onec=262`, `bslls=262`.
- Line/common semantic common: all current private BSL008 diagnostics match by file, range, and code.
- Ours-only categories: none in the current private batch.
- BSLLS-only categories: none in the current private batch.
- Range-only categories: fixed prior range-only mismatch where onec anchored BSL008 on the full method header and BSLLS anchors on the method name.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing procedure model and method body line scan.
- Expected complexity: O(method body lines).
- Shared-pass reuse: shares `ProcInfo` method ranges with BSL002, BSL011, BSL019, BSL031, BSL032, BSL062, BSL148, and method documentation rules.
- Before/after measurement, if changed: private parity improved from range-only mismatch `onec=256`, `bslls=262`, `only_onec=256`, `only_bslls=262` to exact `onec=262`, `bslls=262`.

## Implementation Decision

- Change target: `ProcedureModel.validate_max_returns`, BSL008 contract/audit, and span regression test.
- Delta category fixed by this change: BSL008 method diagnostic range-only mismatch.
- Why not another layer: existing method-domain pass already owns the relevant method body fact; range alignment only needed the existing `proc_name_span` helper used by other method diagnostics.
- Rollback signal: contract validation fails, targeted BSL008 tests fail, or parity uncovers unclassified high-volume deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py tests/test_diagnostic_spans.py -q -k 'bsl008 or method_name_span' --no-cov`.
- Synthetic fixture/module: inline BSL008 tests for too many returns, threshold negative case, and method-name span.
- Related-rule tests: BSL011, BSL019, BSL032, and BSL148 targeted tests if rule logic changes.
- Parity command/artifact: preserved-source-root private batch BSL008 parity, counts only.
- Performance command/artifact: targeted pytest runtime and parity runtime if rule logic changes.
- Ruff/lint: `./.venv/bin/ruff check docs/rule-contracts/BSL008.md src/onec_hbk_bsl/analysis/diagnostic/domain/procedure_model.py tests/test_diagnostics_method_contracts.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
