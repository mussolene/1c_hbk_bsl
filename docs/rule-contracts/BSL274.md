# BSL274 — <span class="doc-lang doc-lang-ru">У полей формы не указан путь к данным</span><span class="doc-lang doc-lang-en">Form fields do not have a data path</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

У полей формы не указан путь к данным

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Form fields do not have a data path

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL274` |
| Совместимый псевдоним / Compatible alias | `WrongDataPathForFormElements` |
| Серьёзность / Severity | `ERROR` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `correctness`, `ui` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL274` и псевдоним `WrongDataPathForFormElements` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL274"]
ignore = ["WrongDataPathForFormElements"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL274
Значение = "пример";  // bsl-disable: BSL274
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:WrongDataPathForFormElements-off
// код без этой диагностики
// BSLLS:WrongDataPathForFormElements-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL274` and alias `WrongDataPathForFormElements` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL274"]
ignore = ["WrongDataPathForFormElements"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL274
Value = "example";  // bsl-disable: BSL274
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:WrongDataPathForFormElements-off
// code without this diagnostic
// BSLLS:WrongDataPathForFormElements-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При активных доработках формы или связанных метаданных на форме могут оставаться элементы, у которых нет связанного элемента данных. Проблемы могут возникать после удаления соответствующих метаданных или реквизитов метаданных, а также в случае изменения основного реквизита формы.
В результате такое поле формы не будет показано.
При проверке подобных формы при включенной расширенной проверке в Конфигураторе 1С возникают ошибки.
```
Справочник.Контрагенты.Форма.ФормаЭлемента.Форма Неразрешимые ссылки на объекты метаданных (12)
```
При этом в файлах форм из выгрузки XML значение свойства "Путь к данным" начинается со знака "~", например, для поля "Наименование": "<DataPath>~Object.Description</DataPath>".

Также при ручном изменении запроса динамического списка очищается свойство "Путь к данным" у его полей на форме. Это приводит к тому, что элемент формы (колонка таблицы) перестает быть связанным с полем динамического списка и исчезает из таблицы на форме.

При этом в файлах форм из выгрузки XML значение свойства "Путь к данным" начинается со знака "~", например, для поля "Наименование": `<DataPath>~Список.Description</DataPath>`.

Аналогичная ситуация для кнопок командной панели, связанных свойством "Данные" со стандартным полем динамического списка, например, для кнопки с заполненным значением "Ссылка" в свойстве "Данные": `<DataPath>~Items.Список.CurrentData.Ссылка</DataPath>`.

## Примеры

## Источники

- [Общие требования к конфигурации - Стандарты 1С](https://its.1c.ru/db/v8std#content:467:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

# Form fields do not have a data path (WrongDataPathForFormElements)

|  Type   | Scope |  Severity  | Activated<br>by default | Minutes<br> to fix |      Tags       |
|:-------:|:-----:|:----------:|:-----------------------------:|:------------------------:|:---------------:|
| `Error` | `BSL` | `Critical` |             `Yes`             |           `5`            | `unpredictable` |

## Diagnostics description

When actively reworking a form or associated metadata, there may be elements on the form that do not have an associated data element. Problems can arise after deleting metadata or metadata attributes, or when changing the main form attribute. As a result, such a form field will not be displayed. Errors occur with forms when extended validation is enabled in the 1C Configurator.
```
Справочник.Контрагенты.Форма.ФормаЭлемента.Форма Неразрешимые ссылки на объекты метаданных (12)
```
In this case, in form files from XML unloading, the value of the "Data path" property begins with a "~" sign, for example, for the "Description" field: "<DataPath>~Object.Description</DataPath>".

When you manually change the query of a dynamic list, the "Data path" property of its fields on the form is cleared. This leads to a breakdown of the connection between the form element (table column) and the dynamic list field, and it disappears from the table on the form.

In form files, when unloaded to XML, the value of the "Data path" property starts with a "~" character, for example, for the "Description" field: `<DataPath> ~ List.Description </DataPath>`.

For command bar buttons associated with the "Data" property with a standard dynamic list field, for example, for a button with a filled "Ref" value in the "CurrentData" property: `<DataPath> ~ Items.List.CurrentData.Ref </DataPath>`.

## Examples

## Sources

- [General requirements - Standards 1C (RU)](https://its.1c.ru/db/v8std#content:467:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL274
- BSLLS compatible name: WrongDataPathForFormElements
- Public name/i18n key: WrongDataPathForFormElements / BSL274
- Default message/i18n key: RULE_MESSAGES_RU["BSL274"]
- Description/i18n key: RULE_METADATA["BSL274"].description
- Severity: ERROR
- Parameters/defaults: none; enabled by default in BSLLS and supported by exact selection here.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: form elements must be bound to valid form data paths; unresolved form item data paths are exported as `DataPath` values starting with `~`.
- Why this diagnostic exists: unresolved form data paths make form fields disappear or fail extended configuration checks.
- Non-goals: do not validate whether every ordinary data path resolves; do not diagnose non-form XML, non-form modules, or form XML without `DataPath` values beginning with `~`.
- Keep/drop decision: keep as a BSLLS-compatible critical form metadata diagnostic.

## Semantic Oracle

- Checked language fact: a parsed form metadata item has `DataPath` beginning with `~`.
- Must diagnose when: a form module belongs to a form whose `Form.xml` contains a form item with unresolved `DataPath`, or the managed application module is checked and the configuration contains a form without modules with such an item.
- Must not diagnose when: `DataPath` is absent, empty, or does not start with `~`; the current module is not a form module or managed application module; a form with its own module is being checked from the managed application module path.
- Diagnostic anchor token/range: BSLLS reports the current module selection range; this implementation anchors at the current module owner range.
- Quick-fix target, if any: repair or remove the unresolved form element data binding in the form XML/design surface.

## CST Or Domain Fact Model

- Parser/tree source: metadata XML files under the configuration root.
- CST/domain object types: current module path, current form XML path, managed application module path, form XML `DataPath` elements.
- Extracted attributes: whether module is a form module, whether module is `Ext/ManagedApplicationModule.bsl`, form XML path, existence of sibling form module, and `DataPath` text.
- Parse-error behavior: if XML cannot be read, produce no diagnostics.
- Textual/regex behavior, only if genuinely textual: simple XML tag extraction is currently used for `DataPath`; the semantic predicate is textual because the exported form metadata stores the unresolved marker in text.

## Examples

### Invalid Examples

1. A form module whose `Form.xml` contains `<DataPath>~Object.Description</DataPath>`.
2. A form module whose dynamic-list column has `<DataPath>~List.Description</DataPath>`.
3. Managed application module when a form without its own module contains an unresolved `DataPath`.

### Valid Negative Twins

1. A form module with `<DataPath>Object.Description</DataPath>`.
2. A form module with no `DataPath` elements.
3. Managed application module when the unresolved form has its own module.

### Edge Examples

- Comments: XML comments are not form item data paths.
- Strings: BSL string contents are irrelevant; the rule reads form XML.
- Preprocessor: BSL preprocessor is irrelevant.
- Multiline: `DataPath` text may include whitespace around the value; the stripped value controls the predicate.
- Nested constructs: nested form item XML is diagnosed the same way if the `DataPath` starts with `~`.
- BSL/SDBL variants, if relevant: not applicable.

## Related-Rule Cluster

- Shared semantic object: form metadata and metadata owner module range.
- Rules that may report on the same object/range: BSL174, BSL189, BSL211, BSL241, BSL246 can share metadata root/object anchoring; BSL244 and BSL245 inspect form modules for runtime/client-server issues.
- Intended duplicate policy: keep BSL274 independent; unresolved form data paths are distinct from metadata naming, permissions, and form-event server-call diagnostics.
- Cluster fixture or probe: metadata tail tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus synthetic form metadata fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final private parity is exact no-signal at `onec=0`, `bslls=0`.
- Line/common semantic common: not applicable; no private corpus signal.
- Ours-only categories: none in final private parity.
- BSLLS-only categories: none in final private parity.
- Range-only categories: none observed before implementation.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing metadata pool and cached XML reads.
- Expected complexity: O(current form XML size) for form modules; O(number of form XML files) only for managed application module BSL274 checks.
- Shared-pass reuse: remains in the BSL189/211/213/214/231/232/241/242/246/274 metadata pool.
- Before/after measurement, if changed: targeted metadata pytest passes in 0.65s; private preserved-source-root BSL274 parity over 31,797 files is exact no-signal at `onec=0`, `bslls=0`.

## Implementation Decision

- Change target: BSL274 branch in `query_metadata_rules.py`.
- Delta category fixed by this change: missing BSLLS-compatible managed-application-module scan for forms without modules.
- Why not another layer: the metadata pool already owns XML/root-aware diagnostics and caches XML reads.
- Rollback signal: form modules stop reporting unresolved `DataPath`, managed application module reports forms that already have modules, or private parity gains unexplained signal.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k 'bsl274 or metadata_tail_pool or path_is_likely_form_module_bsl' --no-cov`.
- Synthetic fixture/module: form module positives/negatives and managed application module form-without-module coverage.
- Related-rule tests: metadata tail pool around BSL174/211/241/246 separation.
- Parity command/artifact: preserved-source-root private batch BSL274 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/query_metadata_rules.py tests/test_diagnostics_query_metadata.py`.
- Leak check: `git diff --check` and changed-file private token scan.
- OACS evidence/checkpoint: ingest verification outputs and add checkpoint before claiming done.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
