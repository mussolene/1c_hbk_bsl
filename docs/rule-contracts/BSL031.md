# BSL031 — <span class="doc-lang doc-lang-ru">Ограничение на количество параметров метода</span><span class="doc-lang doc-lang-en">Number of parameters in method</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Ограничение на количество параметров метода

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Number of parameters in method

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL031` |
| Совместимый псевдоним / Compatible alias | `NumberOfParams` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `design`, `brain-overload` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL031` и псевдоним `NumberOfParams` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL031"]
ignore = ["NumberOfParams"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL031
Значение = "пример";  // bsl-disable: BSL031
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:NumberOfParams-off
// код без этой диагностики
// BSLLS:NumberOfParams-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL031` and alias `NumberOfParams` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL031"]
ignore = ["NumberOfParams"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL031
Value = "example";  // bsl-disable: BSL031
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:NumberOfParams-off
// code without this diagnostic
// BSLLS:NumberOfParams-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Не рекомендуется объявлять в функциях много параметров (нужно ориентироваться на количество не более семи параметров), при этом не должно быть много параметров со значениями по умолчанию (нужно ориентироваться на количество не более трех таких параметров). В противном случае, читаемость вызывающего кода сильно снижается. Например, можно легко ошибиться в количестве запятых при передаче необязательных параметров.

При необходимости передавать в функцию большое число параметров рекомендуется группировать однотипные параметры в один или несколько составных параметров типа Структура.

## Примеры

Неправильно:

```bsl
// Создает элемент справочника "Номенклатура"
Процедура СоздатьЭлементНоменклатуры(Наименование, ТоварУслуга, ЕдиницаИзмерения, ВесНетто, ПроверятьУникальность = Истина)

КонецПроцедуры
```

Правильно:
Cгруппировать параметры, описывающие значения реквизитов номенклатуры, в структуру ЗначенияРеквизитов.

```bsl
// Создает элемент справочника "Номенклатура"
Процедура СоздатьЭлементНоменклатуры(ЗначенияРеквизитов, ПроверятьУникальность = Истина)
КонецПроцедуры
```

## Источники

* [Стандарт: Параметры процедур и функций](https://its.1c.ru/db/v8std#content:640:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

It is not recommended to declare many parameters in functions (best practice to use not more than seven parameters). In So doing there should not be many parameters with default values set (best practice to have not more than three such parameters). Otherwise code readability decreases. For example it is easy to make a mistake in number of commas passing optional parameters.

If need to pass many parameters to a function, it is recommended to group same-type parameters into one or more composite parameters of type Structure.

## Examples

Incorrect:

```bsl
// Create an item in catalog "Goods"
Procedure CreateSKU(Name, Goods, Units, Weight, Check = True)
// ...
EndProcedure
```

Correct:
Group parameters, having goods item properties into Structure Values.

```bsl
// Create item in catalog "Goods"
Procedure CreateNewGoods(Values, Check = True)
EndProcedure
```

## Sources

* [Standard: Parameters of procedures and functions (RU)](https://its.1c.ru/db/v8std#content:640:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL031
- BSLLS compatible name: NumberOfParams
- Public name/i18n key: NumberOfParams / BSL031
- Default message/i18n key: RULE_MESSAGES_RU["BSL031"]
- Description/i18n key: RULE_METADATA["BSL031"].description
- Severity: WARNING
- Parameters/defaults: max_params / default 7.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: methods should not expose parameter lists so large that call sites and documentation become hard to audit.
- Why this diagnostic exists: too many parameters usually indicates a method doing too much or a missing parameter object/structure.
- Non-goals: do not diagnose optional-parameter count; BSL015 owns that. Do not diagnose parameter order; BSL228 owns ordering. Do not diagnose methods at or below the configured total-parameter threshold.
- Keep/drop decision: keep as a BSLLS-compatible method contract diagnostic.

## Semantic Oracle

- Checked language fact: one procedure or function has more parameters than `max_params`.
- Must diagnose when: total method parameter count is greater than the configured threshold.
- Must not diagnose when: total parameter count is less than or equal to the threshold, or parameter-like text is not part of a method signature.
- Diagnostic anchor token/range: the method parameter-list context, matching BSLLS `ParamListContext` range semantics.
- Quick-fix target, if any: none.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` / `ProcInfo` extracted from tree-sitter method definitions.
- CST/domain object types: `parameters` node, child `parameter` nodes, `ProcInfo.params`, and `ProcedureModel`.
- Extracted attributes: method start/end line, first and last parameter ranges, total parameter count, configured threshold.
- Parse-error behavior: skip uncertain procedure containment when the procedure model is unavailable; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: fallback range logic is used only when CST parameter ranges are unavailable.

## Examples

### Invalid Examples

1. A method with eight parameters when `max_params=7`.
2. A multiline method signature with more than seven parameters.
3. A method with mixed required and optional parameters whose total parameter count exceeds the threshold.

### Valid Negative Twins

1. A method with exactly seven parameters when `max_params=7`.
2. A method with many optional-looking values in its body but a short signature.
3. Parameter-like text inside comments or strings rather than a method signature.

### Edge Examples

- Comments: ignored; comments are not parameters.
- Strings: ignored; strings are not parameters.
- Preprocessor: visible parsed method signatures are checked; inactive preprocessor branch handling is not owned by this rule.
- Multiline: diagnostic range spans from the first parameter token to the last parameter token, excluding standalone opening or closing parenthesis lines.
- Nested constructs: not relevant; only the method signature is checked.
- BSL/SDBL variants, if relevant: SDBL query text is not a target.

## Related-Rule Cluster

- Shared semantic object: method parameter list.
- Rules that may report on the same object/range: BSL015 checks optional parameter count; BSL062 checks unused parameters; BSL192/BSL193/BSL194/BSL215 check parameter documentation; BSL228 checks optional parameter ordering.
- Intended duplicate policy: keep BSL031 distinct from BSL015 because total arity and optional-parameter overload are different design smells.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl031NumberOfParams`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: current private parity is exact at `onec=234`, `bslls=234`.
- Line/common semantic common: all current private BSL031 diagnostics match by file, range, and code.
- Ours-only categories: none in the current private batch.
- BSLLS-only categories: none in the current private batch.
- Range-only categories: none after shared parameter-list range alignment from BSL015.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing procedure extraction in `DocumentSnapshot`.
- Expected complexity: O(method parameters).
- Shared-pass reuse: `ProcInfo` parameter ranges are shared with BSL015 and method parameter documentation/order rules.
- Before/after measurement, if changed: no BSL031 logic changed in this iteration; exact parity follows the shared BSL015 parameter-list range alignment.

## Implementation Decision

- Change target: BSL031 contract/audit only.
- Delta category fixed by this change: none in BSL031-specific logic; current rule is exact on the private parity batch.
- Why not another layer: existing `ProcInfo` parameter facts already provide the semantic model and range surface used by BSL031.
- Rollback signal: contract validation fails, targeted BSL031 tests fail, or private parity stops being exact.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl031 --no-cov`.
- Synthetic fixture/module: inline BSL031 tests for too many params, threshold negative case, and multiline parameter range.
- Related-rule tests: BSL015 targeted tests when shared parameter range extraction changes.
- Parity command/artifact: preserved-source-root private batch BSL031 parity, counts only.
- Performance command/artifact: no dedicated performance artifact; no rule logic changed.
- Ruff/lint: `./.venv/bin/ruff check docs/rule-contracts/BSL031.md tests/test_diagnostics_method_contracts.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
