# BSL193 FunctionOutParameter Contract

## Rule Identity

- Code: BSL193
- BSLLS compatible name: FunctionOutParameter
- Public name/i18n key: FunctionOutParameter / BSL193
- Default message/i18n key: RULE_MESSAGES_RU["BSL193"]
- Description/i18n key: RULE_METADATA["BSL193"].description
- Severity: WARNING
- Parameters/defaults: none. BSLLS marks this diagnostic as not activated by default; onec supports explicit selection/config enablement through normal rule selection.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: function parameters should not be used as outgoing mutable values; functions should return values explicitly.
- Why this diagnostic exists: writing to a by-reference function parameter hides an extra output channel and makes call contracts harder to reason about.
- Non-goals: do not diagnose procedures; do not diagnose function parameters declared with `Знач` / `Val`; do not diagnose default values in a multiline function signature.
- Keep/drop decision: keep as a BSLLS-compatible function out-parameter diagnostic.

## Semantic Oracle

- Checked language fact: a function has a parameter not declared by value and that parameter is assigned as an lvalue in the function body.
- Must diagnose when: a non-`Знач` function parameter appears as the left side of an assignment in the executable body.
- Must not diagnose when: the routine is a procedure, the parameter is declared `Знач`, the apparent assignment is a default value in the signature, or the name appears only as a read.
- Diagnostic anchor token/range: the written lvalue occurrence in the function body.
- Quick-fix target, if any: replace the outgoing parameter with an explicit return value or refactor the procedure/function contract.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` / `ProcInfo`, with body start derived after the full parameter list.
- CST/domain object types: function declaration, parameter list, by-value parameter modifier, body assignment lvalue.
- Extracted attributes: routine kind, params, val params, method body start/end, lvalue name/range.
- Parse-error behavior: only parsed procedure/function facts are checked; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: current implementation uses a line-level lvalue assignment regex inside the already delimited function body; it must not scan signature lines.

## Examples

### Invalid Examples

1. `Функция Тест(Параметр)` with `Параметр = 1;` in the body.
2. A multiline function whose non-value parameter is assigned after the signature.
3. A function where the first assignment to a non-value parameter is inside a branch.

### Valid Negative Twins

1. `Функция Тест(Знач Параметр)` with `Параметр = 1;`.
2. `Процедура Тест(Параметр)` with `Параметр = 1;`.
3. `Функция Тест(Параметр = Неопределено)` where the only `=` is the default value in the signature.

### Edge Examples

- Comments: ignored by line comment stripping.
- Strings: not lvalue positions.
- Preprocessor: active parsed body lines are checked by the same body range.
- Multiline: body scanning starts after the complete multiline signature.
- Nested constructs: assignments inside nested blocks of the same function count.
- BSL/SDBL variants, if relevant: query text is irrelevant.

## Related-Rule Cluster

- Shared semantic object: function parameter and function body assignment.
- Rules that may report on the same object/range: BSL062 can report unused parameters; BSL228 can report parameter ordering; BSL240 can report parameter rewrite; BSL254 can report client/server value-transfer issues.
- Intended duplicate policy: BSL193 and BSL240 may both be useful only if both distinct contracts apply; BSL193 is specific to function out parameters.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestBsl193FunctionOutParameter`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final aggregate has exact match, `onec=370`, `bslls=370`.
- Line/common semantic common: all selected diagnostics match by file, range, and code.
- Ours-only categories: none after limiting the scan to the function body instead of multiline signature defaults.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none for this rule in the final batch.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing method contract pass over parsed `ProcInfo` records and body lines.
- Expected complexity: O(total function body lines).
- Shared-pass reuse: shared with BSL192, BSL194, BSL228, and BSL266.
- Before/after measurement, if changed: no dedicated timing needed; scan range was narrowed.

## Implementation Decision

- Change target: BSL193 body scan start, focused tests, contract/audit.
- Delta category fixed by this change: semantic false positives from treating multiline signature default values as body lvalue assignments.
- Why not another layer: existing method-contract pass already owns this rule; the required correction is the function body boundary.
- Rollback signal: private parity shows any BSL193 only_ours/only_bslls/range delta, especially on multiline signatures.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k bsl193 --no-cov`.
- Synthetic fixture/module: inline tests cover body assignment, `Знач` parameter, and multiline signature default.
- Related-rule tests: method-contract regression tests if shared pass changes structurally.
- Parity command/artifact: preserved-source-root private batch BSL193 parity, final exact.
- Performance command/artifact: no dedicated performance artifact; scan range narrowed.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/method_contract_rules.py tests/test_diagnostics_extended.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
