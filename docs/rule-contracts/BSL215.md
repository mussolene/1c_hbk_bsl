# BSL215 MissingParameterDescription Contract

## Rule Identity

- Code: BSL215
- BSLLS compatible name: MissingParameterDescription
- Public name/i18n key: MissingParameterDescription / BSL215
- Default message/i18n key: RULE_MESSAGES_RU["BSL215"]
- Description/i18n key: RULE_METADATA["BSL215"].description
- Severity: WARNING
- Parameters/defaults: no user parameters
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: documented methods must keep their parameter documentation synchronized with the method signature.
- Why this diagnostic exists: stale, missing, empty, or out-of-order parameter descriptions make public and internal method contracts misleading.
- Non-goals: do not require every method to have documentation; BSL233 owns public-method documentation presence. Do not validate semantic correctness of type names.
- Keep/drop decision: keep as a BSLLS-compatible documentation consistency diagnostic.

## Semantic Oracle

- Checked language fact: a method has an adjacent documentation comment block and the block's `Параметры` / `Parameters` section does not match the method signature.
- Must diagnose when: a documented method has parameters but no parameter section, a signature parameter has no documented entry, a documented entry is empty or malformed by BSLLS description parsing rules, an entry names a parameter absent from the signature, or documented parameter order differs from signature order when no missing/extra errors exist.
- Must not diagnose when: the method has no adjacent documentation block, the method has no parameters and no parameter descriptions, every parameter has a valid description in signature order, or the doc block is a recognized non-method block such as structure composition.
- Diagnostic anchor token/range: missing all parameters, extra descriptions, and wrong order anchor on the method name; missing/empty individual parameters anchor on the parameter identifier range in the signature.
- Quick-fix target, if any: add, remove, complete, or reorder the parameter documentation entry.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` plus tree-sitter `line_comment` nodes immediately preceding the method declaration.
- CST/domain object types: `ProcInfo` method signature facts, method-adjacent `line_comment` CST block, parameter section header, parameter description entries, and signature parameter ranges.
- Extracted attributes: method name/range, parameter names/ranges/order, documentation block boundaries, parameter-section entries, valid type-description signal, empty descriptions, extra entries, and documented order.
- Parse-error behavior: only parsed procedures/functions are checked; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: CST selects comment nodes and block boundaries; documentation grammar inside comment text is textual by language convention and may use a bounded BSLLS-compatible mini-parser.

## Examples

### Invalid Examples

1. A documented method with `Параметр` in the signature and no `Параметры:` section.
2. A `Параметры:` section that omits one signature parameter.
3. A parameter entry that has no recognized type/description or appears in a different order than the signature.

### Valid Negative Twins

1. A method with no adjacent documentation block.
2. A documented method whose `Параметры:` entries match all signature parameters in order.
3. A documented method with no parameters and no parameter descriptions.

### Edge Examples

- Comments: only the immediately adjacent `//` block before the method is considered documentation.
- Strings: irrelevant; this rule reads comments and signatures.
- Preprocessor: compiler directives between documentation and method declaration are skipped.
- Multiline: multiline signatures use `ProcInfo` parameter ranges; multiline comment descriptions are handled as continuation lines.
- Nested constructs: not relevant; methods are top-level module members.
- BSL/SDBL variants, if relevant: query text is irrelevant.

## Related-Rule Cluster

- Shared semantic object: method declarations, parameter lists, and documentation comments.
- Rules that may report on the same object/range: BSL015/BSL031 check signature size, BSL062 checks unused parameters, BSL193 checks out parameters, BSL212 checks call arity, BSL228 checks optional parameter order, BSL233 checks public method description presence, and BSL240 checks parameter rewrites.
- Intended duplicate policy: keep BSL215 distinct; it reports documentation consistency only after documentation exists, while BSL233 reports missing public method documentation.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestBsl215MissingParameterDescriptionParity`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: current aggregate after CST comment-block extraction and range fixes has `onec=5574`, `bslls=5411`.
- Line/common semantic common: many documented-method parameter omissions already match.
- Ours-only categories: current final batch has `only_onec=943`. The high-volume semantic residue is incomplete compatibility with BSLLS `BSLDescriptionParser.g4`: list/collection type phrases, hyperlink/simple-type colon handling, split-method fragment context, and method-description grammar recovery can make onec treat a description as empty when BSLLS accepts it.
- BSLLS-only categories: current final batch has `only_bslls=780`. The high-volume semantic residue is the opposite parser-shape mismatch: BSLLS emits method-level malformed/extra-description diagnostics and parameter-level diagnostics from its full description grammar where onec's bounded comment parser does not reconstruct the same `ParameterDescription` list.
- Range-only categories: current line-common bucket has 115 line keys, 151 onec-side and 182 BSLLS-side diagnostics. A continuation-line parameter range mismatch caused by a manual tab-column adjustment was fixed in this iteration.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing method-contract pass over parsed procedures and shared `MethodDocComment` facts built from adjacent comment blocks.
- Expected complexity: O(method count + adjacent documentation block lines).
- Shared-pass reuse: shares `ProcInfo` signature facts with BSL015/BSL031/BSL062/BSL193/BSL212/BSL228/BSL240, and shares adjacent method-documentation presence with BSL233.
- Before/after measurement, if changed: private parity over 32,007 files moved from `onec=5278`, `bslls=5411`, `only_bslls=933`, `only_onec=800` to `onec=5574`, `bslls=5411`, `only_bslls=780`, `only_onec=943`; this iteration is a behavior-preserving extraction verified by synthetic/unit tests and exact BSL215 targeted tests. Exact external parity remains deferred until a dedicated BSLLSDescriptionParser-compatible layer exists.

## Implementation Decision

- Change target: extract BSL215 inline documentation parsing into shared `diagnostic.domain.method_doc_comment` and reuse it for BSL215 plus BSL233 description presence.
- Delta category fixed by this change: architecture/process delta; the rule had no reusable method-documentation fact despite the contract naming one, which encouraged repeated local edits.
- Why not another layer: `ProcInfo` owns method signatures but not comments, `Symbol.doc_comment` is raw index/LSP text, and formatter/autocomment only generate or preserve text; a diagnostic-domain fact is the smallest reusable owner.
- Rollback signal: BSL215 targeted tests fail, BSL233 public-method description presence regresses, or the shared fact starts normalizing non-legacy tabs.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_method_doc_comment.py tests/test_diagnostics_extended.py -q -k 'bsl215 or bsl233 or method_doc_comment' --no-cov`.
- Synthetic fixture/module: inline BSL215 tests cover service comments, missing section, missing individual params, no-param docs, wrong order, multiline/type descriptions, structure fields, `см.` references, tabs, no-space comment entries, return-only docs, and structure composition docs; `tests/test_method_doc_comment.py` covers CST comment-node extraction and inline comments that must not become method docs.
- Related-rule tests: BSL233 targeted path included because description-block presence now uses `MethodDocComment`.
- Parity command/artifact: external private parity deferred because this is behavior-preserving extraction; current known BSL215 taxonomy remains unchanged.
- Performance command/artifact: targeted pytest runtime; no additional module-wide traversal was introduced.
- Ruff/lint: `./.venv/bin/python -m ruff check src/onec_hbk_bsl/analysis/diagnostic/domain/method_doc_comment.py src/onec_hbk_bsl/analysis/diagnostic/domain/__init__.py src/onec_hbk_bsl/analysis/diagnostic/rules/method_contract_rules.py tests/test_method_doc_comment.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
