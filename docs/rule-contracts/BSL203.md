# BSL203 — <span class="doc-lang doc-lang-ru">Обращение к Интернет-ресурсам</span><span class="doc-lang doc-lang-en">Referring to Internet resources</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Обращение к Интернет-ресурсам

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Referring to Internet resources

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL203` |
| Совместимый псевдоним / Compatible alias | `InternetAccess` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `security` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL203` и псевдоним `InternetAccess` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL203"]
ignore = ["InternetAccess"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL203
Значение = "пример";  // bsl-disable: BSL203
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:InternetAccess-off
// код без этой диагностики
// BSLLS:InternetAccess-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL203` and alias `InternetAccess` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL203"]
ignore = ["InternetAccess"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL203
Value = "example";  // bsl-disable: BSL203
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:InternetAccess-off
// code without this diagnostic
// BSLLS:InternetAccess-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Проверьте обращение к Интернет-ресурсам и набор передаваемых данных для исключения передачи конфиденциальной или защищенной информации.

## Примеры

```bsl
HTTPСоединение = Новый HTTPСоединение("zabbix.localhost", 80); // замечание
FTPСоединение = Новый FTPСоединение(Сервер, Порт, Пользователь, Пароль); // замечание
```

## Источники

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Check access to Internet resources and the set of transmitted data to prevent the transfer of confidential or protected information.

## Examples

```bsl
HTTPConnection = New HTTPConnection("zabbix.localhost", 80); // error
FTPConnection = New FTPConnection(Server, Port, User, Pwd); // error
```

## Sources

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL203
- BSLLS compatible name: InternetAccess
- Public name/i18n key: BSL203 / InternetAccess
- Default message/i18n key: RULE_MESSAGES_RU["BSL203"]
- Description/i18n key: RULE_METADATA["BSL203"].description
- Severity: WARNING
- Parameters/defaults: none in current onec-hbk-bsl; BSLLS rule can be disabled by config.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: internet resource access requires security review because it may transfer protected data.
- Why this diagnostic exists: HTTP/FTP connection construction is a security-sensitive integration boundary.
- Non-goals: unrelated identifiers, string/comment text, or member calls not constructing internet connection objects.
- Keep/drop decision: keep as a CST-backed security audit diagnostic.

## Semantic Oracle

- Checked language fact: executable code constructs a configured internet connection/request object.
- Must diagnose when: `Новый HTTPСоединение`, `Новый FTPСоединение`, or dynamic `Новый("HTTPСоединение"/"FTPСоединение")` appears in executable code.
- Must not diagnose when: the same text appears in strings/comments or the constructed type is not an internet-access class.
- Diagnostic anchor token/range: the `Новый` constructor expression.
- Quick-fix target, if any: none; manual security review is required.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through the shared security API detector.
- CST/domain object types: `new_expression` and `new_expression_method`.
- Extracted attributes: static type name, dynamic string type name, source range, and severity.
- Parse-error behavior: structural rule skips when the CST root is unavailable; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: fixed type-name set matching over CST-extracted constructor names.

## Examples

### Invalid Examples

1. `Соединение = Новый HTTPСоединение("example.org");`
2. `Соединение = Новый FTPСоединение(Сервер, Порт);`
3. `Запрос = Новый("HTTPСоединение", "example.org");`

### Valid Negative Twins

1. `Текст = "Новый HTTPСоединение(""example.org"")";`
2. `// Новый FTPСоединение("example.org");`
3. `Объект.HTTPСоединение();`

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored except when the string is the dynamic `Новый("Type")` type argument.
- Preprocessor: ordinary CST parse behavior applies.
- Multiline: constructor range comes from CST.
- Nested constructs: nested constructors are diagnosed independently.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: security-sensitive platform APIs and internet integration.
- Rules that may report on the same object/range: BSL188 filesystem access, BSL253 internet connection without timeout, and BSL264 system information.
- Intended duplicate policy: BSL203 may coexist with BSL253 because one reports internet access and the other timeout robustness.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl203_internet_access_uses_cst_constructors`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 12.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: shared CST constructor detection inherited from `FileSystemAccessRule`.
- Expected complexity: O(CST nodes) with constant-time type-name checks.
- Shared-pass reuse: uses the same base detector as BSL188 and BSL264 with BSL203-specific type names.
- Before/after measurement, if changed: no runtime change; current corpus exact parity onec 12 / bslls 12.

## Implementation Decision

- Change target: contract and audit state only; existing CST implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact.
- Why not another layer: CST constructors already express the semantic target precisely.
- Rollback signal: constructor strings/comments report, dynamic constructor type names stop reporting, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl203 or InternetAccess"`.
- Synthetic fixture/module: existing test covers static/dynamic constructor positives and string/comment negatives.
- Related-rule tests: BSL188/BSL264 only if shared base API detection changes.
- Parity command/artifact: preserved-source-root selected BSL203 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
