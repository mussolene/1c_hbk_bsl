# BSL047 — <span class="doc-lang doc-lang-ru">Магические даты</span><span class="doc-lang doc-lang-en">Magic dates</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Магические даты

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Magic dates

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL047` |
| Совместимый псевдоним / Compatible alias | `MagicDate` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `design`, `date-time` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL047` и псевдоним `MagicDate` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL047"]
ignore = ["MagicDate"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL047
Значение = "пример";  // bsl-disable: BSL047
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MagicDate-off
// код без этой диагностики
// BSLLS:MagicDate-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL047` and alias `MagicDate` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL047"]
ignore = ["MagicDate"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL047
Value = "example";  // bsl-disable: BSL047
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MagicDate-off
// code without this diagnostic
// BSLLS:MagicDate-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики
Магическая дата - это любая дата в вашем коде, которая не очевидна без погружения в контекст.

## Примеры

Плохо

```bsl
Если текДата < '20151021' Тогда
    ХоверБордБудетИзобретен = Неопределено;
КонецЕсли;
```

Хорошо

```bsl
ПредсказаннаяДата = '20151021';
Если текДата < ПредсказаннаяДата Тогда
    ХоверБордБудетИзобретен = Неопределено;
КонецЕсли;
```

Также хорошим решением является использование специального метода с говорящим названием, который возвращает
дату-константу

```bsl
Функция ДатаИзобретенияХовера()
    Возврат '20151021';
КонецФункции

Если текДата < ДатаИзобретенияХовера() Тогда
    ХоверБордБудетИзобретен = Неопределено;
КонецЕсли;
```

## Исключения

Магические даты, используемые в структурах и соответствиях, не считаются ошибками, так как они используются в качестве ключей или значений в структурах данных, где контекст понятен:

```bsl
// Операция Вставить структуры - нет ошибки
Структура = Новый Структура;
Структура.Вставить("ДатаНачала", '20250101'); // Нет ошибки
Структура.Вставить("ДатаОкончания", '20251231'); // Нет ошибки
Структура.Вставить("МаксимальнаяДата", '39991231235959'); // Нет ошибки

// Конструктор структуры - нет ошибки
Структура2 = Новый Структура("ДатаНачала, ДатаОкончания", '20250101', '20251231'); // Нет ошибки

// Прямое присваивание свойству структуры - нет ошибки
СтруктураСПолями = Новый Структура("ДатаНачала, ДатаОкончания");
СтруктураСПолями.ДатаНачала = '20250101'; // Нет ошибки
СтруктураСПолями.ДатаОкончания = '20251231'; // Нет ошибки

// Фиксированная структура - нет ошибки
ФиксированнаяСтруктура = Новый ФиксированнаяСтруктура("Значение", '20240101'); // Нет ошибки

// Соответствие - нет ошибки (как ключ, так и значение)
Соответствие = Новый Соответствие;
Соответствие.Вставить("Код", '20230101'); // Нет ошибки
Соответствие.Вставить('19800101', "Олимпиада в Москве"); // Нет ошибки
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description
Magic date is any date in your code that does not immediately become apparent without being immersed in context.

## Examples

Bad

```bsl
If now < '20151021' Then
    HoverBoardIsReal = Undefined;
EndIf;
```

Good

```bsl
PredictedDate = '20151021';
If now < PredictedDate Then
    HoverBoardIsReal = Undefined;
EndIf;
```

Also, a good solution is to use a special method with "telling name" that returns
constant

```bsl
Function DateInventionHover()
    Return '20151021';
EndFunction

If CurrentDate < DateInventionHover() Then
    HoverBoardWillBeInvented = Undefined;
EndIf;
```

## Exceptions

Magic dates used in structures and correspondences are not considered errors, as they are used as keys or values in data structures where the context is clear:

```bsl
// Structure insert - no error
Structure = New Structure;
Structure.Insert("StartDate", '20250101'); // No error
Structure.Insert("EndDate", '20251231'); // No error
Structure.Insert("MaxDate", '39991231235959'); // No error

// Structure constructor - no error
Structure2 = New Structure("StartDate, EndDate", '20250101', '20251231'); // No error

// Direct structure property assignment - no error
StructureWithFields = New Structure("StartDate, EndDate");
StructureWithFields.StartDate = '20250101'; // No error
StructureWithFields.EndDate = '20251231'; // No error

// Fixed structure - no error
FixedStructure = New FixedStructure("Value", '20240101'); // No error

// Correspondence - no error (both key and value)
Correspondence = New Correspondence;
Correspondence.Insert("Code", '20230101'); // No error
Correspondence.Insert('19800101', "Olympics in Moscow"); // No error
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL047
- BSLLS compatible name: MagicDate
- Public name/i18n key: BSL047 / MagicDate
- Default message/i18n key: RULE_MESSAGES_RU["BSL047"]
- Description/i18n key: RULE_METADATA["BSL047"].description
- Severity: INFORMATION
- Parameters/defaults: `authorizedDates="00010101,00010101000000,000101010000"`.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: meaningful boundary dates in executable code should be named or isolated instead of hidden as unexplained literals.
- Why this diagnostic exists: unexplained date constants make business rules harder to review and safely change.
- Non-goals: authorized platform sentinel dates, comments, invalid date-looking strings, simple constant declarations, return constants, structure/map initializers, or broader magic-number detection.
- Keep/drop decision: keep as a BSLLS-compatible magic-date diagnostic.

## Semantic Oracle

- Checked language fact: a date-like BSL date/string literal is used directly inside an executable expression.
- Must diagnose when: a non-authorized date-like literal participates in an expression, condition, function argument, ternary branch, or date constructor expression where the date meaning is not isolated as a simple constant.
- Must not diagnose when: the literal is in comments, authorized by `authorizedDates`, invalid as a date-like value, used as a simple assignment/return constant, appears in a method declaration line, or belongs to structure/map insertion contexts documented as exceptions.
- Diagnostic anchor token/range: date-like literal span including the surrounding quotes.
- Quick-fix target, if any: extract the date to a named constant/helper or add it to the allowed-date policy when it is a documented sentinel.

## CST Or Domain Fact Model

- Parser/tree source: masked source line scan over executable physical lines.
- CST/domain object types: physical code line, date literal candidate, string literal candidate, line prefix/suffix context, and authorized-date policy.
- Extracted attributes: literal value, normalized digits, line/columns, simple assignment state, return state, structure/map context, date validity, and severity.
- Parse-error behavior: line-local literal facts can still report; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: literal extraction and prefix/suffix exclusions are textual because the rule is line-local and must match BSLLS date-token behavior.

## Examples

### Invalid Examples

1. `Если ТекДата < '20151021' Тогда`
2. `День = Дата("00020101") + Шаг;`
3. `Значение = ?(А = '39990202', '39991231235959', '39990101000000');`

### Valid Negative Twins

1. `Конец = '12340101';`
2. `Возврат '20151021';`
3. `Структура.Вставить("ДатаНачала", '20250101');`

### Edge Examples

- Comments: ignored by line comment masking.
- Strings: date-looking string literals are checked only when they are valid date values and used as expression facts.
- Preprocessor: not a separate target.
- Multiline: current rule is physical-line local; multiline expression facts are represented by their literal line.
- Nested constructs: nested expressions and ternaries are diagnosed when the literal is not in an excluded context.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: literal values in executable code.
- Rules that may report on the same object/range: BSL029 magic numbers, BSL097 deprecated current date usage, BSL012 hardcoded secrets for string literals, and typo/string-content diagnostics may inspect nearby strings.
- Intended duplicate policy: BSL047 owns date-like literal business constants; BSL029 owns numeric literals and security/string rules own their separate policy.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl047MagicDate`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics, default authorized-date policy.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 4.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over source lines with line-local literal candidates.
- Expected complexity: O(total source characters).
- Shared-pass reuse: uses existing source line access and does not build additional global facts.
- Before/after measurement, if changed: no runtime change; current corpus exact parity onec 4 / bslls 4.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact.
- Why not another layer: the documented rule is literal and line-context based; extra CST traversal would not improve the current exact parity surface.
- Rollback signal: authorized dates report, simple assignments/returns report, structure/map exceptions report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl047 or MagicDate"`.
- Synthetic fixture/module: existing selected tests cover expression positives, simple assignment negative, authorized sentinel handling, invalid date-like values, function arguments, ternary branches, and structure/map exceptions.
- Related-rule tests: literal cluster only if shared masking or literal extraction changes.
- Parity command/artifact: preserved-source-root selected BSL047 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
