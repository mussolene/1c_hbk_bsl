# BSL035 DuplicateStringLiteral Contract

## Rule Identity

- Code: BSL035
- BSLLS compatible name: DuplicateStringLiteral
- Public name/i18n key: BSL035 / DuplicateStringLiteral
- Default message/i18n key: RULE_MESSAGES_RU["BSL035"]
- Description/i18n key: RULE_METADATA["BSL035"].description
- Severity: INFORMATION
- Parameters/defaults: `allowedNumberCopies=2`, `analyzeFile=false`, `caseSensitive=false`, `minTextLength=5`; local `min_duplicate_uses=3` is the report threshold equivalent.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: repeated non-trivial string literals inside one method/scope should be extracted or reused to reduce copy-paste maintenance risk.
- Why this diagnostic exists: changing duplicated literal values is error-prone, and repeated literals often indicate missing constants or duplicated code.
- Non-goals: short service literals below the minimum length, string text in comments, pseudo-literals formed across adjacent syntax, cross-method duplicates when `analyzeFile=false`, and syntax-error ownership.
- Keep/drop decision: keep as a BSLLS-compatible badpractice/readability diagnostic.

## Semantic Oracle

- Checked language fact: the same string literal value appears more than the allowed number of copies inside the configured duplicate-analysis scope.
- Must diagnose when: a normalized string literal appears at least `min_duplicate_uses` times in one method/module scope and its quoted length is at least `minTextLength`.
- Must not diagnose when: the literal appears fewer times than the threshold, belongs to different method scopes with default `analyzeFile=false`, is too short, occurs only in comments, or is a syntactic artifact rather than a real literal.
- Diagnostic anchor token/range: the first duplicate string literal token including quotes.
- Quick-fix target, if any: extract the literal to a variable/constant/function and reuse it.

## CST Or Domain Fact Model

- Parser/tree source: document snapshot, procedure model scopes, and comment-stripped source lines.
- CST/domain object types: procedure/function scope model, module-level scope, string literal regex over code-without-comments.
- Extracted attributes: scope line indices, literal raw value, case-folded key when case-insensitive, first occurrence line/column, duplicate count, and minimum duplicate threshold.
- Parse-error behavior: BSL035 is not a parser recovery rule; malformed syntax should be owned by BSL001.
- Textual/regex behavior, only if genuinely textual: string-literal extraction is textual over code-without-comments because the rule compares literal text content and existing snapshot already masks comments/strings consistently.

## Examples

### Invalid Examples

1. Three uses of `"Значение"` inside one procedure with default threshold.
2. Three case-varied uses of `"Раздел"` / `"раздел"` when `caseSensitive=false`.
3. Three escaped-quote literals such as `"Код ""240"" места"` inside one scope.

### Valid Negative Twins

1. Two uses of the same literal with threshold `3`.
2. The same literal once in each of three different methods when `analyzeFile=false`.
3. Short literals below the minimum quoted length.

### Edge Examples

- Comments: string-looking text in comments is ignored.
- Strings: escaped quotes belong to the same literal value.
- Preprocessor: not a separate semantic target for this rule.
- Multiline: current contract is single-line literal comparison; multiline literal parity must be classified before expanding behavior.
- Nested constructs: nested blocks do not create separate scopes; method/module scope controls grouping.
- BSL/SDBL variants, if relevant: query text is still BSL string text; query semantics are not inspected by this rule.

## Related-Rule Cluster

- Shared semantic object: string literals.
- Rules that may report on the same object/range: BSL012 hardcoded secrets, BSL014 line length on the same line, BSL029 must not inspect numbers inside strings, BSL256 typo diagnostics, and query-text diagnostics for query literal contents.
- Intended duplicate policy: BSL035 may coexist with content-specific string diagnostics only when the literal itself is duplicated; it must not create pseudo-literals across adjacent string/index syntax.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestBsl035DuplicateStringLiteral`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 2055.
- Line/common semantic common: 2055 exact reports plus classified occurrence/range-policy tail.
- Ours-only categories: 74 occurrence-selection/range-policy reports, concentrated in multiline query/string literals and split-module mirror scopes.
- BSLLS-only categories: 54 occurrence-selection/range-policy reports for the same duplicated-literal families.
- Range-only categories: paired multiline literal ranges where BSLLS spans a different literal segment/occurrence than onec.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over scope line indices and comment-stripped line text.
- Expected complexity: O(source lines + literal matches).
- Shared-pass reuse: reuses document snapshot comment masking and procedure scopes; no extra CST parse.
- Before/after measurement, if changed: no implementation change; targeted BSL035 tests pass; preserved-source-root private parity is classified at 2055 exact, 54 BSLLS-only, 74 onec-only.

## Implementation Decision

- Change target: BSL035 contract and audit state; no implementation change in this pass.
- Delta category fixed by this change: none; remaining tail is classified as occurrence-selection/range policy, not an unclassified semantic miss.
- Why not another layer: duplicate string detection is value/text-based and scoped by the existing procedure model.
- Rollback signal: short literals report, different-method duplicates report under default config, comments report, escaped quotes split incorrectly, or case-insensitive grouping stops working.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py::TestBsl035DuplicateStringLiteral -q --no-cov`.
- Synthetic fixture/module: existing tests cover threshold, method scope, adjacent access syntax, escaped quotes, and case-insensitive grouping.
- Related-rule tests: run string/suppression tests if implementation changes.
- Parity command/artifact: preserved-source-root selected BSL035 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time if implementation changes.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_diagnostics_extended.py src/onec_hbk_bsl/analysis/diagnostic/domain/module_model.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
