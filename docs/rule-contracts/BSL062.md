# BSL062 — <span class="doc-lang doc-lang-ru">Неиспользуемый параметр</span><span class="doc-lang doc-lang-en">Unused parameter</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Неиспользуемый параметр

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Unused parameter

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL062` |
| Совместимый псевдоним / Compatible alias | `UnusedParameters` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `unused`, `design` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL062` и псевдоним `UnusedParameters` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL062"]
ignore = ["UnusedParameters"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL062
Значение = "пример";  // bsl-disable: BSL062
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UnusedParameters-off
// код без этой диагностики
// BSLLS:UnusedParameters-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL062` and alias `UnusedParameters` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL062"]
ignore = ["UnusedParameters"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL062
Value = "example";  // bsl-disable: BSL062
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UnusedParameters-off
// code without this diagnostic
// BSLLS:UnusedParameters-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики
Методы не должны содержать неиспользуемых параметров.

## Примеры

```bsl
Функция СложитьДваЧисла(Знач ПервоеЧисло, Знач ВтороеЧисло, Знач ЛишнийПараметр)

    Возврат ПервоеЧисло + ВтороеЧисло;

КонецФункции
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description
Methods shouldn't contain unused parameters.

## Examples

```bsl
Function AddTwoNumbers(Val FirstValue, Val SecondValue, Val UnusedParameter)

    Return FirstValue + SecondValue;

EndFunction
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL062
- BSLLS compatible name: UnusedParameters
- Public name/i18n key: UnusedParameters / BSL062
- Default message/i18n key: RULE_MESSAGES_RU["BSL062"]
- Description/i18n key: RULE_METADATA["BSL062"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: method signatures should not contain unused parameters unless the platform or framework requires the signature.
- Why this diagnostic exists: unused parameters make method contracts misleading and often show stale call patterns or incomplete refactoring.
- Non-goals: do not report local variables; BSL007 owns unused locals. Do not report parameters in empty/comment-only method bodies. Do not report `ПриСозданииОбъекта` / `OnObjectCreate` handler parameters.
- Keep/drop decision: keep as a BSLLS-compatible unused method-parameter diagnostic, with CST-based reference facts where available.

## Semantic Oracle

- Checked language fact: a procedure/function parameter has no identifier reference in its method body.
- Must diagnose when: a parsed method has a non-empty executable body and a parameter identifier does not appear as a body identifier.
- Must not diagnose when: the parameter is referenced in the method body, the method has no parameters, the method body is empty/comment-only, the method is `ПриСозданииОбъекта` / `OnObjectCreate`, or the file is an EDT split-method fragment skipped by this rule.
- Diagnostic anchor token/range: the unused parameter identifier token/range.
- Quick-fix target, if any: remove the unused parameter and update call sites manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures`, `ProcInfo.param_ranges`, `proc_node_map`, and tree-sitter identifier facts in the procedure body.
- CST/domain object types: `procedure_definition` / `function_definition`, `parameter` identifier nodes, body identifier nodes, and `ProcedureModel`.
- Extracted attributes: method name/kind/start/end, parameter names and ranges, method body identifier casefold set, split-fragment status.
- Parse-error behavior: if tree-sitter procedure body facts are unavailable, the rule falls back to textual body search through `ProcedureModel.validate_unused_parameters`; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: fallback body search is compatibility-only; clean tree-sitter files use CST identifier facts to avoid comments/strings false reads.

## Examples

### Invalid Examples

1. `Процедура Тест(НеИспользуемый)` with a non-empty body that does not reference `НеИспользуемый`.
2. A method with an unused optional parameter.
3. A client command handler whose standard-looking parameters are not referenced; BSLLS reports them.

### Valid Negative Twins

1. A parameter referenced in an expression inside the method body.
2. `ПриСозданииОбъекта(Параметр)`; BSLLS handler exception.
3. A method with only comments in the body.

### Edge Examples

- Comments: parameter mentions in comments do not count as usage on tree-sitter path.
- Strings: parameter mentions in strings do not count as usage on tree-sitter path.
- Preprocessor: visible parsed method bodies are checked; inactive preprocessor branch handling is not owned by this rule.
- Multiline: multiline signatures use `ProcInfo.param_ranges` for exact parameter anchors.
- Nested constructs: references inside nested blocks within the method body count as usage.
- BSL/SDBL variants, if relevant: query text is not a parameter-reference target unless the parameter is referenced by BSL code outside the query literal.

## Related-Rule Cluster

- Shared semantic object: method parameter list and method body symbol references.
- Rules that may report on the same object/range: BSL007 reports unused locals; BSL015 and BSL031 report parameter-list size; BSL192/BSL193/BSL194/BSL215 report parameter documentation; BSL228 reports parameter ordering; BSL240 reports rewrite of method parameters; BSL254 reports client/server value-transfer parameter issues.
- Intended duplicate policy: keep BSL062 distinct from BSL007 and documentation rules; the same parameter can be both unused and undocumented only if both user actions are useful.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl062UnusedParameter`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files, plus `tests/fixtures/diag_synthetic/bsl062_unused_parameter.bsl`.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: not available with current BSLLS CLI runtime because BSLLS returns zero BSL062 diagnostics.
- Line/common semantic common: not available with current BSLLS CLI runtime.
- Ours-only categories: current private batch has `onec=4509`, `bslls=0`, all caused by BSLLS CLI/runtime no-op for `UnusedParameters` under the local environment, not by a sampled onec semantic false-positive class.
- BSLLS-only categories: none in the current batch.
- Range-only categories: none established in current batch because BSLLS emitted no BSL062 diagnostics.
- Known BSLLS defects: local BSLLS 1.0.0, 0.29.0, and 0.27.0 CLI runs emit zero `UnusedParameters` diagnostics even for minimal synthetic methods with unused parameters. The jar contains `UnusedParametersDiagnostic.class` and docs/config key `UnusedParameters`; bytecode shows it should compare method parameters against body identifiers. Current synthetic fixture parity is `onec=1`, `bslls=0`, `only_onec=1` at the unused parameter range. This is classified as a BSLLS CLI/runtime blocker for this environment.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing procedure model and cached tree-sitter procedure node map.
- Expected complexity: O(method parameters + body identifiers).
- Shared-pass reuse: shares `ProcInfo` parameter ranges with BSL015/BSL031; body identifier collection is lazy and limited to parameter-name candidates.
- Before/after measurement, if changed: compare the same full-rule `largest5` run and require an unchanged diagnostic count; report controlled old/new timings separately from noisy background-loaded runs.

## Implementation Decision

- Change target: BSL062 body-identifier fact construction only; semantic validation and ranges remain in `ProcedureModel`.
- Delta category fixed by this change: performance-only; skip body fact construction when the existing applicability gate rejects the method and stop traversal once all parameter candidates are found.
- Why not another layer: the waste is local to the BSL062 fact request; changing global CST indexing would retain the unnecessary per-method identifier decoding.
- Rollback signal: targeted BSL062 tests fail, contract validation fails, or a working BSLLS runtime later produces unclassified BSL062 semantic deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl062 --no-cov`.
- Synthetic fixture/module: `tests/fixtures/diag_synthetic/bsl062_unused_parameter.bsl`; inline BSL062 tests cover unused/used parameters, underscores, default string commas, handler exceptions, empty body, command handlers, optional parameters, and split fragments.
- Related-rule tests: BSL007 targeted tests if shared symbol logic changes.
- Parity command/artifact: preserved-source-root private batch BSL062 parity, plus minimal synthetic BSLLS CLI probes; current BSLLS side is a runtime blocker.
- Performance command/artifact: no dedicated performance artifact; no rule logic changed.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/domain/procedure_model.py src/onec_hbk_bsl/analysis/diagnostic/domain/module_model.py tests/test_diagnostics_method_contracts.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
