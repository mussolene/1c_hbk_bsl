# BSL264 — <span class="doc-lang doc-lang-ru">Использование системной информации</span><span class="doc-lang doc-lang-en">Use of system information</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование системной информации

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Use of system information

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL264` |
| Совместимый псевдоним / Compatible alias | `UseSystemInformation` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `security` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL264` и псевдоним `UseSystemInformation` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL264"]
ignore = ["UseSystemInformation"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL264
Значение = "пример";  // bsl-disable: BSL264
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UseSystemInformation-off
// код без этой диагностики
// BSLLS:UseSystemInformation-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL264` and alias `UseSystemInformation` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL264"]
ignore = ["UseSystemInformation"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL264
Value = "example";  // bsl-disable: BSL264
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UseSystemInformation-off
// code without this diagnostic
// BSLLS:UseSystemInformation-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Объект `СистемнаяИнформация` предоставляет данные о компьютере и конфигурации в 1С, которая может быть использована в злонамеренных целях.

## Примеры

## Источники

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

`SystemInformation` provides data about the computer and configuration in 1C, which can be used for malicious purposes.

## Examples

## Sources

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL264
- BSLLS compatible name: UseSystemInformation
- Public name/i18n key: BSL264 / UseSystemInformation
- Default message/i18n key: RULE_MESSAGES_RU["BSL264"]
- Description/i18n key: RULE_METADATA["BSL264"].description
- Severity: WARNING
- Parameters/defaults: disabled by default in BSLLS; selectable in onec-hbk-bsl by rule code/name.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: access to computer and environment information is security-sensitive and must be reviewed.
- Why this diagnostic exists: `СистемнаяИнформация` / `SystemInfo` exposes local machine and configuration details that may support unsafe behavior.
- Non-goals: unrelated identifiers, string/comment text, or other constructor types.
- Keep/drop decision: keep as a BSLLS-compatible optional security diagnostic.

## Semantic Oracle

- Checked language fact: executable code constructs a system-information object.
- Must diagnose when: `Новый СистемнаяИнформация` or dynamic `New("SystemInfo")` / `Новый("СистемнаяИнформация")` appears in executable code.
- Must not diagnose when: the same text appears only in strings/comments or the constructed type is not system information.
- Diagnostic anchor token/range: constructor expression span.
- Quick-fix target, if any: remove the call or replace it with a narrower audited source of required data.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through the shared security constructor detector.
- CST/domain object types: `new_expression` and dynamic string constructor forms.
- Extracted attributes: constructed type name, constructor range, and severity.
- Parse-error behavior: structural rule skips when the CST root is unavailable; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: fixed type-name set matching over CST-extracted constructor names.

## Examples

### Invalid Examples

1. `Инфо = Новый СистемнаяИнформация;`
2. `Info = New("SystemInfo");`
3. `Инфо = Новый("СистемнаяИнформация");`

### Valid Negative Twins

1. `Текст = "Новый СистемнаяИнформация";`
2. `// Новый СистемнаяИнформация;`
3. `Инфо = Новый Структура;`

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored except when the string is the dynamic constructor type argument.
- Preprocessor: ordinary CST parse behavior applies.
- Multiline: constructor range comes from CST.
- Nested constructs: nested constructors are diagnosed independently.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: security-sensitive platform APIs and constructors.
- Rules that may report on the same object/range: BSL188 filesystem access, BSL203 internet access, BSL226 OS user enumeration, and BSL247 privileged mode.
- Intended duplicate policy: BSL264 owns system-information object construction specifically.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl264_system_information_uses_cst_constructors`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 46.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: shared CST constructor detection inherited from `FileSystemAccessRule`.
- Expected complexity: O(CST nodes) with constant-time type-name checks.
- Shared-pass reuse: uses the same base detector as BSL188 and BSL203 with BSL264-specific type names.
- Before/after measurement, if changed: no runtime change; current corpus exact parity onec 46 / bslls 46.

## Implementation Decision

- Change target: contract and audit state only; existing CST implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact.
- Why not another layer: CST constructors already express the semantic target precisely.
- Rollback signal: constructor strings/comments report, dynamic constructor type names stop reporting, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl264 or UseSystemInformation"`.
- Synthetic fixture/module: existing selected test covers static/dynamic constructor positives and string/comment negatives.
- Related-rule tests: BSL188/BSL203 only if shared base API detection changes.
- Parity command/artifact: preserved-source-root selected BSL264 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
