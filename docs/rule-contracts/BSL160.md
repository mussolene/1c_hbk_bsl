# BSL160 CommonModuleMissingAPI Contract

## Rule Identity

- Code: BSL160
- BSLLS compatible name: CommonModuleMissingAPI
- Public name/i18n key: BSL160 / CommonModuleMissingAPI
- Default message/i18n key: RULE_MESSAGES_RU["BSL160"] and RULE_MESSAGES_RU_FMT["BSL160"]
- Description/i18n key: RULE_METADATA["BSL160"].description
- Severity: INFORMATION
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: a common module with procedures/functions should expose a clear public API region and exported methods when it is meant to be callable.
- Why this diagnostic exists: common modules without any exported methods or without an API/public-interface region are hard to consume and navigate consistently.
- Non-goals: non-common-module files, common modules without procedures/functions, missing XML metadata, common-module execution-context validity, and common-module naming.
- Keep/drop decision: keep as a BSLLS-compatible common-module structure diagnostic.

## Semantic Oracle

- Checked language fact: a common module has at least one procedure/function but either no exported procedure/function or no public API region.
- Must diagnose when: sibling common-module XML exists, procedure/function facts are present, and either no method is exported or no root region is named `ПрограммныйИнтерфейс`, `Public`, or `Internal`.
- Must not diagnose when: XML metadata is missing, there are no procedures/functions, at least one method is exported and an API region exists, or the file is not a common module.
- Diagnostic anchor token/range: the first non-empty/non-BOM line of the module file.
- Quick-fix target, if any: add an appropriate API/public region and export intended public methods.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST for procedure/function facts, region extraction for module regions, and XML sibling lookup for common-module ownership.
- CST/domain object types: `ProcInfo`, `RegionInfo`, and common-module XML metadata path.
- Extracted attributes: common-module XML presence, region names, procedure/function export flags, and first non-empty module line range.
- Parse-error behavior: use available procedure/region facts; `BSL001` owns syntax errors.
- Textual/regex behavior, only if genuinely textual: API region name matching and XML sibling path lookup are textual conventions.

## Examples

### Invalid Examples

1. Common module with an exported procedure but only a non-API region.
2. Common module with a public API region but procedures/functions without `Экспорт`.
3. Common module with methods and no public/API region.

### Valid Negative Twins

1. Common module with `#Область ПрограммныйИнтерфейс` and an exported procedure.
2. Common module with `#Region Public` and an exported function.
3. File without common-module XML metadata.

### Edge Examples

- Comments: comments may be the diagnostic anchor when they are the first real module line; they do not create API regions by themselves.
- Strings: strings do not affect API-region or export detection.
- Preprocessor: not a separate semantic target for this rule.
- Multiline: procedure/function bodies may be multiline; only export flag and region membership matter.
- Nested constructs: root region names are used for API-region presence.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: common-module XML metadata, root regions, and procedure/function export facts.
- Rules that may report on the same object/range: BSL152 checks cached public common modules, BSL158 assignment to common-module names, BSL159 invalid common-module type, and BSL161-BSL168 common-module naming.
- Intended duplicate policy: keep BSL160 separate; it reports missing API/export shape, not metadata flag validity or naming.
- Cluster fixture or probe: `tests/test_bsl158_160_common_module.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32142 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 23.
- Line/common semantic common: exact selected parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0 after aligning the anchor to the first non-empty/non-BOM line.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing region/procedure facts plus common-module XML sibling lookup.
- Expected complexity: O(regions + procedures + first-line scan).
- Shared-pass reuse: shares `ProcInfo`, `RegionInfo`, and XML helpers with BSL152 and BSL158-BSL168.
- Before/after measurement, if changed: selected private parity is exact; targeted BSL158-BSL160 tests completed.

## Implementation Decision

- Change target: BSL160 diagnostic anchor, shared common-module name-rule anchor compatibility, contract, audit state, and targeted test.
- Delta category fixed by this change: range-only mismatch for modules whose first physical line is BOM-only.
- Why not another layer: current domain facts already model the rule; the only defect was the diagnostic anchor policy.
- Rollback signal: modules with no API/export stop reporting, clean API modules report, BSL161-BSL168 name-rule tests fail, or selected parity regresses from exact.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_bsl158_160_common_module.py -q --no-cov`.
- Synthetic fixture/module: existing tests cover missing API region, clean public+export, API-region names, no-export case, and BOM-only first-line anchoring.
- Related-rule tests: BSL158-BSL159 and BSL161-BSL168 share the same test module because of common XML/range helpers.
- Parity command/artifact: preserved-source-root selected BSL160 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_bsl158_160_common_module.py src/onec_hbk_bsl/analysis/diagnostic/rules/common_module_rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
