# BSL064 ProcedureReturnsValue Contract

## Rule Identity

- Code: BSL064
- BSLLS compatible name: ProcedureReturnsValue
- Public name/i18n key: BSL064 / ProcedureReturnsValue
- Default message/i18n key: RULE_MESSAGES_RU["BSL064"]
- Description/i18n key: RULE_METADATA["BSL064"].description
- Severity: ERROR
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: `Процедура` / `Procedure` must not return a value; value-returning routines must be functions.
- Why this diagnostic exists: `Возврат` with an expression inside a procedure is a language/semantic error and hides an API contract mismatch.
- Non-goals: empty `Возврат;` in procedures, return-path completeness for functions, or excessive-return style checks.
- Keep/drop decision: keep as a BSLLS-compatible correctness diagnostic.

## Semantic Oracle

- Checked language fact: a procedure body contains a `return_statement` with an expression child.
- Must diagnose when: `Возврат <expression>;` / `Return <expression>;` appears inside a procedure.
- Must not diagnose when: the return statement is empty, or the same return-with-expression appears inside a function.
- Diagnostic anchor token/range: full CST return statement span.
- Quick-fix target, if any: remove the returned expression or convert the procedure to a function when the API contract requires a value.

## CST Or Domain Fact Model

- Parser/tree source: procedure facts mapped to tree-sitter procedure definition nodes.
- CST/domain object types: `ProcInfo`, procedure definition node, `return_statement`, and expression child.
- Extracted attributes: method kind, method start line, return statement range, expression presence, and severity.
- Parse-error behavior: skip methods whose CST node cannot be mapped; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: none for the semantic decision; range coordinates are derived from CST byte positions.

## Examples

### Invalid Examples

1. `Процедура Тест() Возврат 42; КонецПроцедуры`
2. `Procedure Test() Return Result; EndProcedure`
3. `Процедура Тест() Если А Тогда Возврат А; КонецЕсли; КонецПроцедуры`

### Valid Negative Twins

1. `Процедура Тест() Возврат; КонецПроцедуры`
2. `Функция Тест() Возврат 42; КонецФункции`
3. `Процедура Тест() А = 42; КонецПроцедуры`

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored by CST.
- Preprocessor: ordinary CST extraction applies where the procedure node is available.
- Multiline: the diagnostic range follows the CST return statement span.
- Nested constructs: returns inside nested branches of the same procedure are reported independently.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: return statements and method kind/contracts.
- Rules that may report on the same object/range: BSL008 too many returns, BSL032 function should have return, BSL051 unreachable code after return, BSL148 function return paths, and BSL194 identical primitive returns.
- Intended duplicate policy: BSL064 owns procedure returns with values; function-return rules own function contracts.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestBsl064ProcedureReturnsValue`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: procedure facts plus CST walk inside procedure nodes.
- Expected complexity: O(number of CST nodes inside procedures).
- Shared-pass reuse: reuses procedure node map from the document snapshot.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing CST implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: CST return statements directly express expression-bearing returns and avoid textual false positives.
- Rollback signal: empty procedure returns report, function returns report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl064 or ProcedureReturnsValue"`.
- Synthetic fixture/module: selected tests cover procedure return value, multiple return values, function return negative, and empty procedure return negative.
- Related-rule tests: return cluster only if shared procedure/return extraction changes.
- Parity command/artifact: preserved-source-root selected BSL064 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
