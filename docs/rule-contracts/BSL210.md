# BSL210 — <span class="doc-lang doc-lang-ru">Использование логического "ИЛИ" в секции "ГДЕ" запроса</span><span class="doc-lang doc-lang-en">Using a logical "OR" in the "WHERE" section of a query</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование логического "ИЛИ" в секции "ГДЕ" запроса

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Using a logical "OR" in the "WHERE" section of a query

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL210` |
| Совместимый псевдоним / Compatible alias | `LogicalOrInTheWhereSectionOfQuery` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `query`, `performance`, `standard` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL210` и псевдоним `LogicalOrInTheWhereSectionOfQuery` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL210"]
ignore = ["LogicalOrInTheWhereSectionOfQuery"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL210
Значение = "пример";  // bsl-disable: BSL210
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:LogicalOrInTheWhereSectionOfQuery-off
// код без этой диагностики
// BSLLS:LogicalOrInTheWhereSectionOfQuery-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL210` and alias `LogicalOrInTheWhereSectionOfQuery` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL210"]
ignore = ["LogicalOrInTheWhereSectionOfQuery"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL210
Value = "example";  // bsl-disable: BSL210
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:LogicalOrInTheWhereSectionOfQuery-off
// code without this diagnostic
// BSLLS:LogicalOrInTheWhereSectionOfQuery-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Не следует использовать `ИЛИ` в секции `ГДЕ` запроса. Это может привести к тому, что СУБД не сможет использовать
индексы таблиц и будет выполнять сканирование, что увеличит время работы запроса и вероятность возникновения блокировок.
Вместо этого следует разбить один запрос на несколько и объединить результаты.

## Примеры

Например, запрос:

```bsl
ВЫБРАТЬ Товар.Наименование ИЗ Справочник.Товары КАК Товар
ГДЕ Артикул = "001" ИЛИ Цена = 10
```

следует заменить на запрос:

```bsl
ВЫБРАТЬ Товар.Наименование ИЗ Справочник.Товары КАК Товар ГДЕ Артикул = "001"
ОБЪЕДИНИТЬ ВСЕ
ВЫБРАТЬ Товар.Наименование ИЗ Справочник.Товары КАК Товар ГДЕ Цена = 10
```

>**Важно** - текущая реализация диагностики срабатывает на любое `ИЛИ` в секции `ГДЕ` и может выдавать ложные срабатывания для некоторых условий.

1) В основном условии оператор `ИЛИ` можно использовать только для последнего из используемых или единственного поля индекса, когда оператор `ИЛИ` можно заменить на оператор `В`.

Правильно:

```bsl
ГДЕ
    Таблица.Поле = &Значение1
    ИЛИ Таблица.Поле = &Значение2
```

т.к. можно переписать при помощи оператора `В` (специально переписывать не нужно, можно оставить, как есть):

```bsl
ГДЕ
    Таблица.Поле В (&Значения)
```

Неправильно:

```bsl
ГДЕ
    Таблица.Поле1 = &Значение1
    ИЛИ Таблица.Поле2 = &Значение2
```

нельзя переписать при помощи `В`, но можно переписать при помощи `ОБЪЕДИНИТЬ ВСЕ` (каждое поле Поле1 и Поле2 должны быть проиндексированы):

```bsl
ГДЕ
    Таблица.Поле1 = &Значение1

ОБЪЕДИНИТЬ ВСЕ

ГДЕ
    Таблица.Поле2 = &Значение1
```

>Примечание: заменить `ИЛИ` на `ОБЪЕДИНИТЬ ВСЕ` можно не всегда, убедитесь, что результат будет действительно тем же, что и при `ИЛИ`, перед тем, как применять.

2) В дополнительном условии оператор ИЛИ можно использовать без ограничений.

Правильно:

```bsl
ГДЕ
    Таблица.Поле1 = &Значение1 // Основное условие (использует индекс)
    И // Дополнительное условие (можно использовать ИЛИ)
    (Таблица.Поле2 = &Значение2 ИЛИ Таблица.Поле3 = &Значение3)
```

Правильно:

```bsl
ГДЕ
    (Таблица.Поле1 = &Значение1 ИЛИ Таблица.Поле1 = &Значение2)
    И
    (Таблица.Поле2 = &Значение3 ИЛИ Таблица.Поле2 = &Значение4)
```

т.к. можно переписать при помощи В (специально переписывать не нужно, можно оставить, как есть):

```bsl
ГДЕ
    Таблица.Поле1 В (&Значения1)   // Основное условие
    И Таблица.Поле2 В (&Значения2) // Дополнительное условие (или наоборот)
```

## Источники

- [Стандарт - Эффективные условия запросов, п.2](https://its.1c.ru/db/v8std/content/658/hdoc)
- [Использование логического ИЛИ в условиях - Типичные причины неоптимальной работы запросов и методы оптимизации](https://its.1c.ru/db/content/metod8dev/src/developers/scalability/standards/i8105842.htm#or)
- [Интересный анализ SQL-запросов в различных СУБД (не про 1С) - Статья на Хабре](https://m.habr.com/ru/company/lsfusion/blog/463095/)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Не следует использовать `ИЛИ` в секции `ГДЕ` запроса. Это может привести к тому, что СУБД не сможет использовать
индексы таблиц и будет выполнять сканирование, что увеличит время работы запроса и вероятность возникновения блокировок.
Вместо этого следует разбить один запрос на несколько и объединить результаты.

## Примеры

For example, query:

```bsl
SELECT Goods.Description FROM Catalog.Goods AS Goods
WHERE Code = "001" OR Cost = 10
```

should instead of a query:

```bsl
SELECT Goods.Description FROM Catalog.Goods AS Goods
WHERE Code = "001"

UNION ALL

SELECT Goods.Description FROM Catalog.Goods AS Goods
WHERE Cost = 10

```

>**Important** - the current implementation of the diagnostic triggers on any `OR` in the `WHERE` section and may issue false positives for some conditions.

1) In the main condition, the `OR` operator can only be used for the last used or the only index field, when the `OR` operator can be replaced by the `IN` operator.

Correct:

```bsl
WHERE
    Table.Filed = &Value1
    OR Table.Filed = &Value2
```

because can be rewritten using the `IN` operator (you don’t need to specifically rewrite it, you can leave it as it is):

```bsl
WHERE
    Table.Field IN (&Value)
```

Incorrect:

```bsl
WHERE
    Table.Field1 = &Value1
    OR  Table.Field2 = &Value2
```

cannot be rewritten with `IN`, but can be rewritten with `UNION ALL` (each Field1 and Field2 must be indexed):

```bsl
WHERE
     Table.Field1 = &Value1

UNION ALL

WHERE
     Table.Field2 = &Value1
```

>Note: it is not always possible to replace `OR` with `UNION ALL`, make sure the result is really the same as with `OR` before applying.

2) Additionally, the 'OR' operator can be used without restriction.

Correct:

```bsl
WHERE
    Table.Filed1 = &Value1 // Main condition (use index)
    AND // Addition condition (can use OR)
    (Table.Filed2 = &Value2 OR Table.Filed3 = &Value3)
```

Correct:

```bsl
WHERE
    (Table.Filed1 = &Value1 OR Table.Filed1 = &Value2)
    AND
    (Table.Filed2 = &Value3 OR Table.Filed2 = &Value4)
```

because can be rewritten using 'IN' (no special rewriting needed, can be left as is):

```bsl
WHERE
    Table.Field1 IN (&Value1)   // Main condition
    AND Table.Field2 IN (&Value2) // Additional condition (or vice versa)
```

## Sources

- [Standard: Effective Query Conditions, Clause 2 (RU)](https://its.1c.ru/db/v8std/content/658/hdoc)
- [Typical Causes of Suboptimal Query Performance and Optimization Techniques: Using Logical OR in Conditions (RU)](https://its.1c.ru/db/content/metod8dev/src/developers/scalability/standards/i8105842.htm#or)
- [Article on Habr: Interesting analysis of SQL queries in various DBMS (not about 1C) (RU)](https://m.habr.com/ru/company/lsfusion/blog/463095/)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL210
- BSLLS compatible name: LogicalOrInTheWhereSectionOfQuery
- Public name/i18n key: BSL210 / LogicalOrInTheWhereSectionOfQuery
- Default message/i18n key: RULE_MESSAGES_RU["BSL210"]
- Description/i18n key: RULE_METADATA["BSL210"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: logical `ИЛИ` / `OR` in a query `ГДЕ` / `WHERE` section can prevent index usage and should be avoided under the BSLLS-compatible performance rule.
- Why this diagnostic exists: `OR` in primary query filters may produce inefficient database plans.
- Non-goals: proving whether a specific `OR` can be safely rewritten to `В` / `IN`, optimizing query packages, or validating metadata indexes.
- Keep/drop decision: keep as a BSLLS-compatible query performance diagnostic.

## Semantic Oracle

- Checked language fact: a complete BSL query text block contains `ИЛИ` / `OR` inside an active `ГДЕ` / `WHERE` clause.
- Must diagnose when: `ИЛИ` / `OR` appears after `ГДЕ` / `WHERE` and before the next top-level post-WHERE clause or query package boundary.
- Must not diagnose when: no `ГДЕ` / `WHERE` clause is active, the token is outside query text, or the token appears in another clause.
- Diagnostic anchor token/range: the `ИЛИ` / `OR` token.
- Quick-fix target, if any: split the query into `ОБЪЕДИНИТЬ ВСЕ` / `UNION ALL`, or rewrite same-field alternatives to `В` / `IN` where semantically valid.

## CST Or Domain Fact Model

- Parser/tree source: BSL query string literals and query text lines.
- CST/domain object types: query literal line, query package boundary, active WHERE stack, and parenthesis depth.
- Extracted attributes: query state, group depth, WHERE depth, clause boundaries, line content base, and OR token range.
- Parse-error behavior: query text scan can run without a complete SDBL parse; malformed query syntax is owned by BSL235/BSL001.
- Textual/regex behavior, only if genuinely textual: current implementation scans query text lines because the diagnostic targets query tokens embedded in BSL strings.

## Examples

### Invalid Examples

1. `ВЫБРАТЬ Ссылка ИЗ Документ.Документ ГДЕ Номер = 1 ИЛИ Номер = 2`
2. `ГДЕ\n    Т.Проведен\n    ИЛИ Т.ПометкаУдаления`
3. `WHERE A.Code = 1 OR A.Description = "x"`

### Valid Negative Twins

1. `ВЫБРАТЬ Ссылка ИЗ Документ.Документ ГДЕ Номер В (&Номера)`
2. `ВЫБРАТЬ Ссылка ИЗ Документ.Документ УПОРЯДОЧИТЬ ПО Дата`
3. `ГДЕ\n    Т.Проведен\nСГРУППИРОВАТЬ ПО\n    Т.Ссылка`

### Edge Examples

- Comments: query-line comments are stripped before token matching.
- Strings: query text is analyzed after BSL string-literal extraction.
- Preprocessor: not a separate semantic target.
- Multiline: active query state survives blank physical lines inside a query literal.
- Nested constructs: parenthesis depth is tracked to avoid ending WHERE too early.
- BSL/SDBL variants, if relevant: this rule analyzes SDBL query text embedded in BSL.

## Related-Rule Cluster

- Shared semantic object: query text and query WHERE/JOIN clauses.
- Rules that may report on the same object/range: BSL187, BSL191, BSL201, BSL206, BSL207, BSL209, BSL220, BSL235, BSL236, BSL238, BSL242, and BSL269 may inspect nearby query text.
- Intended duplicate policy: BSL210 owns logical OR in WHERE; BSL209 owns logical OR in JOIN ON.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl210LogicalOrInWhereSection`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 484.
- Line/common semantic common: 484 exact reports.
- Ours-only categories: 0.
- BSLLS-only categories: 0 after preserving active multiline-query state across blank physical lines.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over source lines with lightweight query-state tracking.
- Expected complexity: O(total source lines + query text length).
- Shared-pass reuse: process-forked as a standalone heavy query rule.
- Before/after measurement, if changed: private selected parity improved from 482/484 with 2 BSLLS-only to exact 484/484.

## Implementation Decision

- Change target: BSL210 runtime query-literal scanner, targeted test, contract, and audit state.
- Delta category fixed by this change: BSLLS-only OR tokens after blank physical lines inside a multiline query literal.
- Why not another layer: migrating to full SDBL CST is larger than the observed bug; the current query-state scanner is sufficient for this exact delta.
- Rollback signal: OR after blank query-literal lines stops reporting, OR outside WHERE reports, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py::TestBsl210LogicalOrInWhereSection -q --no-cov`.
- Synthetic fixture/module: multiline query with a blank physical line before WHERE OR.
- Related-rule tests: BSL209 if shared query OR logic changes.
- Parity command/artifact: preserved-source-root selected BSL210 private parity; sanitized aggregate only.
- Performance command/artifact: no dedicated performance artifact; scan remains linear.
- Ruff/lint: touched runtime/test files.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
