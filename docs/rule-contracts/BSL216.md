# BSL216 — <span class="doc-lang doc-lang-ru">Пропущены пробелы слева или справа от операторов `+ - * / = % < > <> <= >=`, от ключевых слов, а так же справа от `,` и `;`</span><span class="doc-lang doc-lang-en">Missing spaces to the left or right of operators + - * / = % < > <> <= >=, keywords, and also to the right of , and ;</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Пропущены пробелы слева или справа от операторов `+ - * / = % < > <> <= >=`, от ключевых слов, а так же справа от `,` и `;`

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Missing spaces to the left or right of operators + - * / = % < > <> <= >=, keywords, and also to the right of , and ;

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL216` |
| Совместимый псевдоним / Compatible alias | `MissingSpace` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `style`, `convention` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL216` и псевдоним `MissingSpace` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL216"]
ignore = ["MissingSpace"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL216
Значение = "пример";  // bsl-disable: BSL216
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:MissingSpace-off
// код без этой диагностики
// BSLLS:MissingSpace-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL216` and alias `MissingSpace` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL216"]
ignore = ["MissingSpace"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL216
Value = "example";  // bsl-disable: BSL216
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:MissingSpace-off
// code without this diagnostic
// BSLLS:MissingSpace-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Для улучшения читаемости кода слева и справа от операторов `+ - * / = % < > <> <= >=` должны быть пробелы.
Так же пробел должен быть справа от `,` и `;`

## Примеры

Плохо

```bsl
Процедура ВычислитьСумму(Параметр1,Параметр2)
    Если Параметр1=Параметр2 Тогда
        Сумма=Цена*Количество;
    КонецЕсли;
КонецПроцедуры
```

Хорошо

```bsl
Процедура ВычислитьСумму(Параметр1, Параметр2)
    Если Параметр1 = Параметр2 Тогда
        Сумма = Цена * Количество;
    КонецЕсли;
КонецПроцедуры
```

### Использование параметра `checkSpaceToRightOfUnary`

Параметр имеет смысл, только если унарный оператор присутствует в одном из трех основных параметров.

При значении `false`

```bsl
А = -Б;     // Хорошо
А = - Б;    // Хорошо
```

При значении `true`

```bsl
А = -Б;     // Плохо
А = - Б;    // Хорошо
```

### Использование параметра `allowMultipleCommas`

Параметр имеет смысл, только если `,` присутствует в одном из трех основных параметров. Значение по-умолчанию `false`

При значении `false`

```bsl
    ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,,, Отказ);        // Плохо
    ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения, , , , Отказ);     // Хорошо
```

При значении `true`

```bsl
    ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения,,,, Отказ);        // Хорошо
    ОбщегоНазначенияКлиентСервер.СообщитьПользователю(ТекстСообщения, , , , Отказ);     // Хорошо
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

To improve code readability to the left and right of operators `+ - * / = % < > <> <= >=` there must be spaces. Also, the space should be to the right of `,` и `;`

## Examples

Incorrect

```bsl
Procedure Sum(Param1,Param2)
    If Param1=Param2 Then
        Sum=Price*Quantity;
    EndIf;
EndProcedure
```

Correct

```bsl
Procedure Sum(Param1, Param2)
     If Param1 = Param2 Then
         Sum = Price * Quantity;
     EndIf;
EndProcedure
```

### Using `checkSpaceToRightOfUnary` parameter

The parameter makes sense only in case the unary operator is listed in one of three base parameters.

If set to `false`

```bsl
А = -B;     // Correct
А = - B;    // Correct
```

If set to `true`

```bsl
А = -B;     // Incorrect
А = - B;    // Correct
```

### Using `allowMultipleCommas` parameter

The parameter has sense only if `,` is listed in one of three base parameters Defaults to `false`

If set to `false`

```bsl
    CommonModuleClientServer.MessageToUser(MessageText,,,, Cancel);        // Bad
    CommonModuleClientServer.MessageToUser(MessageText, , , , Cancel);     // Correct
```

If set to `true`

```bsl
    CommonModuleClientServer.MessageToUser(MessageText,,,, Cancel);        // Correct
CommonModuleClientServer.MessageToUser(MessageText, , , , Cancel);     // Correct
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL216
- BSLLS compatible name: MissingSpace
- Public name/i18n key: BSL216 / MissingSpace
- Default message/i18n key: get_rule("BSL216").message
- Description/i18n key: RULE_METADATA["BSL216"].description
- Severity: INFORMATION
- Parameters/defaults: spacing token patterns
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Operators, separators, and selected keywords should be spaced according to common BSL formatting conventions.
- Why this diagnostic exists: It flags missing spaces that hurt readability and formatter consistency.
- Non-goals: Tokens inside strings/comments and syntax positions where no space is required, including unary signs after indexing/opening brackets.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A source code token pair lacks required whitespace around comparison, arithmetic, comma, semicolon, or selected keyword tokens.
- Must diagnose when: A code line contains a spacing violation after comments/strings are masked.
- Must not diagnose when: The same token appears in a string/comment or already has required whitespace.
- Diagnostic anchor token/range: the operator, separator, or keyword token missing surrounding whitespace.
- Quick-fix target, if any: insert the missing space manually or through formatter.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.missing_space_facts`.
- CST/domain object types: source code line, masked line, token-specific spacing fact.
- Extracted attributes: line index, token start, token end, token family.
- Parse-error behavior: textual spacing scan can run without valid CST.
- Textual/regex behavior, only if genuinely textual: this is a textual formatting rule, so lexical spacing patterns are intentional.

## Examples

### Invalid Examples

1. A comparison operator has no required left or right space.
2. A semicolon is immediately followed by code or a comment without required space.
3. A comma or keyword lacks required adjacent whitespace.

### Valid Negative Twins

1. The same operator has spaces on both required sides.
2. The token appears inside a string literal.
3. The token appears inside a comment.

### Edge Examples

- Comments: comment text is excluded from code spacing checks.
- Strings: masked before spacing checks.
- Preprocessor: not a separate target unless represented as a checked line.
- Multiline: each physical line is checked independently.
- Nested constructs: not relevant.
- BSL/SDBL variants, if relevant: SDBL query text is not the primary target.

## Related-Rule Cluster

- Shared semantic object: physical source line formatting.
- Rules that may report on the same object/range: BSL014, BSL200, formatter checks, and typo rules can report nearby text.
- Intended duplicate policy: keep spacing diagnostics distinct from line length and line-break diagnostics.
- Cluster fixture or probe: tests/test_diagnostics_core_snapshot.py::TestBsl216MissingSpace

## BSLLS Parity Taxonomy

- Compared input-file set: original-workspace batch aggregate plus local synthetic spacing fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS default behavior.
- File-key normalization: original source-root relative keys for private batch aggregate; synthetic labels for local fixtures.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: private batch exact common remains 4524; sampled `А=1;Б=2` and local spacing fixtures match intended onec behavior.
- Line/common semantic common: ordinary operator/separator spacing semantics match on common facts.
- Ours-only categories: none recorded for this contract.
- BSLLS-only categories: private batch has 28 reports for unary `-` after `[`; onec intentionally does not report these.
- Range-only categories: none recorded for this contract.
- Known BSLLS defects: BSLLS may require a space before unary `-` in indexer/bracket contexts where BSL syntax does not require one.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `missing_space_facts`.
- Expected complexity: O(lines * token-pattern checks).
- Shared-pass reuse: uses masked line/comment state from `DocumentSnapshot`.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: BSL216 contract, audit state, and unary-minus regression coverage.
- Delta category fixed by this change: BSLLS-only unary-minus category classified as not a onec rule bug.
- Why not another layer: spacing is intentionally textual and should remain in the line-style layer.
- Rollback signal: strings/comments report, unary signs after opening brackets report, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_core_snapshot.py -q -k bsl216 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL216 tests.
- Related-rule tests: BSL014 and BSL200 line-style tests.
- Parity command/artifact: original-workspace aggregate plus local synthetic fixtures; no runtime change accepted for unary `-` after `[`.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
