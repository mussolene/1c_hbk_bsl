# BSL265 UselessTernaryOperator Contract

## Rule Identity

- Code: BSL265
- BSLLS compatible name: UselessTernaryOperator
- Public name/i18n key: BSL265 / UselessTernaryOperator
- Default message/i18n key: RULE_MESSAGES_RU["BSL265"]
- Description/i18n key: RULE_METADATA["BSL265"].description
- Severity: INFORMATION
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: ternary expressions should not encode boolean constants in a way that can be replaced by the condition or its negation.
- Why this diagnostic exists: useless ternaries make boolean logic harder to read and often hide a simpler expression.
- Non-goals: simplifying all ternary expressions, proving arbitrary expression equivalence, or rewriting non-boolean branches.
- Keep/drop decision: keep as a BSLLS-compatible badpractice/suspicious diagnostic.

## Semantic Oracle

- Checked language fact: a ternary operator `?(condition, then, else)` has boolean literals in positions that make the ternary redundant or suspicious.
- Must diagnose when: the condition itself is a boolean literal, both branches are boolean literals, or one branch is a boolean literal while the opposite branch is a recognized boolean expression under the BSLLS-compatible policy.
- Must not diagnose when: boolean literals are paired with non-boolean values, the expression is inside comments, the ternary is malformed, or a boolean-looking value is outside the configured simple patterns.
- Diagnostic anchor token/range: the full ternary expression span.
- Quick-fix target, if any: replace with the condition, negated condition, or a direct boolean expression.

## CST Or Domain Fact Model

- Parser/tree source: shared ternary span extractor over masked source content.
- CST/domain object types: ternary expression span, top-level ternary arguments, and boolean-literal/boolean-expression classifiers.
- Extracted attributes: ternary start/end range, condition/then/else text, boolean literal flags, boolean expression flag, and simple member-access flag.
- Parse-error behavior: malformed ternary syntax is skipped; BSL001 owns broken syntax.
- Textual/regex behavior, only if genuinely textual: boolean operand classification is textual after ternary spans are extracted.

## Examples

### Invalid Examples

1. `А = ?(Б = 1, Истина, Ложь);`
2. `А = ?(Б = 0, False, True);`
3. `А = ?(истина, 1, 0);`

### Valid Negative Twins

1. `А = ?(Б = 1, True, 1);`
2. `А = ?(Б = 0, 0, False);`
3. `БулевоЗначение = ?(ЗначениеЗаполнено(Ссылка), Ссылка.БулевоПоле, Ложь);`

### Edge Examples

- Comments: skipped by the comment-line guard and ternary extractor.
- Strings: not treated as executable ternary expressions.
- Preprocessor: not a separate semantic target.
- Multiline: supported by ternary span extraction and top-level argument splitting.
- Nested constructs: top-level argument splitting prevents nested comma confusion.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: ternary expressions and boolean conditions.
- Rules that may report on the same object/range: BSL039 reports nested ternary expressions, BSL251 reports ternary usage as a style rule, and BSL036 can report complex conditions nearby.
- Intended duplicate policy: BSL265 owns useless boolean ternary semantics; style/complexity diagnostics may coexist only when they explain separate issues.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py -k bsl265`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 78.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: shared ternary span scan plus top-level argument split.
- Expected complexity: O(source length + ternary argument length).
- Shared-pass reuse: process-fork grouped with BSL227 on heavy files.
- Before/after measurement, if changed: no runtime change; private selected parity is exact at onec 78 / bslls 78.

## Implementation Decision

- Change target: contract and audit state only.
- Delta category fixed by this change: none; existing implementation already has exact selected parity.
- Why not another layer: the current implementation already uses the shared ternary scanner and matches BSLLS ranges.
- Rollback signal: BSLLS-shaped boolean literal examples stop reporting, non-boolean branches report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py -q -k bsl265 --no-cov`.
- Synthetic fixture/module: existing tests cover boolean branches, boolean calls/member access, and non-boolean negative twins.
- Related-rule tests: BSL039/BSL251 only if shared ternary extraction changes.
- Parity command/artifact: preserved-source-root selected BSL265 private parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because runtime code did not change.
- Ruff/lint: docs-only change unless runtime/test files are edited.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
