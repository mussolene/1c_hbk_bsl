# BSL271 UsingObjectNotAvailableUnix Contract

## Rule Identity

- Code: BSL271
- BSLLS compatible name: UsingObjectNotAvailableUnix
- Public name/i18n key: BSL271 / UsingObjectNotAvailableUnix
- Default message/i18n key: RULE_MESSAGES_RU["BSL271"]
- Description/i18n key: RULE_METADATA["BSL271"].description
- Severity: ERROR
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: cross-platform code should not use objects that are unavailable on Unix/Linux clients without an explicit platform guard.
- Why this diagnostic exists: `COM`, `OLE`, `ActiveDocument`, and related legacy integrations do not work on Linux and can break cross-platform deployments.
- Non-goals: proving actual platform branch truth, replacing all Windows-only integrations, or checking arbitrary object names outside the documented unavailable set.
- Keep/drop decision: keep as a BSLLS-compatible standard/compatibility diagnostic.

## Semantic Oracle

- Checked language fact: a `new_expression` creates `COMОбъект` / `COMObject` or `Почта` / `Mail` outside a recognized platform guard.
- Must diagnose when: executable BSL contains `Новый` / `New` for one of the unavailable object types and no ancestor `if`/`elseif` guard mentions the known platform tokens.
- Must not diagnose when: the new expression is guarded by platform checks, appears in comments/strings, or creates a different type.
- Diagnostic anchor token/range: the full `new_expression`, matching BSLLS selection range.
- Quick-fix target, if any: guard the code by platform or replace the unavailable object with a cross-platform integration.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST through the light-pool module model.
- CST/domain object types: `new_expression`, contained type `identifier`, and ancestor `if_statement` / `elseif_clause` nodes.
- Extracted attributes: type name, full new-expression range, ancestor platform guard text, and line/character coordinates.
- Parse-error behavior: malformed syntax is not the target; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: regex is used only to match the documented unavailable type names and platform-token guard text after CST node selection.

## Examples

### Invalid Examples

1. `Компонента = Новый COMОбъект("Excel.Application");`
2. `Почта = Новый Почта;`
3. `Component = New COMObject("ADODB.Connection");`

### Valid Negative Twins

1. `Если ТипПлатформы() = ТипПлатформы.Windows Тогда Компонента = Новый COMОбъект("Excel.Application"); КонецЕсли;`
2. `Объект = Новый Структура;`
3. `Текст = "Новый COMОбъект(""Excel.Application"")";`

### Edge Examples

- Comments: ignored by CST.
- Strings: ignored by CST.
- Preprocessor: not a separate semantic target.
- Multiline: new-expression range should follow the CST node range.
- Nested constructs: ancestor platform guards can suppress nested new expressions.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: platform- and environment-sensitive object creation.
- Rules that may report on the same object/range: BSL185 external application starting, BSL188 filesystem access, BSL189 internet access, and BSL264 system information may inspect nearby compatibility-sensitive code.
- Intended duplicate policy: BSL271 owns unavailable object construction; related rules may coexist only for distinct runtime integration risks.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py -k bsl271`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 24 after the range/filter fix.
- Line/common semantic common: exact parity.
- Ours-only categories: 0 after spanning the full `new_expression`.
- BSLLS-only categories: 0 after allowing BSL271 full-expression spans to overlap constructor string arguments.
- Range-only categories: 0 after the full-expression range fix.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: CST walk over `new_expression` nodes plus ancestor guard scan.
- Expected complexity: O(number of CST nodes + guarded ancestor depth).
- Shared-pass reuse: light-pool diagnostics pass for module-model rules.
- Before/after measurement, if changed: private selected parity improved from onec 24 / bslls 24 with 24 paired range-only deltas to exact 24 / 24.

## Implementation Decision

- Change target: BSL271 range construction in the light-pool module model, string-overlap allowlist, targeted synthetic range tests, contract, and audit state.
- Delta category fixed by this change: range-only full-new-expression selection plus post-filter allowance for constructor string arguments.
- Why not another layer: semantic facts already matched BSLLS; only the emitted range and generic string-overlap filter blocked exact parity.
- Rollback signal: BSL271 stops reporting invalid `New COMObject`, guarded cases report, constructor arguments make full ranges disappear, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py -q -k bsl271 --no-cov`.
- Synthetic fixture/module: invalid full-range object creation and guarded platform negative.
- Related-rule tests: platform/security cluster tests only if semantics change.
- Parity command/artifact: preserved-source-root selected BSL271 private parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark; range-only fix should not change traversal cost.
- Ruff/lint: touched runtime/test files.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
