# BSL258 — <span class="doc-lang doc-lang-ru">Использование ключевого слова "ОБЪЕДИНИТЬ" в запросах</span><span class="doc-lang doc-lang-en">Using keyword "UNION" in queries</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование ключевого слова "ОБЪЕДИНИТЬ" в запросах

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Using keyword "UNION" in queries

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL258` |
| Совместимый псевдоним / Compatible alias | `UnionAll` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `query`, `performance` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL258` и псевдоним `UnionAll` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL258"]
ignore = ["UnionAll"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL258
Значение = "пример";  // bsl-disable: BSL258
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UnionAll-off
// код без этой диагностики
// BSLLS:UnionAll-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL258` and alias `UnionAll` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL258"]
ignore = ["UnionAll"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL258
Value = "example";  // bsl-disable: BSL258
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UnionAll-off
// code without this diagnostic
// BSLLS:UnionAll-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики
В общем случае, при объединении в запросе результатов нескольких запросов следует использовать конструкцию ОБЪЕДИНИТЬ ВСЕ,
а не ОБЪЕДИНИТЬ. Поскольку во втором варианте, при объединении запросов полностью одинаковые строки заменяются одной,
на что затрачивается дополнительное время, даже в случаях, когда одинаковых строк в запросах заведомо быть не может.

Исключением являются ситуации, когда выполнение замены нескольких одинаковых строк одной является необходимым условием
выполнения запроса.

## Примеры

Неправильно:
```bsl
ВЫБРАТЬ
ПоступлениеТоваровУслуг.Ссылка
ИЗ
Документ.ПоступлениеТоваровУслуг КАК ПоступлениеТоваровУслуг

ОБЪЕДИНИТЬ

ВЫБРАТЬ
РеализацияТоваровУслуг.Ссылка
ИЗ
Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
```

Правильно:

```bsl
ВЫБРАТЬ
ПоступлениеТоваровУслуг.Ссылка
ИЗ
Документ.ПоступлениеТоваровУслуг КАК ПоступлениеТоваровУслуг

ОБЪЕДИНИТЬ ВСЕ

ВЫБРАТЬ
РеализацияТоваровУслуг.Ссылка
ИЗ
Документ.РеализацияТоваровУслуг КАК РеализацияТоваровУслуг
```

## Источники
* Источник: [Стандарт: Использование ключевых слов "ОБЪЕДИНИТЬ" и "ОБЪЕДИНИТЬ ВСЕ" в запросах](https://its.1c.ru/db/v8std#content:434:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description
In most cases, when you need to combine the results of two or more queries into a single result set, employ UNION ALL clause instead of UNION. The recommendation is based on the algorithm of the UNION clause, which searches for and removes duplicates from the united result even when duplicates are impossible by the query design.

Employ UNION only when removing duplicates from the result is required.

## Examples

Incorrect:
```bsl
SELECT
GoodsReceipt.Ref
FROM
Document.GoodsReceipt AS GoodsReceipt

UNION

SELECT
GoodsSale.Ref
FROM
Document.GoodsSale AS GoodsSale
```

Correct:

```bsl
SELECT
GoodsReceipt.Ref
FROM
Document.GoodsReceipt AS GoodsReceipt

UNION ALL

SELECT
GoodsSale.Ref
FROM
Document.GoodsSale AS GoodsSale
```

## Sources
* Link: [Development Standart: Using UNION and UNION ALL words in queries (RU)](https://its.1c.ru/db/v8std#content:434:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL258
- BSLLS compatible name: UnionAll
- Public name/i18n key: UnionAll / BSL258
- Default message/i18n key: RULE_MESSAGES_RU["BSL258"]
- Description/i18n key: RULE_METADATA["BSL258"].description
- Severity: INFORMATION
- Parameters/defaults: none.
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: query text should use `ОБЪЕДИНИТЬ ВСЕ` / `UNION ALL` instead of plain `ОБЪЕДИНИТЬ` / `UNION` unless duplicate elimination is intentionally required.
- Why this diagnostic exists: plain union can add unnecessary duplicate elimination and degrade query performance.
- Non-goals: string constants that are query separators, dynamic incomplete query fragments, comments, and already explicit `UNION ALL`.
- Keep/drop decision: keep as a BSLLS-compatible query performance diagnostic.

## Semantic Oracle

- Checked language fact: a complete extracted SDBL query block contains `ОБЪЕДИНИТЬ` / `UNION` not followed by `ВСЕ` / `ALL`.
- Must diagnose when: a parseable query block has a plain union keyword in query text.
- Must not diagnose when: the keyword appears in ordinary BSL strings, generated separator fragments outside extracted query blocks, comments, non-query text, or already says `ОБЪЕДИНИТЬ ВСЕ` / `UNION ALL`.
- Diagnostic anchor token/range: the union keyword token range in the original BSL query string line.
- Quick-fix target, if any: replace with `ОБЪЕДИНИТЬ ВСЕ` / `UNION ALL` after confirming duplicate elimination is not required.

## CST Or Domain Fact Model

- Parser/tree source: extracted query text blocks from `DocumentSnapshot` and SDBL parse status.
- CST/domain object types: `QueryTextBlockInfo`, `QueryTextLineInfo`, SDBL parse status, and original query line mapping.
- Extracted attributes: content-line head text, UTF-8 byte offsets for the union keyword, and original LSP range.
- Parse-error behavior: SDBL parse errors do not suppress BSL258 once the source has been extracted as a query block; BSLLS also reports plain union keywords in partially dynamic query blocks.
- Textual/regex behavior, only if genuinely textual: keyword match is textual inside already-extracted parseable query blocks.

## Examples

### Invalid Examples

1. A complete query with `ОБЪЕДИНИТЬ` between two `ВЫБРАТЬ` sections.
2. A complete query with English `UNION` between two `SELECT` sections.
3. A multiline query string where the plain union keyword appears on its own line.

### Valid Negative Twins

1. `ОБЪЕДИНИТЬ ВСЕ` / `UNION ALL`.
2. `Разделитель = " ОБЪЕДИНИТЬ ";`.
3. A dynamic fragment string that contains only a union separator without a complete query.

### Edge Examples

- Comments: comments outside query blocks are ignored.
- Strings: only extracted query string blocks are checked.
- Preprocessor: no separate semantic target.
- Multiline: original ranges are mapped from query block rows to BSL lines.
- Nested constructs: nested query blocks are handled if the extractor returns a complete parseable block.
- BSL/SDBL variants, if relevant: Russian and English union keywords are supported.

## Related-Rule Cluster

- Shared semantic object: query text blocks.
- Rules that may report on the same object/range: BSL149, BSL201, BSL234, BSL235, BSL236, BSL238, BSL269.
- Intended duplicate policy: BSL258 owns plain union usage; other query rules report different field, join, alias, or wildcard defects.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl258UnionAll`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 5,803 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final selected private parity is exact at `onec=26`, `bslls=26`.
- Line/common semantic common: baseline had `bslls=26` selected batch facts and BSL258 was the only rule with deltas.
- Ours-only categories: fixed `11` ordinary string/query-separator false positives by checking only extracted query blocks.
- BSLLS-only categories: none in the baseline private batch.
- Range-only categories: none in the baseline private batch.
- Known BSLLS defects: none established for this iteration.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing query text blocks; no scan over arbitrary source lines.
- Expected complexity: O(extracted query text length) only when BSL258 is enabled.
- Shared-pass reuse: reuses `DocumentSnapshot.query_text_blocks` with other query text/runtime rules.
- Before/after measurement, if changed: private selected parity improved from `onec=37`, `bslls=26`, `only_onec=11` to exact `onec=26`, `bslls=26`.

## Implementation Decision

- Change target: `UnionAllRule` in `diagnostic_runtime/rules.py`, synthetic separator negative, contract, and audit state.
- Delta category fixed by this change: semantic mismatch from diagnosing ordinary string query separators outside extracted query blocks as query unions.
- Why not another layer: query block extraction already owns the query/non-query boundary and range mapping; adding a separate tokenizer would duplicate it.
- Rollback signal: plain union in a complete query no longer reports, `UNION ALL` reports, or string separator fragments report.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py::TestBsl258UnionAll -q --no-cov`.
- Synthetic fixture/module: complete plain union positive, union-all negative, separator string negative.
- Related-rule tests: query text/runtime rules if query block extraction changes.
- Parity command/artifact: preserved-source-root private batch `--select BSL252,BSL255,BSL257,BSL258,BSL259`.
- Performance command/artifact: selected parity wall time and no arbitrary line scan.
- Ruff/lint: touched files.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming completion.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
