# BSL212 MissedRequiredParameter Contract

## Rule Identity

- Code: BSL212
- BSLLS compatible name: MissedRequiredParameter
- Public name/i18n key: MissedRequiredParameter / BSL212
- Default message/i18n key: RULE_MESSAGES_RU["BSL212"]
- Description/i18n key: RULE_METADATA["BSL212"].description
- Severity: ERROR
- Parameters/defaults: no user parameters
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: calls to known methods must supply every required positional parameter.
- Why this diagnostic exists: a call with a missing required parameter is a runtime correctness error.
- Non-goals: do not validate unknown dynamic calls, qualified object calls resolved only at runtime, common-module API arity, constructor overloads, or type-specific platform methods in the local method-contract pass.
- Keep/drop decision: keep as a BSLLS-compatible correctness diagnostic for resolved local method calls.

## Semantic Oracle

- Checked language fact: a direct local method call omits at least one non-optional parameter from the resolved local procedure/function signature.
- Must diagnose when: the callee is a local procedure/function, a required parameter position is absent, or an omitted required position appears before later supplied arguments.
- Must not diagnose when: all required positions are supplied, the omitted positions are optional parameters, the call is qualified with an object/module prefix, or the callee is not a known local method.
- Diagnostic anchor token/range: full local method call range from callee identifier through the matching closing parenthesis, excluding the statement semicolon.
- Quick-fix target, if any: add the missing required argument or make the parameter optional intentionally.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures`, `DocumentSnapshot.calls`, and source text argument extraction for the matched local call.
- CST/domain object types: `ProcInfo` local procedure/function facts, semantic `Call` records from `method_call` CST nodes, and parsed call argument positions.
- Extracted attributes: callee name, caller line/character, local signature parameter names, optional parameter set, per-position argument presence, and full call range.
- Parse-error behavior: only parsed local procedures and calls are checked; BSL001 owns syntax errors and malformed calls without a matching closing parenthesis are skipped.
- Textual/regex behavior, only if genuinely textual: source text is used only to recover empty argument positions and the matching closing parenthesis for an already parsed CST call.

## Examples

### Invalid Examples

1. `Обработать();` when `Обработать(Параметр)` requires one parameter.
2. `Обработать(Первый, , Третий);` when the second parameter is required.
3. `Обработать(, "Имя", Значение);` when the first parameter is required and later arguments are supplied.

### Valid Negative Twins

1. `Обработать(Значение);` when only one required parameter exists.
2. `Обработать(Первый);` when later parameters have default values.
3. `Модуль.Обработать();` even if a same-named local method exists; qualified calls are not resolved as local calls.

### Edge Examples

- Comments: comments do not create CST call facts.
- Strings: string literals inside supplied arguments do not suppress the call diagnostic.
- Preprocessor: visible parsed calls are checked; inactive branch semantics are not owned by this rule.
- Multiline: multiline calls are checked only when argument presence and matching call range are recoverable from source text.
- Nested constructs: calls inside conditions, loops, and nested expressions are checked the same way.
- BSL/SDBL variants, if relevant: query text is not a BSL method-call target for this rule.

## Related-Rule Cluster

- Shared semantic object: local method signatures and call sites.
- Rules that may report on the same object/range: BSL007/BSL062 inspect symbol/parameter usage, BSL015/BSL031 inspect signature size/ranges, BSL193/BSL228/BSL240 inspect parameter contract details, BSL213 inspects common-module method existence.
- Intended duplicate policy: keep BSL212 distinct; it reports call-site arity, not declaration style, unused parameters, or common-module method existence.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestBsl212MissedRequiredParameter`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final aggregate has exact parity, `onec=14`, `bslls=14`.
- Line/common semantic common: local calls with missing required parameters, including omitted middle positions, match BSLLS.
- Ours-only categories: none.
- BSLLS-only categories: none after fixing omitted-position detection and string-overlap filtering.
- Range-only categories: none after aligning the diagnostic range to the full call excluding the semicolon.
- Known BSLLS defects: none recorded for the final BSL212 batch.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing document snapshot procedures and call facts.
- Expected complexity: O(local calls + local signatures) per file, with bounded source text matching only for candidate local calls.
- Shared-pass reuse: reuses `DocumentSnapshot.calls` and `DocumentSnapshot.procedures`; no extra CST traversal is introduced.
- Before/after measurement, if changed: targeted pytest 0.58s; private preserved-source-root BSL212 parity over 32,007 files completed with exact `onec=14`, `bslls=14`.

## Implementation Decision

- Change target: BSL212 runner and string-overlap diagnostic filter.
- Delta category fixed by this change: semantic mismatch for omitted required positions with later supplied arguments, range-only end-character mismatch, and false filtering when the full call range contains string arguments.
- Why not another layer: the existing local method-contract runner already owns local arity; the global string-literal filter only needed to allow this structural call-span rule.
- Rollback signal: omitted middle required parameters stop reporting, full-call ranges include semicolons again, calls with string arguments disappear, or private BSL212 parity regains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl212 --no-cov`.
- Synthetic fixture/module: inline BSL212 tests cover missing first parameter, omitted middle parameter, qualified-call negative twin, range without semicolon, and call span containing string arguments.
- Related-rule tests: run method-contract targeted tests if shared procedure/call facts change.
- Parity command/artifact: preserved-source-root private batch BSL212 parity, final exact match.
- Performance command/artifact: parity runtime and targeted pytest runtime; no new traversal added.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/method_contract_rules.py src/onec_hbk_bsl/analysis/diagnostics.py tests/test_diagnostics_method_contracts.py`.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
