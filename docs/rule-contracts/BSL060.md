# BSL060 — <span class="doc-lang doc-lang-ru">Двойные отрицания</span><span class="doc-lang doc-lang-en">Double negatives</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Двойные отрицания

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Double negatives

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL060` |
| Совместимый псевдоним / Compatible alias | `DoubleNegatives` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `brainoverload`, `badpractice` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL060` и псевдоним `DoubleNegatives` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL060"]
ignore = ["DoubleNegatives"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL060
Значение = "пример";  // bsl-disable: BSL060
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:DoubleNegatives-off
// код без этой диагностики
// BSLLS:DoubleNegatives-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL060` and alias `DoubleNegatives` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL060"]
ignore = ["DoubleNegatives"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL060
Value = "example";  // bsl-disable: BSL060
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:DoubleNegatives-off
// code without this diagnostic
// BSLLS:DoubleNegatives-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Использование двойных отрицаний усложняет понимание кода и может приводить к ошибкам, когда вместо истины разработчик "в уме" вычислил Ложь, или наоборот.
Двойные отрицания рекомендуется заменять на выражения условий, которые прямо выражают намерения автора.

## Примеры

### Неправильно

```bsl
Если Не ТаблицаЗначений.Найти(ИскомоеЗначение, "Колонка") <> Неопределено Тогда
    // Сделать действие
КонецЕсли;
```

### Правильно

```bsl
Если ТаблицаЗначений.Найти(ИскомоеЗначение, "Колонка") = Неопределено Тогда
    // Сделать действие
КонецЕсли;
```

## Источники

* Источник: [Remove double negative](https://www.refactoring.com/catalog/removeDoubleNegative.html)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Using double negatives makes the code harder to understand and can lead to errors when the developer mentally computes False instead of True, or vice versa.
It is recommended to replace double negatives with conditional expressions that directly express the author's intentions.

## Examples

### Incorrect

```bsl
If Not ValueTable.Find(SearchValue, "Column") <> Undefined Then
    // Do the action
EndIf;
```

### Correct

```bsl
If ValueTable.Find(LookupValue, "Column") = Undefined Then
    // Perform action
EndIf;
```

## Sources

* Source: [Remove double negative](https://www.refactoring.com/catalog/removeDoubleNegative.html)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL060
- BSLLS compatible name: DoubleNegatives
- Public name/i18n key: BSL060 / DoubleNegatives
- Default message/i18n key: RULE_MESSAGES_RU["BSL060"]
- Description/i18n key: RULE_METADATA["BSL060"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: double negative boolean expressions should be rewritten to state the condition directly.
- Why this diagnostic exists: double negatives increase cognitive load and can lead to inverted-condition mistakes.
- Non-goals: single negation, negation text inside comments/strings, unrelated inequality without unary `Не`/`Not`, and syntax-error ownership.
- Keep/drop decision: keep as a BSLLS-compatible brain-overload/badpractice diagnostic.

## Semantic Oracle

- Checked language fact: a boolean expression contains a double negative pattern.
- Must diagnose when: a clean CST contains `Не Не <expr>` / `Not Not <expr>` or `Не <expr> <> <expr>` patterns compatible with BSLLS DoubleNegatives.
- Must not diagnose when: there is only one unary negation, the text is in comments/strings, the expression is a logical expression excluded by BSLLS-compatible behavior, or parse errors own the code.
- Diagnostic anchor token/range: from the outer `Не`/`Not` operator through the operand/comparison side that completes the double-negative expression.
- Quick-fix target, if any: remove the double negation or rewrite `Не X <> Y` to the direct equivalent.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST.
- CST/domain object types: `unary_expression`, `binary_expression`, `operator`, and nested `expression` nodes.
- Extracted attributes: unary operator text, binary operator text, expression child, comparison sides, parent binary/logical operator, diagnostic start/end nodes, and duplicate range key.
- Parse-error behavior: invalid CST should not produce BSL060 facts; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: none for valid CST paths.

## Examples

### Invalid Examples

1. `Если НЕ НЕ Флаг Тогда`
2. `Если Not Not Flag Then`
3. `Если Не Таблица.Найти(Значение) <> Неопределено Тогда`

### Valid Negative Twins

1. `Если НЕ Флаг Тогда`
2. `Если Флаг Тогда`
3. `// НЕ НЕ Флаг`

### Edge Examples

- Comments: comment text is ignored.
- Strings: string text is ignored by CST expression facts.
- Preprocessor: not a separate semantic target for this rule.
- Multiline: expression range can span lines through the CST nodes.
- Nested constructs: duplicate reporting is de-duplicated by range key.
- BSL/SDBL variants, if relevant: SDBL query text is not relevant.

## Related-Rule Cluster

- Shared semantic object: boolean expressions and conditions.
- Rules that may report on the same object/range: BSL011 cognitive complexity, BSL019 cyclomatic complexity, BSL036 condition complexity, BSL039 nested ternary, and BSL216 spacing nearby.
- Intended duplicate policy: keep BSL060 separate; it reports a specific negative-logic smell, not the overall condition size or boolean operator count.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl060DoubleNegation` and `tests/test_fix_engine.py` for the quick fix.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 4.
- Line/common semantic common: 4 exact reports.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one CST node grouping pass for `binary_expression` and `unary_expression`.
- Expected complexity: O(CST nodes).
- Shared-pass reuse: uses `ts_nodes_for_types` and document tree; no reparsing.
- Before/after measurement, if changed: no implementation change; targeted BSL060/fix tests pass and preserved-source-root private parity is exact at 4/4.

## Implementation Decision

- Change target: BSL060 contract and audit state; no implementation change.
- Delta category fixed by this change: none; existing CST implementation has exact private parity.
- Why not another layer: CST directly exposes unary and binary operators with precise ranges.
- Rollback signal: comments/strings report, single negation reports, `НЕ НЕ` stops reporting, or duplicate diagnostics appear for the same expression.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_local_runtime.py::TestBsl060DoubleNegation tests/test_fix_engine.py -q --no-cov`.
- Synthetic fixture/module: existing tests cover double negation, single negation, and comments.
- Related-rule tests: run BSL036 if condition expression handling changes.
- Parity command/artifact: preserved-source-root selected BSL060 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time if implementation changes.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_diagnostics_local_runtime.py tests/test_fix_engine.py src/onec_hbk_bsl/analysis/diagnostic/diagnostic_runtime/rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
