# BSL051 — <span class="doc-lang doc-lang-ru">Недостижимый код</span><span class="doc-lang doc-lang-en">Unreachable Code</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Недостижимый код

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Unreachable Code

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL051` |
| Совместимый псевдоним / Compatible alias | `UnreachableCode` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `suspicious`, `dead-code` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL051` и псевдоним `UnreachableCode` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL051"]
ignore = ["UnreachableCode"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL051
Значение = "пример";  // bsl-disable: BSL051
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:UnreachableCode-off
// код без этой диагностики
// BSLLS:UnreachableCode-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL051` and alias `UnreachableCode` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL051"]
ignore = ["UnreachableCode"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL051
Value = "example";  // bsl-disable: BSL051
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:UnreachableCode-off
// code without this diagnostic
// BSLLS:UnreachableCode-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Код, расположенный после операторов "Возврат", "Перейти", "ВызватьИсключение", "Прервать", "Продолжить", никогда не будет исполнен.

Ошибки недостижимого кода могут возникать в результате невнимательности разработчика, при редактировании "чужого кода".

## Примеры

```bsl
Процедура Пример()
    Возврат;
    // Код ниже оператора Возврат никогда не будет исполнен
    Для каждого Строка Из Строки Цикл
        Если Условие2 Тогда
            Метод();
        КонецЕсли;
    КонецЦикла;
КонецПроцедуры
```

```bsl
Функция Пример(Параметр1, Параметр2)
    Если Ошибка Тогда
        ВызватьИсключение "Возникла ошибка";
        // После вызова исключения код будет проигнорирован
        Параметр1 = Параметр2;
    КонецЕсли;
    Возврат Параметр1;
КонецПроцедуры
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Code located after operators "Return", "GoTo", "Raise", "Break", "Continue" never will be executed.

Errors of unreachable code can be caused by developer carelessness when editing another's code.

## Examples

```bsl
Procedure Example()
    Return;
    // Code below operator Return will never be executed
    For each Line from Lines Do
        If Condition2 Then
            Method();
        EndIf;
    EndDo;
EndProcedure
```

```bsl
Function Example(Parameter1, Parameter2)
    If Error Then
        Raise "Error occurred";
        // After rise exception the code bellow will be ignored
        Parameter1 = Parameter2;
    EndIf;
    Return Parameter1;
EndFunction
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL051
- BSLLS compatible name: UnreachableCode
- Public name/i18n key: BSL051 / UnreachableCode
- Default message/i18n key: RULE_MESSAGES_RU["BSL051"]
- Description/i18n key: RULE_METADATA["BSL051"].description
- Severity: ERROR
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: code after unconditional control-flow exit in the same execution path is dead code and should be removed or moved before the exit.
- Why this diagnostic exists: unreachable code usually indicates accidental leftovers, disabled logic, or incorrect control flow after editing.
- Non-goals: full symbolic execution, data-dependent reachability, syntax diagnostics, and dead branches that require expression evaluation.
- Keep/drop decision: keep as a BSLLS-compatible suspicious dead-code diagnostic.

## Semantic Oracle

- Checked language fact: an executable statement is placed after an unconditional exit statement in the same method/control-flow segment.
- Must diagnose when: code follows `Возврат`/`Return`, `Перейти`/`Goto`, `ВызватьИсключение`/`Raise`, `Прервать`/`Break`, or `Продолжить`/`Continue` before the enclosing block can execute further.
- Must not diagnose when: the next non-comment token is a method end, an ordinary block delimiter, or code remains reachable through another branch.
- Diagnostic anchor token/range: the first unreachable executable statement, extending to the skipped block end when the unreachable statement starts a block.
- Quick-fix target, if any: remove unreachable code, move it before the exit, or restructure the control flow.

## CST Or Domain Fact Model

- Parser/tree source: procedure facts plus CST block delimiter lines for clean trees; text delimiter fallback only when the full file CST is dirty.
- CST/domain object types: `ProcInfo`, method body lines, unconditional-exit statements, delimiter keyword lines, all-branch-exit `if` summaries, and diagnostic ranges.
- Extracted attributes: method start/end lines, statement indentation, exit keyword, block delimiter line set, preprocessor crossing state, branch-exit state, and severity.
- Parse-error behavior: BSL001 owns syntax errors; BSL051 still reports local unreachable code inside extracted methods when the full-file tree is dirty, using conservative textual delimiter lines.
- Textual/regex behavior, only if genuinely textual: exit-keyword and dirty-tree delimiter fallback matching are textual over already extracted method bodies.

## Examples

### Invalid Examples

1. `Возврат; Сообщить("никогда");`
2. `Прервать; Сообщить("после прерывания");`
3. `Если А Тогда Возврат 1; Иначе Возврат 2; КонецЕсли; Результат = 3;`

### Valid Negative Twins

1. `Сообщить("до выхода"); Возврат;`
2. `Если А Тогда Возврат; КонецЕсли; Сообщить("reachable");`
3. `Попытка А = 1; Исключение Возврат; КонецПопытки`

### Edge Examples

- Comments: skipped when searching for the first unreachable executable line.
- Strings: ignored because reachability is statement-level.
- Preprocessor: crossing preprocessor directives suppresses the simple same-segment emission.
- Multiline: the first unreachable statement line anchors the diagnostic; block starts extend to the block end.
- Nested constructs: all branches of an `if` ending in unconditional exits make following same-level code unreachable.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: method body control flow and return/exit statements.
- Rules that may report on the same object/range: BSL008 too many returns, BSL032 function should have return, BSL064 procedure should not return value, BSL148 function paths should return, and BSL194 same primitive return value.
- Intended duplicate policy: BSL051 reports dead code after exits; return-shape rules may still report separate method contract issues.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl051UnreachableCode`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 2.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over extracted method bodies plus delimiter lines.
- Expected complexity: O(total method body lines).
- Shared-pass reuse: reuses extracted procedure facts, CST delimiter helper, and compiled return/control-flow regexes.
- Before/after measurement, if changed: current corpus changed from onec 1 / bslls 2 with one BSLLS-only dirty-tree skip to exact onec 2 / bslls 2; fallback remains line-local and only activates when the tree delimiter helper returns `None`. Largest5 diagnostics total stayed 49265 after hoisting BSL051 regex compilation; BSL051 task trace in the current pipeline was 3.010s.

## Implementation Decision

- Change target: `ModuleModel.validate_bsl051_unreachable_code` dirty-CST fallback, targeted regression test, and compiled regex reuse in the BSL051 hot path.
- Delta category fixed by this change: one BSLLS-only false negative caused by skipping BSL051 for the whole file when the CST had any parse error.
- Why not another layer: existing method-body control-flow scanner already matched BSLLS when enabled; the defect was the full-file CST-error gate.
- Rollback signal: dirty files lose local BSL051 diagnostics again, block delimiters report as unreachable code, or selected parity gains BSL051 deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl051 or UnreachableCode"`.
- Synthetic fixture/module: dirty-tree regression with a valid early return before `Попытка` and an unrelated syntax error later in the file.
- Related-rule tests: return/control-flow cluster only if shared procedure extraction changes.
- Parity command/artifact: preserved-source-root selected BSL051 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because fallback is line-local and parity corpus remains small for this rule.
- Ruff/lint: not required beyond targeted tests unless wider code style checks fail.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
