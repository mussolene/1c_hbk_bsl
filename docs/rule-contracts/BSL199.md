# BSL199 — <span class="doc-lang doc-lang-ru">Использование синтаксической конструкции Если...Тогда...ИначеЕсли...</span><span class="doc-lang doc-lang-en">Else...The...ElseIf... statement should end with Else branch</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Использование синтаксической конструкции Если...Тогда...ИначеЕсли...

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Else...The...ElseIf... statement should end with Else branch

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL199` |
| Совместимый псевдоним / Compatible alias | `IfElseIfEndsWithElse` |
| Серьёзность / Severity | `INFORMATION` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `design`, `robustness` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL199` и псевдоним `IfElseIfEndsWithElse` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL199"]
ignore = ["IfElseIfEndsWithElse"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL199
Значение = "пример";  // bsl-disable: BSL199
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:IfElseIfEndsWithElse-off
// код без этой диагностики
// BSLLS:IfElseIfEndsWithElse-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL199` and alias `IfElseIfEndsWithElse` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL199"]
ignore = ["IfElseIfEndsWithElse"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL199
Value = "example";  // bsl-disable: BSL199
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:IfElseIfEndsWithElse-off
// code without this diagnostic
// BSLLS:IfElseIfEndsWithElse-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Правило применяется всякий раз, когда условный оператор **Если Тогда ИначеЕсли** содержит одну или несколько конструкций **ИначеЕсли**.
За последней конструкцией **ИначеЕсли** должна следовать конструкция **Иначе**.

Требование к заключительной конструкции **Иначе** - это защитное программирование.
Такие конструкции устойчивы к возможным изменениям и не маскируют возможные ошибки.

Конструкция **Иначе** должна либо принимать соответствующие меры, либо содержать подходящий комментарий относительно того, почему не предпринимается никаких действий.

## Примеры

Неправильно:

```bsl
Если ТипЗнч(ВходящийПараметр) = Тип("Структура") Тогда
	Результат = ЗаполнитьПоСтруктуре(ВходящийПараметр);
ИначеЕсли ТипЗнч(ВходящийПараметр) = Тип("Документ.Ссылка.ВажныйДокумент") Тогда
	Результат = ЗаполнитьПоДокументу(ВходящийПараметр);
КонецЕсли;
```

Правильно:

```bsl
Если ТипЗнч(ВходящийПараметр) = Тип("Структура") Тогда
	Результат = ЗаполнитьПоСтруктуре(ВходящийПараметр);
ИначеЕсли ТипЗнч(ВходящийПараметр) = Тип("Документ.Ссылка.ВажныйДокумент") Тогда
	Результат = ЗаполнитьПоДокументу(ВходящийПараметр);
Иначе
	ВызватьИсключение "Передан параметр неверного типа";
КонецЕсли;
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

The rule is applied whenever the conditional operator **If Then ElseIf** contains one or more blocks **ElseIf **. After block **ElseIf** must be followed by block **Else**.

The requirement to the final block**Else** - it protective programming. Such constructions are resistant to possible changes and do not mask possible errors.

The construct **Else** must either take appropriate action or contain a suitable comment as to why no action is being taken.

## Examples

Incorrect:

```bsl
If TypeOf(InputParameter) = Type("Structure") Then
    Result = FillByStructure(InputParameter);
ElsIf TypeOf(InputParameter) = Type("Document.Ref.MajorDocument") Then
    Result = FillByDocument(InputParameter);
EndIf;
```

Correct:

```bsl
If TypeOf(InputParameter) = Type("Structure") Then
    Result = FillByStructure(InputParameter);
ElsIf TypeOf(InputParameter) = Type("Document.Ref.MajorDocument") Then
    Result = FillByDocument(InputParameter);
Else
    Raise "Parameter of invalid type passed";
EndIf;
```

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL199
- BSLLS compatible name: IfElseIfEndsWithElse
- Rule status: implemented

## Standard And Product Decision

- Standard or product policy: An `Если...Тогда...ИначеЕсли...` chain should end with `Иначе` as defensive programming.
- Non-goals: Do not report plain `Если...Тогда...КонецЕсли` without `ИначеЕсли`.

## Semantic Oracle

- Checked language fact: An if statement contains at least one `elseif_clause` and no `else_clause`.
- Must diagnose when: an if chain has one or more `ИначеЕсли` branches and no final `Иначе`.
- Must diagnose when: the same shape appears in English syntax.
- Must not diagnose when: the if chain contains a final `Иначе`/`Else`.
- Must not diagnose when: there is no `ИначеЕсли`/`ElsIf`.
- Diagnostic anchor token/range: The final `КонецЕсли`/`EndIf` token, matching BSLLS stop-token behavior.

## CST Or Domain Fact Model

- CST/domain object types: `if_statement`, child `elseif_clause`, child `else_clause`, final `ENDIF_KEYWORD`.
- Extracted attributes: presence of elseif branch, presence of else branch, final endif range.
- Parse-error behavior: Do not report if the CST cannot prove the final endif token.
- Shared semantic object: Conditional statement branch shape.

## Examples

### Invalid Examples

1. `Если А Тогда ... ИначеЕсли Б Тогда ... КонецЕсли;`.
2. `If A Then ... ElsIf B Then ... EndIf;`.
3. `Если ТипЗнч(П) = Тип("Строка") Тогда ... ИначеЕсли П = Неопределено Тогда ... КонецЕсли;`.

### Valid Negative Twins

1. `Если А Тогда ... КонецЕсли;`.
2. `Если А Тогда ... ИначеЕсли Б Тогда ... Иначе ... КонецЕсли;`.
3. Equal branch conditions are handled by BSL198, not by this rule.

## Related-Rule Cluster

- Rules that may report on the same object/range: BSL197, BSL198.
- Intended duplicate policy: Keep BSL199 for missing final else; BSL197/BSL198 report duplicated code/conditions.

## BSLLS Parity Taxonomy

- Compared input-file set: Private representative configuration corpus with 5803 BSL files plus synthetic fixtures.
- Config/exclude mode: Preserve source root for BSLLS; normal onec CLI path.
- File-key normalization: Repository parity runner normalized relative paths.
- Coordinate normalization: UTF-8 byte columns converted to LSP character columns.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: Single CST pass over `if_statement` nodes.
- Expected complexity: O(number of if statements).

## Implementation Decision

- Change target: No runtime change required; existing `IfElseIfEndsWithElseRule` matches the contract.
- Delta category fixed by this change: No BSLLS delta on the compared corpus; added contract and non-fixture synthetic coverage.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q -k BSL199 --no-cov`.
- Leak check: `git diff --check` and secret-pattern scan of staged/documented changes.
- OACS evidence/checkpoint: Record verification commands and checkpoint after successful parity run.

## Completion Checklist

- [x] Contract has semantic oracle.
- [x] Contract has CST/domain fact model.
- [x] Contract has positive and negative examples.
- [x] Contract has parity taxonomy.
- [x] Contract has performance contract.
- [x] Contract has verification plan.

</div>

</details>

<!-- engineering-contract:end -->
