# BSL194 — <span class="doc-lang doc-lang-ru">Функция всегда возвращает одно и то же примитивное значение</span><span class="doc-lang doc-lang-en">The function always returns the same primitive value</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Функция всегда возвращает одно и то же примитивное значение

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

The function always returns the same primitive value

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL194` |
| Совместимый псевдоним / Compatible alias | `FunctionReturnsSamePrimitive` |
| Серьёзность / Severity | `ERROR` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `redundant`, `design` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL194` и псевдоним `FunctionReturnsSamePrimitive` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL194"]
ignore = ["FunctionReturnsSamePrimitive"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL194
Значение = "пример";  // bsl-disable: BSL194
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:FunctionReturnsSamePrimitive-off
// код без этой диагностики
// BSLLS:FunctionReturnsSamePrimitive-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL194` and alias `FunctionReturnsSamePrimitive` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL194"]
ignore = ["FunctionReturnsSamePrimitive"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL194
Value = "example";  // bsl-disable: BSL194
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:FunctionReturnsSamePrimitive-off
// code without this diagnostic
// BSLLS:FunctionReturnsSamePrimitive-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Функция не должна возвращать одно и тоже примитивное значение. Если результат функции не учитывается - то нужно функцию
переписать на процедуру.

## Примеры

Плохо:
```bsl
Функция ПроверитьСтроку(Знач СтрокаТаблицы)

    Если ЭтоХорошаяСтрока(СтрокаТаблицы) Тогда
        ДелаемЧтоТоЧтобыНеРугаласьПроверкаНаПовтор();
        Возврат Истина;
    ИначеЕсли ЭтоТожеНеплохаяСтрока(СтрокаТаблицы) Тогда
        ДелаемДругоеЧтоТоЧтобыНеРугаласьПроверкаНаПовтор();
        Возврат Истина;
     Иначе
        Возврат Истина;
    КонецЕсли;

КонецФункции
```

Хорошо:
```bsl
Процедура ПроверитьСтроку(Знач СтрокаТаблицы)

    Если ЭтоХорошаяСтрока(СтрокаТаблицы) Тогда
        ДелаемЧтоТоЧтобыНеРугаласьПроверкаНаПовтор();
    ИначеЕсли ЭтоТожеНеплохаяСтрока(СтрокаТаблицы) Тогда
        ДелаемДругоеЧтоТоЧтобыНеРугаласьПроверкаНаПовтор();
    Иначе
        ДелаемЧтотоДругоеИзВеткиИначе();
    КонецЕсли;

КонецПроцедуры
```

## Особенности

Из проверки исключаются подключаемые Функции. Например:
```bsl
Функция Подключаемый_КакаяТоКоманда(Команда)

    Если ЗначениеЗаполнено(ТекущаяДата) Тогда
        Возврат Неопределено;
    КонецЕсли;

    Возврат Неопределено;

КонецФункции
```

## Источники

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

A function should not return the same primitive value. If the result of the function isn't use into code, then you need the function rewrite to the procedure.

## Examples

Bad:
```bsl
Function CheckString(Val RowTable)

    If ItsGoodString(RowTable) Then
        ActionGood();
        Return True;
    ElsIf ItsNodBadString(RowTable) Then
        ActionNoBad();
        Return True;
     Else
        Return True;
    EndIf;

EndFunction
```

Good:
```bsl
Function CheckString(Val RowTable)

    If ItsGoodString(RowTable) Then
        ActionGood();
    ElsIf ItsNodBadString(RowTable) Then
        ActionNoBad();
    Else
        ActionElse();
    EndIf;

EndFunction
```

## Nuances

Attachable functions excluded from the scan. Example:
```bsl
Function Attachable_RandomAction(Command)

    If ValueIsFilled(CurrentDate) Then
        Return Undefined;
    EndIf;

    Return Undefined;

EndFunction
```

## Sources

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL194
- BSLLS compatible name: FunctionReturnsSamePrimitive
- Public name/i18n key: FunctionReturnsSamePrimitive / BSL194
- Default message/i18n key: RULE_MESSAGES_RU["BSL194"]
- Description/i18n key: RULE_METADATA["BSL194"].description
- Severity: ERROR
- Parameters/defaults: BSLLS has `skipAttachable=true` and `caseSensitiveForString=false`; onec currently implements the default behavior.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: a function should not have multiple return paths that always return the same primitive value.
- Why this diagnostic exists: repeated identical primitive returns usually mean dead branching or an unfinished branch-specific result.
- Non-goals: do not diagnose functions with one return; do not diagnose non-primitive expressions; do not diagnose attachable functions when the default skip policy applies.
- Keep/drop decision: keep as a BSLLS-compatible redundant-return diagnostic.

## Semantic Oracle

- Checked language fact: a non-attachable function has more than one return statement and all returned primitive expressions are the same value under case-insensitive string comparison by default.
- Must diagnose when: more than one `Возврат` / `Return` returns the same primitive literal value and no non-primitive return expression is found.
- Must not diagnose when: there is only one return, return expressions differ, any return expression is non-primitive, or the function name starts with `Подключаемый_` / `Attachable_`.
- Diagnostic anchor token/range: function identifier token.
- Quick-fix target, if any: simplify control flow or return meaningful branch-specific values.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.procedures` / `ProcInfo` plus body line scan in the shared method-contract pass.
- CST/domain object types: function declaration and return statements.
- Extracted attributes: function name/kind/range and simple return expression texts.
- Parse-error behavior: only parsed procedure/function facts are checked; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: current implementation extracts simple single-line return expressions from function body lines; multiline return expressions are outside the current supported parity surface.

## Examples

### Invalid Examples

1. Function with two branches both returning `Истина`.
2. Function with two returns both returning the same number.
3. Function with two returns both returning the same string ignoring case by default.

### Valid Negative Twins

1. Function with only one return.
2. Function with returns `Истина` and `Ложь`.
3. Function named `Подключаемый_...` with same primitive returns.

### Edge Examples

- Comments: stripped before simple return matching.
- Strings: string literals are primitive and compared case-insensitively by default.
- Preprocessor: parsed body lines are scanned under the same simple return model.
- Multiline: multiline return expressions are not part of the current supported surface.
- Nested constructs: returns inside the same function body count; nested routine behavior should remain owned by parser/procedure facts.
- BSL/SDBL variants, if relevant: query text is irrelevant.

## Related-Rule Cluster

- Shared semantic object: function return statements.
- Rules that may report on the same object/range: BSL008 counts excessive returns; BSL032 checks functions with no return; BSL148 checks missing return paths; BSL051 checks unreachable statements after terminal returns.
- Intended duplicate policy: keep BSL194 distinct; it explains redundant identical primitive results, not quantity or missing paths.
- Cluster fixture or probe: `tests/test_diagnostics_method_contracts.py::TestMethodAndStatementMessageParity::test_bsl194_severity_and_message_match_bslls`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final aggregate has exact zero match, `onec=0`, `bslls=0`.
- Line/common semantic common: no diagnostics in the selected batch.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none for this rule in the final batch.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing method contract pass over parsed function body lines.
- Expected complexity: O(total function body lines).
- Shared-pass reuse: shared with BSL192, BSL193, BSL228, and BSL266.
- Before/after measurement, if changed: no code change in this iteration.

## Implementation Decision

- Change target: contract/audit only.
- Delta category fixed by this change: none; private parity is exact zero on the selected corpus.
- Why not another layer: no current evidence requires changing the implementation.
- Rollback signal: future private parity shows BSL194 presence/range/severity/message deltas, especially around multiline returns or non-primitive expressions.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_method_contracts.py -q -k bsl194 --no-cov`.
- Synthetic fixture/module: existing inline BSL194 test covers same primitive returns and ERROR severity/message.
- Related-rule tests: BSL008/BSL148/BSL051 targeted tests if return extraction is changed.
- Parity command/artifact: preserved-source-root private batch BSL194 parity, final exact zero.
- Performance command/artifact: no dedicated performance artifact; no code change.
- Ruff/lint: not required for code in contract-only iteration; run selected ruff if files change.
- Leak check: `git diff --check` and changed-file private marker scan.
- OACS evidence/checkpoint: required before claiming closure.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
