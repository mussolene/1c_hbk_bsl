# BSL152 — <span class="doc-lang doc-lang-ru">Кеширование программного интерфейса</span><span class="doc-lang doc-lang-en">Cached public methods</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Кеширование программного интерфейса

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Cached public methods

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL152` |
| Совместимый псевдоним / Compatible alias | `CachedPublic` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `design`, `performance` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL152` и псевдоним `CachedPublic` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL152"]
ignore = ["CachedPublic"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL152
Значение = "пример";  // bsl-disable: BSL152
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:CachedPublic-off
// код без этой диагностики
// BSLLS:CachedPublic-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL152` and alias `CachedPublic` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL152"]
ignore = ["CachedPublic"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL152
Value = "example";  // bsl-disable: BSL152
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:CachedPublic-off
// code without this diagnostic
// BSLLS:CachedPublic-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Не следует создавать программный интерфейс в модулях с повторным использованием возвращаемых значений.

3.6. Другой пример скрытия деталей реализации библиотеки от потребителя.
Допустим: в первой версии библиотеки потребителям предоставлялась экспортная функция общего модуля с повторным использованием
возвращаемых значений; Но в следующей версии библиотеки это проектное решение пересмотрено в пользу
«обычного» общего модуля, куда эта функция была перенесена (аналогично, если в обратную сторону). В данном примере,
для того чтобы избавить потребителя библиотеки от дополнительных усилий по замене вызовов «старой» функции на новую,
рекомендуется сразу размещать экспортную функцию в «обычном» модуле, в его разделе «программный интерфейс». Тогда эта
функция, в зависимости от текущего проектного решения, может вызывать служебную функцию из модуля с повторным
использованием возвращаемых значений или из любого другого модуля, или непосредственно сама содержать реализацию.
Однако для потребителя ее местоположение уже не будет меняться в следующих версиях библиотеки.

## Примеры

## Источники

* Источник: [Стандарт: Обеспечение совместимости библиотек](https://its.1c.ru/db/v8std#content:644:hdoc:3.6)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

You should not create a programming interface in modules that reuse return values.

3.6. Another example of hiding library implementation details from a consumer. Suppose: in the first version of the library, consumers were provided with an export function of a common module with repeated use of return values; But in the next version of the library, this design decision was revised in favor of the “usual” general module, where this function was transferred (similarly, if in the opposite direction). In this example, in order to save the library user from additional efforts to replace calls of the "old" function with a new one, it is recommended to immediately place the export function in the "regular" module, in its section "program interface". Then this function, depending on the current design decision, can call the utility function from the module with repeated use of the returned values or from any other module, or directly contain the implementation. However, for the consumer, its location will no longer change in future versions of the library.

## Examples

## Sources

* Source: [Standard: Ensuring Library Compatibility (RU)](https://its.1c.ru/db/v8std#content:644:hdoc:3.6)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL152
- BSLLS compatible name: CachedPublic
- Public name/i18n key: BSL152 / CachedPublic
- Default message/i18n key: RULE_MESSAGES_RU["BSL152"]
- Description/i18n key: RULE_METADATA["BSL152"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: cached common modules should not expose reusable methods through the public program interface region.
- Why this diagnostic exists: cached common-module return values are reused by the platform, so public API methods should be moved to the service public interface when the common module uses return-values reuse.
- Non-goals: reporting non-common modules, standalone `.bsl` files, non-cached common modules, empty public regions, service public regions, or validating method export flags.
- Keep/drop decision: keep as a BSLLS-compatible common-module design/performance diagnostic.

## Semantic Oracle

- Checked language fact: a cached common module contains a non-empty root public program interface region.
- Must diagnose when: a common module `Module.bsl` has metadata `ReturnValuesReuse` set to `DuringSession` or `DuringRequest`, and a `ПрограммныйИнтерфейс`/`Public` region contains at least one procedure or function.
- Must not diagnose when: the file is not a common-module `Module.bsl`, metadata XML is absent, `ReturnValuesReuse` is missing or `DontUse`, the public region is empty, the region is not public, or the syntax tree is invalid.
- Diagnostic anchor token/range: the public region name token in the `#Область`/`#Region` directive.
- Quick-fix target, if any: move methods from the public program interface to the service public program interface or disable return-values reuse if the module must remain public.

## CST Or Domain Fact Model

- Parser/tree source: tree-sitter BSL CST converted into `RegionInfo` and `ProcInfo`, plus common-module metadata XML near `Module.bsl`.
- CST/domain object types: common-module `Module.bsl`, sibling or nested common-module XML, root `#Область`/`#Region` region, `procedure_definition`, and `function_definition`.
- Extracted attributes: common-module path shape, `ReturnValuesReuse` value, region name/range, procedure/function start lines, and whether a method starts inside the public region bounds.
- Parse-error behavior: region/procedure facts come from the parsed document snapshot; malformed syntax is owned by BSL001.
- Textual/regex behavior, only if genuinely textual: metadata XML is read with a bounded `ReturnValuesReuse` value extraction; the diagnostic itself uses structured region/procedure facts.

## Examples

### Invalid Examples

1. Cached common module with `#Область ПрограммныйИнтерфейс` containing `Процедура Публичная() Экспорт`.
2. Cached common module with `#Region Public` containing `Procedure PublicMethod() Export`.
3. Cached common module with `ReturnValuesReuse=DuringRequest` and a public region containing a function.

### Valid Negative Twins

1. Common module with `ReturnValuesReuse=DontUse` and public region methods.
2. Cached common module with an empty `ПрограммныйИнтерфейс`/`Public` region.
3. Standalone `.bsl` file with a public region.

### Edge Examples

- Comments: commented region-looking text does not create a region fact.
- Strings: string literals do not create region or method facts.
- Preprocessor: not a separate semantic target for this rule.
- Multiline: method bodies may be multiline; only the method start line is needed to prove region content.
- Nested constructs: nested statements inside methods do not affect the region-level fact.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: common module metadata, module regions, and public/service API regions.
- Rules that may report on the same object/range: BSL003 checks non-export methods in API regions, BSL156 checks code outside regions, BSL158 checks assignment to common modules, BSL159-BSL168 check common-module metadata/name/context rules, and BSL233 checks public method descriptions.
- Intended duplicate policy: keep BSL152 separate; it reports cached common-module public API placement, not region emptiness, export visibility, naming, or documentation quality.
- Cluster fixture or probe: `tests/test_bsl152_cached_public.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32,007 `.bsl` files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 2 BSL152 diagnostics matched by file, range, and code.
- Line/common semantic common: current implementation anchors on the public region name.
- Ours-only categories: none.
- BSLLS-only categories: none.
- Range-only categories: none.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing shared document snapshot for regions/procedures and one adjacent common-module XML read per common module `Module.bsl`.
- Expected complexity: O(regions plus procedures plus metadata XML size for common-module files).
- Shared-pass reuse: shares `RegionInfo` and `ProcInfo` with common-module context diagnostics.
- Before/after measurement, if changed: no runtime change yet; targeted BSL152 tests cover behavior.

## Implementation Decision

- Change target: BSL152 contract, parity classification, audit state, and verification evidence.
- Delta category fixed by this change: missing durable rule state.
- Why not another layer: current implementation already uses structured module region/procedure facts and bounded metadata XML lookup.
- Rollback signal: cached public modules stop reporting, non-cached common modules report, standalone files report, empty public regions report, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_bsl152_cached_public.py -q --no-cov`.
- Synthetic fixture/module: existing tests cover cached DuringSession/DuringRequest positives, Russian and English public regions, `DontUse`, empty public region, Russian common-module folders, and standalone files.
- Related-rule tests: include common-module diagnostics if runtime changes.
- Parity command/artifact: preserved-source-root selected BSL152 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime.
- Ruff/lint: `./.venv/bin/python -m ruff check tests/test_bsl152_cached_public.py src/onec_hbk_bsl/analysis/diagnostic/rules/common_module_rules.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
