# BSL041 — <span class="doc-lang doc-lang-ru">Ограничение на использование устаревшего метода "Сообщить"</span><span class="doc-lang doc-lang-en">Restriction on the use of deprecated "Message" method</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Ограничение на использование устаревшего метода "Сообщить"

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Restriction on the use of deprecated "Message" method

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL041` |
| Совместимый псевдоним / Compatible alias | `DeprecatedMessage` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `deprecated`, `ui` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL041` и псевдоним `DeprecatedMessage` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL041"]
ignore = ["DeprecatedMessage"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL041
Значение = "пример";  // bsl-disable: BSL041
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:DeprecatedMessage-off
// код без этой диагностики
// BSLLS:DeprecatedMessage-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL041` and alias `DeprecatedMessage` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL041"]
ignore = ["DeprecatedMessage"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL041
Value = "example";  // bsl-disable: BSL041
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:DeprecatedMessage-off
// code without this diagnostic
// BSLLS:DeprecatedMessage-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

Для вывода сообщений пользователю во всех случаях следует использовать объект СообщениеПользователю, даже когда сообщение не «привязывается» к некоторому элементу управления формы. Метод Сообщить применять не следует.

*При использовании в конфигурации Библиотеки стандартных подсистем рекомендуется использовать процедуру СообщитьПользователю общего модуля ОбщегоНазначенияКлиентСервер, которая работает с объектом СообщениеПользователю.*

## Источники

* [Стандарт: Ограничение на использование метода Сообщить](https://its.1c.ru/db/v8std#content:418:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

To display messages to the user in all cases, you should use the MessageUser object, even when the message is not “bound” to some form control. The Message method should not be used.

*When used the Standard Subsystems Library it is recommended use procedure MessageUser from common module CommonPurposeClientServer, which use object UserMessage.*

## Sources

* [Standard: Limit use of the obsolete method Message (RU)](https://its.1c.ru/db/v8std#content:418:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL041
- BSLLS compatible name: DeprecatedMessage
- Public name/i18n key: BSL041 / DeprecatedMessage
- Default message/i18n key: RULE_MESSAGES_RU["BSL041"]
- Description/i18n key: RULE_METADATA["BSL041"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: use `СообщениеПользователю` or project message helpers instead of deprecated global `Сообщить`.
- Why this diagnostic exists: `Сообщить` is a legacy user-message API and should not be used for modern UI messaging.
- Non-goals: member methods named `Сообщить`, string/comment text, or broader deprecated API detection.
- Keep/drop decision: keep as a BSLLS-compatible deprecated API diagnostic.

## Semantic Oracle

- Checked language fact: executable code calls global `Сообщить` / `Message`.
- Must diagnose when: a global `Сообщить(...)` or `Message(...)` call appears in code.
- Must not diagnose when: the text appears inside comments/strings or as a member call.
- Diagnostic anchor token/range: method name span.
- Quick-fix target, if any: replace with `СообщениеПользователю` or an approved project helper.

## CST Or Domain Fact Model

- Parser/tree source: masked source line scan.
- CST/domain object types: physical code line and global method call token.
- Extracted attributes: method name, start/end column, line number, member-call prefix state, and severity.
- Parse-error behavior: clear line-local call spelling can still report; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: fixed method-name regex after string/comment masking is sufficient for this global deprecated API detector.

## Examples

### Invalid Examples

1. `Сообщить("Готово");`
2. `Message("Done");`
3. `Если Ошибка Тогда Сообщить(ТекстОшибки); КонецЕсли;`

### Valid Negative Twins

1. `Текст = "Сообщить(""Готово"")";`
2. `// Сообщить("Готово");`
3. `Объект.Сообщить("Готово");`

### Edge Examples

- Comments: ignored by masking.
- Strings: ignored by masking.
- Preprocessor: not a separate target.
- Multiline: simple global call range is line-local in current implementation.
- Nested constructs: nested line-local calls are diagnosed.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: deprecated global/platform API calls.
- Rules that may report on the same object/range: BSL066 deprecated `Найти`, BSL097 deprecated current date, BSL175/BSL176 deprecated API pools, BSL177/BSL178/BSL179 deprecated platform APIs, and typo/security rules may inspect string arguments nearby.
- Intended duplicate policy: BSL041 owns only `Сообщить`/`Message`; other deprecated API rules own their specific APIs.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestBsl041DeprecatedMessage`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 6.
- Line/common semantic common: exact parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over masked source lines.
- Expected complexity: O(total source characters).
- Shared-pass reuse: uses existing string/comment masking helper.
- Before/after measurement, if changed: no runtime change; current corpus exact parity onec 6 / bslls 6.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact.
- Why not another layer: a masked line detector matches the documented global API rule without CST overhead.
- Rollback signal: member calls report, strings/comments report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl041 or DeprecatedMessage"`.
- Synthetic fixture/module: existing selected tests cover Russian/English positives and comment negative.
- Related-rule tests: deprecated API cluster only if shared call detection changes.
- Parity command/artifact: preserved-source-root selected BSL041 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
