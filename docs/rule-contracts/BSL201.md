# BSL201 — <span class="doc-lang doc-lang-ru">Некорректное использование 'ПОДОБНО'</span><span class="doc-lang doc-lang-en">Incorrect use of 'LIKE'</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Некорректное использование 'ПОДОБНО'

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Incorrect use of 'LIKE'

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL201` |
| Совместимый псевдоним / Compatible alias | `IncorrectUseLikeInQuery` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `query`, `correctness` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL201` и псевдоним `IncorrectUseLikeInQuery` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL201"]
ignore = ["IncorrectUseLikeInQuery"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL201
Значение = "пример";  // bsl-disable: BSL201
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:IncorrectUseLikeInQuery-off
// код без этой диагностики
// BSLLS:IncorrectUseLikeInQuery-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL201` and alias `IncorrectUseLikeInQuery` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL201"]
ignore = ["IncorrectUseLikeInQuery"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL201
Value = "example";  // bsl-disable: BSL201
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:IncorrectUseLikeInQuery-off
// code without this diagnostic
// BSLLS:IncorrectUseLikeInQuery-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

При использовании в тексте запроса оператора `ПОДОБНО` допустимо использовать только
- константные строковые литералы
- параметры запроса

Запрещается формировать строку шаблона при помощи вычислений, использовать конкатенацию строк средствами языка запросов.

Запросы, в которых управляющие символы шаблона оператора `ПОДОБНО` находятся в полях запроса или в вычисляемых выражениях,
по-разному интерпретируются на различных СУБД (MS SQL и PostgreSQL).

## Примеры

### Конкатенация строк средствами языка

Допустимо:

```
Реквизит ПОДОБНО "123%"
```

Недопустимо:

```
Реквизит ПОДОБНО "123" + "%"
Реквизит ПОДОБНО Таблица.Шаблон
```

### Управляющие символы шаблона оператора ПОДОБНО находятся в полях запроса или в вычисляемых выражениях

Например, вместо:

```
Запрос = Новый Запрос("
|ВЫБРАТЬ
|    Товары.Ссылка
|ИЗ
|    Справочник.Товары КАК Товары
|ГДЕ
|    Товары.СтранаПроисхождения.Наименование ПОДОБНО &ШаблонНазванияСтраны + "_"
|");

Запрос.УстановитьПараметр("ШаблонНазванияСтраны", "ЧА");
```

Необходимо использовать:

```
Запрос = Новый Запрос("
|ВЫБРАТЬ
|    Товары.Ссылка
|ИЗ
|    Справочник.Товары КАК Товары
|ГДЕ
|    Товары.СтранаПроисхождения.Наименование ПОДОБНО &ШаблонНазванияСтраны
|");

Запрос.УстановитьПараметр("ШаблонНазванияСтраны", "ЧА_");
```

## Источники

- Источник: [Стандарт: Особенности использования в запросах оператора ПОДОБНО](https://its.1c.ru/db/v8std/content/726/hdoc?ysclid=l3g3fkmxsx)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

When using the operator `LIKE` in the query text, it is allowed to use only
- constant string literals
- query parameters

It is forbidden to form a template string using calculations, use string concatenation using the query language.

Queries in which the control characters of the operator template `LIKE` are in query fields or in calculated expressions are interpreted differently on different DBMSs.

## Examples

### String concatenation by language features

Allowed:

```
Field LIKE "123%"
```

Not allowed:

```
Field LIKE "123" + "%"
Field LIKE Table.Template
```

### Operator template control characters LIKE are found in query fields or in calculated expressions

For example, instead of:

```
Query = New Query("
|SELECT
|    Goods.Ref
|FROM
|    Catalog.Goods AS Goods
|WHERE
|    Goods.Country.Description LOKE &NameTemplate + "_"
|");

Query.SetParameter("NameTemplate", "FU");
```

Nessesary to use:

```
Query = New Query("
|SELECT
|    Goods.Ref
|FROM
|    Catalog.Goods AS Goods
|WHERE
|    Goods.Country.Description LOKE &NameTemplate
|");

Query.SetParameter("NameTemplate", "FU_");
```

## Sources

- [Standard. Features of use in operator requests LIKE (RU)](https://its.1c.ru/db/v8std/content/726/hdoc?ysclid=l3g3fkmxsx)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL201
- BSLLS compatible name: IncorrectUseLikeInQuery
- Public name/i18n key: IncorrectUseLikeInQuery / BSL201
- Default message/i18n key: RULE_MESSAGES_RU["BSL201"]
- Description/i18n key: RULE_METADATA["BSL201"].description
- Severity: WARNING
- Parameters/defaults: none.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: query `ПОДОБНО` / `LIKE` right operand must follow the standard form; BSLLS permits a query parameter or a multiline/string literal pattern and reports other expression shapes.
- Why this diagnostic exists: non-parameter, non-literal LIKE operands are hard to reason about, can be wrong at runtime, and deviate from the BSLLS standard check.
- Non-goals: do not diagnose mere LIKE usage without operand-shape defect; BSL269 owns the performance/style warning.
- Keep/drop decision: keep as a BSLLS-compatible query correctness diagnostic.

## Semantic Oracle

- Checked language fact: an SDBL `like_expression` whose right `query_expression` is not a parameter and not a string literal.
- Must diagnose when: right operand is an identifier/field, number, undefined, function call, binary expression, or another non-allowed expression shape.
- Must not diagnose when: right operand is a query parameter such as `&Pattern`, or a query string literal/multiline string literal.
- Diagnostic anchor token/range: full SDBL `like_expression` range mapped to the BSL query-string coordinates, matching BSLLS parent predicate range as closely as possible.
- Quick-fix target, if any: move the pattern into a query parameter or literal pattern accepted by the standard.

## CST Or Domain Fact Model

- Parser/tree source: unescaped SDBL tree-sitter CST from query text blocks.
- CST/domain object types: `QueryTextBlockInfo`, SDBL `like_expression`, child `query_expression` nodes, `parameter`, `string`, and expression-shape descendants.
- Extracted attributes: like-expression start/end row and byte columns, right operand expression kind, and mapped BSL line/character range.
- Parse-error behavior: skip malformed query fragments that do not produce a complete `like_expression` with right operand.
- Textual/regex behavior, only if genuinely textual: legacy regex fallback may remain only for environments without query block/SDBL facts; valid CST paths must be structural.

## Examples

### Invalid Examples

1. `ГДЕ Т.Код ПОДОБНО ДругаяКолонка`.
2. `ГДЕ Т.Код ПОДОБНО Неопределено`.
3. `WHERE T.Code LIKE BuildPattern(&Pattern)`.

### Valid Negative Twins

1. `ГДЕ Т.Код ПОДОБНО &Шаблон`.
2. `ГДЕ Т.Код ПОДОБНО "ABC%"`.
3. `WHERE T.Code LIKE &Pattern`.

### Edge Examples

- Comments: LIKE-like words in comments are not SDBL expressions.
- Strings: `LIKE` inside a query string literal is not an operator.
- Preprocessor: BSL preprocessor is irrelevant after query text extraction.
- Multiline: a LIKE predicate may span query lines; range maps back to BSL lines.
- Nested constructs: LIKE inside nested query expressions is diagnosed the same way.
- BSL/SDBL variants, if relevant: Russian `ПОДОБНО` and English `LIKE` are equivalent.

## Related-Rule Cluster

- Shared semantic object: SDBL `like_expression`.
- Rules that may report on the same object/range: BSL269 reports all LIKE usage as informational performance guidance; BSL235 reports query parse errors.
- Intended duplicate policy: BSL201 and BSL269 may both report the same LIKE predicate because they explain different issues.
- Cluster fixture or probe: query text tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus synthetic query fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final private parity is exact at `onec=2`, `bslls=2`.
- Line/common semantic common: baseline private parity had `onec=2`, `bslls=2`, `only_onec=2`, `only_bslls=2`; final parity has no deltas.
- Ours-only categories: none in final private parity.
- BSLLS-only categories: none in final private parity.
- Range-only categories: fixed by reporting the full parsed `like_expression` range instead of keyword-only range.
- Known BSLLS defects: none established.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing query text blocks and SDBL CST helper shared with BSL269-style LIKE traversal.
- Expected complexity: O(query CST nodes) for selected query blocks.
- Shared-pass reuse: remains in the BSL191/BSL201 query text pool; BSL269 helper traversal may be reused.
- Before/after measurement, if changed: targeted query tests pass in 0.84s; private parity improved from `onec=2`, `bslls=2`, `only_onec=2`, `only_bslls=2` to exact `onec=2`, `bslls=2`.

## Implementation Decision

- Change target: BSL201 branch in `query_text_rules.py`.
- Delta category fixed by this change: regex keyword-range mismatch and over/under-matching caused by textual right-side checks.
- Why not another layer: SDBL CST already models LIKE predicates and right operand shape.
- Rollback signal: BSL201 reports LIKE with parameter/string literal, misses non-parameter/non-string right operands, or parity worsens.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k 'bsl201 or bsl269 or bsl235' --no-cov`.
- Synthetic fixture/module: parameter/string negatives; identifier/number/function/binary positives; LIKE inside string negative.
- Related-rule tests: BSL269 and BSL235 query text cluster.
- Parity command/artifact: preserved-source-root private batch BSL201 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/query_text_rules.py tests/test_diagnostics_query_metadata.py`.
- Leak check: `git diff --check` and changed-file private token scan.
- OACS evidence/checkpoint: ingest verification outputs and add checkpoint before claiming done.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
