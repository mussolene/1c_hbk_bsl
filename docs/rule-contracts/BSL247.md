# BSL247 SetPrivilegedMode Contract

## Rule Identity

- Code: BSL247
- BSLLS compatible name: SetPrivilegedMode
- Public name/i18n key: BSL247 / SetPrivilegedMode
- Default message/i18n key: RULE_MESSAGES_RU["BSL247"]
- Description/i18n key: RULE_METADATA["BSL247"].description
- Severity: WARNING
- Parameters/defaults: none in current onec-hbk-bsl; BSLLS rule can be disabled by config.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: enabling privileged mode must receive manual security review.
- Why this diagnostic exists: `УстановитьПривилегированныйРежим` disables ordinary access-right checks for subsequent code.
- Non-goals: explicit disabling with `Ложь`/`False`, member calls, comments, strings, and unrelated identifiers.
- Keep/drop decision: keep as a BSLLS-compatible security diagnostic.

## Semantic Oracle

- Checked language fact: executable code calls the global method `УстановитьПривилегированныйРежим` / `SetPrivilegedMode`.
- Must diagnose when: the call argument is not the literal `Ложь` / `False`.
- Must not diagnose when: the call argument is exactly `Ложь` / `False`, or the method text is not an executable global call.
- Diagnostic anchor token/range: method-name span.
- Quick-fix target, if any: remove privileged mode or add an explicit access-right check before it.

## CST Or Domain Fact Model

- Parser/tree source: source-line scan after string/comment masking.
- CST/domain object types: physical source line and global call match.
- Extracted attributes: method name, first argument text, line, start/end column, and severity.
- Parse-error behavior: clear line-local call spelling can still report; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: fixed global method-call regex after masking; argument predicate only suppresses literal false.

## Examples

### Invalid Examples

1. `УстановитьПривилегированныйРежим(Истина);`
2. `УстановитьПривилегированныйРежим(Значение);`
3. `SetPrivilegedMode(True);`

### Valid Negative Twins

1. `УстановитьПривилегированныйРежим(Ложь);`
2. `SetPrivilegedMode(False);`
3. `Текст = "УстановитьПривилегированныйРежим(Истина)";`

### Edge Examples

- Comments: ignored by masking.
- Strings: ignored by masking.
- Preprocessor: not a separate target.
- Multiline: current detector is line-local; multiline calls are outside the current contract.
- Nested constructs: nested line-local calls are diagnosed.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: security-sensitive platform APIs.
- Rules that may report on the same object/range: BSL180 safe mode, BSL184 dynamic code execution, BSL188 filesystem access, BSL203 internet access, BSL226 OS user enumeration, and BSL264 system information.
- Intended duplicate policy: BSL247 owns privileged-mode enabling specifically.
- Cluster fixture or probe: `tests/test_diagnostics_line_text.py::TestSecurityApiParityBatch::test_bsl247_set_privileged_mode_matches_bslls_fixture`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over masked source lines.
- Expected complexity: O(total source characters).
- Shared-pass reuse: uses the same lightweight security API line-scan pattern as nearby rules.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: masked line scan matches the BSLLS documented call-level rule without CST overhead for this API.
- Rollback signal: `Ложь`/`False` reports, member calls report, strings/comments report, or selected parity gains deltas.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl247 or SetPrivilegedMode"`.
- Synthetic fixture/module: existing selected test covers true/variable positives and false negative.
- Related-rule tests: broader security API batch only if shared masking changes.
- Parity command/artifact: preserved-source-root selected BSL247 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
