# BSL239 — <span class="doc-lang doc-lang-ru">Зарезервированные имена параметров</span><span class="doc-lang doc-lang-en">Reserved parameter names</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Зарезервированные имена параметров

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Reserved parameter names

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL239` |
| Совместимый псевдоним / Compatible alias | `ReservedParameterNames` |
| Серьёзность / Severity | `WARNING` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `naming`, `suspicious` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL239` и псевдоним `ReservedParameterNames` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL239"]
ignore = ["ReservedParameterNames"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL239
Значение = "пример";  // bsl-disable: BSL239
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ReservedParameterNames-off
// код без этой диагностики
// BSLLS:ReservedParameterNames-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL239` and alias `ReservedParameterNames` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL239"]
ignore = ["ReservedParameterNames"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL239
Value = "example";  // bsl-disable: BSL239
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ReservedParameterNames-off
// code without this diagnostic
// BSLLS:ReservedParameterNames-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики
Если имя параметра совпадает с именем системного перечисления, то невозможно будет обратиться к значениям этого системного перечисления, потому что параметр его скроет.
Синтаксическая проверка кода модуля не выявит такую ошибку. Чтобы предотвратить эту ситуацию имя параметра не должно совпадать с именами системных перечислений.
Список зарезервированных слов задается регулярным выражением.
Поиск производится без учета регистра символов.

**Примеры настройки:**

"ВидГруппыФормы|ВидПоляФормы"

## Источники

* Источник: [Стандарт: Параметры процедур и функций](https://its.1c.ru/db/v8std/content/640/hdoc)
* Источник: [Стандарт: Правила образования имен переменных](https://its.1c.ru/db/v8std#content:454:hdoc)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

If a parameter name matches one of a system enumeration's name, then all values of that enumeration will not be available in the local context.
Module code's syntax checking will not detect an error. To prevent this situation, a parameter name should not match all names of system enumerations.

Parameter names should not contain reserved words such as system enumerations.
The list of reserved words is set by a regular expression.
The search is case-insensitive.

**For example:**

"FormGroupType|FormFieldType"

## Sources

* Source: [Standard: Procedure and Function Parameters (RU)](https://its.1c.ru/db/v8std/content/640/hdoc)
* Source: [Standard: Rules for generating variable names (RU)](https://its.1c.ru/db/v8std#content:454:hdoc)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL239
- BSLLS compatible name: ReservedParameterNames
- Public name/i18n key: BSL239 / ReservedParameterNames
- Default message/i18n key: RULE_MESSAGES_RU["BSL239"]
- Description/i18n key: RULE_METADATA["BSL239"].description
- Severity: WARNING
- Parameters/defaults: `reservedWords=""`; empty default disables concrete matches.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: project teams may forbid parameter names that collide with reserved product/system names.
- Why this diagnostic exists: reserved-like parameter names make procedure signatures ambiguous and harder to maintain.
- Non-goals: local variables, method names, fields, attributes, or parameters when `reservedWords` is empty.
- Keep/drop decision: keep as a BSLLS-compatible configurable naming diagnostic.

## Semantic Oracle

- Checked language fact: a procedure/function parameter name fully matches the configured `reservedWords` regular expression.
- Must diagnose when: `reservedWords` is configured and a declared method parameter name matches it.
- Must not diagnose when: `reservedWords` is empty, the text is not a method parameter, or only a substring matches.
- Diagnostic anchor token/range: parameter identifier span in the method header.
- Quick-fix target, if any: rename the parameter to a non-reserved project name.

## CST Or Domain Fact Model

- Parser/tree source: module procedure/function domain facts collected from CST/header parsing.
- CST/domain object types: procedure/function declaration and parameter identifiers.
- Extracted attributes: parameter name, header line/column, configured regex, and severity.
- Parse-error behavior: report only when method parameters were recovered as method facts; BSL001 owns syntax errors.
- Textual/regex behavior, only if genuinely textual: regex is the user-configured reserved-word matcher over extracted parameter names, not over raw source text.

## Examples

### Invalid Examples

1. `Процедура Заполнить(Дата)` with `reservedWords="Дата|Date"`.
2. `Function Build(Date)` with `reservedWords="Дата|Date"`.
3. `Процедура Проверить(Ссылка)` with `reservedWords="Ссылка|Ref"`.

### Valid Negative Twins

1. `Процедура Заполнить(ДатаДокумента)` with `reservedWords="Дата|Date"`.
2. `Дата = ТекущаяДата();` because a local variable is outside this rule.
3. `Процедура Заполнить(Дата)` when `reservedWords=""`.

### Edge Examples

- Comments: ignored because only method parameter facts are checked.
- Strings: ignored because only method parameter facts are checked.
- Preprocessor: not a separate target.
- Multiline: recovered method parameter facts determine the anchor.
- Nested constructs: not applicable to method declarations.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: method signature parameter identifiers.
- Rules that may report on the same object/range: BSL113 parameter count, BSL145 Latin/Cyrillic variable names, BSL156 unused parameters, BSL240 parameter reassignment.
- Intended duplicate policy: BSL239 owns configurable reserved-name policy only.
- Cluster fixture or probe: `tests/test_diagnostics_local_runtime.py::TestAdditionalParityBatch::test_bsl239_reserved_parameter_names_detected`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root current batch corpus, 5803 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 0; exact-zero parity on the current corpus.
- Line/common semantic common: exact-zero parity.
- Ours-only categories: 0.
- BSLLS-only categories: 0.
- Range-only categories: 0.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: existing method fact pass plus optional parameter-name regex.
- Expected complexity: O(number of recovered method parameters) after method facts exist.
- Shared-pass reuse: reuses the same procedure/function model as other signature diagnostics.
- Before/after measurement, if changed: no runtime change; current corpus exact-zero parity onec 0 / bslls 0 under default empty `reservedWords`.

## Implementation Decision

- Change target: contract and audit state only; existing implementation is retained.
- Delta category fixed by this change: none; current corpus parity is exact-zero.
- Why not another layer: the rule is a configurable predicate over existing parameter facts.
- Rollback signal: reports appear with empty `reservedWords`, substring matches report, or non-parameter names report.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests -q --no-cov -k "Bsl239 or ReservedParameterNames"`.
- Synthetic fixture/module: existing selected test covers configured regex positive detection.
- Related-rule tests: broader signature batch only if method fact extraction changes.
- Parity command/artifact: preserved-source-root selected BSL239 current batch parity; sanitized aggregate only.
- Performance command/artifact: no dedicated benchmark because no runtime change was made.
- Ruff/lint: not required for contract-only close unless code changes.
- Leak check: `git diff --check` and touched-file private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
