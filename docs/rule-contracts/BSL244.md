# BSL244 — <span class="doc-lang doc-lang-ru">Серверные вызовы в событиях форм</span><span class="doc-lang doc-lang-en">Server calls in form events</span>

<!-- generated-rule-header:start -->

<a href="../diagnostic-rules.md"><span class="doc-lang doc-lang-ru">← Все правила</span><span class="doc-lang doc-lang-en">← All rules</span></a>

<div class="doc-lang doc-lang-ru" markdown="1">

## Кратко

Серверные вызовы в событиях форм

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Summary

Server calls in form events

</div>

## Идентификаторы / Identifiers

| Поле / Field | Значение / Value |
|---|---|
| Код правила / Rule code | `BSL244` |
| Совместимый псевдоним / Compatible alias | `ServerCallsInFormEvents` |
| Серьёзность / Severity | `ERROR` |
| Включено по умолчанию / Enabled by default | Да / Yes |
| Реализовано / Implemented | Да / Yes |
| Теги / Tags | `correctness`, `ui`, `performance` |

<div class="doc-lang doc-lang-ru" markdown="1">

## Контракт поведения

- Публичный идентификатор `BSL244` и псевдоним `ServerCallsInFormEvents` стабильны.
- Правило сообщает только о случаях, описанных на этой странице и в
  проверяемом инженерном контракте.
- Подавления и проектная конфигурация применяются до публикации результата.
- Для выполнения правила не требуется внешний анализатор или сетевой доступ.

## Настройка и подавление

Код `BSL###` — основной стабильный идентификатор. Совместимый псевдоним
принимается в `select`, `ignore` и совместимых блоковых комментариях.

```toml
[tool.onec-hbk-bsl]
select = ["BSL244"]
ignore = ["ServerCallsInFormEvents"]
```

Подавить диагностику только на текущей строке:

```bsl
Значение = "пример";  // noqa: BSL244
Значение = "пример";  // bsl-disable: BSL244
```

Отключить правило для блока или всего файла:

```bsl
// BSLLS:ServerCallsInFormEvents-off
// код без этой диагностики
// BSLLS:ServerCallsInFormEvents-on
```

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Behavioral contract

- The public identifier `BSL244` and alias `ServerCallsInFormEvents` are stable.
- The rule reports only the cases documented on this page and in its
  verifiable engineering contract.
- Suppressions and project configuration are applied before publication.
- The rule requires neither an external analyzer nor network access.

## Configuration and suppression

`BSL###` is the primary stable identifier. The compatible alias is accepted
in `select`, `ignore`, and compatible block suppression comments.

```toml
[tool.onec-hbk-bsl]
select = ["BSL244"]
ignore = ["ServerCallsInFormEvents"]
```

Suppress the diagnostic on the current line:

```bsl
Value = "example";  // noqa: BSL244
Value = "example";  // bsl-disable: BSL244
```

Disable the rule for a block or the whole file:

```bsl
// BSLLS:ServerCallsInFormEvents-off
// code without this diagnostic
// BSLLS:ServerCallsInFormEvents-on
```

</div>

<!-- generated-rule-header:end -->

<!-- localized-rule-description:start -->

<div class="doc-lang doc-lang-ru" markdown="1">

## Описание диагностики

В событиях ПриАктивизацииСтроки и НачалоВыбора не должно быть вызовов контекстных серверных процедур. Эти события должны выполняться только на клиенте.

Диагностируются только серверные вызовы методов этой формы, выполняемые в контексте сервера (&НаСервере). Вызовы общих модулей (например, МойМодульСервер.МойСерверныйМетод) или внеконтекстные вызовы процедур самой формы (&НаСервереБезКонтекста) не будут ошибкой.

Согласно статье на [Infostart](https://infostart.ru/1c/articles/1225834/), вызов серверных процедур из этих событий может привести к проблемам с производительностью и корректностью работы формы.

## Примеры

### Неправильно

```bsl
&НаКлиенте
Процедура ПриАктивизацииСтроки(Элемент, ВыбраннаяСтрока, Поле, НовоеЗначение, СтандартнаяОбработка)
    // Ошибка: вызов серверной процедуры из клиентского события
    ТаблицаФормыПриАктивизацииСтрокиНаСервере();
    СтандартнаяОбработка = Ложь;
КонецПроцедуры

&НаСервере
Процедура ТаблицаФормыПриАктивизацииСтрокиНаСервере()
    ВызватьИсключение "тест";
КонецПроцедуры
```

### Правильно

```bsl
&НаКлиенте
Процедура ПриАктивизацииСтроки(Элемент, ВыбраннаяСтрока, Поле, НовоеЗначение, СтандартнаяОбработка)
    // Правильно: внеконтекстный вызов разрешен
    ТаблицаФормыПриАктивизацииСтрокиНаСервереБезКонтекста();
    СтандартнаяОбработка = Ложь;
КонецПроцедуры

&НаСервереБезКонтекста
Процедура ТаблицаФормыПриАктивизацииСтрокиНаСервереБезКонтекста()
    // Логика, не требующая контекста формы
КонецПроцедуры
```

## Источники

* GitHub Issue #3473
* [Infostart: Серверные вызовы, которые нельзя вызывать](https://infostart.ru/1c/articles/1225834/)

</div>

<div class="doc-lang doc-lang-en" markdown="1">

## Description

Events `OnActivateRow` and `OnStartChoice` should not contain contextual server procedure calls. These events should only execute on the client.

Only server calls to methods of this form executed within the server context (`&AtServer`) are diagnosed. Calling general modules (e.g., `MyModuleServer.MyServerMethod`) or non-contextual server procedures of the form itself (`&AtServerNoContext`) will not result in an error.

According to the [Infostart article](https://infostart.ru/1c/articles/1225834/), calling server procedures from these events can lead to performance issues and form behavior problems.

## Examples

### Incorrect

```bsl
&AtClient
Procedure OnActivateRow(Element, SelectedRow, Field, NewValue, StandardProcessing)
    // Error: server procedure call from client event
    TableFormOnActivateRowAtServer();
    StandardProcessing = False;
EndProcedure

&AtServer
Procedure TableFormOnActivateRowAtServer()
    RaiseException "test";
EndProcedure
```

### Correct

```bsl
&AtClient
Procedure OnActivateRow(Element, SelectedRow, Field, NewValue, StandardProcessing)
    // Correct: only client processing
    StandardProcessing = False;
EndProcedure

&AtServerNoContext
Procedure OnActivateRow(Element, SelectedRow, Field, NewValue, StandardProcessing)
    // Correct: non-context server form calls are allowed
    StandardProcessing = False;
EndProcedure
```

## Sources

* GitHub Issue #3473
* [Infostart: Server calls that should not be called](https://infostart.ru/1c/articles/1225834/)

</div>

<!-- localized-rule-description:end -->

<!-- engineering-contract:start -->

## Инженерный контракт / Engineering contract

<details class="engineering-contract">
<summary><span class="doc-lang doc-lang-ru">Показать полный контракт реализации</span><span class="doc-lang doc-lang-en">Show the complete implementation contract</span></summary>

<div markdown="1">

## Rule Identity

- Code: BSL244
- BSLLS compatible name: ServerCallsInFormEvents
- Public name/i18n key: ServerCallsInFormEvents / BSL244
- Default message/i18n key: RULE_MESSAGES_RU["BSL244"]
- Description/i18n key: RULE_METADATA["BSL244"].description
- Severity: ERROR
- Parameters/defaults: none
- Rule status: parity-classified

## Standard And Product Decision

- Standard or product policy: form events `ПриАктивизацииСтроки` / `OnActivateRow` and `НачалоВыбора` / `OnStartChoice` must not call context-dependent server methods.
- Why this diagnostic exists: these form events can run very often or in latency-sensitive UI flows; server round trips from them can make forms slow or unstable.
- Non-goals: do not diagnose every form event, server-side form procedures, ordinary helper calls, calls to `&НаСервереБезКонтекста` / `&AtServerNoContext`, common-module calls, or dynamic calls that are not direct global method calls.
- Keep/drop decision: keep as a BSLLS-compatible form performance/correctness diagnostic.

## Semantic Oracle

- Checked language fact: a direct global method call inside a forbidden form event targets a local form method compiled with `&НаСервере` / `&AtServer`.
- Must diagnose when: the current file is a form module, the containing procedure/function name ends with `ПриАктивизацииСтроки`, `OnActivateRow`, `НачалоВыбора`, or `OnStartChoice`, and a direct call in its body targets a local method with exactly the context-dependent server compiler directive.
- Must not diagnose when: the event is another `При*` / `On*` event, the target method is `&НаСервереБезКонтекста` / `&AtServerNoContext`, the target method is client/both/none-context, the call is a qualified common-module call, or the file is not a form module.
- Diagnostic anchor token/range: direct called method name range in the forbidden event body.
- Quick-fix target, if any: move the call out of the event, use a non-context server method if safe, cache required data, or redesign the form interaction.

## CST Or Domain Fact Model

- Parser/tree source: current form module BSL procedure/function model and cleaned BSL lines.
- CST/domain object types: `ProcInfo` for containing event methods and target methods, compiler directive lines immediately preceding target procedures, direct same-line global method call tokens.
- Extracted attributes: current path module type, event method name suffix, containing method execution context, target method name, target server directive kind, call range, and cleaned-line text.
- Parse-error behavior: missing parsed procedure facts means no structural BSL244 diagnostics.
- Textual/regex behavior, only if genuinely textual: current implementation uses the existing cleaned-line direct call scanner for same-line global method calls; this is a bounded compatibility bridge until a shared CST call fact is used by the metadata/runtime pool.

## Examples

### Invalid Examples

1. `&НаКлиенте Процедура ТоварыПриАктивизацииСтроки()` directly calls `Заполнить()` where `Заполнить` is `&НаСервере`.
2. `&AtClient Procedure ItemsOnActivateRow()` directly calls `Fill()` where `Fill` is `&AtServer`.
3. `&НаКлиенте Процедура ПолеНачалоВыбора()` directly calls a local form method compiled with `&НаСервере`.

### Valid Negative Twins

1. `ПриОткрытии` calls a server helper.
2. `ПриАктивизацииСтроки` calls a helper compiled with `&НаСервереБезКонтекста`.
3. A non-forbidden server-side form event method calls a server helper.
4. A non-form module contains a method with a forbidden suffix.

### Edge Examples

- Comments: cleaned comments must not produce direct call facts.
- Strings: string literals must not produce direct call facts.
- Preprocessor: no special handling beyond visible cleaned lines.
- Multiline: only same-line direct call syntax is currently in scope.
- Nested constructs: calls inside conditionals/loops inside the forbidden event are checked the same way.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: form module procedures, compiler directives, and direct method call facts.
- Rules that may report on the same object/range: BSL245 checks server-side export form methods; BSL253 checks external resource timeout constructors; BSL261 checks `БезопасныйРежим()` boolean usage in the same runtime pool.
- Intended duplicate policy: BSL244 reports only the UI/server-call performance defect; other runtime diagnostics may report separate facts on nearby lines.
- Cluster fixture or probe: runtime tail tests in `tests/test_diagnostics_query_metadata.py`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch over 31,797 `.bsl` files, plus local synthetic form module fixtures.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS parity helper uses diagnostics `ONLY` mode.
- File-key normalization: original source-root relative keys for private batch aggregate.
- Coordinate normalization: BSLLS zero-based LSP ranges normalized to onec CLI one-based line and zero-based character coordinates.
- Exact common: final private parity has `32` exact common facts.
- Line/common semantic common: final private parity has `32` common line facts.
- Ours-only categories: final private parity has none.
- BSLLS-only categories: final private parity has none.
- Range-only categories: final private parity has none; BSL244 anchors the full direct call expression through the closing parenthesis.
- Known BSLLS defects: none established for this iteration.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: current module procedure list, local target-method directive lookup, and cleaned-line scan.
- Expected complexity: O(procedures + lines in form module).
- Shared-pass reuse: shares runtime-pool procedure and cleaned-line facts with BSL253 and BSL261.
- Before/after measurement, if changed: private parity over 31,797 files is the performance guard; final result is recorded in OACS evidence.

## Implementation Decision

- Change target: BSL244 branch in `query_metadata_rules.py`, public rule severity/docs, and synthetic coverage.
- Delta category fixed by this change: semantic mismatch from checking all `При*` / `On*` events and treating `&НаСервереБезКонтекста` as forbidden server calls.
- Why not another layer: existing runtime pool already has current form module procedures and cleaned lines; no metadata crawl or new parser is required.
- Rollback signal: `ПриОткрытии` server helper calls report, `НаСервереБезКонтекста` target calls report, forbidden event `ПриАктивизацииСтроки` no longer reports, or private parity worsens without a classified reason.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_query_metadata.py -q -k 'bsl244 or query_and_runtime_tail_pool' --no-cov`.
- Synthetic fixture/module: form module fixtures for forbidden events, non-forbidden events, server event methods, and server-no-context target methods.
- Related-rule tests: runtime tail pool for BSL244, BSL253, and BSL261.
- Parity command/artifact: preserved-source-root private batch BSL244 parity, counts only.
- Performance command/artifact: targeted pytest runtime and private parity runtime.
- Ruff/lint: `./.venv/bin/ruff check src/onec_hbk_bsl/analysis/diagnostic/rules/query_metadata_rules.py src/onec_hbk_bsl/analysis/diagnostics.py tests/test_diagnostics_query_metadata.py`.
- Leak check: `git diff --check` and changed-file private token scan.
- OACS evidence/checkpoint: ingest verification outputs and add checkpoint before claiming done.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.

</div>

</details>

<!-- engineering-contract:end -->
