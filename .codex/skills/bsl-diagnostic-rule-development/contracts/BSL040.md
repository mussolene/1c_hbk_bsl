# BSL040 Contract

## Rule Identity

- Code: BSL040
- BSLLS compatible name: UsingThisForm
- Public name/i18n key: BSL040 / UsingThisForm
- Default message/i18n key: get_rule("BSL040").message
- Description/i18n key: RULE_METADATA["BSL040"].description
- Severity: INFORMATION
- Parameters/defaults: none
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Managed UI code should not depend on the deprecated form-context property `ЭтаФорма` / `ThisForm`; pass the form explicitly when a helper method needs it.
- Why this diagnostic exists: It prevents hidden coupling to the current form object in form modules.
- Non-goals: Strings, comments, non-form modules, and local parameters named `ЭтаФорма` / `ThisForm` are not deprecated property access facts.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A real identifier token `ЭтаФорма` or `ThisForm` used in a full EDT form module when it is not a method parameter shadowing that name.
- Must diagnose when: A full form module body references `ЭтаФорма` or `ThisForm` as code.
- Must not diagnose when: The same text appears in comments, string literals, non-form modules, split module fragments, non-canonical side `.bsl` files below a form `Ext` directory, or a method that declares a parameter with the same name.
- Diagnostic anchor token/range: the identifier token `ЭтаФорма` / `ThisForm`.
- Quick-fix target, if any: none

## CST Or Domain Fact Model

- Parser/tree source: BSL document snapshot with tree-sitter procedure metadata plus path-derived module-kind context.
- CST/domain object types: identifier occurrence, `ProcInfo` for containing procedure, and canonical full form module path context.
- Extracted attributes: line index, LSP character range, containing procedure parameters, and canonical full-form-module/split-fragment classification.
- Parse-error behavior: skip malformed structural interpretation except for textual occurrences that remain in a tree-built snapshot; `BSL001` owns broken-document diagnostics.
- Textual/regex behavior, only if genuinely textual: token search is textual after strings and comments are masked because the rule detects a deprecated identifier spelling.

## Examples

### Invalid Examples

1. Full form module procedure calls `ЭтаФорма.Закрыть();`.
2. Full form module procedure passes `ThisForm` into a helper call.
3. Full form module function reads a property through `ЭтаФорма`.

### Valid Negative Twins

1. Common module code contains `ЭтаФорма.Закрыть();`.
2. Full form module has `Процедура Команда(ЭтаФорма)` and uses that parameter.
3. Full form module contains `"ЭтаФорма"` in a string literal or `// ЭтаФорма` in a comment.

### Edge Examples

- Comments: masked before fact creation.
- Strings: masked before fact creation.
- Preprocessor: not a special target; normal code lines are inspected after masking.
- Multiline: each identifier occurrence is anchored on its own line.
- Nested constructs: containing procedure parameter shadowing suppresses occurrences inside that procedure.
- BSL/SDBL variants, if relevant: not relevant.

## Related-Rule Cluster

- Shared semantic object: form module code identifier occurrence.
- Rules that may report on the same object/range: BSL017 can also use form-module path context but reports exported methods, not `ЭтаФорма`; BSL216 can report spacing on the same line but not the same semantic object.
- Intended duplicate policy: keep distinct diagnostics only when they describe different facts; do not duplicate `BSL040` for the same identifier occurrence.
- Cluster fixture or probe: tests/test_diagnostics_extended.py::TestBsl040UsingThisForm

## BSLLS Parity Taxonomy

- Compared input-file set: not compared in this iteration because no BSL040 rule logic changed.
- Config/exclude mode: not compared in this iteration.
- File-key normalization: use rule-contract parity procedure when a semantic BSL040 change is made.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters; BSLLS LSP lines must be converted before comparison.
- Exact common: not measured in this iteration.
- Line/common semantic common: not measured in this iteration.
- Ours-only categories: current selected CLI/API probe found additional `Forms/.../Ext/Module.bsl` hits; these are retained as semantically valid full form modules unless the product decision drops the legacy form-module layout.
- BSLLS-only categories: selected CLI comparison had fact-present entries hidden by file-set/config differences, not by BSL040 fact extraction.
- Range-only categories: not measured in this iteration.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached DocumentSnapshot line pass over `this_form_usage_facts`.
- Expected complexity: O(lines + occurrences) for the document after snapshot construction.
- Shared-pass reuse: shares form-module path classification with the core snapshot fact batch.
- Before/after measurement, if changed: no BSL040 semantic traversal changed in this iteration.

## Implementation Decision

- Change target: canonical form-module path classification shared by BSL040 and adjacent form-module rules.
- Delta category fixed by this change: semantic-mismatch for split/non-canonical side `.bsl` files under form `Ext` directories.
- Why not another layer: BSL040 fact creation must know whether a file is a full form module before identifier occurrences are emitted.
- Rollback signal: split form fragments or arbitrary side `.bsl` files below `Forms/.../Ext` are again treated as full form modules.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py::TestBsl040UsingThisForm tests/test_document_snapshot.py::test_line_diagnostic_fact_has_no_user_message_payload tests/test_diagnostic_rule_matrix.py::test_rule_matrix_maps_core_snapshot_fact_rules -q --no-cov`
- Synthetic fixture/module: inline synthetic modules in `TestBsl040UsingThisForm`.
- Related-rule tests: core snapshot batch tests in `tests/test_diagnostics_extended.py`.
- Parity command/artifact: local sanitized probe only; public reporting uses aggregate counts without source paths or text.
- Performance command/artifact: no dedicated performance artifact; the invariant removes per-fact message payload and keeps the same traversal.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
