# BSL011 Contract

## Rule Identity

- Code: BSL011
- BSLLS compatible name: CognitiveComplexity
- Public name/i18n key: BSL011 / CognitiveComplexity
- Default message/i18n key: get_rule("BSL011").message
- Description/i18n key: RULE_METADATA["BSL011"].description
- Severity: WARNING
- Parameters/defaults: max_cognitive_complexity
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: Procedures and functions should stay readable enough that nested control flow and boolean logic can be reviewed safely.
- Why this diagnostic exists: It flags methods whose cognitive complexity exceeds the configured threshold.
- Non-goals: Comments, strings, query text, and methods or module-body blocks below or equal to the threshold.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A procedure, function, or module-body code block has cognitive complexity greater than `max_cognitive_complexity`.
- Must diagnose when: The computed cognitive metric for a method or module-body block exceeds the configured threshold.
- Must not diagnose when: The method or module-body block is at or below the threshold, or the apparent control-flow tokens appear only in comments, strings, or query text.
- Diagnostic anchor token/range: method name range for methods; first token of the module-body code block for module-level code.
- Quick-fix target, if any: none

## CST Or Domain Fact Model

- Parser/tree source: BSL `DocumentSnapshot.procedures`, cached `complexity_metrics_for_procs`, and `module_body_cognitive_complexity_facts`.
- CST/domain object types: `ProcInfo`, method body lines, module-body line ranges, control-flow and boolean operator tokens.
- Extracted attributes: method start line, header column, end line, module-body anchor token range, nesting level, boolean operators, ternary operations, and branch tokens.
- Parse-error behavior: skip missing procedure model facts; `BSL001` owns broken-document reporting.
- Textual/regex behavior, only if genuinely textual: lexical counting is used inside method body lines after comments and strings are masked.

## Examples

### Invalid Examples

1. A method with nested `Если` branches above the threshold.
2. A method with multiple loop and conditional branches above the threshold.
3. A method combining nested branches and boolean chains above the threshold.
4. A module-body code block with nested branches above the threshold.

### Valid Negative Twins

1. A flat method with one simple condition below the threshold.
2. A method whose branch-like words appear only in a string.
3. A method whose branch-like words appear only in comments.
4. A module-body comment whose branch-like words appear only in comments.

### Edge Examples

- Comments: excluded from metric counting.
- Strings: masked before token counting.
- Preprocessor: not a separate target; visible method and module-body lines are counted.
- Multiline: continued boolean expressions contribute to the same method or module-body metric.
- Nested constructs: nesting increases cognitive complexity.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: procedure/function body and module-body code block.
- Rules that may report on the same object/range: BSL019, BSL036, BSL148, BSL062, and method-contract rules can report nearby method facts.
- Intended duplicate policy: keep BSL011 distinct from BSL019 and BSL036 because it measures cognitive complexity, not cyclomatic count or one condition expression.
- Cluster fixture or probe: tests/test_diagnostics_extended.py::TestBsl011CognitiveComplexity

## BSLLS Parity Taxonomy

- Compared input-file set: local synthetic BSL011 taxonomy plus original-workspace batch aggregate.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS default threshold and `checkModuleBody=true`.
- File-key normalization: synthetic labels for local probe; original source-root relative keys for private batch aggregate.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: synthetic high-complexity method and low-complexity method matched BSLLS exactly; private batch exact common remains 1274.
- Line/common semantic common: method-level sampled cases match.
- Ours-only categories: none reproduced in the sampled synthetic taxonomy.
- BSLLS-only categories: sampled complex module-body code blocks were BSLLS-only before this iteration; onec now reports module-body cognitive complexity.
- Range-only categories: none reproduced in the sampled synthetic taxonomy.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `complexity_metrics_for_procs` plus cached module-body cognitive facts.
- Expected complexity: O(source lines).
- Shared-pass reuse: method metrics remain shared with BSL019; module-body facts reuse the same line-level calculator.
- Before/after measurement, if changed: module-body scan adds O(non-method lines) for BSL011 only.

## Implementation Decision

- Change target: `DocumentSnapshot.module_body_cognitive_complexity_facts`, BSL011 runtime emission, contract, and synthetic coverage.
- Delta category fixed by this change: BSLLS-only semantic/file-layout delta for complex module-body code blocks.
- Why not another layer: module-body complexity uses the same line-level cognitive calculator as methods and keeps BSL019 method metric sharing intact.
- Rollback signal: contract validator fails, method BSL011/BSL019 metric sharing breaks, or module-body facts duplicate per procedure.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k bsl011 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL011 tests, including module-body positive and comment negative twins.
- Related-rule tests: BSL019 and BSL036 targeted tests.
- Parity command/artifact: deferred until a BSL011 semantic rule change is made.
- Performance command/artifact: no dedicated performance artifact; traversal source is unchanged.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
