# BSL181 DuplicatedInsertionIntoCollection Contract

## Rule Identity

- Code: BSL181
- BSLLS compatible name: DuplicatedInsertionIntoCollection
- Public name/i18n key: BSL181 / DuplicatedInsertionIntoCollection
- Default message/i18n key: RULE_MESSAGES_RU["BSL181"]
- Description/i18n key: RULE_METADATA["BSL181"].description
- Severity: WARNING
- Parameters/defaults: BSLLS has `isAllowedMethodADD=true`; onec supports `Добавить/Add` and `Вставить/Insert` literal-like facts.
- Rule status: verified

## Standard And Product Decision

- Standard or product policy: inserting or adding the same key/value to the same collection in one straight-line construction block is suspicious and often overwrites or duplicates configuration data.
- Why this diagnostic exists: duplicate collection population statements are usually copy-paste errors.
- Non-goals: full dataflow proving for arbitrary variables, branch-sensitive equivalence, values rebuilt through mutable temporary objects, and exact semantic equality of arbitrary expressions.
- Keep/drop decision: keep as a BSLLS-compatible correctness diagnostic with a local noise guard for mutable/builder patterns.

## Semantic Oracle

- Checked language fact: the same collection target receives repeated literal-like insertion facts in one method body.
- Must diagnose when: a second straight-line `Target.Вставить/Insert(firstArg, ...)` or `Target.Добавить/Add(firstArg, ...)` repeats a normalized supported first argument for the same target.
- Must not diagnose when: the target is reassigned before the repeated insertion, the argument variable is reassigned before repeated `Add`, the duplicate is inside skipped control-flow depth, or the argument is an unsupported arbitrary expression.
- Diagnostic anchor token/range: the duplicate insertion/addition call from target name through the outer call closing parenthesis.
- Quick-fix target, if any: remove the duplicate insertion or change the duplicated key/value.

## CST Or Domain Fact Model

- Parser/tree source: procedure-domain line scan over comment-stripped lines.
- CST/domain object types: procedure body lines, collection insertion facts, target assignment reset facts.
- Extracted attributes: target name, method name, first argument text, normalized argument key, call range, target reassignment, argument-variable reassignment, and shallow control-flow depth.
- Parse-error behavior: broken code should not be aggressively diagnosed; BSL001 owns malformed syntax.
- Textual/regex behavior, only if genuinely textual: line scanning is used because the fact is a small exact method-call surface and must tolerate split-module fragments.

## Examples

### Invalid Examples

1. `Коллекция.Вставить("Ключ");` repeated later in the same straight-line block.
2. `Массив.Добавить("Значение");` repeated later in the same straight-line block.
3. `Карта.Вставить(Префикс + ".Поле", Значение);` repeated with the same normalized first argument.

### Valid Negative Twins

1. `Коллекция = Новый Структура;` between two same-key insertions resets collection facts.
2. `Условие = Новый Структура; Результат.Добавить(Условие);` repeated after reassigning `Условие` does not report.
3. Duplicate-looking calls inside a skipped branch/control depth are not reported by onec to avoid branch noise.

### Edge Examples

- Comments: ignored by the comment-stripped line source.
- Strings: quoted first arguments are preserved and normalized.
- Preprocessor: not a separate semantic target.
- Multiline: current extractor is line-local.
- Nested constructs: nested calls in the first argument are supported for `КодСимвола/CharCode` and string-containing expressions.
- BSL/SDBL variants, if relevant: query text is outside this rule.

## Related-Rule Cluster

- Shared semantic object: collection mutation statements.
- Rules that may report on the same object/range: BSL173 reports deleting collection items while iterating; BSL186 reports extra commas in collection-like syntax; BSL223 reports nested structure constructors.
- Intended duplicate policy: BSL181 owns repeated insertion/addition facts only.
- Cluster fixture or probe: `tests/test_diagnostics_extended.py::TestTailParityBatches`.

## BSLLS Parity Taxonomy

- Compared input-file set: preserved-source-root private batch, 32145 files.
- Config/exclude mode: project config disabled for onec raw rule semantics.
- File-key normalization: original source-root relative keys.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: 26.
- Line/common semantic common: 26 exact reports.
- Ours-only categories: 32 straight-line repeated literal/string-expression insert/add facts that onec intentionally keeps as useful signals.
- BSLLS-only categories: 4 Add facts inside control-flow depth that onec intentionally suppresses to avoid branch noise.
- Range-only categories: resolved for nested second-argument calls by anchoring through the outer call closing parenthesis.
- Known BSLLS defects: none recorded.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: one pass over procedure body lines in the existing light pool.
- Expected complexity: O(total procedure source characters).
- Shared-pass reuse: stays in the existing BSL169/BSL170/BSL181/BSL182/BSL196/BSL260 domain pass.
- Before/after measurement, if changed: private batch after fix reports 58 onec / 30 BSLLS with all deltas classified.

## Implementation Decision

- Change target: BSL181 fact extraction in `ModuleModel`, synthetic tests, contract, and audit state.
- Delta category fixed by this change: missing default `Добавить/Add` support, target/argument reset noise, and nested-call range mismatch.
- Why not another layer: a full CST/dataflow pass would be heavier and still need project-specific noise policy for mutable builder variables.
- Rollback signal: `Добавить("literal")` duplicates stop reporting, target reassignment reports, nested second-argument calls get truncated ranges, or arbitrary variable builder patterns explode in volume.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py::TestTailParityBatches::test_bsl181_duplicate_insert_add_and_expression tests/test_diagnostics_extended.py::TestTailParityBatches::test_bsl181_reassignment_resets_collection_facts tests/test_diagnostics_extended.py::TestTailParityBatches::test_compilation_and_name_tail_pool -q --no-cov`.
- Synthetic fixture/module: temporary BSLLS probe covering repeated Insert, Add, CharCode, and reset negatives.
- Related-rule tests: tail light-pool test covers BSL169/BSL181/BSL182/BSL196 coexistence.
- Parity command/artifact: preserved-source-root selected BSL181 private parity; sanitized aggregate only.
- Performance command/artifact: targeted pytest runtime and parity wall time.
- Ruff/lint: `./.venv/bin/python -m ruff check src/onec_hbk_bsl/analysis/diagnostic/domain/module_model.py tests/test_diagnostics_extended.py`.
- Leak check: `git diff --check` and changed-lines private marker scan.
- OACS evidence/checkpoint: required before commit.

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
