# BSL131 Contract

## Rule Identity

- Code: BSL131
- BSLLS compatible name: DuplicateRegion
- Public name/i18n key: BSL131 / DuplicateRegion
- Default message/i18n key: get_rule("BSL131").message
- Description/i18n key: RULE_METADATA["BSL131"].description
- Severity: INFORMATION
- Parameters/defaults: normalized module-level region name
- Rule status: contracted

## Standard And Product Decision

- Standard or product policy: A module should not contain duplicate module-level regions with the same normalized name.
- Why this diagnostic exists: It keeps module structure unambiguous and avoids scattering the same logical section.
- Non-goals: Unique region names and nested regions with separate scopes.
- Keep/drop decision: keep

## Semantic Oracle

- Checked language fact: A module-level region name repeats after normalization.
- Must diagnose when: A module-level `RegionInfo` belongs to a normalized region-name group with more than one module-level occurrence.
- Must not diagnose when: Region names are unique after normalization or the region is not module-level.
- Diagnostic anchor token/range: the first repeated region group's opening directive line, from after `#` through the region name.
- Quick-fix target, if any: merge or rename duplicate regions manually.

## CST Or Domain Fact Model

- Parser/tree source: `DocumentSnapshot.regions` and `duplicate_region_facts`.
- CST/domain object types: `RegionInfo`, module-level region list, normalized region key.
- Extracted attributes: region name, normalized key, first group start line, opening directive range.
- Parse-error behavior: skip missing region model facts; `BSL001` owns broken-document reporting.
- Textual/regex behavior, only if genuinely textual: region name normalization is textual.

## Examples

### Invalid Examples

1. Two module-level regions named the same.
2. Two module-level regions differing only by normalizable formatting.
3. A third repeated module-level region after an earlier duplicate.

### Valid Negative Twins

1. Two differently named module-level regions.
2. A nested region with the same name in a different scope.
3. Region names that normalize to distinct keys.

### Edge Examples

- Comments: comments inside regions do not affect names.
- Strings: strings are not region directives.
- Preprocessor: not a separate target.
- Multiline: region directives are single-line facts.
- Nested constructs: only module-level duplicate names are checked.
- BSL/SDBL variants, if relevant: SDBL is not relevant.

## Related-Rule Cluster

- Shared semantic object: module region directive/name.
- Rules that may report on the same object/range: BSL016 and BSL026.
- Intended duplicate policy: keep duplicate-name diagnostics distinct from empty or non-standard region diagnostics.
- Cluster fixture or probe: tests/test_diagnostics_extended.py::TestBsl131DuplicateRegion

## BSLLS Parity Taxonomy

- Compared input-file set: local synthetic BSL131 taxonomy plus preserved-source-root BSLLS 1.0 private batch over 31,797 files.
- Config/exclude mode: project config disabled for onec raw rule semantics; BSLLS default behavior.
- File-key normalization: synthetic labels for local probe; original source-root relative keys for private batch aggregate.
- Coordinate normalization: one-based diagnostic lines and zero-based LSP characters.
- Exact common: sampled plain duplicate, standard-alias duplicate, third duplicate, unique, and nested duplicate cases match BSLLS exactly; current preserved-source-root BSLLS 1.0 private batch is exact at onec 6, BSLLS 6.
- Line/common semantic common: duplicate module-level region group semantics match.
- Ours-only categories: none in the current preserved-source-root private batch.
- BSLLS-only categories: none in the current preserved-source-root private batch.
- Range-only categories: closed. Before the representative-fact iteration onec reported later duplicate openings with whole-line ranges; BSLLS reports the first region opening once per duplicate group, from after `#` through the region name.
- Known BSLLS defects: none recorded for this contract.
- Unknown categories: none

## Performance Contract

- Traversal/fact source: cached `duplicate_region_facts`.
- Expected complexity: O(module-level regions).
- Shared-pass reuse: shares `DocumentSnapshot.regions` with BSL016 and BSL026.
- Before/after measurement, if changed: no traversal changed in this iteration.

## Implementation Decision

- Change target: `DocumentSnapshot.duplicate_region_facts`, BSL131 contract, and synthetic coverage.
- Delta category fixed by this change: semantic/range anchor delta for duplicate region groups.
- Why not another layer: duplicate region names are already available through region facts; the fact extractor owns the chosen representative region and range.
- Rollback signal: unique regions report, third duplicates produce multiple reports, or contract validation fails.

## Verification Plan

- Targeted tests: `./.venv/bin/python -m pytest tests/test_diagnostics_extended.py -q -k bsl131 --no-cov`
- Synthetic fixture/module: inline synthetic modules in BSL131 tests.
- Related-rule tests: BSL016 and BSL026 targeted tests.
- Parity command/artifact: sampled local synthetic taxonomy against BSLLS 0.29.0; private batch parity remains aggregate-only.
- Performance command/artifact: no dedicated performance artifact.
- Ruff/lint: `./.venv/bin/python -m ruff check .`
- Leak check: scan changed files for private paths, URLs, domains, and corpus strings.
- OACS evidence/checkpoint: required

## Completion Checklist

- [x] Semantic oracle is explicit.
- [x] CST/domain fact model is explicit.
- [x] At least three invalid examples exist.
- [x] At least three valid negative twins exist.
- [x] Related-rule cluster is reviewed.
- [x] BSLLS deltas are classified or parity is explicitly deferred.
- [x] Unknown high-volume delta categories are zero.
- [x] Implementation target and fixed delta category are named.
- [x] Verification plan covers tests, lint, leak check, evidence, and checkpoint.
