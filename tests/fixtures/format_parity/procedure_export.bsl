// Fixture: header + export (BSLLS does not normalize // comments; we ignore comment text in parity script)
Процедура Пример(Параметр = 0) Экспорт
    Если Параметр = 0 Тогда
        Возврат;
    КонецЕсли;
    //  spaced comment
    А = 1 + 2;
КонецПроцедуры
