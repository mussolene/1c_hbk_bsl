// Синтетический BSL файл для бенчмарков производительности
// Целевой размер: 5000 строк
// Содержит: процедуры, Перем-объявления, циклы, условия, регионы

#Область ОбщиеПеременные1

Процедура ОбработатьСтроку0(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем0;
	Перем ТекущаяСтрока0;
	Перем ИндексЭлемента0;
	Перем КоличествоСтрок0;

	ТекущаяСтрока0 = 0;
	ИндексЭлемента0 = 1;
	КоличествоСтрок0 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока0 = ТекущаяСтрока0 + Индекс;
		Если ТекущаяСтрока0 > 50 Тогда
			ИндексЭлемента0 = ТекущаяСтрока0;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока0 = ТекущаяСтрока0 + 0;
	ИндексЭлемента0 = ИндексЭлемента0 + 1;
	КоличествоСтрок0 = КоличествоСтрок0 + 2;
	ТекущаяСтрока0 = ТекущаяСтрока0 + 3;
	ИндексЭлемента0 = ИндексЭлемента0 + 4;
	КоличествоСтрок0 = КоличествоСтрок0 + 5;
	ТекущаяСтрока0 = ТекущаяСтрока0 + 6;
	ИндексЭлемента0 = ИндексЭлемента0 + 7;
	КоличествоСтрок0 = КоличествоСтрок0 + 8;

КонецПроцедуры

Процедура ВычислитьСумму1(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем1;
	Перем ТекущаяСтрока1;
	Перем ИндексЭлемента1;
	Перем КоличествоСтрок1;

	ТекущаяСтрока1 = 0;
	ИндексЭлемента1 = 1;
	КоличествоСтрок1 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока1 = ТекущаяСтрока1 + Индекс;
		Если ТекущаяСтрока1 > 50 Тогда
			ИндексЭлемента1 = ТекущаяСтрока1;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока1 = ТекущаяСтрока1 + 0;
	ИндексЭлемента1 = ИндексЭлемента1 + 1;
	КоличествоСтрок1 = КоличествоСтрок1 + 2;
	ТекущаяСтрока1 = ТекущаяСтрока1 + 3;
	ИндексЭлемента1 = ИндексЭлемента1 + 4;
	КоличествоСтрок1 = КоличествоСтрок1 + 5;
	ТекущаяСтрока1 = ТекущаяСтрока1 + 6;
	ИндексЭлемента1 = ИндексЭлемента1 + 7;
	КоличествоСтрок1 = КоличествоСтрок1 + 8;

КонецПроцедуры

Процедура ПроверитьДанные2(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем2;
	Перем ТекущаяСтрока2;
	Перем ИндексЭлемента2;
	Перем КоличествоСтрок2;

	ТекущаяСтрока2 = 0;
	ИндексЭлемента2 = 1;
	КоличествоСтрок2 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока2 = ТекущаяСтрока2 + Индекс;
		Если ТекущаяСтрока2 > 50 Тогда
			ИндексЭлемента2 = ТекущаяСтрока2;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока2 = ТекущаяСтрока2 + 0;
	ИндексЭлемента2 = ИндексЭлемента2 + 1;
	КоличествоСтрок2 = КоличествоСтрок2 + 2;
	ТекущаяСтрока2 = ТекущаяСтрока2 + 3;
	ИндексЭлемента2 = ИндексЭлемента2 + 4;
	КоличествоСтрок2 = КоличествоСтрок2 + 5;
	ТекущаяСтрока2 = ТекущаяСтрока2 + 6;
	ИндексЭлемента2 = ИндексЭлемента2 + 7;
	КоличествоСтрок2 = КоличествоСтрок2 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал3(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем3;
	Перем ТекущаяСтрока3;
	Перем ИндексЭлемента3;
	Перем КоличествоСтрок3;

	ТекущаяСтрока3 = 0;
	ИндексЭлемента3 = 1;
	КоличествоСтрок3 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока3 = ТекущаяСтрока3 + Индекс;
		Если ТекущаяСтрока3 > 50 Тогда
			ИндексЭлемента3 = ТекущаяСтрока3;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока3 = ТекущаяСтрока3 + 0;
	ИндексЭлемента3 = ИндексЭлемента3 + 1;
	КоличествоСтрок3 = КоличествоСтрок3 + 2;
	ТекущаяСтрока3 = ТекущаяСтрока3 + 3;
	ИндексЭлемента3 = ИндексЭлемента3 + 4;
	КоличествоСтрок3 = КоличествоСтрок3 + 5;
	ТекущаяСтрока3 = ТекущаяСтрока3 + 6;
	ИндексЭлемента3 = ИндексЭлемента3 + 7;
	КоличествоСтрок3 = КоличествоСтрок3 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры2

Процедура ИнициализироватьМодуль4(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем4;
	Перем ТекущаяСтрока4;
	Перем ИндексЭлемента4;
	Перем КоличествоСтрок4;

	ТекущаяСтрока4 = 0;
	ИндексЭлемента4 = 1;
	КоличествоСтрок4 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока4 = ТекущаяСтрока4 + Индекс;
		Если ТекущаяСтрока4 > 50 Тогда
			ИндексЭлемента4 = ТекущаяСтрока4;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока4 = ТекущаяСтрока4 + 0;
	ИндексЭлемента4 = ИндексЭлемента4 + 1;
	КоличествоСтрок4 = КоличествоСтрок4 + 2;
	ТекущаяСтрока4 = ТекущаяСтрока4 + 3;
	ИндексЭлемента4 = ИндексЭлемента4 + 4;
	КоличествоСтрок4 = КоличествоСтрок4 + 5;
	ТекущаяСтрока4 = ТекущаяСтрока4 + 6;
	ИндексЭлемента4 = ИндексЭлемента4 + 7;
	КоличествоСтрок4 = КоличествоСтрок4 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер5(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем5;
	Перем ТекущаяСтрока5;
	Перем ИндексЭлемента5;
	Перем КоличествоСтрок5;

	ТекущаяСтрока5 = 0;
	ИндексЭлемента5 = 1;
	КоличествоСтрок5 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока5 = ТекущаяСтрока5 + Индекс;
		Если ТекущаяСтрока5 > 50 Тогда
			ИндексЭлемента5 = ТекущаяСтрока5;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока5 = ТекущаяСтрока5 + 0;
	ИндексЭлемента5 = ИндексЭлемента5 + 1;
	КоличествоСтрок5 = КоличествоСтрок5 + 2;
	ТекущаяСтрока5 = ТекущаяСтрока5 + 3;
	ИндексЭлемента5 = ИндексЭлемента5 + 4;
	КоличествоСтрок5 = КоличествоСтрок5 + 5;
	ТекущаяСтрока5 = ТекущаяСтрока5 + 6;
	ИндексЭлемента5 = ИндексЭлемента5 + 7;
	КоличествоСтрок5 = КоличествоСтрок5 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос6(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем6;
	Перем ТекущаяСтрока6;
	Перем ИндексЭлемента6;
	Перем КоличествоСтрок6;

	ТекущаяСтрока6 = 0;
	ИндексЭлемента6 = 1;
	КоличествоСтрок6 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока6 = ТекущаяСтрока6 + Индекс;
		Если ТекущаяСтрока6 > 50 Тогда
			ИндексЭлемента6 = ТекущаяСтрока6;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока6 = ТекущаяСтрока6 + 0;
	ИндексЭлемента6 = ИндексЭлемента6 + 1;
	КоличествоСтрок6 = КоличествоСтрок6 + 2;
	ТекущаяСтрока6 = ТекущаяСтрока6 + 3;
	ИндексЭлемента6 = ИндексЭлемента6 + 4;
	КоличествоСтрок6 = КоличествоСтрок6 + 5;
	ТекущаяСтрока6 = ТекущаяСтрока6 + 6;
	ИндексЭлемента6 = ИндексЭлемента6 + 7;
	КоличествоСтрок6 = КоличествоСтрок6 + 8;

КонецПроцедуры

Процедура СохранитьДанные7(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем7;
	Перем ТекущаяСтрока7;
	Перем ИндексЭлемента7;
	Перем КоличествоСтрок7;

	ТекущаяСтрока7 = 0;
	ИндексЭлемента7 = 1;
	КоличествоСтрок7 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока7 = ТекущаяСтрока7 + Индекс;
		Если ТекущаяСтрока7 > 50 Тогда
			ИндексЭлемента7 = ТекущаяСтрока7;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока7 = ТекущаяСтрока7 + 0;
	ИндексЭлемента7 = ИндексЭлемента7 + 1;
	КоличествоСтрок7 = КоличествоСтрок7 + 2;
	ТекущаяСтрока7 = ТекущаяСтрока7 + 3;
	ИндексЭлемента7 = ИндексЭлемента7 + 4;
	КоличествоСтрок7 = КоличествоСтрок7 + 5;
	ТекущаяСтрока7 = ТекущаяСтрока7 + 6;
	ИндексЭлемента7 = ИндексЭлемента7 + 7;
	КоличествоСтрок7 = КоличествоСтрок7 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды3

Процедура ЗагрузитьКонфигурацию8(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем8;
	Перем ТекущаяСтрока8;
	Перем ИндексЭлемента8;
	Перем КоличествоСтрок8;

	ТекущаяСтрока8 = 0;
	ИндексЭлемента8 = 1;
	КоличествоСтрок8 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока8 = ТекущаяСтрока8 + Индекс;
		Если ТекущаяСтрока8 > 50 Тогда
			ИндексЭлемента8 = ТекущаяСтрока8;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока8 = ТекущаяСтрока8 + 0;
	ИндексЭлемента8 = ИндексЭлемента8 + 1;
	КоличествоСтрок8 = КоличествоСтрок8 + 2;
	ТекущаяСтрока8 = ТекущаяСтрока8 + 3;
	ИндексЭлемента8 = ИндексЭлемента8 + 4;
	КоличествоСтрок8 = КоличествоСтрок8 + 5;
	ТекущаяСтрока8 = ТекущаяСтрока8 + 6;
	ИндексЭлемента8 = ИндексЭлемента8 + 7;
	КоличествоСтрок8 = КоличествоСтрок8 + 8;

КонецПроцедуры

Процедура ПроверитьПрава9(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем9;
	Перем ТекущаяСтрока9;
	Перем ИндексЭлемента9;
	Перем КоличествоСтрок9;

	ТекущаяСтрока9 = 0;
	ИндексЭлемента9 = 1;
	КоличествоСтрок9 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока9 = ТекущаяСтрока9 + Индекс;
		Если ТекущаяСтрока9 > 50 Тогда
			ИндексЭлемента9 = ТекущаяСтрока9;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока9 = ТекущаяСтрока9 + 0;
	ИндексЭлемента9 = ИндексЭлемента9 + 1;
	КоличествоСтрок9 = КоличествоСтрок9 + 2;
	ТекущаяСтрока9 = ТекущаяСтрока9 + 3;
	ИндексЭлемента9 = ИндексЭлемента9 + 4;
	КоличествоСтрок9 = КоличествоСтрок9 + 5;
	ТекущаяСтрока9 = ТекущаяСтрока9 + 6;
	ИндексЭлемента9 = ИндексЭлемента9 + 7;
	КоличествоСтрок9 = КоличествоСтрок9 + 8;

КонецПроцедуры

Процедура ОбновитьКэш10(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем10;
	Перем ТекущаяСтрока10;
	Перем ИндексЭлемента10;
	Перем КоличествоСтрок10;

	ТекущаяСтрока10 = 0;
	ИндексЭлемента10 = 1;
	КоличествоСтрок10 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока10 = ТекущаяСтрока10 + Индекс;
		Если ТекущаяСтрока10 > 50 Тогда
			ИндексЭлемента10 = ТекущаяСтрока10;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока10 = ТекущаяСтрока10 + 0;
	ИндексЭлемента10 = ИндексЭлемента10 + 1;
	КоличествоСтрок10 = КоличествоСтрок10 + 2;
	ТекущаяСтрока10 = ТекущаяСтрока10 + 3;
	ИндексЭлемента10 = ИндексЭлемента10 + 4;
	КоличествоСтрок10 = КоличествоСтрок10 + 5;
	ТекущаяСтрока10 = ТекущаяСтрока10 + 6;
	ИндексЭлемента10 = ИндексЭлемента10 + 7;
	КоличествоСтрок10 = КоличествоСтрок10 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление11(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем11;
	Перем ТекущаяСтрока11;
	Перем ИндексЭлемента11;
	Перем КоличествоСтрок11;

	ТекущаяСтрока11 = 0;
	ИндексЭлемента11 = 1;
	КоличествоСтрок11 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока11 = ТекущаяСтрока11 + Индекс;
		Если ТекущаяСтрока11 > 50 Тогда
			ИндексЭлемента11 = ТекущаяСтрока11;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока11 = ТекущаяСтрока11 + 0;
	ИндексЭлемента11 = ИндексЭлемента11 + 1;
	КоличествоСтрок11 = КоличествоСтрок11 + 2;
	ТекущаяСтрока11 = ТекущаяСтрока11 + 3;
	ИндексЭлемента11 = ИндексЭлемента11 + 4;
	КоличествоСтрок11 = КоличествоСтрок11 + 5;
	ТекущаяСтрока11 = ТекущаяСтрока11 + 6;
	ИндексЭлемента11 = ИндексЭлемента11 + 7;
	КоличествоСтрок11 = КоличествоСтрок11 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции4

Процедура ОбработатьСтроку12(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем12;
	Перем ТекущаяСтрока12;
	Перем ИндексЭлемента12;
	Перем КоличествоСтрок12;

	ТекущаяСтрока12 = 0;
	ИндексЭлемента12 = 1;
	КоличествоСтрок12 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока12 = ТекущаяСтрока12 + Индекс;
		Если ТекущаяСтрока12 > 50 Тогда
			ИндексЭлемента12 = ТекущаяСтрока12;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока12 = ТекущаяСтрока12 + 0;
	ИндексЭлемента12 = ИндексЭлемента12 + 1;
	КоличествоСтрок12 = КоличествоСтрок12 + 2;
	ТекущаяСтрока12 = ТекущаяСтрока12 + 3;
	ИндексЭлемента12 = ИндексЭлемента12 + 4;
	КоличествоСтрок12 = КоличествоСтрок12 + 5;
	ТекущаяСтрока12 = ТекущаяСтрока12 + 6;
	ИндексЭлемента12 = ИндексЭлемента12 + 7;
	КоличествоСтрок12 = КоличествоСтрок12 + 8;

КонецПроцедуры

Процедура ВычислитьСумму13(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем13;
	Перем ТекущаяСтрока13;
	Перем ИндексЭлемента13;
	Перем КоличествоСтрок13;

	ТекущаяСтрока13 = 0;
	ИндексЭлемента13 = 1;
	КоличествоСтрок13 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока13 = ТекущаяСтрока13 + Индекс;
		Если ТекущаяСтрока13 > 50 Тогда
			ИндексЭлемента13 = ТекущаяСтрока13;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока13 = ТекущаяСтрока13 + 0;
	ИндексЭлемента13 = ИндексЭлемента13 + 1;
	КоличествоСтрок13 = КоличествоСтрок13 + 2;
	ТекущаяСтрока13 = ТекущаяСтрока13 + 3;
	ИндексЭлемента13 = ИндексЭлемента13 + 4;
	КоличествоСтрок13 = КоличествоСтрок13 + 5;
	ТекущаяСтрока13 = ТекущаяСтрока13 + 6;
	ИндексЭлемента13 = ИндексЭлемента13 + 7;
	КоличествоСтрок13 = КоличествоСтрок13 + 8;

КонецПроцедуры

Процедура ПроверитьДанные14(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем14;
	Перем ТекущаяСтрока14;
	Перем ИндексЭлемента14;
	Перем КоличествоСтрок14;

	ТекущаяСтрока14 = 0;
	ИндексЭлемента14 = 1;
	КоличествоСтрок14 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока14 = ТекущаяСтрока14 + Индекс;
		Если ТекущаяСтрока14 > 50 Тогда
			ИндексЭлемента14 = ТекущаяСтрока14;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока14 = ТекущаяСтрока14 + 0;
	ИндексЭлемента14 = ИндексЭлемента14 + 1;
	КоличествоСтрок14 = КоличествоСтрок14 + 2;
	ТекущаяСтрока14 = ТекущаяСтрока14 + 3;
	ИндексЭлемента14 = ИндексЭлемента14 + 4;
	КоличествоСтрок14 = КоличествоСтрок14 + 5;
	ТекущаяСтрока14 = ТекущаяСтрока14 + 6;
	ИндексЭлемента14 = ИндексЭлемента14 + 7;
	КоличествоСтрок14 = КоличествоСтрок14 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал15(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем15;
	Перем ТекущаяСтрока15;
	Перем ИндексЭлемента15;
	Перем КоличествоСтрок15;

	ТекущаяСтрока15 = 0;
	ИндексЭлемента15 = 1;
	КоличествоСтрок15 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока15 = ТекущаяСтрока15 + Индекс;
		Если ТекущаяСтрока15 > 50 Тогда
			ИндексЭлемента15 = ТекущаяСтрока15;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока15 = ТекущаяСтрока15 + 0;
	ИндексЭлемента15 = ИндексЭлемента15 + 1;
	КоличествоСтрок15 = КоличествоСтрок15 + 2;
	ТекущаяСтрока15 = ТекущаяСтрока15 + 3;
	ИндексЭлемента15 = ИндексЭлемента15 + 4;
	КоличествоСтрок15 = КоличествоСтрок15 + 5;
	ТекущаяСтрока15 = ТекущаяСтрока15 + 6;
	ИндексЭлемента15 = ИндексЭлемента15 + 7;
	КоличествоСтрок15 = КоличествоСтрок15 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий5

Процедура ИнициализироватьМодуль16(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем16;
	Перем ТекущаяСтрока16;
	Перем ИндексЭлемента16;
	Перем КоличествоСтрок16;

	ТекущаяСтрока16 = 0;
	ИндексЭлемента16 = 1;
	КоличествоСтрок16 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока16 = ТекущаяСтрока16 + Индекс;
		Если ТекущаяСтрока16 > 50 Тогда
			ИндексЭлемента16 = ТекущаяСтрока16;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока16 = ТекущаяСтрока16 + 0;
	ИндексЭлемента16 = ИндексЭлемента16 + 1;
	КоличествоСтрок16 = КоличествоСтрок16 + 2;
	ТекущаяСтрока16 = ТекущаяСтрока16 + 3;
	ИндексЭлемента16 = ИндексЭлемента16 + 4;
	КоличествоСтрок16 = КоличествоСтрок16 + 5;
	ТекущаяСтрока16 = ТекущаяСтрока16 + 6;
	ИндексЭлемента16 = ИндексЭлемента16 + 7;
	КоличествоСтрок16 = КоличествоСтрок16 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер17(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем17;
	Перем ТекущаяСтрока17;
	Перем ИндексЭлемента17;
	Перем КоличествоСтрок17;

	ТекущаяСтрока17 = 0;
	ИндексЭлемента17 = 1;
	КоличествоСтрок17 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока17 = ТекущаяСтрока17 + Индекс;
		Если ТекущаяСтрока17 > 50 Тогда
			ИндексЭлемента17 = ТекущаяСтрока17;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока17 = ТекущаяСтрока17 + 0;
	ИндексЭлемента17 = ИндексЭлемента17 + 1;
	КоличествоСтрок17 = КоличествоСтрок17 + 2;
	ТекущаяСтрока17 = ТекущаяСтрока17 + 3;
	ИндексЭлемента17 = ИндексЭлемента17 + 4;
	КоличествоСтрок17 = КоличествоСтрок17 + 5;
	ТекущаяСтрока17 = ТекущаяСтрока17 + 6;
	ИндексЭлемента17 = ИндексЭлемента17 + 7;
	КоличествоСтрок17 = КоличествоСтрок17 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос18(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем18;
	Перем ТекущаяСтрока18;
	Перем ИндексЭлемента18;
	Перем КоличествоСтрок18;

	ТекущаяСтрока18 = 0;
	ИндексЭлемента18 = 1;
	КоличествоСтрок18 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока18 = ТекущаяСтрока18 + Индекс;
		Если ТекущаяСтрока18 > 50 Тогда
			ИндексЭлемента18 = ТекущаяСтрока18;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока18 = ТекущаяСтрока18 + 0;
	ИндексЭлемента18 = ИндексЭлемента18 + 1;
	КоличествоСтрок18 = КоличествоСтрок18 + 2;
	ТекущаяСтрока18 = ТекущаяСтрока18 + 3;
	ИндексЭлемента18 = ИндексЭлемента18 + 4;
	КоличествоСтрок18 = КоличествоСтрок18 + 5;
	ТекущаяСтрока18 = ТекущаяСтрока18 + 6;
	ИндексЭлемента18 = ИндексЭлемента18 + 7;
	КоличествоСтрок18 = КоличествоСтрок18 + 8;

КонецПроцедуры

Процедура СохранитьДанные19(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем19;
	Перем ТекущаяСтрока19;
	Перем ИндексЭлемента19;
	Перем КоличествоСтрок19;

	ТекущаяСтрока19 = 0;
	ИндексЭлемента19 = 1;
	КоличествоСтрок19 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока19 = ТекущаяСтрока19 + Индекс;
		Если ТекущаяСтрока19 > 50 Тогда
			ИндексЭлемента19 = ТекущаяСтрока19;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока19 = ТекущаяСтрока19 + 0;
	ИндексЭлемента19 = ИндексЭлемента19 + 1;
	КоличествоСтрок19 = КоличествоСтрок19 + 2;
	ТекущаяСтрока19 = ТекущаяСтрока19 + 3;
	ИндексЭлемента19 = ИндексЭлемента19 + 4;
	КоличествоСтрок19 = КоличествоСтрок19 + 5;
	ТекущаяСтрока19 = ТекущаяСтрока19 + 6;
	ИндексЭлемента19 = ИндексЭлемента19 + 7;
	КоличествоСтрок19 = КоличествоСтрок19 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции6

Процедура ЗагрузитьКонфигурацию20(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем20;
	Перем ТекущаяСтрока20;
	Перем ИндексЭлемента20;
	Перем КоличествоСтрок20;

	ТекущаяСтрока20 = 0;
	ИндексЭлемента20 = 1;
	КоличествоСтрок20 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока20 = ТекущаяСтрока20 + Индекс;
		Если ТекущаяСтрока20 > 50 Тогда
			ИндексЭлемента20 = ТекущаяСтрока20;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока20 = ТекущаяСтрока20 + 0;
	ИндексЭлемента20 = ИндексЭлемента20 + 1;
	КоличествоСтрок20 = КоличествоСтрок20 + 2;
	ТекущаяСтрока20 = ТекущаяСтрока20 + 3;
	ИндексЭлемента20 = ИндексЭлемента20 + 4;
	КоличествоСтрок20 = КоличествоСтрок20 + 5;
	ТекущаяСтрока20 = ТекущаяСтрока20 + 6;
	ИндексЭлемента20 = ИндексЭлемента20 + 7;
	КоличествоСтрок20 = КоличествоСтрок20 + 8;

КонецПроцедуры

Процедура ПроверитьПрава21(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем21;
	Перем ТекущаяСтрока21;
	Перем ИндексЭлемента21;
	Перем КоличествоСтрок21;

	ТекущаяСтрока21 = 0;
	ИндексЭлемента21 = 1;
	КоличествоСтрок21 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока21 = ТекущаяСтрока21 + Индекс;
		Если ТекущаяСтрока21 > 50 Тогда
			ИндексЭлемента21 = ТекущаяСтрока21;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока21 = ТекущаяСтрока21 + 0;
	ИндексЭлемента21 = ИндексЭлемента21 + 1;
	КоличествоСтрок21 = КоличествоСтрок21 + 2;
	ТекущаяСтрока21 = ТекущаяСтрока21 + 3;
	ИндексЭлемента21 = ИндексЭлемента21 + 4;
	КоличествоСтрок21 = КоличествоСтрок21 + 5;
	ТекущаяСтрока21 = ТекущаяСтрока21 + 6;
	ИндексЭлемента21 = ИндексЭлемента21 + 7;
	КоличествоСтрок21 = КоличествоСтрок21 + 8;

КонецПроцедуры

Процедура ОбновитьКэш22(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем22;
	Перем ТекущаяСтрока22;
	Перем ИндексЭлемента22;
	Перем КоличествоСтрок22;

	ТекущаяСтрока22 = 0;
	ИндексЭлемента22 = 1;
	КоличествоСтрок22 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока22 = ТекущаяСтрока22 + Индекс;
		Если ТекущаяСтрока22 > 50 Тогда
			ИндексЭлемента22 = ТекущаяСтрока22;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока22 = ТекущаяСтрока22 + 0;
	ИндексЭлемента22 = ИндексЭлемента22 + 1;
	КоличествоСтрок22 = КоличествоСтрок22 + 2;
	ТекущаяСтрока22 = ТекущаяСтрока22 + 3;
	ИндексЭлемента22 = ИндексЭлемента22 + 4;
	КоличествоСтрок22 = КоличествоСтрок22 + 5;
	ТекущаяСтрока22 = ТекущаяСтрока22 + 6;
	ИндексЭлемента22 = ИндексЭлемента22 + 7;
	КоличествоСтрок22 = КоличествоСтрок22 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление23(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем23;
	Перем ТекущаяСтрока23;
	Перем ИндексЭлемента23;
	Перем КоличествоСтрок23;

	ТекущаяСтрока23 = 0;
	ИндексЭлемента23 = 1;
	КоличествоСтрок23 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока23 = ТекущаяСтрока23 + Индекс;
		Если ТекущаяСтрока23 > 50 Тогда
			ИндексЭлемента23 = ТекущаяСтрока23;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока23 = ТекущаяСтрока23 + 0;
	ИндексЭлемента23 = ИндексЭлемента23 + 1;
	КоличествоСтрок23 = КоличествоСтрок23 + 2;
	ТекущаяСтрока23 = ТекущаяСтрока23 + 3;
	ИндексЭлемента23 = ИндексЭлемента23 + 4;
	КоличествоСтрок23 = КоличествоСтрок23 + 5;
	ТекущаяСтрока23 = ТекущаяСтрока23 + 6;
	ИндексЭлемента23 = ИндексЭлемента23 + 7;
	КоличествоСтрок23 = КоличествоСтрок23 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные7

Процедура ОбработатьСтроку24(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем24;
	Перем ТекущаяСтрока24;
	Перем ИндексЭлемента24;
	Перем КоличествоСтрок24;

	ТекущаяСтрока24 = 0;
	ИндексЭлемента24 = 1;
	КоличествоСтрок24 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока24 = ТекущаяСтрока24 + Индекс;
		Если ТекущаяСтрока24 > 50 Тогда
			ИндексЭлемента24 = ТекущаяСтрока24;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока24 = ТекущаяСтрока24 + 0;
	ИндексЭлемента24 = ИндексЭлемента24 + 1;
	КоличествоСтрок24 = КоличествоСтрок24 + 2;
	ТекущаяСтрока24 = ТекущаяСтрока24 + 3;
	ИндексЭлемента24 = ИндексЭлемента24 + 4;
	КоличествоСтрок24 = КоличествоСтрок24 + 5;
	ТекущаяСтрока24 = ТекущаяСтрока24 + 6;
	ИндексЭлемента24 = ИндексЭлемента24 + 7;
	КоличествоСтрок24 = КоличествоСтрок24 + 8;

КонецПроцедуры

Процедура ВычислитьСумму25(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем25;
	Перем ТекущаяСтрока25;
	Перем ИндексЭлемента25;
	Перем КоличествоСтрок25;

	ТекущаяСтрока25 = 0;
	ИндексЭлемента25 = 1;
	КоличествоСтрок25 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока25 = ТекущаяСтрока25 + Индекс;
		Если ТекущаяСтрока25 > 50 Тогда
			ИндексЭлемента25 = ТекущаяСтрока25;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока25 = ТекущаяСтрока25 + 0;
	ИндексЭлемента25 = ИндексЭлемента25 + 1;
	КоличествоСтрок25 = КоличествоСтрок25 + 2;
	ТекущаяСтрока25 = ТекущаяСтрока25 + 3;
	ИндексЭлемента25 = ИндексЭлемента25 + 4;
	КоличествоСтрок25 = КоличествоСтрок25 + 5;
	ТекущаяСтрока25 = ТекущаяСтрока25 + 6;
	ИндексЭлемента25 = ИндексЭлемента25 + 7;
	КоличествоСтрок25 = КоличествоСтрок25 + 8;

КонецПроцедуры

Процедура ПроверитьДанные26(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем26;
	Перем ТекущаяСтрока26;
	Перем ИндексЭлемента26;
	Перем КоличествоСтрок26;

	ТекущаяСтрока26 = 0;
	ИндексЭлемента26 = 1;
	КоличествоСтрок26 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока26 = ТекущаяСтрока26 + Индекс;
		Если ТекущаяСтрока26 > 50 Тогда
			ИндексЭлемента26 = ТекущаяСтрока26;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока26 = ТекущаяСтрока26 + 0;
	ИндексЭлемента26 = ИндексЭлемента26 + 1;
	КоличествоСтрок26 = КоличествоСтрок26 + 2;
	ТекущаяСтрока26 = ТекущаяСтрока26 + 3;
	ИндексЭлемента26 = ИндексЭлемента26 + 4;
	КоличествоСтрок26 = КоличествоСтрок26 + 5;
	ТекущаяСтрока26 = ТекущаяСтрока26 + 6;
	ИндексЭлемента26 = ИндексЭлемента26 + 7;
	КоличествоСтрок26 = КоличествоСтрок26 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал27(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем27;
	Перем ТекущаяСтрока27;
	Перем ИндексЭлемента27;
	Перем КоличествоСтрок27;

	ТекущаяСтрока27 = 0;
	ИндексЭлемента27 = 1;
	КоличествоСтрок27 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока27 = ТекущаяСтрока27 + Индекс;
		Если ТекущаяСтрока27 > 50 Тогда
			ИндексЭлемента27 = ТекущаяСтрока27;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока27 = ТекущаяСтрока27 + 0;
	ИндексЭлемента27 = ИндексЭлемента27 + 1;
	КоличествоСтрок27 = КоличествоСтрок27 + 2;
	ТекущаяСтрока27 = ТекущаяСтрока27 + 3;
	ИндексЭлемента27 = ИндексЭлемента27 + 4;
	КоличествоСтрок27 = КоличествоСтрок27 + 5;
	ТекущаяСтрока27 = ТекущаяСтрока27 + 6;
	ИндексЭлемента27 = ИндексЭлемента27 + 7;
	КоличествоСтрок27 = КоличествоСтрок27 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры8

Процедура ИнициализироватьМодуль28(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем28;
	Перем ТекущаяСтрока28;
	Перем ИндексЭлемента28;
	Перем КоличествоСтрок28;

	ТекущаяСтрока28 = 0;
	ИндексЭлемента28 = 1;
	КоличествоСтрок28 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока28 = ТекущаяСтрока28 + Индекс;
		Если ТекущаяСтрока28 > 50 Тогда
			ИндексЭлемента28 = ТекущаяСтрока28;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока28 = ТекущаяСтрока28 + 0;
	ИндексЭлемента28 = ИндексЭлемента28 + 1;
	КоличествоСтрок28 = КоличествоСтрок28 + 2;
	ТекущаяСтрока28 = ТекущаяСтрока28 + 3;
	ИндексЭлемента28 = ИндексЭлемента28 + 4;
	КоличествоСтрок28 = КоличествоСтрок28 + 5;
	ТекущаяСтрока28 = ТекущаяСтрока28 + 6;
	ИндексЭлемента28 = ИндексЭлемента28 + 7;
	КоличествоСтрок28 = КоличествоСтрок28 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер29(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем29;
	Перем ТекущаяСтрока29;
	Перем ИндексЭлемента29;
	Перем КоличествоСтрок29;

	ТекущаяСтрока29 = 0;
	ИндексЭлемента29 = 1;
	КоличествоСтрок29 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока29 = ТекущаяСтрока29 + Индекс;
		Если ТекущаяСтрока29 > 50 Тогда
			ИндексЭлемента29 = ТекущаяСтрока29;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока29 = ТекущаяСтрока29 + 0;
	ИндексЭлемента29 = ИндексЭлемента29 + 1;
	КоличествоСтрок29 = КоличествоСтрок29 + 2;
	ТекущаяСтрока29 = ТекущаяСтрока29 + 3;
	ИндексЭлемента29 = ИндексЭлемента29 + 4;
	КоличествоСтрок29 = КоличествоСтрок29 + 5;
	ТекущаяСтрока29 = ТекущаяСтрока29 + 6;
	ИндексЭлемента29 = ИндексЭлемента29 + 7;
	КоличествоСтрок29 = КоличествоСтрок29 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос30(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем30;
	Перем ТекущаяСтрока30;
	Перем ИндексЭлемента30;
	Перем КоличествоСтрок30;

	ТекущаяСтрока30 = 0;
	ИндексЭлемента30 = 1;
	КоличествоСтрок30 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока30 = ТекущаяСтрока30 + Индекс;
		Если ТекущаяСтрока30 > 50 Тогда
			ИндексЭлемента30 = ТекущаяСтрока30;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока30 = ТекущаяСтрока30 + 0;
	ИндексЭлемента30 = ИндексЭлемента30 + 1;
	КоличествоСтрок30 = КоличествоСтрок30 + 2;
	ТекущаяСтрока30 = ТекущаяСтрока30 + 3;
	ИндексЭлемента30 = ИндексЭлемента30 + 4;
	КоличествоСтрок30 = КоличествоСтрок30 + 5;
	ТекущаяСтрока30 = ТекущаяСтрока30 + 6;
	ИндексЭлемента30 = ИндексЭлемента30 + 7;
	КоличествоСтрок30 = КоличествоСтрок30 + 8;

КонецПроцедуры

Процедура СохранитьДанные31(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем31;
	Перем ТекущаяСтрока31;
	Перем ИндексЭлемента31;
	Перем КоличествоСтрок31;

	ТекущаяСтрока31 = 0;
	ИндексЭлемента31 = 1;
	КоличествоСтрок31 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока31 = ТекущаяСтрока31 + Индекс;
		Если ТекущаяСтрока31 > 50 Тогда
			ИндексЭлемента31 = ТекущаяСтрока31;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока31 = ТекущаяСтрока31 + 0;
	ИндексЭлемента31 = ИндексЭлемента31 + 1;
	КоличествоСтрок31 = КоличествоСтрок31 + 2;
	ТекущаяСтрока31 = ТекущаяСтрока31 + 3;
	ИндексЭлемента31 = ИндексЭлемента31 + 4;
	КоличествоСтрок31 = КоличествоСтрок31 + 5;
	ТекущаяСтрока31 = ТекущаяСтрока31 + 6;
	ИндексЭлемента31 = ИндексЭлемента31 + 7;
	КоличествоСтрок31 = КоличествоСтрок31 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды9

Процедура ЗагрузитьКонфигурацию32(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем32;
	Перем ТекущаяСтрока32;
	Перем ИндексЭлемента32;
	Перем КоличествоСтрок32;

	ТекущаяСтрока32 = 0;
	ИндексЭлемента32 = 1;
	КоличествоСтрок32 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока32 = ТекущаяСтрока32 + Индекс;
		Если ТекущаяСтрока32 > 50 Тогда
			ИндексЭлемента32 = ТекущаяСтрока32;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока32 = ТекущаяСтрока32 + 0;
	ИндексЭлемента32 = ИндексЭлемента32 + 1;
	КоличествоСтрок32 = КоличествоСтрок32 + 2;
	ТекущаяСтрока32 = ТекущаяСтрока32 + 3;
	ИндексЭлемента32 = ИндексЭлемента32 + 4;
	КоличествоСтрок32 = КоличествоСтрок32 + 5;
	ТекущаяСтрока32 = ТекущаяСтрока32 + 6;
	ИндексЭлемента32 = ИндексЭлемента32 + 7;
	КоличествоСтрок32 = КоличествоСтрок32 + 8;

КонецПроцедуры

Процедура ПроверитьПрава33(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем33;
	Перем ТекущаяСтрока33;
	Перем ИндексЭлемента33;
	Перем КоличествоСтрок33;

	ТекущаяСтрока33 = 0;
	ИндексЭлемента33 = 1;
	КоличествоСтрок33 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока33 = ТекущаяСтрока33 + Индекс;
		Если ТекущаяСтрока33 > 50 Тогда
			ИндексЭлемента33 = ТекущаяСтрока33;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока33 = ТекущаяСтрока33 + 0;
	ИндексЭлемента33 = ИндексЭлемента33 + 1;
	КоличествоСтрок33 = КоличествоСтрок33 + 2;
	ТекущаяСтрока33 = ТекущаяСтрока33 + 3;
	ИндексЭлемента33 = ИндексЭлемента33 + 4;
	КоличествоСтрок33 = КоличествоСтрок33 + 5;
	ТекущаяСтрока33 = ТекущаяСтрока33 + 6;
	ИндексЭлемента33 = ИндексЭлемента33 + 7;
	КоличествоСтрок33 = КоличествоСтрок33 + 8;

КонецПроцедуры

Процедура ОбновитьКэш34(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем34;
	Перем ТекущаяСтрока34;
	Перем ИндексЭлемента34;
	Перем КоличествоСтрок34;

	ТекущаяСтрока34 = 0;
	ИндексЭлемента34 = 1;
	КоличествоСтрок34 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока34 = ТекущаяСтрока34 + Индекс;
		Если ТекущаяСтрока34 > 50 Тогда
			ИндексЭлемента34 = ТекущаяСтрока34;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока34 = ТекущаяСтрока34 + 0;
	ИндексЭлемента34 = ИндексЭлемента34 + 1;
	КоличествоСтрок34 = КоличествоСтрок34 + 2;
	ТекущаяСтрока34 = ТекущаяСтрока34 + 3;
	ИндексЭлемента34 = ИндексЭлемента34 + 4;
	КоличествоСтрок34 = КоличествоСтрок34 + 5;
	ТекущаяСтрока34 = ТекущаяСтрока34 + 6;
	ИндексЭлемента34 = ИндексЭлемента34 + 7;
	КоличествоСтрок34 = КоличествоСтрок34 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление35(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем35;
	Перем ТекущаяСтрока35;
	Перем ИндексЭлемента35;
	Перем КоличествоСтрок35;

	ТекущаяСтрока35 = 0;
	ИндексЭлемента35 = 1;
	КоличествоСтрок35 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока35 = ТекущаяСтрока35 + Индекс;
		Если ТекущаяСтрока35 > 50 Тогда
			ИндексЭлемента35 = ТекущаяСтрока35;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока35 = ТекущаяСтрока35 + 0;
	ИндексЭлемента35 = ИндексЭлемента35 + 1;
	КоличествоСтрок35 = КоличествоСтрок35 + 2;
	ТекущаяСтрока35 = ТекущаяСтрока35 + 3;
	ИндексЭлемента35 = ИндексЭлемента35 + 4;
	КоличествоСтрок35 = КоличествоСтрок35 + 5;
	ТекущаяСтрока35 = ТекущаяСтрока35 + 6;
	ИндексЭлемента35 = ИндексЭлемента35 + 7;
	КоличествоСтрок35 = КоличествоСтрок35 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции10

Процедура ОбработатьСтроку36(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем36;
	Перем ТекущаяСтрока36;
	Перем ИндексЭлемента36;
	Перем КоличествоСтрок36;

	ТекущаяСтрока36 = 0;
	ИндексЭлемента36 = 1;
	КоличествоСтрок36 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока36 = ТекущаяСтрока36 + Индекс;
		Если ТекущаяСтрока36 > 50 Тогда
			ИндексЭлемента36 = ТекущаяСтрока36;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока36 = ТекущаяСтрока36 + 0;
	ИндексЭлемента36 = ИндексЭлемента36 + 1;
	КоличествоСтрок36 = КоличествоСтрок36 + 2;
	ТекущаяСтрока36 = ТекущаяСтрока36 + 3;
	ИндексЭлемента36 = ИндексЭлемента36 + 4;
	КоличествоСтрок36 = КоличествоСтрок36 + 5;
	ТекущаяСтрока36 = ТекущаяСтрока36 + 6;
	ИндексЭлемента36 = ИндексЭлемента36 + 7;
	КоличествоСтрок36 = КоличествоСтрок36 + 8;

КонецПроцедуры

Процедура ВычислитьСумму37(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем37;
	Перем ТекущаяСтрока37;
	Перем ИндексЭлемента37;
	Перем КоличествоСтрок37;

	ТекущаяСтрока37 = 0;
	ИндексЭлемента37 = 1;
	КоличествоСтрок37 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока37 = ТекущаяСтрока37 + Индекс;
		Если ТекущаяСтрока37 > 50 Тогда
			ИндексЭлемента37 = ТекущаяСтрока37;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока37 = ТекущаяСтрока37 + 0;
	ИндексЭлемента37 = ИндексЭлемента37 + 1;
	КоличествоСтрок37 = КоличествоСтрок37 + 2;
	ТекущаяСтрока37 = ТекущаяСтрока37 + 3;
	ИндексЭлемента37 = ИндексЭлемента37 + 4;
	КоличествоСтрок37 = КоличествоСтрок37 + 5;
	ТекущаяСтрока37 = ТекущаяСтрока37 + 6;
	ИндексЭлемента37 = ИндексЭлемента37 + 7;
	КоличествоСтрок37 = КоличествоСтрок37 + 8;

КонецПроцедуры

Процедура ПроверитьДанные38(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем38;
	Перем ТекущаяСтрока38;
	Перем ИндексЭлемента38;
	Перем КоличествоСтрок38;

	ТекущаяСтрока38 = 0;
	ИндексЭлемента38 = 1;
	КоличествоСтрок38 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока38 = ТекущаяСтрока38 + Индекс;
		Если ТекущаяСтрока38 > 50 Тогда
			ИндексЭлемента38 = ТекущаяСтрока38;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока38 = ТекущаяСтрока38 + 0;
	ИндексЭлемента38 = ИндексЭлемента38 + 1;
	КоличествоСтрок38 = КоличествоСтрок38 + 2;
	ТекущаяСтрока38 = ТекущаяСтрока38 + 3;
	ИндексЭлемента38 = ИндексЭлемента38 + 4;
	КоличествоСтрок38 = КоличествоСтрок38 + 5;
	ТекущаяСтрока38 = ТекущаяСтрока38 + 6;
	ИндексЭлемента38 = ИндексЭлемента38 + 7;
	КоличествоСтрок38 = КоличествоСтрок38 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал39(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем39;
	Перем ТекущаяСтрока39;
	Перем ИндексЭлемента39;
	Перем КоличествоСтрок39;

	ТекущаяСтрока39 = 0;
	ИндексЭлемента39 = 1;
	КоличествоСтрок39 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока39 = ТекущаяСтрока39 + Индекс;
		Если ТекущаяСтрока39 > 50 Тогда
			ИндексЭлемента39 = ТекущаяСтрока39;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока39 = ТекущаяСтрока39 + 0;
	ИндексЭлемента39 = ИндексЭлемента39 + 1;
	КоличествоСтрок39 = КоличествоСтрок39 + 2;
	ТекущаяСтрока39 = ТекущаяСтрока39 + 3;
	ИндексЭлемента39 = ИндексЭлемента39 + 4;
	КоличествоСтрок39 = КоличествоСтрок39 + 5;
	ТекущаяСтрока39 = ТекущаяСтрока39 + 6;
	ИндексЭлемента39 = ИндексЭлемента39 + 7;
	КоличествоСтрок39 = КоличествоСтрок39 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий11

Процедура ИнициализироватьМодуль40(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем40;
	Перем ТекущаяСтрока40;
	Перем ИндексЭлемента40;
	Перем КоличествоСтрок40;

	ТекущаяСтрока40 = 0;
	ИндексЭлемента40 = 1;
	КоличествоСтрок40 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока40 = ТекущаяСтрока40 + Индекс;
		Если ТекущаяСтрока40 > 50 Тогда
			ИндексЭлемента40 = ТекущаяСтрока40;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока40 = ТекущаяСтрока40 + 0;
	ИндексЭлемента40 = ИндексЭлемента40 + 1;
	КоличествоСтрок40 = КоличествоСтрок40 + 2;
	ТекущаяСтрока40 = ТекущаяСтрока40 + 3;
	ИндексЭлемента40 = ИндексЭлемента40 + 4;
	КоличествоСтрок40 = КоличествоСтрок40 + 5;
	ТекущаяСтрока40 = ТекущаяСтрока40 + 6;
	ИндексЭлемента40 = ИндексЭлемента40 + 7;
	КоличествоСтрок40 = КоличествоСтрок40 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер41(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем41;
	Перем ТекущаяСтрока41;
	Перем ИндексЭлемента41;
	Перем КоличествоСтрок41;

	ТекущаяСтрока41 = 0;
	ИндексЭлемента41 = 1;
	КоличествоСтрок41 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока41 = ТекущаяСтрока41 + Индекс;
		Если ТекущаяСтрока41 > 50 Тогда
			ИндексЭлемента41 = ТекущаяСтрока41;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока41 = ТекущаяСтрока41 + 0;
	ИндексЭлемента41 = ИндексЭлемента41 + 1;
	КоличествоСтрок41 = КоличествоСтрок41 + 2;
	ТекущаяСтрока41 = ТекущаяСтрока41 + 3;
	ИндексЭлемента41 = ИндексЭлемента41 + 4;
	КоличествоСтрок41 = КоличествоСтрок41 + 5;
	ТекущаяСтрока41 = ТекущаяСтрока41 + 6;
	ИндексЭлемента41 = ИндексЭлемента41 + 7;
	КоличествоСтрок41 = КоличествоСтрок41 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос42(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем42;
	Перем ТекущаяСтрока42;
	Перем ИндексЭлемента42;
	Перем КоличествоСтрок42;

	ТекущаяСтрока42 = 0;
	ИндексЭлемента42 = 1;
	КоличествоСтрок42 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока42 = ТекущаяСтрока42 + Индекс;
		Если ТекущаяСтрока42 > 50 Тогда
			ИндексЭлемента42 = ТекущаяСтрока42;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока42 = ТекущаяСтрока42 + 0;
	ИндексЭлемента42 = ИндексЭлемента42 + 1;
	КоличествоСтрок42 = КоличествоСтрок42 + 2;
	ТекущаяСтрока42 = ТекущаяСтрока42 + 3;
	ИндексЭлемента42 = ИндексЭлемента42 + 4;
	КоличествоСтрок42 = КоличествоСтрок42 + 5;
	ТекущаяСтрока42 = ТекущаяСтрока42 + 6;
	ИндексЭлемента42 = ИндексЭлемента42 + 7;
	КоличествоСтрок42 = КоличествоСтрок42 + 8;

КонецПроцедуры

Процедура СохранитьДанные43(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем43;
	Перем ТекущаяСтрока43;
	Перем ИндексЭлемента43;
	Перем КоличествоСтрок43;

	ТекущаяСтрока43 = 0;
	ИндексЭлемента43 = 1;
	КоличествоСтрок43 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока43 = ТекущаяСтрока43 + Индекс;
		Если ТекущаяСтрока43 > 50 Тогда
			ИндексЭлемента43 = ТекущаяСтрока43;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока43 = ТекущаяСтрока43 + 0;
	ИндексЭлемента43 = ИндексЭлемента43 + 1;
	КоличествоСтрок43 = КоличествоСтрок43 + 2;
	ТекущаяСтрока43 = ТекущаяСтрока43 + 3;
	ИндексЭлемента43 = ИндексЭлемента43 + 4;
	КоличествоСтрок43 = КоличествоСтрок43 + 5;
	ТекущаяСтрока43 = ТекущаяСтрока43 + 6;
	ИндексЭлемента43 = ИндексЭлемента43 + 7;
	КоличествоСтрок43 = КоличествоСтрок43 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции12

Процедура ЗагрузитьКонфигурацию44(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем44;
	Перем ТекущаяСтрока44;
	Перем ИндексЭлемента44;
	Перем КоличествоСтрок44;

	ТекущаяСтрока44 = 0;
	ИндексЭлемента44 = 1;
	КоличествоСтрок44 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока44 = ТекущаяСтрока44 + Индекс;
		Если ТекущаяСтрока44 > 50 Тогда
			ИндексЭлемента44 = ТекущаяСтрока44;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока44 = ТекущаяСтрока44 + 0;
	ИндексЭлемента44 = ИндексЭлемента44 + 1;
	КоличествоСтрок44 = КоличествоСтрок44 + 2;
	ТекущаяСтрока44 = ТекущаяСтрока44 + 3;
	ИндексЭлемента44 = ИндексЭлемента44 + 4;
	КоличествоСтрок44 = КоличествоСтрок44 + 5;
	ТекущаяСтрока44 = ТекущаяСтрока44 + 6;
	ИндексЭлемента44 = ИндексЭлемента44 + 7;
	КоличествоСтрок44 = КоличествоСтрок44 + 8;

КонецПроцедуры

Процедура ПроверитьПрава45(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем45;
	Перем ТекущаяСтрока45;
	Перем ИндексЭлемента45;
	Перем КоличествоСтрок45;

	ТекущаяСтрока45 = 0;
	ИндексЭлемента45 = 1;
	КоличествоСтрок45 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока45 = ТекущаяСтрока45 + Индекс;
		Если ТекущаяСтрока45 > 50 Тогда
			ИндексЭлемента45 = ТекущаяСтрока45;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока45 = ТекущаяСтрока45 + 0;
	ИндексЭлемента45 = ИндексЭлемента45 + 1;
	КоличествоСтрок45 = КоличествоСтрок45 + 2;
	ТекущаяСтрока45 = ТекущаяСтрока45 + 3;
	ИндексЭлемента45 = ИндексЭлемента45 + 4;
	КоличествоСтрок45 = КоличествоСтрок45 + 5;
	ТекущаяСтрока45 = ТекущаяСтрока45 + 6;
	ИндексЭлемента45 = ИндексЭлемента45 + 7;
	КоличествоСтрок45 = КоличествоСтрок45 + 8;

КонецПроцедуры

Процедура ОбновитьКэш46(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем46;
	Перем ТекущаяСтрока46;
	Перем ИндексЭлемента46;
	Перем КоличествоСтрок46;

	ТекущаяСтрока46 = 0;
	ИндексЭлемента46 = 1;
	КоличествоСтрок46 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока46 = ТекущаяСтрока46 + Индекс;
		Если ТекущаяСтрока46 > 50 Тогда
			ИндексЭлемента46 = ТекущаяСтрока46;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока46 = ТекущаяСтрока46 + 0;
	ИндексЭлемента46 = ИндексЭлемента46 + 1;
	КоличествоСтрок46 = КоличествоСтрок46 + 2;
	ТекущаяСтрока46 = ТекущаяСтрока46 + 3;
	ИндексЭлемента46 = ИндексЭлемента46 + 4;
	КоличествоСтрок46 = КоличествоСтрок46 + 5;
	ТекущаяСтрока46 = ТекущаяСтрока46 + 6;
	ИндексЭлемента46 = ИндексЭлемента46 + 7;
	КоличествоСтрок46 = КоличествоСтрок46 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление47(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем47;
	Перем ТекущаяСтрока47;
	Перем ИндексЭлемента47;
	Перем КоличествоСтрок47;

	ТекущаяСтрока47 = 0;
	ИндексЭлемента47 = 1;
	КоличествоСтрок47 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока47 = ТекущаяСтрока47 + Индекс;
		Если ТекущаяСтрока47 > 50 Тогда
			ИндексЭлемента47 = ТекущаяСтрока47;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока47 = ТекущаяСтрока47 + 0;
	ИндексЭлемента47 = ИндексЭлемента47 + 1;
	КоличествоСтрок47 = КоличествоСтрок47 + 2;
	ТекущаяСтрока47 = ТекущаяСтрока47 + 3;
	ИндексЭлемента47 = ИндексЭлемента47 + 4;
	КоличествоСтрок47 = КоличествоСтрок47 + 5;
	ТекущаяСтрока47 = ТекущаяСтрока47 + 6;
	ИндексЭлемента47 = ИндексЭлемента47 + 7;
	КоличествоСтрок47 = КоличествоСтрок47 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные13

Процедура ОбработатьСтроку48(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем48;
	Перем ТекущаяСтрока48;
	Перем ИндексЭлемента48;
	Перем КоличествоСтрок48;

	ТекущаяСтрока48 = 0;
	ИндексЭлемента48 = 1;
	КоличествоСтрок48 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока48 = ТекущаяСтрока48 + Индекс;
		Если ТекущаяСтрока48 > 50 Тогда
			ИндексЭлемента48 = ТекущаяСтрока48;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока48 = ТекущаяСтрока48 + 0;
	ИндексЭлемента48 = ИндексЭлемента48 + 1;
	КоличествоСтрок48 = КоличествоСтрок48 + 2;
	ТекущаяСтрока48 = ТекущаяСтрока48 + 3;
	ИндексЭлемента48 = ИндексЭлемента48 + 4;
	КоличествоСтрок48 = КоличествоСтрок48 + 5;
	ТекущаяСтрока48 = ТекущаяСтрока48 + 6;
	ИндексЭлемента48 = ИндексЭлемента48 + 7;
	КоличествоСтрок48 = КоличествоСтрок48 + 8;

КонецПроцедуры

Процедура ВычислитьСумму49(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем49;
	Перем ТекущаяСтрока49;
	Перем ИндексЭлемента49;
	Перем КоличествоСтрок49;

	ТекущаяСтрока49 = 0;
	ИндексЭлемента49 = 1;
	КоличествоСтрок49 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока49 = ТекущаяСтрока49 + Индекс;
		Если ТекущаяСтрока49 > 50 Тогда
			ИндексЭлемента49 = ТекущаяСтрока49;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока49 = ТекущаяСтрока49 + 0;
	ИндексЭлемента49 = ИндексЭлемента49 + 1;
	КоличествоСтрок49 = КоличествоСтрок49 + 2;
	ТекущаяСтрока49 = ТекущаяСтрока49 + 3;
	ИндексЭлемента49 = ИндексЭлемента49 + 4;
	КоличествоСтрок49 = КоличествоСтрок49 + 5;
	ТекущаяСтрока49 = ТекущаяСтрока49 + 6;
	ИндексЭлемента49 = ИндексЭлемента49 + 7;
	КоличествоСтрок49 = КоличествоСтрок49 + 8;

КонецПроцедуры

Процедура ПроверитьДанные50(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем50;
	Перем ТекущаяСтрока50;
	Перем ИндексЭлемента50;
	Перем КоличествоСтрок50;

	ТекущаяСтрока50 = 0;
	ИндексЭлемента50 = 1;
	КоличествоСтрок50 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока50 = ТекущаяСтрока50 + Индекс;
		Если ТекущаяСтрока50 > 50 Тогда
			ИндексЭлемента50 = ТекущаяСтрока50;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока50 = ТекущаяСтрока50 + 0;
	ИндексЭлемента50 = ИндексЭлемента50 + 1;
	КоличествоСтрок50 = КоличествоСтрок50 + 2;
	ТекущаяСтрока50 = ТекущаяСтрока50 + 3;
	ИндексЭлемента50 = ИндексЭлемента50 + 4;
	КоличествоСтрок50 = КоличествоСтрок50 + 5;
	ТекущаяСтрока50 = ТекущаяСтрока50 + 6;
	ИндексЭлемента50 = ИндексЭлемента50 + 7;
	КоличествоСтрок50 = КоличествоСтрок50 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал51(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем51;
	Перем ТекущаяСтрока51;
	Перем ИндексЭлемента51;
	Перем КоличествоСтрок51;

	ТекущаяСтрока51 = 0;
	ИндексЭлемента51 = 1;
	КоличествоСтрок51 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока51 = ТекущаяСтрока51 + Индекс;
		Если ТекущаяСтрока51 > 50 Тогда
			ИндексЭлемента51 = ТекущаяСтрока51;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока51 = ТекущаяСтрока51 + 0;
	ИндексЭлемента51 = ИндексЭлемента51 + 1;
	КоличествоСтрок51 = КоличествоСтрок51 + 2;
	ТекущаяСтрока51 = ТекущаяСтрока51 + 3;
	ИндексЭлемента51 = ИндексЭлемента51 + 4;
	КоличествоСтрок51 = КоличествоСтрок51 + 5;
	ТекущаяСтрока51 = ТекущаяСтрока51 + 6;
	ИндексЭлемента51 = ИндексЭлемента51 + 7;
	КоличествоСтрок51 = КоличествоСтрок51 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры14

Процедура ИнициализироватьМодуль52(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем52;
	Перем ТекущаяСтрока52;
	Перем ИндексЭлемента52;
	Перем КоличествоСтрок52;

	ТекущаяСтрока52 = 0;
	ИндексЭлемента52 = 1;
	КоличествоСтрок52 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока52 = ТекущаяСтрока52 + Индекс;
		Если ТекущаяСтрока52 > 50 Тогда
			ИндексЭлемента52 = ТекущаяСтрока52;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока52 = ТекущаяСтрока52 + 0;
	ИндексЭлемента52 = ИндексЭлемента52 + 1;
	КоличествоСтрок52 = КоличествоСтрок52 + 2;
	ТекущаяСтрока52 = ТекущаяСтрока52 + 3;
	ИндексЭлемента52 = ИндексЭлемента52 + 4;
	КоличествоСтрок52 = КоличествоСтрок52 + 5;
	ТекущаяСтрока52 = ТекущаяСтрока52 + 6;
	ИндексЭлемента52 = ИндексЭлемента52 + 7;
	КоличествоСтрок52 = КоличествоСтрок52 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер53(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем53;
	Перем ТекущаяСтрока53;
	Перем ИндексЭлемента53;
	Перем КоличествоСтрок53;

	ТекущаяСтрока53 = 0;
	ИндексЭлемента53 = 1;
	КоличествоСтрок53 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока53 = ТекущаяСтрока53 + Индекс;
		Если ТекущаяСтрока53 > 50 Тогда
			ИндексЭлемента53 = ТекущаяСтрока53;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока53 = ТекущаяСтрока53 + 0;
	ИндексЭлемента53 = ИндексЭлемента53 + 1;
	КоличествоСтрок53 = КоличествоСтрок53 + 2;
	ТекущаяСтрока53 = ТекущаяСтрока53 + 3;
	ИндексЭлемента53 = ИндексЭлемента53 + 4;
	КоличествоСтрок53 = КоличествоСтрок53 + 5;
	ТекущаяСтрока53 = ТекущаяСтрока53 + 6;
	ИндексЭлемента53 = ИндексЭлемента53 + 7;
	КоличествоСтрок53 = КоличествоСтрок53 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос54(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем54;
	Перем ТекущаяСтрока54;
	Перем ИндексЭлемента54;
	Перем КоличествоСтрок54;

	ТекущаяСтрока54 = 0;
	ИндексЭлемента54 = 1;
	КоличествоСтрок54 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока54 = ТекущаяСтрока54 + Индекс;
		Если ТекущаяСтрока54 > 50 Тогда
			ИндексЭлемента54 = ТекущаяСтрока54;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока54 = ТекущаяСтрока54 + 0;
	ИндексЭлемента54 = ИндексЭлемента54 + 1;
	КоличествоСтрок54 = КоличествоСтрок54 + 2;
	ТекущаяСтрока54 = ТекущаяСтрока54 + 3;
	ИндексЭлемента54 = ИндексЭлемента54 + 4;
	КоличествоСтрок54 = КоличествоСтрок54 + 5;
	ТекущаяСтрока54 = ТекущаяСтрока54 + 6;
	ИндексЭлемента54 = ИндексЭлемента54 + 7;
	КоличествоСтрок54 = КоличествоСтрок54 + 8;

КонецПроцедуры

Процедура СохранитьДанные55(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем55;
	Перем ТекущаяСтрока55;
	Перем ИндексЭлемента55;
	Перем КоличествоСтрок55;

	ТекущаяСтрока55 = 0;
	ИндексЭлемента55 = 1;
	КоличествоСтрок55 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока55 = ТекущаяСтрока55 + Индекс;
		Если ТекущаяСтрока55 > 50 Тогда
			ИндексЭлемента55 = ТекущаяСтрока55;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока55 = ТекущаяСтрока55 + 0;
	ИндексЭлемента55 = ИндексЭлемента55 + 1;
	КоличествоСтрок55 = КоличествоСтрок55 + 2;
	ТекущаяСтрока55 = ТекущаяСтрока55 + 3;
	ИндексЭлемента55 = ИндексЭлемента55 + 4;
	КоличествоСтрок55 = КоличествоСтрок55 + 5;
	ТекущаяСтрока55 = ТекущаяСтрока55 + 6;
	ИндексЭлемента55 = ИндексЭлемента55 + 7;
	КоличествоСтрок55 = КоличествоСтрок55 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды15

Процедура ЗагрузитьКонфигурацию56(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем56;
	Перем ТекущаяСтрока56;
	Перем ИндексЭлемента56;
	Перем КоличествоСтрок56;

	ТекущаяСтрока56 = 0;
	ИндексЭлемента56 = 1;
	КоличествоСтрок56 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока56 = ТекущаяСтрока56 + Индекс;
		Если ТекущаяСтрока56 > 50 Тогда
			ИндексЭлемента56 = ТекущаяСтрока56;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока56 = ТекущаяСтрока56 + 0;
	ИндексЭлемента56 = ИндексЭлемента56 + 1;
	КоличествоСтрок56 = КоличествоСтрок56 + 2;
	ТекущаяСтрока56 = ТекущаяСтрока56 + 3;
	ИндексЭлемента56 = ИндексЭлемента56 + 4;
	КоличествоСтрок56 = КоличествоСтрок56 + 5;
	ТекущаяСтрока56 = ТекущаяСтрока56 + 6;
	ИндексЭлемента56 = ИндексЭлемента56 + 7;
	КоличествоСтрок56 = КоличествоСтрок56 + 8;

КонецПроцедуры

Процедура ПроверитьПрава57(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем57;
	Перем ТекущаяСтрока57;
	Перем ИндексЭлемента57;
	Перем КоличествоСтрок57;

	ТекущаяСтрока57 = 0;
	ИндексЭлемента57 = 1;
	КоличествоСтрок57 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока57 = ТекущаяСтрока57 + Индекс;
		Если ТекущаяСтрока57 > 50 Тогда
			ИндексЭлемента57 = ТекущаяСтрока57;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока57 = ТекущаяСтрока57 + 0;
	ИндексЭлемента57 = ИндексЭлемента57 + 1;
	КоличествоСтрок57 = КоличествоСтрок57 + 2;
	ТекущаяСтрока57 = ТекущаяСтрока57 + 3;
	ИндексЭлемента57 = ИндексЭлемента57 + 4;
	КоличествоСтрок57 = КоличествоСтрок57 + 5;
	ТекущаяСтрока57 = ТекущаяСтрока57 + 6;
	ИндексЭлемента57 = ИндексЭлемента57 + 7;
	КоличествоСтрок57 = КоличествоСтрок57 + 8;

КонецПроцедуры

Процедура ОбновитьКэш58(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем58;
	Перем ТекущаяСтрока58;
	Перем ИндексЭлемента58;
	Перем КоличествоСтрок58;

	ТекущаяСтрока58 = 0;
	ИндексЭлемента58 = 1;
	КоличествоСтрок58 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока58 = ТекущаяСтрока58 + Индекс;
		Если ТекущаяСтрока58 > 50 Тогда
			ИндексЭлемента58 = ТекущаяСтрока58;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока58 = ТекущаяСтрока58 + 0;
	ИндексЭлемента58 = ИндексЭлемента58 + 1;
	КоличествоСтрок58 = КоличествоСтрок58 + 2;
	ТекущаяСтрока58 = ТекущаяСтрока58 + 3;
	ИндексЭлемента58 = ИндексЭлемента58 + 4;
	КоличествоСтрок58 = КоличествоСтрок58 + 5;
	ТекущаяСтрока58 = ТекущаяСтрока58 + 6;
	ИндексЭлемента58 = ИндексЭлемента58 + 7;
	КоличествоСтрок58 = КоличествоСтрок58 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление59(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем59;
	Перем ТекущаяСтрока59;
	Перем ИндексЭлемента59;
	Перем КоличествоСтрок59;

	ТекущаяСтрока59 = 0;
	ИндексЭлемента59 = 1;
	КоличествоСтрок59 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока59 = ТекущаяСтрока59 + Индекс;
		Если ТекущаяСтрока59 > 50 Тогда
			ИндексЭлемента59 = ТекущаяСтрока59;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока59 = ТекущаяСтрока59 + 0;
	ИндексЭлемента59 = ИндексЭлемента59 + 1;
	КоличествоСтрок59 = КоличествоСтрок59 + 2;
	ТекущаяСтрока59 = ТекущаяСтрока59 + 3;
	ИндексЭлемента59 = ИндексЭлемента59 + 4;
	КоличествоСтрок59 = КоличествоСтрок59 + 5;
	ТекущаяСтрока59 = ТекущаяСтрока59 + 6;
	ИндексЭлемента59 = ИндексЭлемента59 + 7;
	КоличествоСтрок59 = КоличествоСтрок59 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции16

Процедура ОбработатьСтроку60(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем60;
	Перем ТекущаяСтрока60;
	Перем ИндексЭлемента60;
	Перем КоличествоСтрок60;

	ТекущаяСтрока60 = 0;
	ИндексЭлемента60 = 1;
	КоличествоСтрок60 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока60 = ТекущаяСтрока60 + Индекс;
		Если ТекущаяСтрока60 > 50 Тогда
			ИндексЭлемента60 = ТекущаяСтрока60;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока60 = ТекущаяСтрока60 + 0;
	ИндексЭлемента60 = ИндексЭлемента60 + 1;
	КоличествоСтрок60 = КоличествоСтрок60 + 2;
	ТекущаяСтрока60 = ТекущаяСтрока60 + 3;
	ИндексЭлемента60 = ИндексЭлемента60 + 4;
	КоличествоСтрок60 = КоличествоСтрок60 + 5;
	ТекущаяСтрока60 = ТекущаяСтрока60 + 6;
	ИндексЭлемента60 = ИндексЭлемента60 + 7;
	КоличествоСтрок60 = КоличествоСтрок60 + 8;

КонецПроцедуры

Процедура ВычислитьСумму61(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем61;
	Перем ТекущаяСтрока61;
	Перем ИндексЭлемента61;
	Перем КоличествоСтрок61;

	ТекущаяСтрока61 = 0;
	ИндексЭлемента61 = 1;
	КоличествоСтрок61 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока61 = ТекущаяСтрока61 + Индекс;
		Если ТекущаяСтрока61 > 50 Тогда
			ИндексЭлемента61 = ТекущаяСтрока61;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока61 = ТекущаяСтрока61 + 0;
	ИндексЭлемента61 = ИндексЭлемента61 + 1;
	КоличествоСтрок61 = КоличествоСтрок61 + 2;
	ТекущаяСтрока61 = ТекущаяСтрока61 + 3;
	ИндексЭлемента61 = ИндексЭлемента61 + 4;
	КоличествоСтрок61 = КоличествоСтрок61 + 5;
	ТекущаяСтрока61 = ТекущаяСтрока61 + 6;
	ИндексЭлемента61 = ИндексЭлемента61 + 7;
	КоличествоСтрок61 = КоличествоСтрок61 + 8;

КонецПроцедуры

Процедура ПроверитьДанные62(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем62;
	Перем ТекущаяСтрока62;
	Перем ИндексЭлемента62;
	Перем КоличествоСтрок62;

	ТекущаяСтрока62 = 0;
	ИндексЭлемента62 = 1;
	КоличествоСтрок62 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока62 = ТекущаяСтрока62 + Индекс;
		Если ТекущаяСтрока62 > 50 Тогда
			ИндексЭлемента62 = ТекущаяСтрока62;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока62 = ТекущаяСтрока62 + 0;
	ИндексЭлемента62 = ИндексЭлемента62 + 1;
	КоличествоСтрок62 = КоличествоСтрок62 + 2;
	ТекущаяСтрока62 = ТекущаяСтрока62 + 3;
	ИндексЭлемента62 = ИндексЭлемента62 + 4;
	КоличествоСтрок62 = КоличествоСтрок62 + 5;
	ТекущаяСтрока62 = ТекущаяСтрока62 + 6;
	ИндексЭлемента62 = ИндексЭлемента62 + 7;
	КоличествоСтрок62 = КоличествоСтрок62 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал63(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем63;
	Перем ТекущаяСтрока63;
	Перем ИндексЭлемента63;
	Перем КоличествоСтрок63;

	ТекущаяСтрока63 = 0;
	ИндексЭлемента63 = 1;
	КоличествоСтрок63 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока63 = ТекущаяСтрока63 + Индекс;
		Если ТекущаяСтрока63 > 50 Тогда
			ИндексЭлемента63 = ТекущаяСтрока63;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока63 = ТекущаяСтрока63 + 0;
	ИндексЭлемента63 = ИндексЭлемента63 + 1;
	КоличествоСтрок63 = КоличествоСтрок63 + 2;
	ТекущаяСтрока63 = ТекущаяСтрока63 + 3;
	ИндексЭлемента63 = ИндексЭлемента63 + 4;
	КоличествоСтрок63 = КоличествоСтрок63 + 5;
	ТекущаяСтрока63 = ТекущаяСтрока63 + 6;
	ИндексЭлемента63 = ИндексЭлемента63 + 7;
	КоличествоСтрок63 = КоличествоСтрок63 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий17

Процедура ИнициализироватьМодуль64(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем64;
	Перем ТекущаяСтрока64;
	Перем ИндексЭлемента64;
	Перем КоличествоСтрок64;

	ТекущаяСтрока64 = 0;
	ИндексЭлемента64 = 1;
	КоличествоСтрок64 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока64 = ТекущаяСтрока64 + Индекс;
		Если ТекущаяСтрока64 > 50 Тогда
			ИндексЭлемента64 = ТекущаяСтрока64;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока64 = ТекущаяСтрока64 + 0;
	ИндексЭлемента64 = ИндексЭлемента64 + 1;
	КоличествоСтрок64 = КоличествоСтрок64 + 2;
	ТекущаяСтрока64 = ТекущаяСтрока64 + 3;
	ИндексЭлемента64 = ИндексЭлемента64 + 4;
	КоличествоСтрок64 = КоличествоСтрок64 + 5;
	ТекущаяСтрока64 = ТекущаяСтрока64 + 6;
	ИндексЭлемента64 = ИндексЭлемента64 + 7;
	КоличествоСтрок64 = КоличествоСтрок64 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер65(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем65;
	Перем ТекущаяСтрока65;
	Перем ИндексЭлемента65;
	Перем КоличествоСтрок65;

	ТекущаяСтрока65 = 0;
	ИндексЭлемента65 = 1;
	КоличествоСтрок65 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока65 = ТекущаяСтрока65 + Индекс;
		Если ТекущаяСтрока65 > 50 Тогда
			ИндексЭлемента65 = ТекущаяСтрока65;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока65 = ТекущаяСтрока65 + 0;
	ИндексЭлемента65 = ИндексЭлемента65 + 1;
	КоличествоСтрок65 = КоличествоСтрок65 + 2;
	ТекущаяСтрока65 = ТекущаяСтрока65 + 3;
	ИндексЭлемента65 = ИндексЭлемента65 + 4;
	КоличествоСтрок65 = КоличествоСтрок65 + 5;
	ТекущаяСтрока65 = ТекущаяСтрока65 + 6;
	ИндексЭлемента65 = ИндексЭлемента65 + 7;
	КоличествоСтрок65 = КоличествоСтрок65 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос66(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем66;
	Перем ТекущаяСтрока66;
	Перем ИндексЭлемента66;
	Перем КоличествоСтрок66;

	ТекущаяСтрока66 = 0;
	ИндексЭлемента66 = 1;
	КоличествоСтрок66 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока66 = ТекущаяСтрока66 + Индекс;
		Если ТекущаяСтрока66 > 50 Тогда
			ИндексЭлемента66 = ТекущаяСтрока66;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока66 = ТекущаяСтрока66 + 0;
	ИндексЭлемента66 = ИндексЭлемента66 + 1;
	КоличествоСтрок66 = КоличествоСтрок66 + 2;
	ТекущаяСтрока66 = ТекущаяСтрока66 + 3;
	ИндексЭлемента66 = ИндексЭлемента66 + 4;
	КоличествоСтрок66 = КоличествоСтрок66 + 5;
	ТекущаяСтрока66 = ТекущаяСтрока66 + 6;
	ИндексЭлемента66 = ИндексЭлемента66 + 7;
	КоличествоСтрок66 = КоличествоСтрок66 + 8;

КонецПроцедуры

Процедура СохранитьДанные67(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем67;
	Перем ТекущаяСтрока67;
	Перем ИндексЭлемента67;
	Перем КоличествоСтрок67;

	ТекущаяСтрока67 = 0;
	ИндексЭлемента67 = 1;
	КоличествоСтрок67 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока67 = ТекущаяСтрока67 + Индекс;
		Если ТекущаяСтрока67 > 50 Тогда
			ИндексЭлемента67 = ТекущаяСтрока67;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока67 = ТекущаяСтрока67 + 0;
	ИндексЭлемента67 = ИндексЭлемента67 + 1;
	КоличествоСтрок67 = КоличествоСтрок67 + 2;
	ТекущаяСтрока67 = ТекущаяСтрока67 + 3;
	ИндексЭлемента67 = ИндексЭлемента67 + 4;
	КоличествоСтрок67 = КоличествоСтрок67 + 5;
	ТекущаяСтрока67 = ТекущаяСтрока67 + 6;
	ИндексЭлемента67 = ИндексЭлемента67 + 7;
	КоличествоСтрок67 = КоличествоСтрок67 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции18

Процедура ЗагрузитьКонфигурацию68(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем68;
	Перем ТекущаяСтрока68;
	Перем ИндексЭлемента68;
	Перем КоличествоСтрок68;

	ТекущаяСтрока68 = 0;
	ИндексЭлемента68 = 1;
	КоличествоСтрок68 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока68 = ТекущаяСтрока68 + Индекс;
		Если ТекущаяСтрока68 > 50 Тогда
			ИндексЭлемента68 = ТекущаяСтрока68;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока68 = ТекущаяСтрока68 + 0;
	ИндексЭлемента68 = ИндексЭлемента68 + 1;
	КоличествоСтрок68 = КоличествоСтрок68 + 2;
	ТекущаяСтрока68 = ТекущаяСтрока68 + 3;
	ИндексЭлемента68 = ИндексЭлемента68 + 4;
	КоличествоСтрок68 = КоличествоСтрок68 + 5;
	ТекущаяСтрока68 = ТекущаяСтрока68 + 6;
	ИндексЭлемента68 = ИндексЭлемента68 + 7;
	КоличествоСтрок68 = КоличествоСтрок68 + 8;

КонецПроцедуры

Процедура ПроверитьПрава69(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем69;
	Перем ТекущаяСтрока69;
	Перем ИндексЭлемента69;
	Перем КоличествоСтрок69;

	ТекущаяСтрока69 = 0;
	ИндексЭлемента69 = 1;
	КоличествоСтрок69 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока69 = ТекущаяСтрока69 + Индекс;
		Если ТекущаяСтрока69 > 50 Тогда
			ИндексЭлемента69 = ТекущаяСтрока69;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока69 = ТекущаяСтрока69 + 0;
	ИндексЭлемента69 = ИндексЭлемента69 + 1;
	КоличествоСтрок69 = КоличествоСтрок69 + 2;
	ТекущаяСтрока69 = ТекущаяСтрока69 + 3;
	ИндексЭлемента69 = ИндексЭлемента69 + 4;
	КоличествоСтрок69 = КоличествоСтрок69 + 5;
	ТекущаяСтрока69 = ТекущаяСтрока69 + 6;
	ИндексЭлемента69 = ИндексЭлемента69 + 7;
	КоличествоСтрок69 = КоличествоСтрок69 + 8;

КонецПроцедуры

Процедура ОбновитьКэш70(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем70;
	Перем ТекущаяСтрока70;
	Перем ИндексЭлемента70;
	Перем КоличествоСтрок70;

	ТекущаяСтрока70 = 0;
	ИндексЭлемента70 = 1;
	КоличествоСтрок70 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока70 = ТекущаяСтрока70 + Индекс;
		Если ТекущаяСтрока70 > 50 Тогда
			ИндексЭлемента70 = ТекущаяСтрока70;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока70 = ТекущаяСтрока70 + 0;
	ИндексЭлемента70 = ИндексЭлемента70 + 1;
	КоличествоСтрок70 = КоличествоСтрок70 + 2;
	ТекущаяСтрока70 = ТекущаяСтрока70 + 3;
	ИндексЭлемента70 = ИндексЭлемента70 + 4;
	КоличествоСтрок70 = КоличествоСтрок70 + 5;
	ТекущаяСтрока70 = ТекущаяСтрока70 + 6;
	ИндексЭлемента70 = ИндексЭлемента70 + 7;
	КоличествоСтрок70 = КоличествоСтрок70 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление71(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем71;
	Перем ТекущаяСтрока71;
	Перем ИндексЭлемента71;
	Перем КоличествоСтрок71;

	ТекущаяСтрока71 = 0;
	ИндексЭлемента71 = 1;
	КоличествоСтрок71 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока71 = ТекущаяСтрока71 + Индекс;
		Если ТекущаяСтрока71 > 50 Тогда
			ИндексЭлемента71 = ТекущаяСтрока71;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока71 = ТекущаяСтрока71 + 0;
	ИндексЭлемента71 = ИндексЭлемента71 + 1;
	КоличествоСтрок71 = КоличествоСтрок71 + 2;
	ТекущаяСтрока71 = ТекущаяСтрока71 + 3;
	ИндексЭлемента71 = ИндексЭлемента71 + 4;
	КоличествоСтрок71 = КоличествоСтрок71 + 5;
	ТекущаяСтрока71 = ТекущаяСтрока71 + 6;
	ИндексЭлемента71 = ИндексЭлемента71 + 7;
	КоличествоСтрок71 = КоличествоСтрок71 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные19

Процедура ОбработатьСтроку72(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем72;
	Перем ТекущаяСтрока72;
	Перем ИндексЭлемента72;
	Перем КоличествоСтрок72;

	ТекущаяСтрока72 = 0;
	ИндексЭлемента72 = 1;
	КоличествоСтрок72 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока72 = ТекущаяСтрока72 + Индекс;
		Если ТекущаяСтрока72 > 50 Тогда
			ИндексЭлемента72 = ТекущаяСтрока72;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока72 = ТекущаяСтрока72 + 0;
	ИндексЭлемента72 = ИндексЭлемента72 + 1;
	КоличествоСтрок72 = КоличествоСтрок72 + 2;
	ТекущаяСтрока72 = ТекущаяСтрока72 + 3;
	ИндексЭлемента72 = ИндексЭлемента72 + 4;
	КоличествоСтрок72 = КоличествоСтрок72 + 5;
	ТекущаяСтрока72 = ТекущаяСтрока72 + 6;
	ИндексЭлемента72 = ИндексЭлемента72 + 7;
	КоличествоСтрок72 = КоличествоСтрок72 + 8;

КонецПроцедуры

Процедура ВычислитьСумму73(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем73;
	Перем ТекущаяСтрока73;
	Перем ИндексЭлемента73;
	Перем КоличествоСтрок73;

	ТекущаяСтрока73 = 0;
	ИндексЭлемента73 = 1;
	КоличествоСтрок73 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока73 = ТекущаяСтрока73 + Индекс;
		Если ТекущаяСтрока73 > 50 Тогда
			ИндексЭлемента73 = ТекущаяСтрока73;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока73 = ТекущаяСтрока73 + 0;
	ИндексЭлемента73 = ИндексЭлемента73 + 1;
	КоличествоСтрок73 = КоличествоСтрок73 + 2;
	ТекущаяСтрока73 = ТекущаяСтрока73 + 3;
	ИндексЭлемента73 = ИндексЭлемента73 + 4;
	КоличествоСтрок73 = КоличествоСтрок73 + 5;
	ТекущаяСтрока73 = ТекущаяСтрока73 + 6;
	ИндексЭлемента73 = ИндексЭлемента73 + 7;
	КоличествоСтрок73 = КоличествоСтрок73 + 8;

КонецПроцедуры

Процедура ПроверитьДанные74(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем74;
	Перем ТекущаяСтрока74;
	Перем ИндексЭлемента74;
	Перем КоличествоСтрок74;

	ТекущаяСтрока74 = 0;
	ИндексЭлемента74 = 1;
	КоличествоСтрок74 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока74 = ТекущаяСтрока74 + Индекс;
		Если ТекущаяСтрока74 > 50 Тогда
			ИндексЭлемента74 = ТекущаяСтрока74;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока74 = ТекущаяСтрока74 + 0;
	ИндексЭлемента74 = ИндексЭлемента74 + 1;
	КоличествоСтрок74 = КоличествоСтрок74 + 2;
	ТекущаяСтрока74 = ТекущаяСтрока74 + 3;
	ИндексЭлемента74 = ИндексЭлемента74 + 4;
	КоличествоСтрок74 = КоличествоСтрок74 + 5;
	ТекущаяСтрока74 = ТекущаяСтрока74 + 6;
	ИндексЭлемента74 = ИндексЭлемента74 + 7;
	КоличествоСтрок74 = КоличествоСтрок74 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал75(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем75;
	Перем ТекущаяСтрока75;
	Перем ИндексЭлемента75;
	Перем КоличествоСтрок75;

	ТекущаяСтрока75 = 0;
	ИндексЭлемента75 = 1;
	КоличествоСтрок75 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока75 = ТекущаяСтрока75 + Индекс;
		Если ТекущаяСтрока75 > 50 Тогда
			ИндексЭлемента75 = ТекущаяСтрока75;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока75 = ТекущаяСтрока75 + 0;
	ИндексЭлемента75 = ИндексЭлемента75 + 1;
	КоличествоСтрок75 = КоличествоСтрок75 + 2;
	ТекущаяСтрока75 = ТекущаяСтрока75 + 3;
	ИндексЭлемента75 = ИндексЭлемента75 + 4;
	КоличествоСтрок75 = КоличествоСтрок75 + 5;
	ТекущаяСтрока75 = ТекущаяСтрока75 + 6;
	ИндексЭлемента75 = ИндексЭлемента75 + 7;
	КоличествоСтрок75 = КоличествоСтрок75 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры20

Процедура ИнициализироватьМодуль76(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем76;
	Перем ТекущаяСтрока76;
	Перем ИндексЭлемента76;
	Перем КоличествоСтрок76;

	ТекущаяСтрока76 = 0;
	ИндексЭлемента76 = 1;
	КоличествоСтрок76 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока76 = ТекущаяСтрока76 + Индекс;
		Если ТекущаяСтрока76 > 50 Тогда
			ИндексЭлемента76 = ТекущаяСтрока76;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока76 = ТекущаяСтрока76 + 0;
	ИндексЭлемента76 = ИндексЭлемента76 + 1;
	КоличествоСтрок76 = КоличествоСтрок76 + 2;
	ТекущаяСтрока76 = ТекущаяСтрока76 + 3;
	ИндексЭлемента76 = ИндексЭлемента76 + 4;
	КоличествоСтрок76 = КоличествоСтрок76 + 5;
	ТекущаяСтрока76 = ТекущаяСтрока76 + 6;
	ИндексЭлемента76 = ИндексЭлемента76 + 7;
	КоличествоСтрок76 = КоличествоСтрок76 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер77(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем77;
	Перем ТекущаяСтрока77;
	Перем ИндексЭлемента77;
	Перем КоличествоСтрок77;

	ТекущаяСтрока77 = 0;
	ИндексЭлемента77 = 1;
	КоличествоСтрок77 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока77 = ТекущаяСтрока77 + Индекс;
		Если ТекущаяСтрока77 > 50 Тогда
			ИндексЭлемента77 = ТекущаяСтрока77;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока77 = ТекущаяСтрока77 + 0;
	ИндексЭлемента77 = ИндексЭлемента77 + 1;
	КоличествоСтрок77 = КоличествоСтрок77 + 2;
	ТекущаяСтрока77 = ТекущаяСтрока77 + 3;
	ИндексЭлемента77 = ИндексЭлемента77 + 4;
	КоличествоСтрок77 = КоличествоСтрок77 + 5;
	ТекущаяСтрока77 = ТекущаяСтрока77 + 6;
	ИндексЭлемента77 = ИндексЭлемента77 + 7;
	КоличествоСтрок77 = КоличествоСтрок77 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос78(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем78;
	Перем ТекущаяСтрока78;
	Перем ИндексЭлемента78;
	Перем КоличествоСтрок78;

	ТекущаяСтрока78 = 0;
	ИндексЭлемента78 = 1;
	КоличествоСтрок78 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока78 = ТекущаяСтрока78 + Индекс;
		Если ТекущаяСтрока78 > 50 Тогда
			ИндексЭлемента78 = ТекущаяСтрока78;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока78 = ТекущаяСтрока78 + 0;
	ИндексЭлемента78 = ИндексЭлемента78 + 1;
	КоличествоСтрок78 = КоличествоСтрок78 + 2;
	ТекущаяСтрока78 = ТекущаяСтрока78 + 3;
	ИндексЭлемента78 = ИндексЭлемента78 + 4;
	КоличествоСтрок78 = КоличествоСтрок78 + 5;
	ТекущаяСтрока78 = ТекущаяСтрока78 + 6;
	ИндексЭлемента78 = ИндексЭлемента78 + 7;
	КоличествоСтрок78 = КоличествоСтрок78 + 8;

КонецПроцедуры

Процедура СохранитьДанные79(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем79;
	Перем ТекущаяСтрока79;
	Перем ИндексЭлемента79;
	Перем КоличествоСтрок79;

	ТекущаяСтрока79 = 0;
	ИндексЭлемента79 = 1;
	КоличествоСтрок79 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока79 = ТекущаяСтрока79 + Индекс;
		Если ТекущаяСтрока79 > 50 Тогда
			ИндексЭлемента79 = ТекущаяСтрока79;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока79 = ТекущаяСтрока79 + 0;
	ИндексЭлемента79 = ИндексЭлемента79 + 1;
	КоличествоСтрок79 = КоличествоСтрок79 + 2;
	ТекущаяСтрока79 = ТекущаяСтрока79 + 3;
	ИндексЭлемента79 = ИндексЭлемента79 + 4;
	КоличествоСтрок79 = КоличествоСтрок79 + 5;
	ТекущаяСтрока79 = ТекущаяСтрока79 + 6;
	ИндексЭлемента79 = ИндексЭлемента79 + 7;
	КоличествоСтрок79 = КоличествоСтрок79 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды21

Процедура ЗагрузитьКонфигурацию80(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем80;
	Перем ТекущаяСтрока80;
	Перем ИндексЭлемента80;
	Перем КоличествоСтрок80;

	ТекущаяСтрока80 = 0;
	ИндексЭлемента80 = 1;
	КоличествоСтрок80 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока80 = ТекущаяСтрока80 + Индекс;
		Если ТекущаяСтрока80 > 50 Тогда
			ИндексЭлемента80 = ТекущаяСтрока80;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока80 = ТекущаяСтрока80 + 0;
	ИндексЭлемента80 = ИндексЭлемента80 + 1;
	КоличествоСтрок80 = КоличествоСтрок80 + 2;
	ТекущаяСтрока80 = ТекущаяСтрока80 + 3;
	ИндексЭлемента80 = ИндексЭлемента80 + 4;
	КоличествоСтрок80 = КоличествоСтрок80 + 5;
	ТекущаяСтрока80 = ТекущаяСтрока80 + 6;
	ИндексЭлемента80 = ИндексЭлемента80 + 7;
	КоличествоСтрок80 = КоличествоСтрок80 + 8;

КонецПроцедуры

Процедура ПроверитьПрава81(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем81;
	Перем ТекущаяСтрока81;
	Перем ИндексЭлемента81;
	Перем КоличествоСтрок81;

	ТекущаяСтрока81 = 0;
	ИндексЭлемента81 = 1;
	КоличествоСтрок81 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока81 = ТекущаяСтрока81 + Индекс;
		Если ТекущаяСтрока81 > 50 Тогда
			ИндексЭлемента81 = ТекущаяСтрока81;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока81 = ТекущаяСтрока81 + 0;
	ИндексЭлемента81 = ИндексЭлемента81 + 1;
	КоличествоСтрок81 = КоличествоСтрок81 + 2;
	ТекущаяСтрока81 = ТекущаяСтрока81 + 3;
	ИндексЭлемента81 = ИндексЭлемента81 + 4;
	КоличествоСтрок81 = КоличествоСтрок81 + 5;
	ТекущаяСтрока81 = ТекущаяСтрока81 + 6;
	ИндексЭлемента81 = ИндексЭлемента81 + 7;
	КоличествоСтрок81 = КоличествоСтрок81 + 8;

КонецПроцедуры

Процедура ОбновитьКэш82(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем82;
	Перем ТекущаяСтрока82;
	Перем ИндексЭлемента82;
	Перем КоличествоСтрок82;

	ТекущаяСтрока82 = 0;
	ИндексЭлемента82 = 1;
	КоличествоСтрок82 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока82 = ТекущаяСтрока82 + Индекс;
		Если ТекущаяСтрока82 > 50 Тогда
			ИндексЭлемента82 = ТекущаяСтрока82;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока82 = ТекущаяСтрока82 + 0;
	ИндексЭлемента82 = ИндексЭлемента82 + 1;
	КоличествоСтрок82 = КоличествоСтрок82 + 2;
	ТекущаяСтрока82 = ТекущаяСтрока82 + 3;
	ИндексЭлемента82 = ИндексЭлемента82 + 4;
	КоличествоСтрок82 = КоличествоСтрок82 + 5;
	ТекущаяСтрока82 = ТекущаяСтрока82 + 6;
	ИндексЭлемента82 = ИндексЭлемента82 + 7;
	КоличествоСтрок82 = КоличествоСтрок82 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление83(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем83;
	Перем ТекущаяСтрока83;
	Перем ИндексЭлемента83;
	Перем КоличествоСтрок83;

	ТекущаяСтрока83 = 0;
	ИндексЭлемента83 = 1;
	КоличествоСтрок83 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока83 = ТекущаяСтрока83 + Индекс;
		Если ТекущаяСтрока83 > 50 Тогда
			ИндексЭлемента83 = ТекущаяСтрока83;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока83 = ТекущаяСтрока83 + 0;
	ИндексЭлемента83 = ИндексЭлемента83 + 1;
	КоличествоСтрок83 = КоличествоСтрок83 + 2;
	ТекущаяСтрока83 = ТекущаяСтрока83 + 3;
	ИндексЭлемента83 = ИндексЭлемента83 + 4;
	КоличествоСтрок83 = КоличествоСтрок83 + 5;
	ТекущаяСтрока83 = ТекущаяСтрока83 + 6;
	ИндексЭлемента83 = ИндексЭлемента83 + 7;
	КоличествоСтрок83 = КоличествоСтрок83 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции22

Процедура ОбработатьСтроку84(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем84;
	Перем ТекущаяСтрока84;
	Перем ИндексЭлемента84;
	Перем КоличествоСтрок84;

	ТекущаяСтрока84 = 0;
	ИндексЭлемента84 = 1;
	КоличествоСтрок84 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока84 = ТекущаяСтрока84 + Индекс;
		Если ТекущаяСтрока84 > 50 Тогда
			ИндексЭлемента84 = ТекущаяСтрока84;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока84 = ТекущаяСтрока84 + 0;
	ИндексЭлемента84 = ИндексЭлемента84 + 1;
	КоличествоСтрок84 = КоличествоСтрок84 + 2;
	ТекущаяСтрока84 = ТекущаяСтрока84 + 3;
	ИндексЭлемента84 = ИндексЭлемента84 + 4;
	КоличествоСтрок84 = КоличествоСтрок84 + 5;
	ТекущаяСтрока84 = ТекущаяСтрока84 + 6;
	ИндексЭлемента84 = ИндексЭлемента84 + 7;
	КоличествоСтрок84 = КоличествоСтрок84 + 8;

КонецПроцедуры

Процедура ВычислитьСумму85(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем85;
	Перем ТекущаяСтрока85;
	Перем ИндексЭлемента85;
	Перем КоличествоСтрок85;

	ТекущаяСтрока85 = 0;
	ИндексЭлемента85 = 1;
	КоличествоСтрок85 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока85 = ТекущаяСтрока85 + Индекс;
		Если ТекущаяСтрока85 > 50 Тогда
			ИндексЭлемента85 = ТекущаяСтрока85;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока85 = ТекущаяСтрока85 + 0;
	ИндексЭлемента85 = ИндексЭлемента85 + 1;
	КоличествоСтрок85 = КоличествоСтрок85 + 2;
	ТекущаяСтрока85 = ТекущаяСтрока85 + 3;
	ИндексЭлемента85 = ИндексЭлемента85 + 4;
	КоличествоСтрок85 = КоличествоСтрок85 + 5;
	ТекущаяСтрока85 = ТекущаяСтрока85 + 6;
	ИндексЭлемента85 = ИндексЭлемента85 + 7;
	КоличествоСтрок85 = КоличествоСтрок85 + 8;

КонецПроцедуры

Процедура ПроверитьДанные86(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем86;
	Перем ТекущаяСтрока86;
	Перем ИндексЭлемента86;
	Перем КоличествоСтрок86;

	ТекущаяСтрока86 = 0;
	ИндексЭлемента86 = 1;
	КоличествоСтрок86 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока86 = ТекущаяСтрока86 + Индекс;
		Если ТекущаяСтрока86 > 50 Тогда
			ИндексЭлемента86 = ТекущаяСтрока86;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока86 = ТекущаяСтрока86 + 0;
	ИндексЭлемента86 = ИндексЭлемента86 + 1;
	КоличествоСтрок86 = КоличествоСтрок86 + 2;
	ТекущаяСтрока86 = ТекущаяСтрока86 + 3;
	ИндексЭлемента86 = ИндексЭлемента86 + 4;
	КоличествоСтрок86 = КоличествоСтрок86 + 5;
	ТекущаяСтрока86 = ТекущаяСтрока86 + 6;
	ИндексЭлемента86 = ИндексЭлемента86 + 7;
	КоличествоСтрок86 = КоличествоСтрок86 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал87(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем87;
	Перем ТекущаяСтрока87;
	Перем ИндексЭлемента87;
	Перем КоличествоСтрок87;

	ТекущаяСтрока87 = 0;
	ИндексЭлемента87 = 1;
	КоличествоСтрок87 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока87 = ТекущаяСтрока87 + Индекс;
		Если ТекущаяСтрока87 > 50 Тогда
			ИндексЭлемента87 = ТекущаяСтрока87;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока87 = ТекущаяСтрока87 + 0;
	ИндексЭлемента87 = ИндексЭлемента87 + 1;
	КоличествоСтрок87 = КоличествоСтрок87 + 2;
	ТекущаяСтрока87 = ТекущаяСтрока87 + 3;
	ИндексЭлемента87 = ИндексЭлемента87 + 4;
	КоличествоСтрок87 = КоличествоСтрок87 + 5;
	ТекущаяСтрока87 = ТекущаяСтрока87 + 6;
	ИндексЭлемента87 = ИндексЭлемента87 + 7;
	КоличествоСтрок87 = КоличествоСтрок87 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий23

Процедура ИнициализироватьМодуль88(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем88;
	Перем ТекущаяСтрока88;
	Перем ИндексЭлемента88;
	Перем КоличествоСтрок88;

	ТекущаяСтрока88 = 0;
	ИндексЭлемента88 = 1;
	КоличествоСтрок88 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока88 = ТекущаяСтрока88 + Индекс;
		Если ТекущаяСтрока88 > 50 Тогда
			ИндексЭлемента88 = ТекущаяСтрока88;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока88 = ТекущаяСтрока88 + 0;
	ИндексЭлемента88 = ИндексЭлемента88 + 1;
	КоличествоСтрок88 = КоличествоСтрок88 + 2;
	ТекущаяСтрока88 = ТекущаяСтрока88 + 3;
	ИндексЭлемента88 = ИндексЭлемента88 + 4;
	КоличествоСтрок88 = КоличествоСтрок88 + 5;
	ТекущаяСтрока88 = ТекущаяСтрока88 + 6;
	ИндексЭлемента88 = ИндексЭлемента88 + 7;
	КоличествоСтрок88 = КоличествоСтрок88 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер89(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем89;
	Перем ТекущаяСтрока89;
	Перем ИндексЭлемента89;
	Перем КоличествоСтрок89;

	ТекущаяСтрока89 = 0;
	ИндексЭлемента89 = 1;
	КоличествоСтрок89 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока89 = ТекущаяСтрока89 + Индекс;
		Если ТекущаяСтрока89 > 50 Тогда
			ИндексЭлемента89 = ТекущаяСтрока89;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока89 = ТекущаяСтрока89 + 0;
	ИндексЭлемента89 = ИндексЭлемента89 + 1;
	КоличествоСтрок89 = КоличествоСтрок89 + 2;
	ТекущаяСтрока89 = ТекущаяСтрока89 + 3;
	ИндексЭлемента89 = ИндексЭлемента89 + 4;
	КоличествоСтрок89 = КоличествоСтрок89 + 5;
	ТекущаяСтрока89 = ТекущаяСтрока89 + 6;
	ИндексЭлемента89 = ИндексЭлемента89 + 7;
	КоличествоСтрок89 = КоличествоСтрок89 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос90(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем90;
	Перем ТекущаяСтрока90;
	Перем ИндексЭлемента90;
	Перем КоличествоСтрок90;

	ТекущаяСтрока90 = 0;
	ИндексЭлемента90 = 1;
	КоличествоСтрок90 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока90 = ТекущаяСтрока90 + Индекс;
		Если ТекущаяСтрока90 > 50 Тогда
			ИндексЭлемента90 = ТекущаяСтрока90;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока90 = ТекущаяСтрока90 + 0;
	ИндексЭлемента90 = ИндексЭлемента90 + 1;
	КоличествоСтрок90 = КоличествоСтрок90 + 2;
	ТекущаяСтрока90 = ТекущаяСтрока90 + 3;
	ИндексЭлемента90 = ИндексЭлемента90 + 4;
	КоличествоСтрок90 = КоличествоСтрок90 + 5;
	ТекущаяСтрока90 = ТекущаяСтрока90 + 6;
	ИндексЭлемента90 = ИндексЭлемента90 + 7;
	КоличествоСтрок90 = КоличествоСтрок90 + 8;

КонецПроцедуры

Процедура СохранитьДанные91(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем91;
	Перем ТекущаяСтрока91;
	Перем ИндексЭлемента91;
	Перем КоличествоСтрок91;

	ТекущаяСтрока91 = 0;
	ИндексЭлемента91 = 1;
	КоличествоСтрок91 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока91 = ТекущаяСтрока91 + Индекс;
		Если ТекущаяСтрока91 > 50 Тогда
			ИндексЭлемента91 = ТекущаяСтрока91;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока91 = ТекущаяСтрока91 + 0;
	ИндексЭлемента91 = ИндексЭлемента91 + 1;
	КоличествоСтрок91 = КоличествоСтрок91 + 2;
	ТекущаяСтрока91 = ТекущаяСтрока91 + 3;
	ИндексЭлемента91 = ИндексЭлемента91 + 4;
	КоличествоСтрок91 = КоличествоСтрок91 + 5;
	ТекущаяСтрока91 = ТекущаяСтрока91 + 6;
	ИндексЭлемента91 = ИндексЭлемента91 + 7;
	КоличествоСтрок91 = КоличествоСтрок91 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции24

Процедура ЗагрузитьКонфигурацию92(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем92;
	Перем ТекущаяСтрока92;
	Перем ИндексЭлемента92;
	Перем КоличествоСтрок92;

	ТекущаяСтрока92 = 0;
	ИндексЭлемента92 = 1;
	КоличествоСтрок92 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока92 = ТекущаяСтрока92 + Индекс;
		Если ТекущаяСтрока92 > 50 Тогда
			ИндексЭлемента92 = ТекущаяСтрока92;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока92 = ТекущаяСтрока92 + 0;
	ИндексЭлемента92 = ИндексЭлемента92 + 1;
	КоличествоСтрок92 = КоличествоСтрок92 + 2;
	ТекущаяСтрока92 = ТекущаяСтрока92 + 3;
	ИндексЭлемента92 = ИндексЭлемента92 + 4;
	КоличествоСтрок92 = КоличествоСтрок92 + 5;
	ТекущаяСтрока92 = ТекущаяСтрока92 + 6;
	ИндексЭлемента92 = ИндексЭлемента92 + 7;
	КоличествоСтрок92 = КоличествоСтрок92 + 8;

КонецПроцедуры

Процедура ПроверитьПрава93(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем93;
	Перем ТекущаяСтрока93;
	Перем ИндексЭлемента93;
	Перем КоличествоСтрок93;

	ТекущаяСтрока93 = 0;
	ИндексЭлемента93 = 1;
	КоличествоСтрок93 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока93 = ТекущаяСтрока93 + Индекс;
		Если ТекущаяСтрока93 > 50 Тогда
			ИндексЭлемента93 = ТекущаяСтрока93;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока93 = ТекущаяСтрока93 + 0;
	ИндексЭлемента93 = ИндексЭлемента93 + 1;
	КоличествоСтрок93 = КоличествоСтрок93 + 2;
	ТекущаяСтрока93 = ТекущаяСтрока93 + 3;
	ИндексЭлемента93 = ИндексЭлемента93 + 4;
	КоличествоСтрок93 = КоличествоСтрок93 + 5;
	ТекущаяСтрока93 = ТекущаяСтрока93 + 6;
	ИндексЭлемента93 = ИндексЭлемента93 + 7;
	КоличествоСтрок93 = КоличествоСтрок93 + 8;

КонецПроцедуры

Процедура ОбновитьКэш94(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем94;
	Перем ТекущаяСтрока94;
	Перем ИндексЭлемента94;
	Перем КоличествоСтрок94;

	ТекущаяСтрока94 = 0;
	ИндексЭлемента94 = 1;
	КоличествоСтрок94 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока94 = ТекущаяСтрока94 + Индекс;
		Если ТекущаяСтрока94 > 50 Тогда
			ИндексЭлемента94 = ТекущаяСтрока94;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока94 = ТекущаяСтрока94 + 0;
	ИндексЭлемента94 = ИндексЭлемента94 + 1;
	КоличествоСтрок94 = КоличествоСтрок94 + 2;
	ТекущаяСтрока94 = ТекущаяСтрока94 + 3;
	ИндексЭлемента94 = ИндексЭлемента94 + 4;
	КоличествоСтрок94 = КоличествоСтрок94 + 5;
	ТекущаяСтрока94 = ТекущаяСтрока94 + 6;
	ИндексЭлемента94 = ИндексЭлемента94 + 7;
	КоличествоСтрок94 = КоличествоСтрок94 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление95(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем95;
	Перем ТекущаяСтрока95;
	Перем ИндексЭлемента95;
	Перем КоличествоСтрок95;

	ТекущаяСтрока95 = 0;
	ИндексЭлемента95 = 1;
	КоличествоСтрок95 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока95 = ТекущаяСтрока95 + Индекс;
		Если ТекущаяСтрока95 > 50 Тогда
			ИндексЭлемента95 = ТекущаяСтрока95;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока95 = ТекущаяСтрока95 + 0;
	ИндексЭлемента95 = ИндексЭлемента95 + 1;
	КоличествоСтрок95 = КоличествоСтрок95 + 2;
	ТекущаяСтрока95 = ТекущаяСтрока95 + 3;
	ИндексЭлемента95 = ИндексЭлемента95 + 4;
	КоличествоСтрок95 = КоличествоСтрок95 + 5;
	ТекущаяСтрока95 = ТекущаяСтрока95 + 6;
	ИндексЭлемента95 = ИндексЭлемента95 + 7;
	КоличествоСтрок95 = КоличествоСтрок95 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные25

Процедура ОбработатьСтроку96(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем96;
	Перем ТекущаяСтрока96;
	Перем ИндексЭлемента96;
	Перем КоличествоСтрок96;

	ТекущаяСтрока96 = 0;
	ИндексЭлемента96 = 1;
	КоличествоСтрок96 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока96 = ТекущаяСтрока96 + Индекс;
		Если ТекущаяСтрока96 > 50 Тогда
			ИндексЭлемента96 = ТекущаяСтрока96;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока96 = ТекущаяСтрока96 + 0;
	ИндексЭлемента96 = ИндексЭлемента96 + 1;
	КоличествоСтрок96 = КоличествоСтрок96 + 2;
	ТекущаяСтрока96 = ТекущаяСтрока96 + 3;
	ИндексЭлемента96 = ИндексЭлемента96 + 4;
	КоличествоСтрок96 = КоличествоСтрок96 + 5;
	ТекущаяСтрока96 = ТекущаяСтрока96 + 6;
	ИндексЭлемента96 = ИндексЭлемента96 + 7;
	КоличествоСтрок96 = КоличествоСтрок96 + 8;

КонецПроцедуры

Процедура ВычислитьСумму97(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем97;
	Перем ТекущаяСтрока97;
	Перем ИндексЭлемента97;
	Перем КоличествоСтрок97;

	ТекущаяСтрока97 = 0;
	ИндексЭлемента97 = 1;
	КоличествоСтрок97 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока97 = ТекущаяСтрока97 + Индекс;
		Если ТекущаяСтрока97 > 50 Тогда
			ИндексЭлемента97 = ТекущаяСтрока97;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока97 = ТекущаяСтрока97 + 0;
	ИндексЭлемента97 = ИндексЭлемента97 + 1;
	КоличествоСтрок97 = КоличествоСтрок97 + 2;
	ТекущаяСтрока97 = ТекущаяСтрока97 + 3;
	ИндексЭлемента97 = ИндексЭлемента97 + 4;
	КоличествоСтрок97 = КоличествоСтрок97 + 5;
	ТекущаяСтрока97 = ТекущаяСтрока97 + 6;
	ИндексЭлемента97 = ИндексЭлемента97 + 7;
	КоличествоСтрок97 = КоличествоСтрок97 + 8;

КонецПроцедуры

Процедура ПроверитьДанные98(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем98;
	Перем ТекущаяСтрока98;
	Перем ИндексЭлемента98;
	Перем КоличествоСтрок98;

	ТекущаяСтрока98 = 0;
	ИндексЭлемента98 = 1;
	КоличествоСтрок98 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока98 = ТекущаяСтрока98 + Индекс;
		Если ТекущаяСтрока98 > 50 Тогда
			ИндексЭлемента98 = ТекущаяСтрока98;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока98 = ТекущаяСтрока98 + 0;
	ИндексЭлемента98 = ИндексЭлемента98 + 1;
	КоличествоСтрок98 = КоличествоСтрок98 + 2;
	ТекущаяСтрока98 = ТекущаяСтрока98 + 3;
	ИндексЭлемента98 = ИндексЭлемента98 + 4;
	КоличествоСтрок98 = КоличествоСтрок98 + 5;
	ТекущаяСтрока98 = ТекущаяСтрока98 + 6;
	ИндексЭлемента98 = ИндексЭлемента98 + 7;
	КоличествоСтрок98 = КоличествоСтрок98 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал99(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем99;
	Перем ТекущаяСтрока99;
	Перем ИндексЭлемента99;
	Перем КоличествоСтрок99;

	ТекущаяСтрока99 = 0;
	ИндексЭлемента99 = 1;
	КоличествоСтрок99 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока99 = ТекущаяСтрока99 + Индекс;
		Если ТекущаяСтрока99 > 50 Тогда
			ИндексЭлемента99 = ТекущаяСтрока99;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока99 = ТекущаяСтрока99 + 0;
	ИндексЭлемента99 = ИндексЭлемента99 + 1;
	КоличествоСтрок99 = КоличествоСтрок99 + 2;
	ТекущаяСтрока99 = ТекущаяСтрока99 + 3;
	ИндексЭлемента99 = ИндексЭлемента99 + 4;
	КоличествоСтрок99 = КоличествоСтрок99 + 5;
	ТекущаяСтрока99 = ТекущаяСтрока99 + 6;
	ИндексЭлемента99 = ИндексЭлемента99 + 7;
	КоличествоСтрок99 = КоличествоСтрок99 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры26

Процедура ИнициализироватьМодуль100(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем100;
	Перем ТекущаяСтрока100;
	Перем ИндексЭлемента100;
	Перем КоличествоСтрок100;

	ТекущаяСтрока100 = 0;
	ИндексЭлемента100 = 1;
	КоличествоСтрок100 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока100 = ТекущаяСтрока100 + Индекс;
		Если ТекущаяСтрока100 > 50 Тогда
			ИндексЭлемента100 = ТекущаяСтрока100;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока100 = ТекущаяСтрока100 + 0;
	ИндексЭлемента100 = ИндексЭлемента100 + 1;
	КоличествоСтрок100 = КоличествоСтрок100 + 2;
	ТекущаяСтрока100 = ТекущаяСтрока100 + 3;
	ИндексЭлемента100 = ИндексЭлемента100 + 4;
	КоличествоСтрок100 = КоличествоСтрок100 + 5;
	ТекущаяСтрока100 = ТекущаяСтрока100 + 6;
	ИндексЭлемента100 = ИндексЭлемента100 + 7;
	КоличествоСтрок100 = КоличествоСтрок100 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер101(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем101;
	Перем ТекущаяСтрока101;
	Перем ИндексЭлемента101;
	Перем КоличествоСтрок101;

	ТекущаяСтрока101 = 0;
	ИндексЭлемента101 = 1;
	КоличествоСтрок101 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока101 = ТекущаяСтрока101 + Индекс;
		Если ТекущаяСтрока101 > 50 Тогда
			ИндексЭлемента101 = ТекущаяСтрока101;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока101 = ТекущаяСтрока101 + 0;
	ИндексЭлемента101 = ИндексЭлемента101 + 1;
	КоличествоСтрок101 = КоличествоСтрок101 + 2;
	ТекущаяСтрока101 = ТекущаяСтрока101 + 3;
	ИндексЭлемента101 = ИндексЭлемента101 + 4;
	КоличествоСтрок101 = КоличествоСтрок101 + 5;
	ТекущаяСтрока101 = ТекущаяСтрока101 + 6;
	ИндексЭлемента101 = ИндексЭлемента101 + 7;
	КоличествоСтрок101 = КоличествоСтрок101 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос102(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем102;
	Перем ТекущаяСтрока102;
	Перем ИндексЭлемента102;
	Перем КоличествоСтрок102;

	ТекущаяСтрока102 = 0;
	ИндексЭлемента102 = 1;
	КоличествоСтрок102 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока102 = ТекущаяСтрока102 + Индекс;
		Если ТекущаяСтрока102 > 50 Тогда
			ИндексЭлемента102 = ТекущаяСтрока102;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока102 = ТекущаяСтрока102 + 0;
	ИндексЭлемента102 = ИндексЭлемента102 + 1;
	КоличествоСтрок102 = КоличествоСтрок102 + 2;
	ТекущаяСтрока102 = ТекущаяСтрока102 + 3;
	ИндексЭлемента102 = ИндексЭлемента102 + 4;
	КоличествоСтрок102 = КоличествоСтрок102 + 5;
	ТекущаяСтрока102 = ТекущаяСтрока102 + 6;
	ИндексЭлемента102 = ИндексЭлемента102 + 7;
	КоличествоСтрок102 = КоличествоСтрок102 + 8;

КонецПроцедуры

Процедура СохранитьДанные103(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем103;
	Перем ТекущаяСтрока103;
	Перем ИндексЭлемента103;
	Перем КоличествоСтрок103;

	ТекущаяСтрока103 = 0;
	ИндексЭлемента103 = 1;
	КоличествоСтрок103 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока103 = ТекущаяСтрока103 + Индекс;
		Если ТекущаяСтрока103 > 50 Тогда
			ИндексЭлемента103 = ТекущаяСтрока103;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока103 = ТекущаяСтрока103 + 0;
	ИндексЭлемента103 = ИндексЭлемента103 + 1;
	КоличествоСтрок103 = КоличествоСтрок103 + 2;
	ТекущаяСтрока103 = ТекущаяСтрока103 + 3;
	ИндексЭлемента103 = ИндексЭлемента103 + 4;
	КоличествоСтрок103 = КоличествоСтрок103 + 5;
	ТекущаяСтрока103 = ТекущаяСтрока103 + 6;
	ИндексЭлемента103 = ИндексЭлемента103 + 7;
	КоличествоСтрок103 = КоличествоСтрок103 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды27

Процедура ЗагрузитьКонфигурацию104(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем104;
	Перем ТекущаяСтрока104;
	Перем ИндексЭлемента104;
	Перем КоличествоСтрок104;

	ТекущаяСтрока104 = 0;
	ИндексЭлемента104 = 1;
	КоличествоСтрок104 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока104 = ТекущаяСтрока104 + Индекс;
		Если ТекущаяСтрока104 > 50 Тогда
			ИндексЭлемента104 = ТекущаяСтрока104;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока104 = ТекущаяСтрока104 + 0;
	ИндексЭлемента104 = ИндексЭлемента104 + 1;
	КоличествоСтрок104 = КоличествоСтрок104 + 2;
	ТекущаяСтрока104 = ТекущаяСтрока104 + 3;
	ИндексЭлемента104 = ИндексЭлемента104 + 4;
	КоличествоСтрок104 = КоличествоСтрок104 + 5;
	ТекущаяСтрока104 = ТекущаяСтрока104 + 6;
	ИндексЭлемента104 = ИндексЭлемента104 + 7;
	КоличествоСтрок104 = КоличествоСтрок104 + 8;

КонецПроцедуры

Процедура ПроверитьПрава105(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем105;
	Перем ТекущаяСтрока105;
	Перем ИндексЭлемента105;
	Перем КоличествоСтрок105;

	ТекущаяСтрока105 = 0;
	ИндексЭлемента105 = 1;
	КоличествоСтрок105 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока105 = ТекущаяСтрока105 + Индекс;
		Если ТекущаяСтрока105 > 50 Тогда
			ИндексЭлемента105 = ТекущаяСтрока105;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока105 = ТекущаяСтрока105 + 0;
	ИндексЭлемента105 = ИндексЭлемента105 + 1;
	КоличествоСтрок105 = КоличествоСтрок105 + 2;
	ТекущаяСтрока105 = ТекущаяСтрока105 + 3;
	ИндексЭлемента105 = ИндексЭлемента105 + 4;
	КоличествоСтрок105 = КоличествоСтрок105 + 5;
	ТекущаяСтрока105 = ТекущаяСтрока105 + 6;
	ИндексЭлемента105 = ИндексЭлемента105 + 7;
	КоличествоСтрок105 = КоличествоСтрок105 + 8;

КонецПроцедуры

Процедура ОбновитьКэш106(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем106;
	Перем ТекущаяСтрока106;
	Перем ИндексЭлемента106;
	Перем КоличествоСтрок106;

	ТекущаяСтрока106 = 0;
	ИндексЭлемента106 = 1;
	КоличествоСтрок106 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока106 = ТекущаяСтрока106 + Индекс;
		Если ТекущаяСтрока106 > 50 Тогда
			ИндексЭлемента106 = ТекущаяСтрока106;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока106 = ТекущаяСтрока106 + 0;
	ИндексЭлемента106 = ИндексЭлемента106 + 1;
	КоличествоСтрок106 = КоличествоСтрок106 + 2;
	ТекущаяСтрока106 = ТекущаяСтрока106 + 3;
	ИндексЭлемента106 = ИндексЭлемента106 + 4;
	КоличествоСтрок106 = КоличествоСтрок106 + 5;
	ТекущаяСтрока106 = ТекущаяСтрока106 + 6;
	ИндексЭлемента106 = ИндексЭлемента106 + 7;
	КоличествоСтрок106 = КоличествоСтрок106 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление107(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем107;
	Перем ТекущаяСтрока107;
	Перем ИндексЭлемента107;
	Перем КоличествоСтрок107;

	ТекущаяСтрока107 = 0;
	ИндексЭлемента107 = 1;
	КоличествоСтрок107 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока107 = ТекущаяСтрока107 + Индекс;
		Если ТекущаяСтрока107 > 50 Тогда
			ИндексЭлемента107 = ТекущаяСтрока107;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока107 = ТекущаяСтрока107 + 0;
	ИндексЭлемента107 = ИндексЭлемента107 + 1;
	КоличествоСтрок107 = КоличествоСтрок107 + 2;
	ТекущаяСтрока107 = ТекущаяСтрока107 + 3;
	ИндексЭлемента107 = ИндексЭлемента107 + 4;
	КоличествоСтрок107 = КоличествоСтрок107 + 5;
	ТекущаяСтрока107 = ТекущаяСтрока107 + 6;
	ИндексЭлемента107 = ИндексЭлемента107 + 7;
	КоличествоСтрок107 = КоличествоСтрок107 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции28

Процедура ОбработатьСтроку108(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем108;
	Перем ТекущаяСтрока108;
	Перем ИндексЭлемента108;
	Перем КоличествоСтрок108;

	ТекущаяСтрока108 = 0;
	ИндексЭлемента108 = 1;
	КоличествоСтрок108 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока108 = ТекущаяСтрока108 + Индекс;
		Если ТекущаяСтрока108 > 50 Тогда
			ИндексЭлемента108 = ТекущаяСтрока108;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока108 = ТекущаяСтрока108 + 0;
	ИндексЭлемента108 = ИндексЭлемента108 + 1;
	КоличествоСтрок108 = КоличествоСтрок108 + 2;
	ТекущаяСтрока108 = ТекущаяСтрока108 + 3;
	ИндексЭлемента108 = ИндексЭлемента108 + 4;
	КоличествоСтрок108 = КоличествоСтрок108 + 5;
	ТекущаяСтрока108 = ТекущаяСтрока108 + 6;
	ИндексЭлемента108 = ИндексЭлемента108 + 7;
	КоличествоСтрок108 = КоличествоСтрок108 + 8;

КонецПроцедуры

Процедура ВычислитьСумму109(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем109;
	Перем ТекущаяСтрока109;
	Перем ИндексЭлемента109;
	Перем КоличествоСтрок109;

	ТекущаяСтрока109 = 0;
	ИндексЭлемента109 = 1;
	КоличествоСтрок109 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока109 = ТекущаяСтрока109 + Индекс;
		Если ТекущаяСтрока109 > 50 Тогда
			ИндексЭлемента109 = ТекущаяСтрока109;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока109 = ТекущаяСтрока109 + 0;
	ИндексЭлемента109 = ИндексЭлемента109 + 1;
	КоличествоСтрок109 = КоличествоСтрок109 + 2;
	ТекущаяСтрока109 = ТекущаяСтрока109 + 3;
	ИндексЭлемента109 = ИндексЭлемента109 + 4;
	КоличествоСтрок109 = КоличествоСтрок109 + 5;
	ТекущаяСтрока109 = ТекущаяСтрока109 + 6;
	ИндексЭлемента109 = ИндексЭлемента109 + 7;
	КоличествоСтрок109 = КоличествоСтрок109 + 8;

КонецПроцедуры

Процедура ПроверитьДанные110(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем110;
	Перем ТекущаяСтрока110;
	Перем ИндексЭлемента110;
	Перем КоличествоСтрок110;

	ТекущаяСтрока110 = 0;
	ИндексЭлемента110 = 1;
	КоличествоСтрок110 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока110 = ТекущаяСтрока110 + Индекс;
		Если ТекущаяСтрока110 > 50 Тогда
			ИндексЭлемента110 = ТекущаяСтрока110;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока110 = ТекущаяСтрока110 + 0;
	ИндексЭлемента110 = ИндексЭлемента110 + 1;
	КоличествоСтрок110 = КоличествоСтрок110 + 2;
	ТекущаяСтрока110 = ТекущаяСтрока110 + 3;
	ИндексЭлемента110 = ИндексЭлемента110 + 4;
	КоличествоСтрок110 = КоличествоСтрок110 + 5;
	ТекущаяСтрока110 = ТекущаяСтрока110 + 6;
	ИндексЭлемента110 = ИндексЭлемента110 + 7;
	КоличествоСтрок110 = КоличествоСтрок110 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал111(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем111;
	Перем ТекущаяСтрока111;
	Перем ИндексЭлемента111;
	Перем КоличествоСтрок111;

	ТекущаяСтрока111 = 0;
	ИндексЭлемента111 = 1;
	КоличествоСтрок111 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока111 = ТекущаяСтрока111 + Индекс;
		Если ТекущаяСтрока111 > 50 Тогда
			ИндексЭлемента111 = ТекущаяСтрока111;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока111 = ТекущаяСтрока111 + 0;
	ИндексЭлемента111 = ИндексЭлемента111 + 1;
	КоличествоСтрок111 = КоличествоСтрок111 + 2;
	ТекущаяСтрока111 = ТекущаяСтрока111 + 3;
	ИндексЭлемента111 = ИндексЭлемента111 + 4;
	КоличествоСтрок111 = КоличествоСтрок111 + 5;
	ТекущаяСтрока111 = ТекущаяСтрока111 + 6;
	ИндексЭлемента111 = ИндексЭлемента111 + 7;
	КоличествоСтрок111 = КоличествоСтрок111 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий29

Процедура ИнициализироватьМодуль112(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем112;
	Перем ТекущаяСтрока112;
	Перем ИндексЭлемента112;
	Перем КоличествоСтрок112;

	ТекущаяСтрока112 = 0;
	ИндексЭлемента112 = 1;
	КоличествоСтрок112 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока112 = ТекущаяСтрока112 + Индекс;
		Если ТекущаяСтрока112 > 50 Тогда
			ИндексЭлемента112 = ТекущаяСтрока112;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока112 = ТекущаяСтрока112 + 0;
	ИндексЭлемента112 = ИндексЭлемента112 + 1;
	КоличествоСтрок112 = КоличествоСтрок112 + 2;
	ТекущаяСтрока112 = ТекущаяСтрока112 + 3;
	ИндексЭлемента112 = ИндексЭлемента112 + 4;
	КоличествоСтрок112 = КоличествоСтрок112 + 5;
	ТекущаяСтрока112 = ТекущаяСтрока112 + 6;
	ИндексЭлемента112 = ИндексЭлемента112 + 7;
	КоличествоСтрок112 = КоличествоСтрок112 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер113(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем113;
	Перем ТекущаяСтрока113;
	Перем ИндексЭлемента113;
	Перем КоличествоСтрок113;

	ТекущаяСтрока113 = 0;
	ИндексЭлемента113 = 1;
	КоличествоСтрок113 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока113 = ТекущаяСтрока113 + Индекс;
		Если ТекущаяСтрока113 > 50 Тогда
			ИндексЭлемента113 = ТекущаяСтрока113;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока113 = ТекущаяСтрока113 + 0;
	ИндексЭлемента113 = ИндексЭлемента113 + 1;
	КоличествоСтрок113 = КоличествоСтрок113 + 2;
	ТекущаяСтрока113 = ТекущаяСтрока113 + 3;
	ИндексЭлемента113 = ИндексЭлемента113 + 4;
	КоличествоСтрок113 = КоличествоСтрок113 + 5;
	ТекущаяСтрока113 = ТекущаяСтрока113 + 6;
	ИндексЭлемента113 = ИндексЭлемента113 + 7;
	КоличествоСтрок113 = КоличествоСтрок113 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос114(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем114;
	Перем ТекущаяСтрока114;
	Перем ИндексЭлемента114;
	Перем КоличествоСтрок114;

	ТекущаяСтрока114 = 0;
	ИндексЭлемента114 = 1;
	КоличествоСтрок114 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока114 = ТекущаяСтрока114 + Индекс;
		Если ТекущаяСтрока114 > 50 Тогда
			ИндексЭлемента114 = ТекущаяСтрока114;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока114 = ТекущаяСтрока114 + 0;
	ИндексЭлемента114 = ИндексЭлемента114 + 1;
	КоличествоСтрок114 = КоличествоСтрок114 + 2;
	ТекущаяСтрока114 = ТекущаяСтрока114 + 3;
	ИндексЭлемента114 = ИндексЭлемента114 + 4;
	КоличествоСтрок114 = КоличествоСтрок114 + 5;
	ТекущаяСтрока114 = ТекущаяСтрока114 + 6;
	ИндексЭлемента114 = ИндексЭлемента114 + 7;
	КоличествоСтрок114 = КоличествоСтрок114 + 8;

КонецПроцедуры

Процедура СохранитьДанные115(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем115;
	Перем ТекущаяСтрока115;
	Перем ИндексЭлемента115;
	Перем КоличествоСтрок115;

	ТекущаяСтрока115 = 0;
	ИндексЭлемента115 = 1;
	КоличествоСтрок115 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока115 = ТекущаяСтрока115 + Индекс;
		Если ТекущаяСтрока115 > 50 Тогда
			ИндексЭлемента115 = ТекущаяСтрока115;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока115 = ТекущаяСтрока115 + 0;
	ИндексЭлемента115 = ИндексЭлемента115 + 1;
	КоличествоСтрок115 = КоличествоСтрок115 + 2;
	ТекущаяСтрока115 = ТекущаяСтрока115 + 3;
	ИндексЭлемента115 = ИндексЭлемента115 + 4;
	КоличествоСтрок115 = КоличествоСтрок115 + 5;
	ТекущаяСтрока115 = ТекущаяСтрока115 + 6;
	ИндексЭлемента115 = ИндексЭлемента115 + 7;
	КоличествоСтрок115 = КоличествоСтрок115 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции30

Процедура ЗагрузитьКонфигурацию116(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем116;
	Перем ТекущаяСтрока116;
	Перем ИндексЭлемента116;
	Перем КоличествоСтрок116;

	ТекущаяСтрока116 = 0;
	ИндексЭлемента116 = 1;
	КоличествоСтрок116 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока116 = ТекущаяСтрока116 + Индекс;
		Если ТекущаяСтрока116 > 50 Тогда
			ИндексЭлемента116 = ТекущаяСтрока116;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока116 = ТекущаяСтрока116 + 0;
	ИндексЭлемента116 = ИндексЭлемента116 + 1;
	КоличествоСтрок116 = КоличествоСтрок116 + 2;
	ТекущаяСтрока116 = ТекущаяСтрока116 + 3;
	ИндексЭлемента116 = ИндексЭлемента116 + 4;
	КоличествоСтрок116 = КоличествоСтрок116 + 5;
	ТекущаяСтрока116 = ТекущаяСтрока116 + 6;
	ИндексЭлемента116 = ИндексЭлемента116 + 7;
	КоличествоСтрок116 = КоличествоСтрок116 + 8;

КонецПроцедуры

Процедура ПроверитьПрава117(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем117;
	Перем ТекущаяСтрока117;
	Перем ИндексЭлемента117;
	Перем КоличествоСтрок117;

	ТекущаяСтрока117 = 0;
	ИндексЭлемента117 = 1;
	КоличествоСтрок117 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока117 = ТекущаяСтрока117 + Индекс;
		Если ТекущаяСтрока117 > 50 Тогда
			ИндексЭлемента117 = ТекущаяСтрока117;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока117 = ТекущаяСтрока117 + 0;
	ИндексЭлемента117 = ИндексЭлемента117 + 1;
	КоличествоСтрок117 = КоличествоСтрок117 + 2;
	ТекущаяСтрока117 = ТекущаяСтрока117 + 3;
	ИндексЭлемента117 = ИндексЭлемента117 + 4;
	КоличествоСтрок117 = КоличествоСтрок117 + 5;
	ТекущаяСтрока117 = ТекущаяСтрока117 + 6;
	ИндексЭлемента117 = ИндексЭлемента117 + 7;
	КоличествоСтрок117 = КоличествоСтрок117 + 8;

КонецПроцедуры

Процедура ОбновитьКэш118(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем118;
	Перем ТекущаяСтрока118;
	Перем ИндексЭлемента118;
	Перем КоличествоСтрок118;

	ТекущаяСтрока118 = 0;
	ИндексЭлемента118 = 1;
	КоличествоСтрок118 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока118 = ТекущаяСтрока118 + Индекс;
		Если ТекущаяСтрока118 > 50 Тогда
			ИндексЭлемента118 = ТекущаяСтрока118;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока118 = ТекущаяСтрока118 + 0;
	ИндексЭлемента118 = ИндексЭлемента118 + 1;
	КоличествоСтрок118 = КоличествоСтрок118 + 2;
	ТекущаяСтрока118 = ТекущаяСтрока118 + 3;
	ИндексЭлемента118 = ИндексЭлемента118 + 4;
	КоличествоСтрок118 = КоличествоСтрок118 + 5;
	ТекущаяСтрока118 = ТекущаяСтрока118 + 6;
	ИндексЭлемента118 = ИндексЭлемента118 + 7;
	КоличествоСтрок118 = КоличествоСтрок118 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление119(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем119;
	Перем ТекущаяСтрока119;
	Перем ИндексЭлемента119;
	Перем КоличествоСтрок119;

	ТекущаяСтрока119 = 0;
	ИндексЭлемента119 = 1;
	КоличествоСтрок119 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока119 = ТекущаяСтрока119 + Индекс;
		Если ТекущаяСтрока119 > 50 Тогда
			ИндексЭлемента119 = ТекущаяСтрока119;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока119 = ТекущаяСтрока119 + 0;
	ИндексЭлемента119 = ИндексЭлемента119 + 1;
	КоличествоСтрок119 = КоличествоСтрок119 + 2;
	ТекущаяСтрока119 = ТекущаяСтрока119 + 3;
	ИндексЭлемента119 = ИндексЭлемента119 + 4;
	КоличествоСтрок119 = КоличествоСтрок119 + 5;
	ТекущаяСтрока119 = ТекущаяСтрока119 + 6;
	ИндексЭлемента119 = ИндексЭлемента119 + 7;
	КоличествоСтрок119 = КоличествоСтрок119 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные31

Процедура ОбработатьСтроку120(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем120;
	Перем ТекущаяСтрока120;
	Перем ИндексЭлемента120;
	Перем КоличествоСтрок120;

	ТекущаяСтрока120 = 0;
	ИндексЭлемента120 = 1;
	КоличествоСтрок120 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока120 = ТекущаяСтрока120 + Индекс;
		Если ТекущаяСтрока120 > 50 Тогда
			ИндексЭлемента120 = ТекущаяСтрока120;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока120 = ТекущаяСтрока120 + 0;
	ИндексЭлемента120 = ИндексЭлемента120 + 1;
	КоличествоСтрок120 = КоличествоСтрок120 + 2;
	ТекущаяСтрока120 = ТекущаяСтрока120 + 3;
	ИндексЭлемента120 = ИндексЭлемента120 + 4;
	КоличествоСтрок120 = КоличествоСтрок120 + 5;
	ТекущаяСтрока120 = ТекущаяСтрока120 + 6;
	ИндексЭлемента120 = ИндексЭлемента120 + 7;
	КоличествоСтрок120 = КоличествоСтрок120 + 8;

КонецПроцедуры

Процедура ВычислитьСумму121(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем121;
	Перем ТекущаяСтрока121;
	Перем ИндексЭлемента121;
	Перем КоличествоСтрок121;

	ТекущаяСтрока121 = 0;
	ИндексЭлемента121 = 1;
	КоличествоСтрок121 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока121 = ТекущаяСтрока121 + Индекс;
		Если ТекущаяСтрока121 > 50 Тогда
			ИндексЭлемента121 = ТекущаяСтрока121;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока121 = ТекущаяСтрока121 + 0;
	ИндексЭлемента121 = ИндексЭлемента121 + 1;
	КоличествоСтрок121 = КоличествоСтрок121 + 2;
	ТекущаяСтрока121 = ТекущаяСтрока121 + 3;
	ИндексЭлемента121 = ИндексЭлемента121 + 4;
	КоличествоСтрок121 = КоличествоСтрок121 + 5;
	ТекущаяСтрока121 = ТекущаяСтрока121 + 6;
	ИндексЭлемента121 = ИндексЭлемента121 + 7;
	КоличествоСтрок121 = КоличествоСтрок121 + 8;

КонецПроцедуры

Процедура ПроверитьДанные122(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем122;
	Перем ТекущаяСтрока122;
	Перем ИндексЭлемента122;
	Перем КоличествоСтрок122;

	ТекущаяСтрока122 = 0;
	ИндексЭлемента122 = 1;
	КоличествоСтрок122 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока122 = ТекущаяСтрока122 + Индекс;
		Если ТекущаяСтрока122 > 50 Тогда
			ИндексЭлемента122 = ТекущаяСтрока122;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока122 = ТекущаяСтрока122 + 0;
	ИндексЭлемента122 = ИндексЭлемента122 + 1;
	КоличествоСтрок122 = КоличествоСтрок122 + 2;
	ТекущаяСтрока122 = ТекущаяСтрока122 + 3;
	ИндексЭлемента122 = ИндексЭлемента122 + 4;
	КоличествоСтрок122 = КоличествоСтрок122 + 5;
	ТекущаяСтрока122 = ТекущаяСтрока122 + 6;
	ИндексЭлемента122 = ИндексЭлемента122 + 7;
	КоличествоСтрок122 = КоличествоСтрок122 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал123(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем123;
	Перем ТекущаяСтрока123;
	Перем ИндексЭлемента123;
	Перем КоличествоСтрок123;

	ТекущаяСтрока123 = 0;
	ИндексЭлемента123 = 1;
	КоличествоСтрок123 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока123 = ТекущаяСтрока123 + Индекс;
		Если ТекущаяСтрока123 > 50 Тогда
			ИндексЭлемента123 = ТекущаяСтрока123;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока123 = ТекущаяСтрока123 + 0;
	ИндексЭлемента123 = ИндексЭлемента123 + 1;
	КоличествоСтрок123 = КоличествоСтрок123 + 2;
	ТекущаяСтрока123 = ТекущаяСтрока123 + 3;
	ИндексЭлемента123 = ИндексЭлемента123 + 4;
	КоличествоСтрок123 = КоличествоСтрок123 + 5;
	ТекущаяСтрока123 = ТекущаяСтрока123 + 6;
	ИндексЭлемента123 = ИндексЭлемента123 + 7;
	КоличествоСтрок123 = КоличествоСтрок123 + 8;

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедуры32

Процедура ИнициализироватьМодуль124(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем124;
	Перем ТекущаяСтрока124;
	Перем ИндексЭлемента124;
	Перем КоличествоСтрок124;

	ТекущаяСтрока124 = 0;
	ИндексЭлемента124 = 1;
	КоличествоСтрок124 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока124 = ТекущаяСтрока124 + Индекс;
		Если ТекущаяСтрока124 > 50 Тогда
			ИндексЭлемента124 = ТекущаяСтрока124;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока124 = ТекущаяСтрока124 + 0;
	ИндексЭлемента124 = ИндексЭлемента124 + 1;
	КоличествоСтрок124 = КоличествоСтрок124 + 2;
	ТекущаяСтрока124 = ТекущаяСтрока124 + 3;
	ИндексЭлемента124 = ИндексЭлемента124 + 4;
	КоличествоСтрок124 = КоличествоСтрок124 + 5;
	ТекущаяСтрока124 = ТекущаяСтрока124 + 6;
	ИндексЭлемента124 = ИндексЭлемента124 + 7;
	КоличествоСтрок124 = КоличествоСтрок124 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер125(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем125;
	Перем ТекущаяСтрока125;
	Перем ИндексЭлемента125;
	Перем КоличествоСтрок125;

	ТекущаяСтрока125 = 0;
	ИндексЭлемента125 = 1;
	КоличествоСтрок125 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока125 = ТекущаяСтрока125 + Индекс;
		Если ТекущаяСтрока125 > 50 Тогда
			ИндексЭлемента125 = ТекущаяСтрока125;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока125 = ТекущаяСтрока125 + 0;
	ИндексЭлемента125 = ИндексЭлемента125 + 1;
	КоличествоСтрок125 = КоличествоСтрок125 + 2;
	ТекущаяСтрока125 = ТекущаяСтрока125 + 3;
	ИндексЭлемента125 = ИндексЭлемента125 + 4;
	КоличествоСтрок125 = КоличествоСтрок125 + 5;
	ТекущаяСтрока125 = ТекущаяСтрока125 + 6;
	ИндексЭлемента125 = ИндексЭлемента125 + 7;
	КоличествоСтрок125 = КоличествоСтрок125 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос126(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем126;
	Перем ТекущаяСтрока126;
	Перем ИндексЭлемента126;
	Перем КоличествоСтрок126;

	ТекущаяСтрока126 = 0;
	ИндексЭлемента126 = 1;
	КоличествоСтрок126 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока126 = ТекущаяСтрока126 + Индекс;
		Если ТекущаяСтрока126 > 50 Тогда
			ИндексЭлемента126 = ТекущаяСтрока126;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока126 = ТекущаяСтрока126 + 0;
	ИндексЭлемента126 = ИндексЭлемента126 + 1;
	КоличествоСтрок126 = КоличествоСтрок126 + 2;
	ТекущаяСтрока126 = ТекущаяСтрока126 + 3;
	ИндексЭлемента126 = ИндексЭлемента126 + 4;
	КоличествоСтрок126 = КоличествоСтрок126 + 5;
	ТекущаяСтрока126 = ТекущаяСтрока126 + 6;
	ИндексЭлемента126 = ИндексЭлемента126 + 7;
	КоличествоСтрок126 = КоличествоСтрок126 + 8;

КонецПроцедуры

Процедура СохранитьДанные127(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем127;
	Перем ТекущаяСтрока127;
	Перем ИндексЭлемента127;
	Перем КоличествоСтрок127;

	ТекущаяСтрока127 = 0;
	ИндексЭлемента127 = 1;
	КоличествоСтрок127 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока127 = ТекущаяСтрока127 + Индекс;
		Если ТекущаяСтрока127 > 50 Тогда
			ИндексЭлемента127 = ТекущаяСтрока127;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока127 = ТекущаяСтрока127 + 0;
	ИндексЭлемента127 = ИндексЭлемента127 + 1;
	КоличествоСтрок127 = КоличествоСтрок127 + 2;
	ТекущаяСтрока127 = ТекущаяСтрока127 + 3;
	ИндексЭлемента127 = ИндексЭлемента127 + 4;
	КоличествоСтрок127 = КоличествоСтрок127 + 5;
	ТекущаяСтрока127 = ТекущаяСтрока127 + 6;
	ИндексЭлемента127 = ИндексЭлемента127 + 7;
	КоличествоСтрок127 = КоличествоСтрок127 + 8;

КонецПроцедуры

#КонецОбласти

#Область ПубличныеМетоды33

Процедура ЗагрузитьКонфигурацию128(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем128;
	Перем ТекущаяСтрока128;
	Перем ИндексЭлемента128;
	Перем КоличествоСтрок128;

	ТекущаяСтрока128 = 0;
	ИндексЭлемента128 = 1;
	КоличествоСтрок128 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока128 = ТекущаяСтрока128 + Индекс;
		Если ТекущаяСтрока128 > 50 Тогда
			ИндексЭлемента128 = ТекущаяСтрока128;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока128 = ТекущаяСтрока128 + 0;
	ИндексЭлемента128 = ИндексЭлемента128 + 1;
	КоличествоСтрок128 = КоличествоСтрок128 + 2;
	ТекущаяСтрока128 = ТекущаяСтрока128 + 3;
	ИндексЭлемента128 = ИндексЭлемента128 + 4;
	КоличествоСтрок128 = КоличествоСтрок128 + 5;
	ТекущаяСтрока128 = ТекущаяСтрока128 + 6;
	ИндексЭлемента128 = ИндексЭлемента128 + 7;
	КоличествоСтрок128 = КоличествоСтрок128 + 8;

КонецПроцедуры

Процедура ПроверитьПрава129(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем129;
	Перем ТекущаяСтрока129;
	Перем ИндексЭлемента129;
	Перем КоличествоСтрок129;

	ТекущаяСтрока129 = 0;
	ИндексЭлемента129 = 1;
	КоличествоСтрок129 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока129 = ТекущаяСтрока129 + Индекс;
		Если ТекущаяСтрока129 > 50 Тогда
			ИндексЭлемента129 = ТекущаяСтрока129;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока129 = ТекущаяСтрока129 + 0;
	ИндексЭлемента129 = ИндексЭлемента129 + 1;
	КоличествоСтрок129 = КоличествоСтрок129 + 2;
	ТекущаяСтрока129 = ТекущаяСтрока129 + 3;
	ИндексЭлемента129 = ИндексЭлемента129 + 4;
	КоличествоСтрок129 = КоличествоСтрок129 + 5;
	ТекущаяСтрока129 = ТекущаяСтрока129 + 6;
	ИндексЭлемента129 = ИндексЭлемента129 + 7;
	КоличествоСтрок129 = КоличествоСтрок129 + 8;

КонецПроцедуры

Процедура ОбновитьКэш130(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем130;
	Перем ТекущаяСтрока130;
	Перем ИндексЭлемента130;
	Перем КоличествоСтрок130;

	ТекущаяСтрока130 = 0;
	ИндексЭлемента130 = 1;
	КоличествоСтрок130 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока130 = ТекущаяСтрока130 + Индекс;
		Если ТекущаяСтрока130 > 50 Тогда
			ИндексЭлемента130 = ТекущаяСтрока130;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока130 = ТекущаяСтрока130 + 0;
	ИндексЭлемента130 = ИндексЭлемента130 + 1;
	КоличествоСтрок130 = КоличествоСтрок130 + 2;
	ТекущаяСтрока130 = ТекущаяСтрока130 + 3;
	ИндексЭлемента130 = ИндексЭлемента130 + 4;
	КоличествоСтрок130 = КоличествоСтрок130 + 5;
	ТекущаяСтрока130 = ТекущаяСтрока130 + 6;
	ИндексЭлемента130 = ИндексЭлемента130 + 7;
	КоличествоСтрок130 = КоличествоСтрок130 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление131(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем131;
	Перем ТекущаяСтрока131;
	Перем ИндексЭлемента131;
	Перем КоличествоСтрок131;

	ТекущаяСтрока131 = 0;
	ИндексЭлемента131 = 1;
	КоличествоСтрок131 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока131 = ТекущаяСтрока131 + Индекс;
		Если ТекущаяСтрока131 > 50 Тогда
			ИндексЭлемента131 = ТекущаяСтрока131;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока131 = ТекущаяСтрока131 + 0;
	ИндексЭлемента131 = ИндексЭлемента131 + 1;
	КоличествоСтрок131 = КоличествоСтрок131 + 2;
	ТекущаяСтрока131 = ТекущаяСтрока131 + 3;
	ИндексЭлемента131 = ИндексЭлемента131 + 4;
	КоличествоСтрок131 = КоличествоСтрок131 + 5;
	ТекущаяСтрока131 = ТекущаяСтрока131 + 6;
	ИндексЭлемента131 = ИндексЭлемента131 + 7;
	КоличествоСтрок131 = КоличествоСтрок131 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВнутренниеФункции34

Процедура ОбработатьСтроку132(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем132;
	Перем ТекущаяСтрока132;
	Перем ИндексЭлемента132;
	Перем КоличествоСтрок132;

	ТекущаяСтрока132 = 0;
	ИндексЭлемента132 = 1;
	КоличествоСтрок132 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока132 = ТекущаяСтрока132 + Индекс;
		Если ТекущаяСтрока132 > 50 Тогда
			ИндексЭлемента132 = ТекущаяСтрока132;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока132 = ТекущаяСтрока132 + 0;
	ИндексЭлемента132 = ИндексЭлемента132 + 1;
	КоличествоСтрок132 = КоличествоСтрок132 + 2;
	ТекущаяСтрока132 = ТекущаяСтрока132 + 3;
	ИндексЭлемента132 = ИндексЭлемента132 + 4;
	КоличествоСтрок132 = КоличествоСтрок132 + 5;
	ТекущаяСтрока132 = ТекущаяСтрока132 + 6;
	ИндексЭлемента132 = ИндексЭлемента132 + 7;
	КоличествоСтрок132 = КоличествоСтрок132 + 8;

КонецПроцедуры

Процедура ВычислитьСумму133(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем133;
	Перем ТекущаяСтрока133;
	Перем ИндексЭлемента133;
	Перем КоличествоСтрок133;

	ТекущаяСтрока133 = 0;
	ИндексЭлемента133 = 1;
	КоличествоСтрок133 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока133 = ТекущаяСтрока133 + Индекс;
		Если ТекущаяСтрока133 > 50 Тогда
			ИндексЭлемента133 = ТекущаяСтрока133;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока133 = ТекущаяСтрока133 + 0;
	ИндексЭлемента133 = ИндексЭлемента133 + 1;
	КоличествоСтрок133 = КоличествоСтрок133 + 2;
	ТекущаяСтрока133 = ТекущаяСтрока133 + 3;
	ИндексЭлемента133 = ИндексЭлемента133 + 4;
	КоличествоСтрок133 = КоличествоСтрок133 + 5;
	ТекущаяСтрока133 = ТекущаяСтрока133 + 6;
	ИндексЭлемента133 = ИндексЭлемента133 + 7;
	КоличествоСтрок133 = КоличествоСтрок133 + 8;

КонецПроцедуры

Процедура ПроверитьДанные134(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем134;
	Перем ТекущаяСтрока134;
	Перем ИндексЭлемента134;
	Перем КоличествоСтрок134;

	ТекущаяСтрока134 = 0;
	ИндексЭлемента134 = 1;
	КоличествоСтрок134 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока134 = ТекущаяСтрока134 + Индекс;
		Если ТекущаяСтрока134 > 50 Тогда
			ИндексЭлемента134 = ТекущаяСтрока134;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока134 = ТекущаяСтрока134 + 0;
	ИндексЭлемента134 = ИндексЭлемента134 + 1;
	КоличествоСтрок134 = КоличествоСтрок134 + 2;
	ТекущаяСтрока134 = ТекущаяСтрока134 + 3;
	ИндексЭлемента134 = ИндексЭлемента134 + 4;
	КоличествоСтрок134 = КоличествоСтрок134 + 5;
	ТекущаяСтрока134 = ТекущаяСтрока134 + 6;
	ИндексЭлемента134 = ИндексЭлемента134 + 7;
	КоличествоСтрок134 = КоличествоСтрок134 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал135(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем135;
	Перем ТекущаяСтрока135;
	Перем ИндексЭлемента135;
	Перем КоличествоСтрок135;

	ТекущаяСтрока135 = 0;
	ИндексЭлемента135 = 1;
	КоличествоСтрок135 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока135 = ТекущаяСтрока135 + Индекс;
		Если ТекущаяСтрока135 > 50 Тогда
			ИндексЭлемента135 = ТекущаяСтрока135;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока135 = ТекущаяСтрока135 + 0;
	ИндексЭлемента135 = ИндексЭлемента135 + 1;
	КоличествоСтрок135 = КоличествоСтрок135 + 2;
	ТекущаяСтрока135 = ТекущаяСтрока135 + 3;
	ИндексЭлемента135 = ИндексЭлемента135 + 4;
	КоличествоСтрок135 = КоличествоСтрок135 + 5;
	ТекущаяСтрока135 = ТекущаяСтрока135 + 6;
	ИндексЭлемента135 = ИндексЭлемента135 + 7;
	КоличествоСтрок135 = КоличествоСтрок135 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытий35

Процедура ИнициализироватьМодуль136(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем136;
	Перем ТекущаяСтрока136;
	Перем ИндексЭлемента136;
	Перем КоличествоСтрок136;

	ТекущаяСтрока136 = 0;
	ИндексЭлемента136 = 1;
	КоличествоСтрок136 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока136 = ТекущаяСтрока136 + Индекс;
		Если ТекущаяСтрока136 > 50 Тогда
			ИндексЭлемента136 = ТекущаяСтрока136;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока136 = ТекущаяСтрока136 + 0;
	ИндексЭлемента136 = ИндексЭлемента136 + 1;
	КоличествоСтрок136 = КоличествоСтрок136 + 2;
	ТекущаяСтрока136 = ТекущаяСтрока136 + 3;
	ИндексЭлемента136 = ИндексЭлемента136 + 4;
	КоличествоСтрок136 = КоличествоСтрок136 + 5;
	ТекущаяСтрока136 = ТекущаяСтрока136 + 6;
	ИндексЭлемента136 = ИндексЭлемента136 + 7;
	КоличествоСтрок136 = КоличествоСтрок136 + 8;

КонецПроцедуры

Процедура ОчиститьБуфер137(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем137;
	Перем ТекущаяСтрока137;
	Перем ИндексЭлемента137;
	Перем КоличествоСтрок137;

	ТекущаяСтрока137 = 0;
	ИндексЭлемента137 = 1;
	КоличествоСтрок137 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока137 = ТекущаяСтрока137 + Индекс;
		Если ТекущаяСтрока137 > 50 Тогда
			ИндексЭлемента137 = ТекущаяСтрока137;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока137 = ТекущаяСтрока137 + 0;
	ИндексЭлемента137 = ИндексЭлемента137 + 1;
	КоличествоСтрок137 = КоличествоСтрок137 + 2;
	ТекущаяСтрока137 = ТекущаяСтрока137 + 3;
	ИндексЭлемента137 = ИндексЭлемента137 + 4;
	КоличествоСтрок137 = КоличествоСтрок137 + 5;
	ТекущаяСтрока137 = ТекущаяСтрока137 + 6;
	ИндексЭлемента137 = ИндексЭлемента137 + 7;
	КоличествоСтрок137 = КоличествоСтрок137 + 8;

КонецПроцедуры

Процедура ВыполнитьЗапрос138(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем138;
	Перем ТекущаяСтрока138;
	Перем ИндексЭлемента138;
	Перем КоличествоСтрок138;

	ТекущаяСтрока138 = 0;
	ИндексЭлемента138 = 1;
	КоличествоСтрок138 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока138 = ТекущаяСтрока138 + Индекс;
		Если ТекущаяСтрока138 > 50 Тогда
			ИндексЭлемента138 = ТекущаяСтрока138;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока138 = ТекущаяСтрока138 + 0;
	ИндексЭлемента138 = ИндексЭлемента138 + 1;
	КоличествоСтрок138 = КоличествоСтрок138 + 2;
	ТекущаяСтрока138 = ТекущаяСтрока138 + 3;
	ИндексЭлемента138 = ИндексЭлемента138 + 4;
	КоличествоСтрок138 = КоличествоСтрок138 + 5;
	ТекущаяСтрока138 = ТекущаяСтрока138 + 6;
	ИндексЭлемента138 = ИндексЭлемента138 + 7;
	КоличествоСтрок138 = КоличествоСтрок138 + 8;

КонецПроцедуры

Процедура СохранитьДанные139(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем139;
	Перем ТекущаяСтрока139;
	Перем ИндексЭлемента139;
	Перем КоличествоСтрок139;

	ТекущаяСтрока139 = 0;
	ИндексЭлемента139 = 1;
	КоличествоСтрок139 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока139 = ТекущаяСтрока139 + Индекс;
		Если ТекущаяСтрока139 > 50 Тогда
			ИндексЭлемента139 = ТекущаяСтрока139;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока139 = ТекущаяСтрока139 + 0;
	ИндексЭлемента139 = ИндексЭлемента139 + 1;
	КоличествоСтрок139 = КоличествоСтрок139 + 2;
	ТекущаяСтрока139 = ТекущаяСтрока139 + 3;
	ИндексЭлемента139 = ИндексЭлемента139 + 4;
	КоличествоСтрок139 = КоличествоСтрок139 + 5;
	ТекущаяСтрока139 = ТекущаяСтрока139 + 6;
	ИндексЭлемента139 = ИндексЭлемента139 + 7;
	КоличествоСтрок139 = КоличествоСтрок139 + 8;

КонецПроцедуры

#КонецОбласти

#Область ВспомогательныеФункции36

Процедура ЗагрузитьКонфигурацию140(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем140;
	Перем ТекущаяСтрока140;
	Перем ИндексЭлемента140;
	Перем КоличествоСтрок140;

	ТекущаяСтрока140 = 0;
	ИндексЭлемента140 = 1;
	КоличествоСтрок140 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока140 = ТекущаяСтрока140 + Индекс;
		Если ТекущаяСтрока140 > 50 Тогда
			ИндексЭлемента140 = ТекущаяСтрока140;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока140 = ТекущаяСтрока140 + 0;
	ИндексЭлемента140 = ИндексЭлемента140 + 1;
	КоличествоСтрок140 = КоличествоСтрок140 + 2;
	ТекущаяСтрока140 = ТекущаяСтрока140 + 3;
	ИндексЭлемента140 = ИндексЭлемента140 + 4;
	КоличествоСтрок140 = КоличествоСтрок140 + 5;
	ТекущаяСтрока140 = ТекущаяСтрока140 + 6;
	ИндексЭлемента140 = ИндексЭлемента140 + 7;
	КоличествоСтрок140 = КоличествоСтрок140 + 8;

КонецПроцедуры

Процедура ПроверитьПрава141(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем141;
	Перем ТекущаяСтрока141;
	Перем ИндексЭлемента141;
	Перем КоличествоСтрок141;

	ТекущаяСтрока141 = 0;
	ИндексЭлемента141 = 1;
	КоличествоСтрок141 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока141 = ТекущаяСтрока141 + Индекс;
		Если ТекущаяСтрока141 > 50 Тогда
			ИндексЭлемента141 = ТекущаяСтрока141;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока141 = ТекущаяСтрока141 + 0;
	ИндексЭлемента141 = ИндексЭлемента141 + 1;
	КоличествоСтрок141 = КоличествоСтрок141 + 2;
	ТекущаяСтрока141 = ТекущаяСтрока141 + 3;
	ИндексЭлемента141 = ИндексЭлемента141 + 4;
	КоличествоСтрок141 = КоличествоСтрок141 + 5;
	ТекущаяСтрока141 = ТекущаяСтрока141 + 6;
	ИндексЭлемента141 = ИндексЭлемента141 + 7;
	КоличествоСтрок141 = КоличествоСтрок141 + 8;

КонецПроцедуры

Процедура ОбновитьКэш142(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем142;
	Перем ТекущаяСтрока142;
	Перем ИндексЭлемента142;
	Перем КоличествоСтрок142;

	ТекущаяСтрока142 = 0;
	ИндексЭлемента142 = 1;
	КоличествоСтрок142 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока142 = ТекущаяСтрока142 + Индекс;
		Если ТекущаяСтрока142 > 50 Тогда
			ИндексЭлемента142 = ТекущаяСтрока142;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока142 = ТекущаяСтрока142 + 0;
	ИндексЭлемента142 = ИндексЭлемента142 + 1;
	КоличествоСтрок142 = КоличествоСтрок142 + 2;
	ТекущаяСтрока142 = ТекущаяСтрока142 + 3;
	ИндексЭлемента142 = ИндексЭлемента142 + 4;
	КоличествоСтрок142 = КоличествоСтрок142 + 5;
	ТекущаяСтрока142 = ТекущаяСтрока142 + 6;
	ИндексЭлемента142 = ИндексЭлемента142 + 7;
	КоличествоСтрок142 = КоличествоСтрок142 + 8;

КонецПроцедуры

Процедура ОтправитьУведомление143(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем143;
	Перем ТекущаяСтрока143;
	Перем ИндексЭлемента143;
	Перем КоличествоСтрок143;

	ТекущаяСтрока143 = 0;
	ИндексЭлемента143 = 1;
	КоличествоСтрок143 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока143 = ТекущаяСтрока143 + Индекс;
		Если ТекущаяСтрока143 > 50 Тогда
			ИндексЭлемента143 = ТекущаяСтрока143;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока143 = ТекущаяСтрока143 + 0;
	ИндексЭлемента143 = ИндексЭлемента143 + 1;
	КоличествоСтрок143 = КоличествоСтрок143 + 2;
	ТекущаяСтрока143 = ТекущаяСтрока143 + 3;
	ИндексЭлемента143 = ИндексЭлемента143 + 4;
	КоличествоСтрок143 = КоличествоСтрок143 + 5;
	ТекущаяСтрока143 = ТекущаяСтрока143 + 6;
	ИндексЭлемента143 = ИндексЭлемента143 + 7;
	КоличествоСтрок143 = КоличествоСтрок143 + 8;

КонецПроцедуры

#КонецОбласти

#Область ОбщиеПеременные37

Процедура ОбработатьСтроку144(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем144;
	Перем ТекущаяСтрока144;
	Перем ИндексЭлемента144;
	Перем КоличествоСтрок144;

	ТекущаяСтрока144 = 0;
	ИндексЭлемента144 = 1;
	КоличествоСтрок144 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока144 = ТекущаяСтрока144 + Индекс;
		Если ТекущаяСтрока144 > 50 Тогда
			ИндексЭлемента144 = ТекущаяСтрока144;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока144 = ТекущаяСтрока144 + 0;
	ИндексЭлемента144 = ИндексЭлемента144 + 1;
	КоличествоСтрок144 = КоличествоСтрок144 + 2;
	ТекущаяСтрока144 = ТекущаяСтрока144 + 3;
	ИндексЭлемента144 = ИндексЭлемента144 + 4;
	КоличествоСтрок144 = КоличествоСтрок144 + 5;
	ТекущаяСтрока144 = ТекущаяСтрока144 + 6;
	ИндексЭлемента144 = ИндексЭлемента144 + 7;
	КоличествоСтрок144 = КоличествоСтрок144 + 8;

КонецПроцедуры

Процедура ВычислитьСумму145(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем145;
	Перем ТекущаяСтрока145;
	Перем ИндексЭлемента145;
	Перем КоличествоСтрок145;

	ТекущаяСтрока145 = 0;
	ИндексЭлемента145 = 1;
	КоличествоСтрок145 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока145 = ТекущаяСтрока145 + Индекс;
		Если ТекущаяСтрока145 > 50 Тогда
			ИндексЭлемента145 = ТекущаяСтрока145;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока145 = ТекущаяСтрока145 + 0;
	ИндексЭлемента145 = ИндексЭлемента145 + 1;
	КоличествоСтрок145 = КоличествоСтрок145 + 2;
	ТекущаяСтрока145 = ТекущаяСтрока145 + 3;
	ИндексЭлемента145 = ИндексЭлемента145 + 4;
	КоличествоСтрок145 = КоличествоСтрок145 + 5;
	ТекущаяСтрока145 = ТекущаяСтрока145 + 6;
	ИндексЭлемента145 = ИндексЭлемента145 + 7;
	КоличествоСтрок145 = КоличествоСтрок145 + 8;

КонецПроцедуры

Процедура ПроверитьДанные146(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем146;
	Перем ТекущаяСтрока146;
	Перем ИндексЭлемента146;
	Перем КоличествоСтрок146;

	ТекущаяСтрока146 = 0;
	ИндексЭлемента146 = 1;
	КоличествоСтрок146 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока146 = ТекущаяСтрока146 + Индекс;
		Если ТекущаяСтрока146 > 50 Тогда
			ИндексЭлемента146 = ТекущаяСтрока146;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока146 = ТекущаяСтрока146 + 0;
	ИндексЭлемента146 = ИндексЭлемента146 + 1;
	КоличествоСтрок146 = КоличествоСтрок146 + 2;
	ТекущаяСтрока146 = ТекущаяСтрока146 + 3;
	ИндексЭлемента146 = ИндексЭлемента146 + 4;
	КоличествоСтрок146 = КоличествоСтрок146 + 5;
	ТекущаяСтрока146 = ТекущаяСтрока146 + 6;
	ИндексЭлемента146 = ИндексЭлемента146 + 7;
	КоличествоСтрок146 = КоличествоСтрок146 + 8;

КонецПроцедуры

Процедура ЗаписатьВЖурнал147(Параметр1, Знач ПараметрЗнач = Неопределено)
	Перем НеиспользуемаяПерем147;
	Перем ТекущаяСтрока147;
	Перем ИндексЭлемента147;
	Перем КоличествоСтрок147;

	ТекущаяСтрока147 = 0;
	ИндексЭлемента147 = 1;
	КоличествоСтрок147 = 2;

	Если Параметр1 = Неопределено Тогда
		Возврат;
	КонецЕсли;

	Для Индекс = 1 По 10 Цикл
		ТекущаяСтрока147 = ТекущаяСтрока147 + Индекс;
		Если ТекущаяСтрока147 > 50 Тогда
			ИндексЭлемента147 = ТекущаяСтрока147;
		КонецЕсли;
	КонецЦикла;

	ТекущаяСтрока147 = ТекущаяСтрока147 + 0;
	ИндексЭлемента147 = ИндексЭлемента147 + 1;
	КоличествоСтрок147 = КоличествоСтрок147 + 2;
	ТекущаяСтрока147 = ТекущаяСтрока147 + 3;
	ИндексЭлемента147 = ИндексЭлемента147 + 4;
	КоличествоСтрок147 = КоличествоСтрок147 + 5;
	ТекущаяСтрока147 = ТекущаяСтрока147 + 6;
	ИндексЭлемента147 = ИндексЭлемента147 + 7;
	КоличествоСтрок147 = КоличествоСтрок147 + 8;

КонецПроцедуры

#КонецОбласти
